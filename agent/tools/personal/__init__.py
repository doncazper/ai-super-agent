"""Selected-scope personal data interfaces.

These tools do not scrape private macOS databases. They are disabled by default
and only operate on explicitly provided selected data until real approved
connectors are added.
"""
