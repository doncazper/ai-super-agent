from __future__ import annotations

from typing import Iterable


def join_stream_chunks(chunks: Iterable[str]) -> str:
    """Small placeholder helper until LM Studio streaming is enabled."""

    return "".join(chunks)
