# Risk Register

| Risk | Affected Modules | Likelihood | Impact | Mitigation | Owner | Status |
|---|---|---:|---:|---|---|---|
| News source fabrication or stale current-fact claims | future news module, web research, citations, router | Medium | High | News Intelligence policy requires current claims to be grounded in returned source data with provider, URL/source ID, retrieved_at, freshness window, publication/event dates when available, coverage limitations, and failed/unavailable source reporting. Timeline and comparison outputs must distinguish source-backed facts from model inference, avoid fabricated headlines/dates/URLs/citations, and label all article/web content as untrusted data. The news capability manifest now declares planned source-grounded workflows as disabled placeholders before runtime implementation. | Project | Mitigated at docs/roadmap/policy/manifest level; runtime news implementation, providers, and dogfood/evals remain future work |
| Native skill docs drift or generated-doc overclaiming | native skills, command registry, feature maturity, docs | Medium | Medium | Native skill docs generator reads reviewed manifests and local tracker metadata only, defaults command-triggered generation to dry-run, preserves manual notes outside marked generated sections, reports missing docs, includes deprecated/blocked skills, and copies manifest maturity without inferring upgrades. `skills docs-check`, command-registry validation, and the native skill release gate catch stale catalog/registry drift. | Project | Mitigated for metadata-only v1; reviewed real lockfile/pinning workflow and manual external-skill QA remain future work |
| News provider, paid API, retention, or bypass overreach | future news providers, cache, feeds, sitemaps, GDELT, Media Cloud, NewsAPI, search providers | Medium | High | Provider strategy is free-first and cache-first: local news cache, user-provided URLs, configured feeds, news sitemaps, GDELT, optional configured providers, then graceful unavailable. Paid providers are skipped unless explicitly configured/allowed; login/paywall/CAPTCHA/anti-bot bypass is FORBIDDEN; search/news history and full article bodies are not stored by default; binary/downloaded article files are HIGH or disabled; provider decisions must be audited. `NEWS_ALLOW_PAID_APIS=false`, `NEWS_STORE_HISTORY=false`, disabled/planned manifest entries, and provider-policy tests now enforce the scaffold defaults. | Project | Mitigated at docs/roadmap/policy/manifest/config/provider-policy-test level; runtime provider implementation, retention enforcement, and dogfood/evals remain future work |
| Platform bridge bypass or native coupling | platform bridges, core runtime, ToolBroker, PolicyEngine, approvals, audit | Medium | Critical | Cross-Platform Core + Platform Bridge architecture docs require the Python core to remain platform-neutral, bridges to be optional/lazy adapters, platform-specific imports to stay out of core startup, all bridge actions to route through ToolBroker/PolicyEngine/PermissionManager/ApprovalManager/AuditLogger, future capabilities to be manifest-declared before execution, and status checks to avoid personal data. Platform Capability Registry v1 keeps planned/stubbed capability metadata disabled and non-executable, returns structured unsupported records for unknowns, and records CRITICAL future capabilities as per-action approval-required. Platform bridge base interfaces now add `NullPlatformBridge`, side-effect-free `prepare_action()`, blocked direct `execute_action()` behavior, lazy bridge loader tests, and audit correlation fields for future brokered results. Read-only `platform.*` doctor/status/capability commands now route through SAFE ToolBroker/PolicyEngine/AuditLogger metadata tools and do not execute bridge actions. macOS/iOS/Windows/web bridge stubs now lazy-load through `PlatformBridgeRegistry`, inherit fail-closed `NullPlatformBridge` behavior, and return only blocked/requires_setup without native imports, personal-data reads, or side effects. The cross-platform release gate and future build guides add explicit macOS/iOS/Windows/app frontend implementation order, ToolBroker mapping requirements, command/maturity update rules, unsupported behavior rules, and forbidden-without-approval lists. | Project | Mitigated at docs/registry/interface/command/stub/contract/release-gate level; manifest mapping and broader startup guardrails remain future work |
| App Bridge privilege confusion | app bridge contract, future native frontends, ApprovalManager, ToolBroker, audit | Medium | Critical | App Bridge v1 is a contract/schema layer only and defaults to `APP_BRIDGE_ENABLED=false`, localhost/IPC transport, pairing required, and remote access forced off. Validation requires audit correlation IDs, denies unpaired sensitive requests, denies ApprovalManager bypass attempts, requires explicit user interaction for approval payloads, and enforces exact preview, per-action approval, and no approval reuse for future CRITICAL actions. Status payloads reject personal-data fields and import tests verify no server starts. | Project | Mitigated at contract/schema level; no runtime app bridge, server, pairing flow, or execution path exists yet |
| Cross-platform startup overhead regression | platform bridges, CLI startup, app bridge | Medium | Medium | Performance overhead policy forbids native/heavy bridge imports, network calls, subprocesses, personal file scans, permission prompts, app bridge server startup, polling loops, or background workers at core startup; future release gates must use import-graph and side-effect tests. Platform Capability Registry v1 tests verify registry imports do not import native macOS/iOS/Windows modules. Platform-aware config/detection/path scaffolding now uses `sys.platform` plus explicit env flags only, defaults bridges disabled/lazy, computes project-local paths without `Path.home()`, directory creation, or personal scans, and tests no native imports. Platform doctor command tests verify read-only command imports/outputs do not load native modules or start platform actions. Bridge stub tests verify default registry registration does not import stub modules, individual stubs lazy-load on demand, and stub imports do not import native modules. The cross-platform release gate includes an import smoke confirming `smart_agent` startup does not load platform stub bridge modules or checked native/heavy platform modules. | Project | Partially mitigated by registry/config/path/platform-command/stub/import tests; broader dedicated startup guardrails remain future work |
| Tracker density hides current release state or causes inconsistent updates | docs, release process, command registry, feature maturity, prompt tracking | Medium | Medium | Tracker Hygiene, Indexing, and Compaction Pass adds a short dashboard, tracker index, maintenance guide, archive policy, consistency report, AGENTS anchored-edit rules, README tracking links, and regression tests so dense source-of-truth trackers remain detailed but navigable | Project | Mitigated by tracker hygiene pass; exact automated crosswalks remain future work |
| Release readiness false confidence from a large dirty working tree | release process, git, docs, tests, feature maturity | Medium | High | Release Hardening Loop v2 records branch/commit/dirty-tree state, separates local validation from live/manual validation, creates release blockers and a hardening plan, keeps readiness YELLOW until a clean release candidate boundary and dogfood/live evidence exist, and forbids hiding failures or promoting maturity without evidence. Generated artifact hygiene now has narrow ignore rules plus `docs/release/GENERATED_ARTIFACT_HYGIENE.md` so local eval/lead/messaging/export artifacts are not confused with source changes. | Project | Open; generated artifact hygiene mitigated, clean release boundary still pending |
| Model hallucination causes false claims | core, ui | Medium | Medium | Model writes answers only from messages/tool results; debug mode shows payloads | Project | Open |
| Prompt queue drift causes skipped or duplicated work | docs, sdlc, tracking | Medium | Medium | Maintain `docs/PROMPT_LEDGER.md`, `docs/PROMPT_QUEUE.md`, `docs/PROMPT_AUDIT.md`, prompt record directories, prompt CLI commands, and AGENTS rules requiring prompt status updates before/after work | Project | Mitigated by prompt tracking v1 |
| Prompt completion evidence becomes stale or over-trusted | docs, sdlc, tracking | Medium | Medium | Prompt evidence classifier reports `complete_verified`, `likely_complete`, `partial`, `no_evidence`, `failed`, `blocked`, `superseded`, and `stale`; `mark-complete` requires test/docs evidence or explicit unknown; recovery commands are report-only | Project | Mitigated by PTM release gate |
| Prompt pack import accidentally executes high-risk prompts | docs, sdlc, prompt tracking | Low | High | Prompt pack importer supports `import_only` only, rejects `execute_all`, stores prompts as queued files, blocks approval-gated prompts from `prompts next`, and never executes imported prompt text | Project | Mitigated by prompt pack import v1 |
| PromptOps autopilot runs unsafe prompt work | docs, sdlc, prompt tracking | Low | High | `work run-next` is disabled by default, `work autopilot` requires `--safe-only`, refuses HIGH/CRITICAL/FORBIDDEN risks, stops at approval gates, forbids send/write/personal-data/persistence/package-install categories, and writes redacted reports | Project | Mitigated by PromptOps Workbench v1 |
| Overnight self-improvement overreach | workflows/self_improvement, docs, tests | Medium | Critical | Overnight runbook allows only docs/tests/diagnostics/evals/tracking/low-risk refactors, forbids personal data, sends, writes, policy weakening, approval bypass, audit disabling, package installs, external scripts, full disk access, hidden persistence, unapproved commits, and unplanned network; `improve overnight-plan` ranks safe candidates and excludes high/critical or personal-data work | Project | Mitigated by overnight safe-mode runbook |
| Command registry drift causes stale or unsafe manual QA guidance | docs, ui, tests | Medium | Medium | Maintain `docs/COMMAND_REGISTRY.md`, `docs/COMMAND_TEST_MATRIX.md`, `docs/COMMAND_LEGACY.md`, `docs/COMMAND_QA_RUNBOOK.md`, templates, command metadata validation, and AGENTS rules requiring registry/matrix updates for command changes | Project | Mitigated by Command Registry v1 |
| Golden eval false confidence or stale scorecards | evals, docs, release gates | Medium | Medium | Store golden cases as reviewed data files under `eval_cases/`, report pass/fail/skipped reasons and category scorecards, keep live and personal-data evals opt-in/skipped by default, and require docs/maturity updates after eval changes | Project | Mitigated by Golden Eval Suite v1 |
| Model-router benchmark false confidence | router, evals, prompts | Medium | Medium | Model-router and prompt-quality evals use reviewed fixture prompts, compare expected vs actual route/tool/policy fields, skip live LM Studio answer checks unless explicitly requested, and report personal-data/send requests as no-auto-execute gate checks rather than executing tools | Project | Mitigated by model-router benchmark v1 |
| Manual QA command runner accidentally executes risky commands | ui/commands, qa docs | Low | High | `commands qa-run` is non-executing in v1 and only prints SAFE/LOW active command examples; high-risk/personal/write/send commands remain checklist items with approval gates and manual review | Project | Mitigated by Command Registry v1 |
| Dogfood suite accidentally executes unsafe or stale commands | dogfood, session_logs, qa docs | Medium | High | Dogfood suite commands are validated as `python smart_agent.py ...` only, personal-data suite commands are preflight-only, `all_safe` excludes personal-data commands, expected safe failures are explicit, `--dry-run` previews without execution, `--session` requires an existing active session, and delegated commands retain normal ToolBroker/policy/approval/audit behavior | Project | Mitigated by Manual Dogfood Command Suites v1 |
| Forum dogfood/eval false confidence or live-provider overreach | dogfood, evals, forums, Reddit, V2EX, Chinese forum discovery | Medium | High | Forum suites are default-disabled and setup-gated; `all_safe` excludes live Reddit/V2EX/Chinese forum commands; fixture-backed `eval run --forums` makes no live provider calls; docs require dry-run/review before session runs; the Forum Intelligence release gate runs dry-runs, fixture evals, status checks, and no-bypass/no-write scans only; live Reddit/V2EX/Chinese forum validation remains explicit/config-dependent; no suite may scrape, use cookies/browser sessions, bypass CAPTCHA/anti-bot controls, use paid providers by default, write forum content to memory, or expose raw cached author/query/body data | Project | Mitigated by Forum Intelligence dogfood/eval suite v1 and local release gate; live validation still pending |
| Messaging dogfood accidentally sends or touches private Messages data | dogfood, messaging, lead response | Medium | Critical | Messaging dogfood suites are default-disabled, fixture/mock/preflight oriented, exclude `messages macos live-send-probe`, exclude direct `send --from-action` execution outside preflight checks, keep live-send tests out of `all_safe`, and validate that lead-response dogfood uses mock data only. No suite accesses `~/Library/Messages`, requests Full Disk Access, silently sends, bulk sends, or enables personal-data connectors. | Project | Mitigated by Messaging Dogfood Suites v1 |
| Messaging release gate mistaken for live send readiness | messaging, release gate, iMessage, lead response | Medium | Critical | Messaging release gate explicitly separates local tests/dogfood/metadata probes from live delivery. The 2026-05-23 gate ran no live send, confirmed send capabilities remain default-disabled CRITICAL per-action/no-reuse, confirmed macOS probe `send_capability_known=false`, and records that future live sends require explicit configuration, allowlisted self-test recipient, approved live-send probe, exact Action Center approval, and a separate release gate. | Project | Mitigated by Messaging / iMessage release gate; live send remains unavailable |
| Daily dogfood process misses review or regression follow-up | dogfood, session_logs, bugs, tests, docs | Medium | Medium | Live test runbook and `dogfood plan/next/checklist` require session logs, feedback, session review, bug generation, and regression-test follow-up when feasible; planning commands read only suite/session metadata and never execute personal-data workflows | Project | Mitigated by Live Test Runbook v1 |
| Quality dashboard exposes sensitive session or bug content | ui/quality, session_logs, bugs, evals | Medium | High | Product Quality Dashboard reads redacted local metadata only, redacts feedback/bug text before display, avoids raw session outputs and personal connector reads, writes no memory, and performs no tool execution | Project | Mitigated by Product Quality Dashboard v1 |
| Session log privacy leakage | session_logs, reports, dogfood | Medium | High | Session logging redacts secret-looking tokens, email addresses, phone numbers, and command-line secret args by default; does not capture environment variables; stores raw reports under gitignored `reports/sessions/`; personal-data tools remain disabled/approval-gated; raw logging requires explicit `--unsafe-raw` | Project | Mitigated for v1; redaction remains heuristic |
| Feedback notes contain secrets, personal data, or unsafe instructions | session_logs, feedback, dogfood | Medium | High | Feedback text is redacted before storage, feedback is stored only in session-local `feedback.jsonl`, no memory write occurs by default, unsafe feedback escalates severity instead of executing anything, and feedback operations are audited as review evidence rather than policy-changing instructions | Project | Mitigated by feedback capture v1 |
| Session review bug reports leak sensitive data or become unsafe instructions | session_logs, bugs, dogfood | Medium | High | Session review uses redacted command previews and feedback only, does not read raw personal connector data or full raw outputs, redacts bug fields before writing, ignores bug text as policy instructions, writes no memory, and classifies policy/approval/audit/data-leak signals as P0 for human review | Project | Mitigated by Session Review and Bug Generator v1 |
| External skill supply-chain compromise | native_skills, docs, future workflows | Medium | High | Native Skills Program treats external skill text as untrusted, forbids install/run during intake, rejects opaque binaries and hidden scripts, requires license/source review, and maps accepted behavior to local ToolBroker capabilities only | Project | Mitigated by Native Skills Program foundation |
| Native skill permission bypass | native_skills, tools, policy | Medium | Critical | Native skills are defined as reviewed local workflows, not direct tools; manifests cannot grant permissions; manifest validation rejects unknown capabilities, executable fields, bypass language, enabled personal-data defaults, and CRITICAL skills without per-action approval; any future behavior must go through ToolBroker, PolicyEngine, ApprovalManager, AuditLogger, memory rules, and trust labels | Project | Mitigated by Native Skills Program foundation and manifest validator |
| Unreviewed skill root shadowing trusted native behavior | native_skills, docs, cli | Medium | High | Native skill root metadata marks workspace, personal, reconstructed, and experimental roots as untrusted candidate sources; experimental roots are disabled by default; precedence resolution reports duplicates and shadowed candidates; untrusted roots cannot silently override trusted project/bundled native skills without explicit shadowing configuration and later vetting | Project | Mitigated by SKILL-01 roots/precedence scaffold |
| Native skill dependency checks become installers or execution paths | native_skills, cli, docs | Medium | High | Dependency gates are detection-only: they check env var presence with redaction, config key presence, binary presence, workspace/project file presence, current platform, capability IDs, Python version, and setup hints. They do not install packages, execute scripts, import untrusted modules, call providers, call connectors, request permissions, or scan personal files. | Project | Mitigated by SKILL-02 schema/dependency gating |
| Silent native skill mutation changes behavior | native_skills, cli, docs | Medium | High | Provenance records include source/review/license/trust metadata and file hashes; lockfile verification compares manifest hashes, dependency hashes, and file hashes; pinned or changed skills are reported as changed instead of silently accepted. Status/verify commands are read-only and do not auto-update or write lockfiles. | Project | Mitigated by SKILL-03 provenance/lockfile diagnostics |
| Native skill vetting false negative | native_skills, cli, docs | Medium | High | Static inspection/vetting is heuristic and treats low-risk reports as review evidence only, not permission to import, install, run, or enable a skill. Reports flag scripts, package installs, network calls, secrets, filesystem escapes, personal-data requests, browser sessions, prompt injection, approval bypass language, opaque binaries, missing metadata, and recommend manual review for risky findings. | Project | Mitigated by SKILL-04 inspection/vetting CLI and still requires human review before native porting |
| Native skill profile mistaken for permission grant | native_skills, profiles, policy | Medium | High | Profiles are advisory visibility metadata only. They can hide skills by allowlist/blocklist/category/risk/network/write/personal-data/CRITICAL rules, but they do not enable skills, change capability manifests, grant permissions, modify policy, or execute tools. ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, and capability defaults remain final authority. | Project | Mitigated by SKILL-05 profile allowlist docs/tests |
| Native skill compatibility mistaken for execution readiness | native_skills, compatibility, platform metadata | Medium | High | Compatibility records are advisory metadata derived from manifests and setup checks only. They can report supported, unsupported, requires_setup, planned, disabled, or blocked dimensions, but they do not enable skills, load native platform modules, call providers, install dependencies, grant capabilities, or change policy. ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, vetting, and capability manifests remain final authority for any future execution. | Project | Mitigated by SKILL-06 compatibility matrix docs/tests |
| Native skill conflict report mistaken for auto-resolution or permission grant | native_skills, conflicts, policy | Medium | High | Conflict reports are advisory release-gate evidence only. They flag duplicate skill IDs, command/capability overlap, unsafe shadowing, missing dependencies, disabled providers, platform incompatibility, approval/risk/memory mismatches, and missing docs/tests, but they do not auto-resolve conflicts, enable/disable skills, install dependencies, call providers, import plugin runtimes, execute scripts, grant permissions, or change policy. | Project | Mitigated by SKILL-07 conflict detector docs/tests |
| Native skill harness mistaken for skill execution approval | native_skills, dogfood, evals | Medium | High | The harness and dogfood plan are advisory metadata/fixture checks only. HIGH/CRITICAL/FORBIDDEN and personal-data skills are skipped by default; missing dependencies report requires_setup; dogfood suites use reviewed `smart_agent.py` commands; no external skill scripts, package installers, providers, connectors, plugin runtimes, skill enablement, permission grants, or memory writes are added. | Project | Mitigated by SKILL-08 test/dogfood harness docs/tests |
| Native skill discovery drift | native_skills, docs, command registry | Medium | Medium | `native_skills.find_skill` searches only local reviewed manifests and tracking docs, reports maturity/readiness/next work conservatively, and points to candidate creation when no match exists; future runs must keep feature registry, maturity, and command registry current | Project | Mitigated by Native skill finder v1 and tracking docs |
| Malicious or malformed PDFs | documents.pdf, workspace files | Medium | Medium | PDF v1 reads only approved workspace paths, uses pure-Python `pypdf`, does not execute embedded actions/scripts or external binaries, disables OCR, enforces file/page/text limits, labels content `UNTRUSTED_DOCUMENT`, filters instruction-like text in summaries, audits reads/failures, and stores no content in memory by default | Project | Mitigated by PDF workspace native skill v1 |
| Scheduler creates hidden background autonomy | workflows/scheduler, local runtime | Low | Critical | Scheduler v1 is manual-run only, stores explicit local JSON records, never installs LaunchAgents/cron/daemons/login items, and requires a future decision record plus approval before any system-level runner | Project | Mitigated by Scheduler v1 |
| Scheduled workflow bypasses approval or executes CRITICAL actions | workflows/scheduler, approvals, action center | Medium | Critical | Supported workflows are allowlisted; scheduled tool workflows still use ToolBroker/PolicyEngine/ApprovalManager/AuditLogger; personal sections require normal approval; unsupported workflows are rejected; result metadata states `critical_actions_executed=false`; scheduled `backup_create` is redacted-only and uses brokered `backup.create` | Project | Mitigated by Scheduler v1 |
| Scheduled backups retain too much sensitive local state | workflows/scheduler, tools/backup | Low | High | Scheduler `backup_create` forces redacted backups, rejects unredacted args, reads no personal connectors, writes only local backup archives, and relies on Backup v1 secret exclusion/redaction plus ToolBroker audit metadata | Project | Mitigated by Scheduler v1 backup_create hardening |
| Pending action preview leakage | safety/actions, approvals, audit | Medium | High | Action Center keeps exact secret-redacted previews only for local approval review; lifecycle audit entries and `actions export` use minimized/redacted payloads for sensitive bodies, drafts, notes, and personal-memory content | Project | Mitigated by Action Center export/audit minimization |
| Prompt injection from web pages | web, workflows | High | High | Wrap untrusted content; never execute page instructions; tests verify webpage instructions remain data | Project | Mitigated in M4 |
| Search-provider data leakage | web, audit | Medium | Medium | Search provider is opt-in; `WEB_ACCESS_ENABLED` can disable all web access; audit redacts search queries by default; no query history is persisted | Project | Mitigated in Phase B |
| Search provider registry mistaken for live provider readiness | web, connectors, docs | Medium | Medium | Registry v1 is metadata-only for SearXNG/Brave/SerpAPI setup and capability support. `web search-providers` performs no provider calls, explicit provider search returns setup hints when unavailable, query hashes/redacted query are returned instead of history storage, and live provider implementations still require separate ToolBroker/capability/policy/audit tests. | Project | Mitigated by Search Provider Registry v1 |
| Self-hosted SearXNG misconfiguration or JSON-output failure | web, connectors, audit | Medium | Medium | SearXNG is disabled by default, requires `SEARXNG_BASE_URL` and `SEARXNG_ENABLED=true`, uses only the configured instance, never defaults to public instances, requests JSON output only, returns clear setup errors for disabled/malformed/403/429/timeout cases, labels results `UNTRUSTED_WEB`, audits the configured provider domain, and stores no query history by default. | Project | Mitigated by SearXNG provider v1 |
| Brave Search quota overuse or API-key leakage | web, connectors, audit | Low | High | Brave is disabled by default, requires `BRAVE_SEARCH_API_KEY`, `BRAVE_SEARCH_ENABLED=true`, `ALLOW_PAID_APIS=true`, and `MAX_PAID_API_CALLS_PER_DAY>0`; it is never selected by key presence alone, returns setup/cost-policy errors before network calls when gates are missing, redacts query/key material, audits `api.search.brave.com`, normalizes errors, and stores no query history by default. | Project | Mitigated by Brave provider v1 |
| Surprise paid or quota-limited provider use | connectors, web, weather, personal communications | Medium | High | Cost-aware provider policy defaults to `free_first`, `ALLOW_PAID_APIS=false`, `MAX_PAID_API_CALLS_PER_DAY=0`, and `SEARCH_STORE_HISTORY=false`; provider order prefers cache, user-provided URLs, feeds, sitemaps, official/self-hosted sources before paid APIs; `web providers/provider-policy/provider-decision` expose deterministic skip reasons without provider calls; Brave and SerpAPI require explicit provider enablement plus paid/quota opt-in and are not made default by key presence; WeatherAPI is only explicit or paid-policy allowed and is not made default by key presence; provider decisions are auditable and secrets/queries are redacted | Project | Mitigated by cost-aware provider policy v1 hardening, Brave provider v1, SerpAPI fallback hardening, and weather selector v1 |
| SerpAPI paid fallback overuse or key leakage | web, connectors, audit | Low | High | SerpAPI is a separate `web.search.serpapi` capability, never chosen by default under `free_first`, requires `SERPAPI_API_KEY`, `SERPAPI_ENABLED=true`, `ALLOW_PAID_APIS=true`, and `MAX_PAID_API_CALLS_PER_DAY>0` before network calls, redacts query/key material in audit/log output, rate-limits provider calls, normalizes results as `UNTRUSTED_WEB`, provides a config-only `web serpapi doctor`, and uses mocked unit tests for provider behavior | Project | Mitigated by optional SerpAPI fallback v1 hardening |
| Web acquisition accidentally bypasses robots, CAPTCHA, or paid-provider gates | web, connectors, audit | Medium | High | Free-first acquisition routes through ToolBroker capabilities, checks robots.txt for direct URL acquisition, returns blocked/CAPTCHA pages as unavailable with `bypass_attempted=false`, audits network domains/source decisions, uses TTL cache, treats content as untrusted, and reports paid-provider skips when cost policy disallows them. Core robots/sitemap/feed modules cover malformed input, sitemap indexes, RSS/Atom parsing, common RSS/Atom MIME types, sitemap/feed binary denial, timeout handling, blocklist denial, TTL cache reuse, and default limits. Safe selected-URL fetch/extraction now validates URL/scheme/domain, strips tracking parameters, enforces timeout/redirect/content-type/size bounds, blocks binary downloads by default, strips scripts/event handlers, extracts metadata, wraps prompt-injection text as untrusted data, audits domain/result status, and reports CAPTCHA/block pages as unavailable rather than bypassing. The central `agent.web_acquisition` router exposes a no-fetch `web.source_status` inspection path so source/provider classification can be tested without network content retrieval. | Project | Mitigated by free-first web acquisition v1, robots/sitemap/feed reconciliation, Web Acquisition Layer core, and safe fetch/extraction hardening |
| Internet access graduation drift | web, research, connectors, router, docs | Medium | High | Internet access work must follow `docs/decisions/internet_access_graduation_track.md`, `docs/web/WEB_ACCESS_POLICY.md`, provider strategy, source-grounding requirements, and blocked-source policy before adding providers or broader acquisition behavior; future changes must keep free-first/cache-first defaults, no bypass attempts, no query/content memory storage by default, and ToolBroker/audit enforcement | Project | Mitigated by docs-only release gate reset |
| Source-grounding false confidence during provider expansion | web, research, evals | Medium | High | Source-grounded research v1 distinguishes snippet-only from fetched-page evidence, assigns stable source IDs, separates failed fetches from citation support, reports unavailable sources, notes basic conflicts, includes coverage limitations, and says when no source supports a claim. The citation layer verifies source IDs and denies citations that reference unknown or failed sources. | Project | Mitigated for local v1; live provider/source validation pending |
| Web cache/index retention or poisoning | web_acquisition/cache.py, web_acquisition/index.py, tools/web/cache_index.py | Medium | Medium | Cache/index v1 stores public unauthenticated web metadata only by default, denies personal/authenticated/private/cookie/session sources, hashes/redacts queries, disables full content storage by default, stores blocked/CAPTCHA content as status metadata only, labels entries untrusted, audits file operations, and provides clear/status/delete commands. | Project | Mitigated for local v1; live source freshness validation pending |
| Official API connector readiness confusion | web_acquisition/official_apis, tools/web/official_apis.py, docs/web/providers | Medium | Medium | Official API framework v1 is stub/mock-first: list/status are metadata-only, provider search requires ToolBroker routing, built-in providers return setup hints unless mocked/configured by a later implementation, GitHub/Wikipedia/arXiv/Reddit results remain `UNTRUSTED_WEB`, API/OAuth secrets are never printed, Reddit is read-only/setup-gated, and no Reddit web scraping fallback is exposed. | Project | Mitigated for framework v1; live provider validation pending |
| Internet dogfood/eval false confidence | dogfood_suites, eval_cases/internet, docs/web/INTERNET_DOGFOOD_RUNBOOK.md | Medium | Medium | Internet dogfood/eval v1 is explicitly mock-first and fixture-backed. Live/provider suites are default-disabled or opt-in, `eval run --internet` performs no provider calls, paid providers remain skipped by default, fixtures assert no query/web-content persistence, failed fetches are separated from citations, and the runbook states that live provider account health still requires separate validation. | Project | Mitigated for local release gate; live provider validation remains opt-in |
| Forum intelligence overreach or scraping drift | forums, web_acquisition, future reddit/v2ex/cn-forums connectors | Medium | High | The Reddit + Multilingual Forum Intelligence Track starts with policy docs and a metadata-only provider registry. Future forum access must prefer official APIs, use approved search-provider discovery only when policy allows, use safe selected public URL fetch only where allowed, report login/CAPTCHA/anti-bot/private pages as unavailable, and forbid private/logged-in scraping, cookie/session automation, proxy evasion, and platform-specific scrapers without a future decision and release gate. The registry marks non-V2EX Chinese platforms discovery-only with site-filter guidance and no provider-status network or personal reads. | Project | Mitigated by planning policy and provider registry v1; live connectors still future |
| Forum source-grounding false consensus | forums, research, language, summaries | Medium | High | Forum outputs must label snippet-only versus fetched-thread evidence, preserve source IDs/permalinks/retrieved timestamps, include anecdotal/bias caveats, report disagreements and unavailable sources, label translations as generated, and avoid statistical, cultural, or market-wide consensus claims from sparse posts. Reddit summarization v1 applies these rules with deterministic source lists, low-data warnings, and anecdotal caveats. | Project | Partly mitigated by Reddit summarization local v1; broader forum/language runtime still future |
| Reddit/forum retention or author metadata leakage | forums, cache, index, reddit connector | Medium | High | Forum retention policy forbids permanent forum-content storage by default, disables author metadata storage by default, requires TTL-bounded public cache only, excludes deleted/removed content, provides count-only privacy reports, and forbids use of Reddit/forum content for model training without explicit rights and permission. Reddit read-only/search workflows strip author metadata by default, avoid query-history persistence, redact query-like fields before cache write, skip deleted/removed payloads in cache, disable cache writes when TTL policy cannot be guaranteed, store TTL-bounded cache entries only when enabled, label search results as snippet-only until fetched, and provide brokered cache status, cache clear, retention status, retention sweep, privacy report, and local explain-result metadata commands. | Project | Mitigated for local search/read-only/retention v1; live provider retention QA pending |
| Reddit API compliance confusion | reddit, official APIs, provider policy | Medium | High | Reddit access policy requires read-only official Data API use, OAuth/config gating before live calls, no unauthenticated Reddit web scraping substitute, no posting/commenting/voting/DM/moderation actions in this track, rate-limit handling, token redaction, and setup/disabled/unavailable statuses before content fetch. Reddit doctor/status remain metadata-only; explicit `reddit auth-check` is OAuth/token-status only; search/fetch commands use `https://oauth.reddit.com` via ToolBroker and return setup guidance unless Reddit is explicitly enabled and OAuth configured; `reddit explain-result` is local metadata-only and performs no network fallback. | Project | Mitigated for local search/read-only v1; live Reddit API validation pending |
| Reddit thread export over-retention or workspace escape | reddit, forums, workspace files | Low | Medium | `reddit thread-export` is explicit-only, fetches through the official API connector, writes only under the approved workspace `workspace/reddit_threads/`, labels files `UNTRUSTED_DOCUMENT`, strips author metadata by default, writes no memory, audits `files_written`, and is covered by regression tests. | Project | Mitigated for local mocked v1; live Reddit API validation pending |
| Reddit summarization prompt injection or citation fabrication | reddit, forums, summaries | Medium | High | Reddit summarization v1 uses only official API search/thread payload source IDs and permalinks, excludes deleted/removed content and prompt-injection-like comments from evidence, labels snippet-only versus fetched-thread data, includes low-data and anecdotal caveats, writes no summary memory, and is covered by regression tests. | Project | Mitigated for local mocked v1; live Reddit API validation pending |
| Chinese forum discovery bypass pressure | cn-forums, web search, language workflows | Medium | High | Chinese forum discovery v1 is brokered and constrained to approved search-provider site filters plus safe selected public URL fetches where allowed. Non-V2EX platforms remain discovery/search-first; no private login scraping, cookies/session automation, browser sessions, platform-specific scrapers, CAPTCHA/anti-bot bypass, paid provider default, long-term storage, or memory write path is added. Blocked/login/CAPTCHA pages return unavailable, and fetched public content is `UNTRUSTED_WEB`. | Project | Mitigated for local mocked discovery v1; live search-provider/public-fetch validation remains opt-in |
| Multilingual forum translation overclaiming | language, forums, future cross-language research | Medium | Medium | The local language layer labels translations as `MODEL_GENERATED_TRANSLATION`, preserves source/chunk IDs, treats forum/web/file text as untrusted data, ignores prompt-injection-like source text, writes no translations/source text to memory, and uses no external or paid translation API by default. Missing local LM Studio/Qwopus config returns `setup_required` instead of cloud fallback. | Project | Mitigated for local language v1; cross-language research workflow still future |
| Cross-language forum research overreach | forums, reddit, v2ex, web discovery, language | Medium | High | `forums research` and `forums compare` are brokered, read-only, no-memory workflows that use configured official/API providers, report unsupported/setup-gated sources as unavailable, label translations as generated, preserve source IDs/permalinks, exclude prompt-injection-like text from evidence, use no paid providers by default, and avoid cultural/statistical consensus claims from sparse data. | Project | Mitigated for local mocked v1; live Reddit/V2EX/web discovery validation remains future |
| V2EX connector drift into scraping or account data | v2ex, forums, language | Medium | High | V2EX v1 uses documented API endpoints only, is disabled by default, routes through ToolBroker/PolicyEngine/AuditLogger, redacts optional tokens and author metadata by default, has no member/profile/notification/write endpoints, uses local rate limiting plus TTL cache, and labels all content `UNTRUSTED_WEB`. | Project | Mitigated for local mocked v1; live API validation remains opt-in |
| Secret/config diagnostics leak credentials or imply paid-provider readiness | connectors, config, docs/tests/config | Medium | High | Secret/config doctor prints only redacted credential presence, warns on tracked `.env`, repo-local Gmail/Reddit token paths, broad Gmail scopes, missing Telegram chat allowlists/defaults, generic Reddit user agents, and secret-looking docs/tests/config values. Status/doctor commands make no provider API calls, except explicit Reddit `auth-check` which is OAuth-only and audited. Paid/quota-limited providers remain disabled unless cost policy explicitly allows them, Gmail/Telegram sends remain CRITICAL/disabled, and Reddit write/content capabilities remain disabled. | Project | Mitigated by secret/config doctor v1 plus Gmail/Telegram and Reddit doctor focus |
| Apple ecosystem connector overreach | apple ecosystem, personal connectors, lead inbox | Medium | Critical | Apple ecosystem planning requires selected-scope adapters, EventKit or permissioned native bridges for Calendar/Reminders, selected Contacts access, metadata/read-selected/draft-first Mail/Gmail behavior, no private database scraping, no broad Full Disk Access, ToolBroker-only execution, and disabled-by-default personal connectors | Project | Planning mitigations documented |
| Consumer iMessage unsafe access or send path | messages, apple ecosystem, lead response | Medium | Critical | Consumer Messages/iMessage strategy rejects `~/Library/Messages` database scraping, Full Disk Access, broad history reads, AppleScript/Accessibility auto-send, hidden inbox polling, and automatic sends; current path remains manual selected text plus draft/handoff only | Project | Planning mitigations documented; send remains deferred |
| Apple messaging path confusion | messages, iOS compose, macOS Messages, Apple Messages for Business, lead response | Medium | Critical | Apple messaging planning splits iOS user-confirmed compose, macOS Messages automation probe, and Apple Messages for Business; message reads are HIGH, personal handoff is HIGH when personal data is present, sends are CRITICAL with exact per-action approval and no reuse, bulk sending is FORBIDDEN in v1, private database scraping is FORBIDDEN unless a future explicit decision overrides after review, and unsafe platform gaps must return clear limitations | Project | Planning mitigations documented; runtime implementation deferred |
| Message channel abstraction accidentally becomes a send bypass | messaging, messages, action center | Low | Critical | Messaging abstraction v1 is draft/preview/validation first; no direct/silent `messaging.send` tool is registered; generic send execution remains unregistered; send request models default to CRITICAL, exact-preview, approval-required, and no approval reuse; unknown channels, group/bulk recipients, and unsupported attachments are rejected; adapter-specific sends such as macOS must execute through ToolBroker and Action Center with their own disabled-by-default gates | Project | Mitigated by Message Channel Abstraction v1 and macOS adapter gates |
| Message send action preview or approval misuse | messaging, action center, approvals, audit | Medium | Critical | Message send proposals can only be created from local `MessageDraft` records, queue CRITICAL Action Center items with exact local previews, force explicit per-action approval with no reuse, invalidate prior approvals when drafts change, reject untrusted-content-created sends without explicit user request, deny bulk/group recipients and attachments in v1, redact audit/export copies, keep generic send execution unregistered, and require adapter-specific gates for any executable send | Project | Mitigated by Message Safety Policy and Action Center integration v1 |
| iOS compose handoff mistaken for silent send | messaging, ios compose, action center | Medium | Critical | iOS compose v1 creates only local handoff payloads and status/result records through `ToolBroker`; payloads require an approved Action Center action or explicit user-confirmed compose mode, expire, include nonce/integrity hash, verify recipient/body against approved previews when applicable, require the user to tap Send in iOS, and register no silent send tool or adapter | Project | Mitigated by iOS user-confirmed compose bridge v1 |
| macOS Messages probe mistaken for send readiness | messaging, macOS Messages, connector status | Medium | Critical | `messages.probe` is metadata-only, brokered, audited, and records `send_capability_known=false`; it checks platform, standard app path, and harmless app identity/version lookup only, never reads `~/Library/Messages`, never requires Full Disk Access, never reads account/message content, and never executes a send verb | Project | Mitigated by macOS Messages probe v1 |
| macOS approved iMessage send sends wrong recipient/body or fakes success | messaging, macOS Messages, action center, approvals, audit | Medium | Critical | `messages.macos.send_approved` is disabled by default, CRITICAL per-action/no-reuse, requires an approved exact `messages.macos.send_approved` Action Center item, current draft fingerprint match, one allowlisted recipient, recent passing live-send probe, daily rate limit, and ToolBroker execution; it rejects attachments, bulk/group sends, unsupported platforms, failed automation, private DB/FDA paths, and returns unsupported rather than fake success | Project | Mitigated by macOS approved iMessage send adapter v1; live validation still required before user use |
| Incoming message intake overreach or private Messages scraping | messaging, lead inbox | Medium | Critical | Incoming message v1 supports only manual workspace-file import and mock fixtures. It blocks `~/Library/Messages/chat.db` and paths outside `./workspace`, requires no Full Disk Access, runs no watcher/poller, treats content as `UNTRUSTED_MESSAGE`, creates draft replies only, writes no memory by default, and audits import/list/show/draft operations through ToolBroker. | Project | Mitigated by incoming message manual/mock inbox v1 |
| Lead response sends wrong, premature, or injected response | lead inbox, email, messages, telegram, apple messages for business | Medium | Critical | Lead Response Drafting v1 treats source text as `UNTRUSTED_MESSAGE`, filters instruction-like content from summaries/drafts, includes source and assumptions in local `MessageDraft` records, queues follow-up tasks only as pending Action Center records, and writes no memory by default. Approved Lead Response Send v1 adds CRITICAL exact-preview send proposals, no approval reuse, draft-edit invalidation, single-recipient/no-attachment checks, unsupported-channel fallback, iOS compose handoff only, manual save/copy handoff only, and macOS delegation through existing disabled-by-default probe/allowlist/rate-limit gates. | Project | Mitigated for local approval-gated orchestration; live provider sends still require channel-specific setup and validation |
| Lead source adapter overreach | lead inbox, gmail, telegram, apple messages for business, web forms | Medium | Critical | Future lead source adapters must be selected-scope, disabled by default, provider-specific, and brokered; token presence must not trigger inbox/chat reads; no private Messages database scraping, broad Full Disk Access, CRM sync, bulk ingestion, hidden polling, or memory storage is allowed without separate decision and release gate | Project | Open for future adapters |
| Apple Messages for Business provider confusion | apple_business, lead inbox, messaging | Medium | Critical | The v1 Apple Business provider is mock/local only: doctor/status are config metadata, mock-inbound writes local untrusted Lead Inbox records, draft-response creates local MessageDrafts only, live provider calls and sends are absent, and future `apple_business.message.send_approved` remains disabled, CRITICAL, exact-preview, per-action, and no-reuse | Project | Mitigated by Apple Business provider stub v1; live adapter still future |
| Search-provider outage or malformed results | web | Medium | Medium | Provider errors are normalized; missing provider returns structured error; tests use mocks instead of live internet | Project | Mitigated in Phase B |
| Weather provider geocoder drift | weather, evals | Medium | Medium | Safe evals include live weather checks when available; Open-Meteo/NWS normalize structured provider errors and now retry comma-separated city/state inputs by searching the city and preferring matching administrative regions; unit tests remain mocked | Project | Mitigated in 2026-05-23 release gate |
| Research citation fabrication | workflows/research.py, web_acquisition/citations.py | Medium | High | Source-grounded research summarizes only returned search/fetch data, includes source URLs, stable source IDs, metadata-only source bundles, citation spans, claim attributions, and `fetch_failures`; verification rejects unknown source IDs and failed-source citations; provider-missing/no-source cases return structured limitations instead of invented URLs | Project | Mitigated in web research and citation/source attribution hardening |
| Web research instruction injection | workflows/research.py, tools/web | High | High | Web content remains `UNTRUSTED_WEB`; excerpts filter instructions to reveal secrets, change policy, call tools, send messages, disable audit, or store private data; pure injection content is not reused as fallback evidence | Project | Mitigated in web research hardening |
| Internet router overreach or underreach | router, preflight, web | Medium | Medium | The deterministic router now exposes `needs_internet`, reason, suggested sources, provider policy, tools, risk hint, and clarification fields; no-tools mode disables internet; local-repo/stable prompts stay clean; prompt-injection-like text cannot force web routing; provider availability is handled by downstream brokered web/research tools with setup hints. | Project | Mitigated in INTERNET-ROUTING-POLICY local tests; live provider behavior remains downstream |
| Prompt injection from email | personal, workflows | High | High | Treat email metadata and bodies as `UNTRUSTED_EMAIL`; wrap bodies with untrusted-content warning; filter instruction-injection phrases from summaries/drafts; email text cannot approve actions, change policy, reveal secrets, request tools, or trigger sending | Project | Mitigated in email draft-only pass |
| Prompt injection from messages | personal, workflows | High | High | Treat selected message content and manual fallback files as `UNTRUSTED_MESSAGE`; wrap bodies with untrusted-content warning; filter instruction-injection phrases from summaries/drafts; message text cannot approve actions, change policy, reveal secrets, request tools, harvest contacts, or trigger sending | Project | Mitigated in messages draft-only pass |
| Malicious documents | web, personal | Medium | High | Treat as `UNTRUSTED_DOCUMENT`; no automatic macros/binary execution; web fetch refuses binary downloads by default | Project | Partially mitigated in M4 |
| Tool-broker bypass | core, tools | Medium | Critical | Central registry and tests; direct tool execution out of policy scope prohibited | Project | Open |
| Path traversal | filesystem tools | Medium | High | Resolve paths; allowed roots; deny private paths; tests cover traversal and outside-root writes | Project | Mitigated in M3 |
| Secrets leakage | audit, memory, debug | Medium | Critical | Redaction, forbidden paths, no secret memory; memory audit content redacted | Project | Mitigated in M5 |
| Unsafe memory storage | memory | Medium | High | Categorized memory; approval for personal data; deletion lifecycle; tests cover secret/personal rejection | Project | Mitigated in M5 |
| Overbroad personal-data access | personal | Medium | Critical | Disabled by default; selected-scope; approvals; no private DB scraping or Full Disk Access dependency | Project | Mitigated in M6 |
| Personal connector readiness drift | personal, safety, connectors, memory | Medium | Critical | Added `docs/checklists/personal_connector_readiness.md`; gate verifies ToolBroker/PolicyEngine/ApprovalManager/AuditLogger, disabled personal defaults, HIGH/CRITICAL approval rules, untrusted wrappers, personal-memory defaults, denial tests, non-interactive blocking, and direct-call scans before connector-specific work | Project | Mitigated by readiness gate |
| Calendar read overreach | personal/calendar | Medium | High | Calendar connector disabled by default; HIGH-risk approval required; selected date range enforced; max range defaults to 31 days; no notes/body returned; locations redacted by default; event text is labeled as data rather than instructions; no long-term memory storage | Project | Mitigated in selected-scope calendar pass |
| Meeting prep personal-data overreach | workflows/meeting_prep.py, personal/calendar, personal/contacts, web | Medium | High | Meeting prep reads one selected calendar event only after approval, uses explicit contact queries only after approval, performs optional web search only when requested, sends/edits nothing, performs no bulk export, redacts calendar event args in audit, and writes no memory by default | Project | Mitigated in Meeting Prep v1 |
| Contacts read overreach | personal/contacts | Medium | High | Contacts connector disabled by default; HIGH-risk approval required; search requires a query and returns compact candidates only; selected reads require a token and explicit requested fields; contact text is labeled as data rather than instructions; notes are never returned; email/phone/address values require requested fields plus explicit config gates and are redacted by default; no long-term memory storage | Project | Mitigated in selected-scope contacts pass |
| Email read overreach | personal/email | Medium | Critical | Email connector disabled by default; approval required for metadata, selected thread reads, summaries, and draft replies; no send/delete/move/archive capability is enabled; no bulk inbox ingestion; single selected thread only; metadata and bodies labeled `UNTRUSTED_EMAIL`; body text not stored by default | Project | Mitigated in email draft-only pass |
| Email triage overreach | workflows/email_triage.py, personal/email | Medium | Critical | Email triage uses metadata only by default, requires approval for metadata and selected thread reads, reads at most one selected body, redacts selected body from the triage report and audit args, sends/mutates nothing, and writes no memory by default | Project | Mitigated in Email Triage v1 |
| Messages read overreach | personal/messages | Medium | Critical | Messages connector disabled by default; approval required for selected-thread reads, summaries, draft replies, and `messages.draft_from_text`; no send/delete/move/archive/contact harvesting; no macOS Messages database scraping; no Full Disk Access dependency; manual fallback reads only `./workspace` files and wraps content as `UNTRUSTED_MESSAGE`; body text not stored by default | Project | Mitigated in messages draft-only pass |
| Live smoke test overreach | ui, tests, personal | Medium | High | Smoke harness reports unconfigured live services as skipped; personal connector smoke is dry-run/check-only by default; pytest `personal_data` marker is skipped by default | Project | Mitigated in Phase F |
| Weather location leakage | tools/weather, audit, memory | Medium | Medium | Weather connector uses user-provided locations only, avoids IP/device geolocation, redacts location arguments in audit, keeps provider decisions separate from raw location text, and never persists location history by default | Project | Mitigated in Phase H1 and weather selector v1 |
| Weather default-location overreach | tools/weather/preferences.py, router, workflows | Medium | Medium | Default location is opt-in only through explicit env/config or CLI set-default, never inferred from system/device/IP location; default use is audited by source and not stored in memory automatically | Project | Mitigated in safe weather preferences |
| Weather provider outage or stale data | tools/weather, workflows | Medium | Medium | Open-Meteo errors, malformed responses, geocoding failures, and timestamps are normalized; stale or unavailable results return structured errors rather than fabricated weather claims | Project | Mitigated in Phase H2 |
| Weather cache precise-location retention | tools/weather/cache | Low | High | Cache keys are hashed, cache is TTL-only, and precise-looking street addresses or direct coordinates are not cached; audit still redacts location args | Project | Mitigated in weather hardening |
| NWS U.S.-only scope mismatch | tools/weather/provider.py | Medium | Low | NWS provider rejects non-U.S. geocoded locations and maps NWS points 400/404 responses to structured unsupported-location errors instead of falling back silently | Project | Mitigated in NWS provider pass |
| NWS alert overstatement | tools/weather/formatter.py, tools/weather/provider.py | Low | Medium | Alerts are normalized from provider data only; formatter states source/limitations and does not invent alerts when unsupported or absent | Project | Mitigated in NWS provider pass |
| Weather-impact hallucination | core/router.py, workflows/research.py | Medium | High | Simple weather stays weather-only; delay/closure/storm-current-context queries add web only when needed; web-disabled/provider errors become limitations; workflow output separates weather from web and warns not to claim closures/delays without web sources | Project | Mitigated in weather-aware research pass |
| WeatherKit credential leakage | tools/weather/provider.py, docs/decisions | Low | High | WeatherKit remains a stub; env presence checks return booleans only, JWT signing is not implemented, credential values are never returned or logged, and full implementation requires decision review | Project | Mitigated in WeatherKit stub pass |
| Manifest drift after connectors | config, safety | Medium | High | Startup validator now requires `default_enabled`, `approval_required`, `stores_data`, `trust_level`, and `audit_fields` for every capability; web capabilities with network access require rate limits | Project | Mitigated in Phase G |
| Prompt-injection phrase expansion | web, personal, workflows | Medium | High | Regression tests now cover requests to ignore instructions, reveal secrets, change policy, call tools, send email/text, disable audit logs, and store private data across web, email, messages, and workspace-file input | Project | Mitigated in Phase G |
| Email/text sending abuse | personal, workflows | Medium | Critical | Draft-only first; M8 send tools disabled by default and require per-action approval with preflight | Project | Mitigated in M8 |
| Calendar/contact modification abuse | personal | Medium | High | M8 write tools disabled by default and require per-action approval with preflight | Project | Mitigated in M8 |
| Calendar approved write misuse | personal/calendar | Medium | Critical | Calendar write commands first create Action Center drafts with exact previews; create/update/delete capabilities remain disabled by default, CRITICAL, per-action approval only, one-shot on execution, audited, and low-level write tools reject direct broker execution unless Action Center verifies the approved action id; no automatic invites, no recurring events in v1, no full export, and no notes/body unless explicitly allowed | Project | Mitigated in Calendar approved writes v1 stub; live native write provider deferred |
| Contacts approved edit misuse | personal/contacts | Medium | Critical | Contact edit/create commands first create Action Center drafts with exact field-level previews; write capabilities remain disabled by default, CRITICAL, per-action approval only, one-shot on execution, audited, low-level write tools reject direct broker execution without verified Action Center action ids, submitted args must match the approved preview, no bulk edit/export, sensitive phone/email/address values redacted in persisted previews/audit logs, no delete implementation, and no memory storage | Project | Mitigated in Contacts approved edits v1 stub; live native write provider deferred |
| Email send abuse | personal/email | Medium | Critical | Email sends must originate from Action Center reviewed drafts; `email.send_approved` remains disabled by default, CRITICAL, per-action approval only, no approval reuse, no bulk/background sends, attachments blocked in v1, low-level send tools reject direct broker execution without verified Action Center action ids, submitted args must match the approved reviewed draft, all lifecycle events audited, and real provider implementation is deferred | Project | Mitigated in Email approved send v1 mock/stub path |
| Messages handoff leakage | personal/messages | Medium | High | Messages safe handoff never sends automatically; draft-from-text reads only workspace files, content is `UNTRUSTED_MESSAGE`, local drafts stay under `./workspace/messaging/drafts`, draft-id save/copy commands require matching approved Action Center items before execution, submitted args must match the approved preview, saved drafts stay inside `./workspace`, clipboard risk is documented, no message body is stored in long-term memory, and all handoff actions are audited | Project | Mitigated in Messages safe handoff v1; draft-id workflow added 2026-05-23 |
| Browser clipping overreach | workflows/browser_clipping.py, tools/web, low_risk/workspace_files.py | Medium | High | Browser clipping v1 is explicit-URL only, fetches through `web.fetch_url`, writes clips through `filesystem.write` under `./workspace`, labels stored content as `UNTRUSTED_DOCUMENT`, blocks private/blocked domains via web policy, does not read history/cookies/sessions/passwords/profile databases, tracks canonical `browser.read_url`/`browser.summarize_url`/`browser.selected_tab` capabilities, and leaves native selected-tab as a disabled stub | Project | Mitigated in Browser clipping v1 |
| Knowledge capture storing secrets or personal data | workflows/knowledge_capture.py, memory/tools.py | Medium | High | Capture v1 stores only under `./workspace/captures`, rejects secret-looking content before writing, treats URL/file captures as untrusted data by default, allows trusted file labels only with explicit `--trusted-user`, performs no Apple Notes/private app database access, and promotes to memory only through `memory.store` where personal-looking content is blocked by default and Memory v2 policy rejects personal/untrusted document storage without approval | Project | Mitigated in Knowledge Capture v1 |
| Privacy inventory reveals sensitive data or reads personal providers | ui/privacy_center.py | Medium | High | Privacy Center v1 builds inventory from metadata/status stores only, does not call personal connectors, redacts exports by default, summarizes audit logs without raw args, audits every privacy operation, and requires confirmation before brokered memory deletion | Project | Mitigated by Privacy Center v1 |
| Backup archive leaks secrets or personal connector data | tools/backup, ui/backup | Medium | High | Backup v1 excludes `.env` and key/certificate files, redacts secret-like values in copied config/docs/memory/action/capture data, never queries personal connectors, stores archives locally under workspace/configured backup dirs, and audits all backup/export/inspect/verify/restore calls through ToolBroker | Project | Mitigated by Backup / Restore v1 |
| Restore weakens policy or enables personal connectors | tools/backup, config, safety | Low | Critical | `backup.restore` is HIGH approval-required, verifies manifest/file hashes, creates pre-restore copies, and validates backed-up `config/capabilities.yaml` with the current startup validator before applying changes, blocking bypass flags, enabled personal connectors, CRITICAL approval reuse, or missing safety metadata | Project | Mitigated by Backup / Restore v1 |
| Task/reminder overreach or mutation abuse | personal/tasks | Medium | Critical | Tasks connector provider access is disabled by default; list is HIGH and approval-required; create/update/complete/delete are CRITICAL per-action; draft creation uses brokered `tasks.draft_create` to queue an Action Center item only; selected-scope listing only; no full export; no memory writes; native provider deferred; no private database scraping or Full Disk Access | Project | Mitigated in Reminders / Tasks connector v1 mock/adapter |
| Self-improvement weakening safety | workflows | Medium | Critical | Branches, diff review, policy-protection tests, protected safety files, brokered `create-action-for-commit` test/diff checkpoint, and approval-gated commit | Project | Mitigated in M9 and commit-action checkpoint |
| Audit-log tampering | audit | Medium | High | Hash-chained JSONL; later viewer verification | Project | Open |
| Configuration tampering | config, safety | Medium | High | Startup validator; dangerous config rejected | Project | Planned |
| Runtime orchestrator becomes a tool or approval bypass | runtime, orchestration, frontend bridge | Low | Critical | Runtime v1 is metadata-only. It cannot execute tools, approve actions, grant permissions, change policy, call LM Studio from status/doctor, read personal connectors, or start background services. Real side effects remain behind ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger. Tests cover event-bus untrusted-control denial, frontend approval/tool/policy denial, CRITICAL workflow blocking, scheduler CRITICAL/background blocking, and command registry validation. | Project | Mitigated in Agent Runtime Orchestration v1 |
| Runtime startup overhead regression | runtime, CLI | Medium | Medium | `agent.runtime` is designed for lightweight imports and status/doctor metadata checks only; runtime commands do not import connector adapters, model clients, app bridges, or background schedulers. Release gate includes startup/status smoke without `LMSTUDIO_MODEL`. | Project | Mitigated in Agent Runtime Orchestration v1 |
