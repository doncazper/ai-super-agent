# Risk Register

| Risk | Affected Modules | Likelihood | Impact | Mitigation | Owner | Status |
|---|---|---:|---:|---|---|---|
| Model hallucination causes false claims | core, ui | Medium | Medium | Model writes answers only from messages/tool results; debug mode shows payloads | Project | Open |
| Prompt queue drift causes skipped or duplicated work | docs, sdlc, tracking | Medium | Medium | Maintain `docs/PROMPT_LEDGER.md`, `docs/PROMPT_QUEUE.md`, `docs/PROMPT_AUDIT.md`, prompt record directories, prompt CLI commands, and AGENTS rules requiring prompt status updates before/after work | Project | Mitigated by prompt tracking v1 |
| Prompt pack import accidentally executes high-risk prompts | docs, sdlc, prompt tracking | Low | High | Prompt pack importer supports `import_only` only, rejects `execute_all`, stores prompts as queued files, blocks approval-gated prompts from `prompts next`, and never executes imported prompt text | Project | Mitigated by prompt pack import v1 |
| PromptOps autopilot runs unsafe prompt work | docs, sdlc, prompt tracking | Low | High | `work run-next` is disabled by default, `work autopilot` requires `--safe-only`, refuses HIGH/CRITICAL/FORBIDDEN risks, stops at approval gates, forbids send/write/personal-data/persistence/package-install categories, and writes redacted reports | Project | Mitigated by PromptOps Workbench v1 |
| Overnight self-improvement overreach | workflows/self_improvement, docs, tests | Medium | Critical | Overnight runbook allows only docs/tests/diagnostics/evals/tracking/low-risk refactors, forbids personal data, sends, writes, policy weakening, approval bypass, audit disabling, package installs, external scripts, full disk access, hidden persistence, unapproved commits, and unplanned network; `improve overnight-plan` ranks safe candidates and excludes high/critical or personal-data work | Project | Mitigated by overnight safe-mode runbook |
| Command registry drift causes stale or unsafe manual QA guidance | docs, ui, tests | Medium | Medium | Maintain `docs/COMMAND_REGISTRY.md`, `docs/COMMAND_TEST_MATRIX.md`, `docs/COMMAND_LEGACY.md`, `docs/COMMAND_QA_RUNBOOK.md`, templates, command metadata validation, and AGENTS rules requiring registry/matrix updates for command changes | Project | Mitigated by Command Registry v1 |
| Golden eval false confidence or stale scorecards | evals, docs, release gates | Medium | Medium | Store golden cases as reviewed data files under `eval_cases/`, report pass/fail/skipped reasons and category scorecards, keep live and personal-data evals opt-in/skipped by default, and require docs/maturity updates after eval changes | Project | Mitigated by Golden Eval Suite v1 |
| Model-router benchmark false confidence | router, evals, prompts | Medium | Medium | Model-router and prompt-quality evals use reviewed fixture prompts, compare expected vs actual route/tool/policy fields, skip live LM Studio answer checks unless explicitly requested, and report personal-data/send requests as no-auto-execute gate checks rather than executing tools | Project | Mitigated by model-router benchmark v1 |
| Manual QA command runner accidentally executes risky commands | ui/commands, qa docs | Low | High | `commands qa-run` is non-executing in v1 and only prints SAFE/LOW active command examples; high-risk/personal/write/send commands remain checklist items with approval gates and manual review | Project | Mitigated by Command Registry v1 |
| Dogfood suite accidentally executes unsafe or stale commands | dogfood, session_logs, qa docs | Medium | High | Dogfood suite commands are validated as `python smart_agent.py ...` only, personal-data suite commands are preflight-only, `all_safe` excludes personal-data commands, expected safe failures are explicit, `--dry-run` previews without execution, `--session` requires an existing active session, and delegated commands retain normal ToolBroker/policy/approval/audit behavior | Project | Mitigated by Manual Dogfood Command Suites v1 |
| Session log privacy leakage | session_logs, reports, dogfood | Medium | High | Session logging redacts secret-looking tokens, email addresses, phone numbers, and command-line secret args by default; does not capture environment variables; stores raw reports under gitignored `reports/sessions/`; personal-data tools remain disabled/approval-gated; raw logging requires explicit `--unsafe-raw` | Project | Mitigated for v1; redaction remains heuristic |
| Feedback notes contain secrets, personal data, or unsafe instructions | session_logs, feedback, dogfood | Medium | High | Feedback text is redacted before storage, feedback is stored only in session-local `feedback.jsonl`, no memory write occurs by default, unsafe feedback escalates severity instead of executing anything, and feedback operations are audited as review evidence rather than policy-changing instructions | Project | Mitigated by feedback capture v1 |
| Session review bug reports leak sensitive data or become unsafe instructions | session_logs, bugs, dogfood | Medium | High | Session review uses redacted command previews and feedback only, does not read raw personal connector data or full raw outputs, redacts bug fields before writing, ignores bug text as policy instructions, writes no memory, and classifies policy/approval/audit/data-leak signals as P0 for human review | Project | Mitigated by Session Review and Bug Generator v1 |
| External skill supply-chain compromise | native_skills, docs, future workflows | Medium | High | Native Skills Program treats external skill text as untrusted, forbids install/run during intake, rejects opaque binaries and hidden scripts, requires license/source review, and maps accepted behavior to local ToolBroker capabilities only | Project | Mitigated by Native Skills Program foundation |
| Native skill permission bypass | native_skills, tools, policy | Medium | Critical | Native skills are defined as reviewed local workflows, not direct tools; manifests cannot grant permissions; manifest validation rejects unknown capabilities, executable fields, bypass language, enabled personal-data defaults, and CRITICAL skills without per-action approval; any future behavior must go through ToolBroker, PolicyEngine, ApprovalManager, AuditLogger, memory rules, and trust labels | Project | Mitigated by Native Skills Program foundation and manifest validator |
| Native skill discovery drift | native_skills, docs, command registry | Medium | Medium | `native_skills.find_skill` searches only local reviewed manifests and tracking docs, reports maturity/readiness/next work conservatively, and points to candidate creation when no match exists; future runs must keep feature registry, maturity, and command registry current | Project | Mitigated by Native skill finder v1 and tracking docs |
| Malicious or malformed PDFs | documents.pdf, workspace files | Medium | Medium | PDF v1 reads only approved workspace paths, uses pure-Python `pypdf`, does not execute embedded actions/scripts or external binaries, disables OCR, enforces file/page/text limits, labels content `UNTRUSTED_DOCUMENT`, filters instruction-like text in summaries, audits reads/failures, and stores no content in memory by default | Project | Mitigated by PDF workspace native skill v1 |
| Scheduler creates hidden background autonomy | workflows/scheduler, local runtime | Low | Critical | Scheduler v1 is manual-run only, stores explicit local JSON records, never installs LaunchAgents/cron/daemons/login items, and requires a future decision record plus approval before any system-level runner | Project | Mitigated by Scheduler v1 |
| Scheduled workflow bypasses approval or executes CRITICAL actions | workflows/scheduler, approvals, action center | Medium | Critical | Supported workflows are allowlisted; scheduled tool workflows still use ToolBroker/PolicyEngine/ApprovalManager/AuditLogger; personal sections require normal approval; unsupported workflows are rejected; result metadata states `critical_actions_executed=false`; scheduled `backup_create` is redacted-only and uses brokered `backup.create` | Project | Mitigated by Scheduler v1 |
| Scheduled backups retain too much sensitive local state | workflows/scheduler, tools/backup | Low | High | Scheduler `backup_create` forces redacted backups, rejects unredacted args, reads no personal connectors, writes only local backup archives, and relies on Backup v1 secret exclusion/redaction plus ToolBroker audit metadata | Project | Mitigated by Scheduler v1 backup_create hardening |
| Pending action preview leakage | safety/actions, approvals, audit | Medium | High | Action Center keeps exact secret-redacted previews only for local approval review; lifecycle audit entries and `actions export` use minimized/redacted payloads for sensitive bodies, drafts, notes, and personal-memory content | Project | Mitigated by Action Center export/audit minimization |
| Prompt injection from web pages | web, workflows | High | High | Wrap untrusted content; never execute page instructions; tests verify webpage instructions remain data | Project | Mitigated in M4 |
| Search-provider data leakage | web, audit | Medium | Medium | Search provider is opt-in; `WEB_ACCESS_ENABLED` can disable all web access; audit redacts search queries by default; no query history is persisted | Project | Mitigated in Phase B |
| Search-provider outage or malformed results | web | Medium | Medium | Provider errors are normalized; missing provider returns structured error; tests use mocks instead of live internet | Project | Mitigated in Phase B |
| Weather provider geocoder drift | weather, evals | Medium | Medium | Safe evals include live weather checks when available; Open-Meteo/NWS normalize structured provider errors and now retry comma-separated city/state inputs by searching the city and preferring matching administrative regions; unit tests remain mocked | Project | Mitigated in 2026-05-23 release gate |
| Research citation fabrication | workflows/research.py | Medium | High | Source-grounded research summarizes only returned search/fetch data, includes source URLs, exposes `fetch_failures`, returns structured provider-missing errors, and tests no-source/no-fetch-failure cases | Project | Mitigated in web research hardening |
| Web research instruction injection | workflows/research.py, tools/web | High | High | Web content remains `UNTRUSTED_WEB`; excerpts filter instructions to reveal secrets, change policy, call tools, send messages, disable audit, or store private data; pure injection content is not reused as fallback evidence | Project | Mitigated in web research hardening |
| Prompt injection from email | personal, workflows | High | High | Treat email metadata and bodies as `UNTRUSTED_EMAIL`; wrap bodies with untrusted-content warning; filter instruction-injection phrases from summaries/drafts; email text cannot approve actions, change policy, reveal secrets, request tools, or trigger sending | Project | Mitigated in email draft-only pass |
| Prompt injection from messages | personal, workflows | High | High | Treat selected message content and manual fallback files as `UNTRUSTED_MESSAGE`; wrap bodies with untrusted-content warning; filter instruction-injection phrases from summaries/drafts; message text cannot approve actions, change policy, reveal secrets, request tools, harvest contacts, or trigger sending | Project | Mitigated in messages draft-only pass |
| Malicious documents | web, personal | Medium | High | Treat as `UNTRUSTED_DOCUMENT`; no automatic macros/binary execution; web fetch refuses binary downloads by default | Project | Partially mitigated in M4 |
| Tool-broker bypass | core, tools | Medium | Critical | Central registry and tests; direct tool execution out of policy scope prohibited | Project | Open |
| Path traversal | filesystem tools | Medium | High | Resolve paths; allowed roots; deny private paths; tests cover traversal and outside-root writes | Project | Mitigated in M3 |
| Secrets leakage | audit, memory, debug | Medium | Critical | Redaction, forbidden paths, no secret memory; memory audit content redacted | Project | Mitigated in M5 |
| Unsafe memory storage | memory | Medium | High | Categorized memory; approval for personal data; deletion lifecycle; tests cover secret/personal rejection | Project | Mitigated in M5 |
| Overbroad personal-data access | personal | Medium | Critical | Disabled by default; selected-scope; approvals; no private DB scraping or Full Disk Access dependency | Project | Mitigated in M6 |
| Personal connector readiness drift | personal, safety, connectors, memory | Medium | Critical | Added `docs/checklists/personal_connector_readiness.md`; gate verifies ToolBroker/PolicyEngine/ApprovalManager/AuditLogger, disabled personal defaults, HIGH/CRITICAL approval rules, untrusted wrappers, personal-memory defaults, denial tests, non-interactive blocking, and direct-call scans before connector-specific work | Project | Mitigated by readiness gate |
| Calendar read overreach | personal/calendar | Medium | High | Calendar connector disabled by default; HIGH-risk approval required; selected date range enforced; max range defaults to 31 days; no notes/body returned; locations redacted by default; event text is labeled as data rather than instructions; no long-term memory storage | Project | Mitigated in selected-scope calendar pass |
| Meeting prep personal-data overreach | workflows/meeting_prep.py, personal/calendar, personal/contacts, web | Medium | High | Meeting prep reads one selected calendar event only after approval, uses explicit contact queries only after approval, performs optional web search only when requested, sends/edits nothing, performs no bulk export, redacts calendar event args in audit, and writes no memory by default | Project | Mitigated in Meeting Prep v1 |
| Contacts read overreach | personal/contacts | Medium | High | Contacts connector disabled by default; HIGH-risk approval required; search requires a query and returns compact candidates only; selected reads require a token and explicit requested fields; contact text is labeled as data rather than instructions; notes are never returned; email/phone/address values require requested fields plus explicit config gates and are redacted by default; no long-term memory storage | Project | Mitigated in selected-scope contacts pass |
| Email read overreach | personal/email | Medium | Critical | Email connector disabled by default; approval required for metadata, selected thread reads, summaries, and draft replies; no send/delete/move/archive capability is enabled; no bulk inbox ingestion; single selected thread only; metadata and bodies labeled `UNTRUSTED_EMAIL`; body text not stored by default | Project | Mitigated in email draft-only pass |
| Email triage overreach | workflows/email_triage.py, personal/email | Medium | Critical | Email triage uses metadata only by default, requires approval for metadata and selected thread reads, reads at most one selected body, redacts selected body from the triage report and audit args, sends/mutates nothing, and writes no memory by default | Project | Mitigated in Email Triage v1 |
| Messages read overreach | personal/messages | Medium | Critical | Messages connector disabled by default; approval required for selected-thread reads, summaries, draft replies, and `messages.draft_from_text`; no send/delete/move/archive/contact harvesting; no macOS Messages database scraping; no Full Disk Access dependency; manual fallback reads only `./workspace` files and wraps content as `UNTRUSTED_MESSAGE`; body text not stored by default | Project | Mitigated in messages draft-only pass |
| Live smoke test overreach | ui, tests, personal | Medium | High | Smoke harness reports unconfigured live services as skipped; personal connector smoke is dry-run/check-only by default; pytest `personal_data` marker is skipped by default | Project | Mitigated in Phase F |
| Weather location leakage | tools/weather, audit, memory | Medium | Medium | Weather connector uses user-provided locations only, avoids IP/device geolocation, redacts location arguments in audit, and never persists location history by default | Project | Mitigated in Phase H1 |
| Weather default-location overreach | tools/weather/preferences.py, router, workflows | Medium | Medium | Default location is opt-in only through explicit env/config or CLI set-default, never inferred from system/device/IP location; default use is audited by source and not stored in memory automatically | Project | Mitigated in safe weather preferences |
| Weather provider outage or stale data | tools/weather, workflows | Medium | Medium | Open-Meteo errors, malformed responses, geocoding failures, and timestamps are normalized; stale or unavailable results return structured errors rather than fabricated weather claims | Project | Mitigated in Phase H2 |
| Weather cache precise-location retention | tools/weather/cache | Low | High | Cache keys are hashed, cache is TTL-only, and precise-looking street addresses or direct coordinates are not cached; audit still redacts location args | Project | Mitigated in weather hardening |
| NWS U.S.-only scope mismatch | tools/weather/provider.py | Medium | Low | NWS provider rejects non-U.S. geocoded locations and maps NWS points 400/404 responses to structured unsupported-location errors instead of falling back silently | Project | Mitigated in NWS provider pass |
| NWS alert overstatement | tools/weather/formatter.py, tools/weather/provider.py | Low | Medium | Alerts are normalized from provider data only; formatter states source/limitations and does not invent alerts when unsupported or absent | Project | Mitigated in NWS provider pass |
| Weather-impact hallucination | core/router.py, workflows/research.py | Medium | High | Simple weather stays weather-only; delay/closure/storm-current-context queries add web only when needed; web-disabled/provider errors become limitations; workflow output separates weather from web and warns not to claim closures/delays without web sources | Project | Mitigated in weather-aware research pass |
| WeatherKit credential leakage | tools/weather/provider.py, docs/decisions | Low | High | WeatherKit remains a stub; env presence checks return booleans only, JWT signing is not implemented, credential values are never returned or logged, and full implementation requires decision review | Project | Mitigated in WeatherKit stub pass |
| Manifest drift after connectors | config, safety | Medium | High | Startup validator now requires `default_enabled`, `approval_required`, `stores_data`, `trust_level`, and `audit_fields` for every capability; web capabilities with network access require rate limits | Project | Mitigated in Phase G |
| Prompt-injection phrase expansion | web, personal, workflows | Medium | High | Regression tests now cover requests to ignore instructions, reveal secrets, change policy, call tools, send email/text, disable audit logs, and store private data across web, email, messages, and workspace-file input | Project | Mitigated in Phase G |
| Email/text sending abuse | personal, workflows | Medium | Critical | Draft-only first; M8 send tools disabled by default and require per-action approval with preflight | Project | Mitigated in M8 |
| Calendar/contact modification abuse | personal | Medium | High | M8 write tools disabled by default and require per-action approval with preflight | Project | Mitigated in M8 |
| Calendar approved write misuse | personal/calendar | Medium | Critical | Calendar write commands first create Action Center drafts with exact previews; create/update/delete capabilities remain disabled by default, CRITICAL, per-action approval only, one-shot on execution, audited, and low-level write tools reject direct broker execution unless Action Center verifies the approved action id; no automatic invites, no recurring events in v1, no full export, and no notes/body unless explicitly allowed | Project | Mitigated in Calendar approved writes v1 stub; live native write provider deferred |
| Contacts approved edit misuse | personal/contacts | Medium | Critical | Contact edit/create commands first create Action Center drafts with exact field-level previews; write capabilities remain disabled by default, CRITICAL, per-action approval only, one-shot on execution, audited, low-level write tools reject direct broker execution without verified Action Center action ids, submitted args must match the approved preview, no bulk edit/export, sensitive phone/email/address values redacted in persisted previews/audit logs, no delete implementation, and no memory storage | Project | Mitigated in Contacts approved edits v1 stub; live native write provider deferred |
| Email send abuse | personal/email | Medium | Critical | Email sends must originate from Action Center reviewed drafts; `email.send_approved` remains disabled by default, CRITICAL, per-action approval only, no approval reuse, no bulk/background sends, attachments blocked in v1, low-level send tools reject direct broker execution without verified Action Center action ids, submitted args must match the approved reviewed draft, all lifecycle events audited, and real provider implementation is deferred | Project | Mitigated in Email approved send v1 mock/stub path |
| Messages handoff leakage | personal/messages | Medium | High | Messages safe handoff never sends automatically; draft-from-text reads only workspace files, content is `UNTRUSTED_MESSAGE`, save/copy require verified Action Center action ids and submitted args matching the approved preview, saved drafts stay inside `./workspace`, clipboard risk is documented, no message body is stored in long-term memory, and all handoff actions are audited | Project | Mitigated in Messages safe handoff v1 |
| Browser clipping overreach | workflows/browser_clipping.py, tools/web, low_risk/workspace_files.py | Medium | High | Browser clipping v1 is explicit-URL only, fetches through `web.fetch_url`, writes clips through `filesystem.write` under `./workspace`, labels stored content as `UNTRUSTED_DOCUMENT`, blocks private/blocked domains via web policy, does not read history/cookies/sessions/passwords/profile databases, tracks canonical `browser.read_url`/`browser.summarize_url`/`browser.selected_tab` capabilities, and leaves native selected-tab as a disabled stub | Project | Mitigated in Browser clipping v1 |
| Knowledge capture storing secrets or personal data | workflows/knowledge_capture.py, memory/tools.py | Medium | High | Capture v1 stores only under `./workspace/captures`, rejects secret-looking content before writing, treats URL/file captures as untrusted data by default, allows trusted file labels only with explicit `--trusted-user`, performs no Apple Notes/private app database access, and promotes to memory only through `memory.store` where personal-looking content is blocked by default and Memory v2 policy rejects personal/untrusted document storage without approval | Project | Mitigated in Knowledge Capture v1 |
| Privacy inventory reveals sensitive data or reads personal providers | ui/privacy_center.py | Medium | High | Privacy Center v1 builds inventory from metadata/status stores only, does not call personal connectors, redacts exports by default, summarizes audit logs without raw args, audits every privacy operation, and requires confirmation before brokered memory deletion | Project | Mitigated by Privacy Center v1 |
| Backup archive leaks secrets or personal connector data | tools/backup, ui/backup | Medium | High | Backup v1 excludes `.env` and key/certificate files, redacts secret-like values in copied config/docs/memory/action/capture data, never queries personal connectors, stores archives locally under workspace/configured backup dirs, and audits all backup/export/inspect/verify/restore calls through ToolBroker | Project | Mitigated by Backup / Restore v1 |
| Restore weakens policy or enables personal connectors | tools/backup, config, safety | Low | Critical | `backup.restore` is HIGH approval-required, verifies manifest/file hashes, creates pre-restore copies, and validates backed-up `config/capabilities.yaml` with the current startup validator before applying changes, blocking bypass flags, enabled personal connectors, CRITICAL approval reuse, or missing safety metadata | Project | Mitigated by Backup / Restore v1 |
| Task/reminder overreach or mutation abuse | personal/tasks | Medium | Critical | Tasks connector provider access is disabled by default; list is HIGH and approval-required; create/update/complete/delete are CRITICAL per-action; draft creation uses brokered `tasks.draft_create` to queue an Action Center item only; selected-scope listing only; no full export; no memory writes; native provider deferred; no private database scraping or Full Disk Access | Project | Mitigated in Reminders / Tasks connector v1 mock/adapter |
| Self-improvement weakening safety | workflows | Medium | Critical | Branches, diff review, policy-protection tests, protected safety files, brokered `create-action-for-commit` test/diff checkpoint, and approval-gated commit | Project | Mitigated in M9 and commit-action checkpoint |
| Audit-log tampering | audit | Medium | High | Hash-chained JSONL; later viewer verification | Project | Open |
| Configuration tampering | config, safety | Medium | High | Startup validator; dangerous config rejected | Project | Planned |
