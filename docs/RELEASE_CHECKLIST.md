# Release Checklist

## Performance Bottleneck Scanner Gate

- [x] CANON-01 canonical runtime state model verified locally: metadata-only JSON state, source-of-truth hierarchy, and reconciliation preview commands exist; targeted runtime tests passed; command registry validation passed; no provider calls, tool execution, tracker mutation, personal-data access, web server, background service, runtime rewrite, package install, model download, commit, or push was added.
- [x] CANON-02 durable execution record contracts verified locally: record/status schema, redacted hashing, approval-required validation for HIGH/CRITICAL records, and read-only `runtime records` commands exist; targeted runtime tests passed; command registry validation passed; no record writer, resume executor, queued prompt execution, background worker, tracker mutation, raw secret/personal-data store, package install, commit, or push was added.
- [x] CANON-03 Agent Gateway / Runtime Kernel boundary verified locally: gateway/kernel/frontend contract metadata exists; direct tool execution attempts are blocked in previews; approval-required requests return review status; secrets are redacted; CLI remains first frontend; no server/listener/native UI/channel/tool exposure/approval bypass/policy mutation was added.
- [x] CANON-04 resume/recovery/checkpoint model verified locally: checkpoint and recovery report contracts exist; recovery preview is non-executing; active approval gates remain active; CRITICAL resumes require fresh approval; prompt-pack resume depends on tracker evidence; targeted runtime tests passed; command registry validation passed; no auto-resume, rollback executor, checkpoint writer, tracker mutation, or approval reuse was added.
- [x] CANON-05 code-mode/self-heal artifact hashes and safety lints verified locally: deterministic redacted hash reports and conservative safe-only blocker lints exist; targeted self-improvement tests passed; command registry validation passed; commands are inspection-only and do not patch, execute self-heal, install packages, run providers, start services, commit, or push.
- [x] CANON-06 surface regression lanes verified locally: safe surface lane registry, matrix, dry-run commands, and fixture-safe dogfood suites exist; targeted QA/dogfood tests passed; command registry validation passed; no lane command execution, personal-data access, HIGH/CRITICAL execution, live provider check, service startup, commit, or push was added.
- [x] CANON-07 backup roundtrip and restore hardening verified locally: brokered read-only roundtrip/policy/restore-check commands exist; restore-check validates hashes and policy before restore; tests cover dry-run no-write behavior, pre-restore copies, CRITICAL approval reuse refusal, unredacted secret refusal, path traversal refusal, command registry validation, and policy-check. Live restore smoke remains disposable-workspace-only.
- [x] CANON-08 canonical state dashboard verified locally: read-only dashboard and handoff commands aggregate canonical state, validation evidence, tracker conflicts, dirty worktree, safety status, gateway/kernel status, recovery hints, and handoff file recommendations; targeted runtime tests passed; no file write, tracker mutation, tool execution, provider call, server startup, or auto-resume was added.
- [x] CANON-09 tracker-to-canonical-state migration plan verified locally: tracker-sync preview and conflict commands are read-only; docs define canonical state as machine-readable active-work truth while prompt ledger/queue/audit remain evidence/history views; tests cover no-auto-overwrite flags, conflict reporting, and CLI output; no broad tracker rewrite was added.
- [x] PERF-01 architecture/policy docs exist.
- [x] Planned PERF command registry rows are present and validate.
- [x] PERF-01 docs tests pass.
- [x] Runtime report store exists.
- [x] Static scanner proves it does not execute scanned code.
- [x] Startup/import scanner uses bounded subprocesses and avoids provider/model/personal-data paths.
- [x] Safe benchmark runner rejects HIGH/CRITICAL/personal/mutating/live/paid/background commands.
- [x] Test-suite profiler requires explicit full-suite opt-in and does not delete, skip, xfail, or edit tests.
- [x] Optimization recommendation engine is advisory only and applies zero patches.
- [x] Performance baselines store redacted local metadata and regression comparison runs no providers or benchmarks.
- [x] Patch planner creates metadata-only plans with `applied_patches=0`.
- [x] Performance dashboard/status/next/trends views are read-only and execute no scans, tests, benchmarks, or fixes.
- [x] Performance baseline/regression and dashboard checks exist.
- [x] PERF-11 release gate complete.
- [x] PERF-11 safe static/startup/benchmark/test-profile/recommendation/baseline/dashboard evidence refreshed.

- [x] All tests pass or failures are documented.
- [x] Secrets and API Key Management SECRETS-08 release gate verified locally: full suite passed with 1616 tests; secrets tests passed with 31 tests; command registry validation passed with 541 commands; startup policy and capability manifest validation passed; tracked/staged secret scans and Git preflight passed with redacted placeholder/test-fixture findings only. Real Keychain access, live credential validation, external scanner parity, commit, and push remain outside this gate.
- [x] SOURCE-TRUTH-RECONCILE-01 source-of-truth, tracker, wiring, artifact/git hygiene, QA/test/dogfood, and clean release-candidate boundary reports were created under `docs/reconciliation/`; stale completed prompt rows were reconciled from evidence; Media and Secrets recovered prompt packs are `needs_review`, not imported/run; clean commit-boundary review and external secret scan remain required before push.
- [x] Creative Media Generation MEDIA-12 release gate verified locally: full suite passed with 1580 tests; focused media release tests passed with 77 tests; command registry validation passed with 529 commands; startup policy and capability manifest validation passed via `make policy-check`; media dogfood suite metadata checks passed; `eval run --media` passed with 6 pass, 0 fail, and 5 personal-data skips; maturity remains conservative at local `4 Tested` with no live provider validation or real media generation.
- [x] Creative Media Generation MEDIA-11 dogfood/eval suite verified locally: mock/fixture-only `media_core`, `media_safety`, `media_image_planning`, and `media_video_audio_planning` suites were added; `eval run --media` validates no real generation, no paid APIs, no model downloads, unsafe-prompt denial, voice-clone denial/deferment, license warnings, workspace-bounded assets, and command registry coverage; no provider calls, uploads, publishing, personal-data access, model installs/downloads, or safety-control bypass were added.
- [x] Command QA frontend/backend boundary verified locally: `QAService` owns read-only QA summaries and safe action wrappers, dashboard/status consume the service boundary, read-only methods do not execute commands, action methods block unsupported tiers plus HIGH/CRITICAL/FORBIDDEN/personal-data candidates, summaries are redacted, targeted QA boundary tests passed, full QA tests passed, command registry validation passed, and startup policy/capability manifest validation passed.
- [x] Command QA Sandbox and Self-Healing Loop QA-01 through QA-10 release gate verified locally: full suite passed with 1484 passed and 1 skipped; QA-focused tests passed with 47 passed; startup policy and capability manifest validation passed; command registry validation passed with 522 commands; `eval run --command-qa` passed with 3 pass, 0 fail, and 5 personal-data skips; command QA dogfood suites were dry-run only; Tier 0/Tier 1 command runs were constrained and redacted; Tier 3 requires disposable sandbox; self-heal remains plan/report-only with no auto-patching, package installs, commits, or pushes.
- [x] Natural-Language Command Understanding NLCMD-10 release gate verified locally: full suite passed with 1437 passed, 1 skipped; startup policy and capability manifest validation passed; command registry validation passed with 498 commands; natural-language eval passed with 15 pass, 0 fail, 5 skips; core/risky dogfood suites passed; exact-command/NL regression focused tests passed; maturity remains conservative at local-tested advisory UX.
- [x] Natural-Language Command Understanding NLCMD-09 bug feedback loop verified locally: NL feedback tags are accepted; `feedback nl-bug` creates redacted local bug records; `bugs create-nl-regression` generates sanitized eval fixtures that load; `nl regressions list` reports generated cases; no personal data, command execution, provider calls, memory writes, or policy weakening was added.
- [x] Natural-Language Command Understanding NLCMD-08 dogfood suites verified locally: `natural_language_core` and `natural_language_risky` suite tests passed, both suites ran locally with 0 failures, command registry validation passed, and risky phrases use `nl preflight` only with no sends/writes/personal-data reads/provider calls/mapped command execution.
- [x] Natural-Language Command Understanding NLCMD-07 eval fixtures verified locally: `eval run --natural-language` passed with 15 fixture checks, 0 failures, and 5 personal-data skips; fixtures cover safe suggestions, missing-provider/reporting, unsupported categories, ambiguity, personal-data requests, send/write requests, and legacy phrasing without mapped command execution.
- [x] Natural-Language Command Understanding NLCMD-06 conversational CLI UX verified locally: `nl "<request>"` and `ask "<request>"` explain understood intent, matched command, safety flags, setup gaps, and next safe step; exact commands remain separate; no-tools mode is preserved; no command execution, provider call, personal-data access, memory write, LM Studio call, approval consumption, or safety-control bypass was added.
- [x] Natural-Language Command Understanding NLCMD-05 safe preflight verified locally: `nl preflight`, `nl explain`, and `nl suggest` produce metadata-only execution plans with exact command candidates, risk/trust, approval and dry-run flags, provider/setup requirements, audit preview, memory behavior, and `safe_to_execute`; no command execution, provider call, personal-data access, memory write, approval consumption, or safety-control bypass was added.
- [x] Natural-Language Command Understanding NLCMD-04 clarification/confirmation flow verified locally: missing arguments, ambiguous matches, provider/setup hints, deprecated/stubbed commands, unsupported intents, personal-data requests, and risky send/write requests produce structured clarifying questions and redacted previews; no command execution, provider call, personal-data access, memory write, or safety-control bypass was added.
- [x] Natural-Language Command Understanding NLCMD-03 parser/router verified locally: deterministic rules classify common requests into route decisions with intent, confidence, suggestions, safety outcome, risk, approval/dry-run/clarification flags, evidence, and audit-summary metadata; no-tools mode disables suggestions; exact commands are left to the normal path; personal/send/write/HIGH/CRITICAL requests are not executable from natural language; no tool/provider call, personal-data access, memory write, command execution, or safety-control bypass was added.
- [x] Natural-Language Command Understanding NLCMD-02 command intent index verified locally: command registry metadata is indexed into aliases, triggers, intents, risk, approval, provider/connector setup hints, safe-to-run flags, and dry-run flags; `commands intents` and `commands suggest` are read-only metadata commands; planned/stubbed/deprecated entries are non-primary; HIGH/CRITICAL entries are not directly runnable from natural language; no command execution, provider call, personal-data access, memory write, or safety-control bypass was added.
- [x] Natural-Language Command Understanding NLCMD-01 architecture verified as documentation/specification only: docs define the track, intent taxonomy, safety outcomes, and decision record; deterministic rules plus command-registry metadata remain first; exact commands are preserved; future runtime work cannot bypass ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, or AuditLogger; no parser/router/preflight/CLI execution behavior, personal-data access, send/write behavior, or risky auto-execution was added.
- [x] Codebase Bug Review and Hardening CODEBUG-01 through CODEBUG-08 verified locally: focused bug-review docs exist, one scoped closed-pipe CLI traceback was fixed with regression coverage, static-scan findings are documented, prompt rows were reconciled, full suite passed with 1387 passed and 1 skipped, command registry validation passed with 484 commands, startup policy/capability validation passed via `make policy-check`, safe eval passed with 40 pass / 0 fail / 6 skipped, and all_safe dogfood dry-run passed without executing live risky actions.
- [x] Full feature status, maturity, and prompt-tracker audit completed as docs/review work only: productization audit docs, scorecard, missed-prompts audit, next maturity queue, feature expansion candidates, manual validation plan, tracker updates, and docs tests are in place; no runtime feature, connector, live provider call, paid API, personal-data tool, send/write action, background service, or queued prompt execution was added.
- [x] Hermes-inspired safe autonomy architecture verified as documentation/policy only: safe groundwork tracks are specified, high-risk autonomy gates are explicit, unauthorized third-party CAPTCHA/Cloudflare/anti-bot/proxy/login-wall/paywall bypass and human impersonation are forbidden, and no runtime autonomy, channel connection, background persistence, personal-data access, send/write behavior, browser automation, subagent write permission, cloud private-data execution, or safety-control bypass was added.
- [x] Hermes-inspired gateway/channel process scaffold verified locally: `channels list/status/show` are brokered SAFE metadata commands, remote channels are disabled by default, unknown channels are denied, gateway submissions require correlation ids, metadata secrets are redacted, the gateway cannot execute tools or approve actions, and no external channel connection, send/write path, personal-data read, background persistence, or listener was added.
- [x] Hermes-inspired Telegram/mobile scaffolding verified locally: `telegram doctor/status` and `mobile status/pairing-status` are brokered SAFE metadata commands; Telegram is disabled by default; send, polling, and webhook config default false; mobile companion and mobile approvals default false; status commands make no provider calls, start no bot/listener/background service, read no chats, send no messages, and do not enable mobile approvals.
- [x] Hermes-inspired repeated-task skill proposals verified locally: `skills propose-from-commands`, `skills propose-from-sessions`, `skills proposals list`, `skills proposals show`, and `skills proposals approve --dry-run` are brokered metadata/report commands; proposals use redacted metadata, skip personal-data patterns by default, mark high-risk candidates review-required, and cannot create, import, enable, install, or execute skills.
- [x] Hermes-inspired skill improvement proposals verified locally: `skills improve-propose`, `skills improve-from-bugs`, `skills improve-from-dogfood`, `skills improvements list`, and `skills improvements show` are brokered metadata/report commands; proposals use redacted bug/dogfood metadata, skip unredacted and personal-data evidence by default, include tests/docs/rollback/lockfile-impact fields, mark high-risk proposals human-review-required, and cannot edit skill files, update lockfiles, enable/import skills, install dependencies, execute scripts, raise maturity, or bypass review.
- [x] Hermes-inspired scheduler UX verified locally: `schedule explain`, `schedule templates`, `schedule preview`, `schedule dry-run`, `schedule risks`, and `schedule review` are metadata/dry-run commands; dry-run executes no workflows or tools, creates no Action Center items, installs no cron/LaunchAgent/daemon/login item, starts no background service, keeps personal-data workflows disabled/approval-required, and blocks CRITICAL send/write workflows from automatic scheduling.
- [x] Hermes-inspired subagent isolation groundwork verified locally: `subagents list`, `subagents show`, `subagents policy`, and `subagents dry-run` are metadata/mock commands; dry-run launches no subagent, executes no tools, grants no write permissions, accesses no personal data, executes no CRITICAL actions, approves no actions, and cannot bypass ToolBroker or approvals.
- [x] Hermes-inspired sandbox backend abstraction verified locally: `sandbox backends`, `sandbox policy`, and `sandbox dry-run` are brokered metadata/dry-run commands; dry-run starts no sandbox, executes no commands, uses no network by default, denies personal data by default, restricts filesystem roots to workspace policy, and keeps Docker/VM/browser/cloud execution planned/stubbed/deferred.
- [x] Hermes-inspired model switching and session continuity groundwork verified locally: `brain switch <provider>` returns switch-record compatibility and rollback metadata, non-dry-run switch requests are blocked without config persistence, paid/cloud switches are denied by default, and `session continuity status/export --redacted/clear` carries no personal data, writes no memory, exports no raw transcript, and creates no continuity store.
- [x] Hermes-inspired memory continuity groundwork verified locally: `memory continuity status/build-summary/clear` and `memory context-preview` are brokered, redacted, non-personal by default, token-bounded, no-store/no-cloud/no-automatic-injection, and covered by focused regression tests.
- [x] Hermes-inspired authorized web automation boundary specified locally: docs/tests forbid third-party CAPTCHA/Cloudflare/anti-bot/proxy/rate-limit/login-wall/paywall/cookie/session/human-impersonation/stealth bypass, define blocked/unavailable behavior, and keep deep scan commands planned only.
- [x] Hermes-inspired safe autonomy dogfood/eval suite verified locally: mock-first dogfood suites and fixture-backed `eval run --safe-autonomy` cover channels, Telegram/mobile, skill proposals, scheduler dry-runs, subagent isolation, sandbox mock-only behavior, model-switch dry-run, memory continuity, and blocked web automation boundaries without live providers, personal data, external scripts, background persistence, browser automation, or HIGH/CRITICAL action execution.
- [x] Hermes-inspired safe autonomy release gate verified locally: full suite, startup policy, capability manifest, command registry, focused HERMES tests, safe autonomy dogfood dry-run, fixture-backed safe autonomy eval, static no-bypass/no-hidden-persistence scan, release-gate docs, and conservative maturity review are recorded. Safe autonomy groundwork is ready for future scoped prompts only; risky autonomy, background persistence, browser automation, subagent writes, executable sandboxes, personal-data access, send/write behavior, paid/cloud defaults, and bypass/evasion remain forbidden/deferred.
- [x] Brain Runtime Independence BRAIN-11 release gate verified locally: BrainProvider interface, lazy registry, LM Studio provider wrapping, disabled llama.cpp server/Ollama/llama-cpp-python/MLX scaffolds, mock benchmark/eval reports, fallback disabled by default, cloud fallback disabled, dry-run provider switching, MCP disabled stubs, command registry validation, startup/capability validation, and optional import guard checks are recorded. LM Studio is partially optional, not removed; normal live chat still uses the existing LM Studio-compatible path until future gateway wiring.
- [x] Native Skill System Hardening SKILL-10 release gate verified locally: full suite, startup policy, capability manifest, docs, command registry, native skill manifest validation, lockfile verification status, conflict detection, safe dogfood suites, native skill evals, docs-check, and conservative maturity review are recorded. Native skill system v1 is safe for metadata review/vetting/docs/evidence, not external skill execution.
- [x] Native Skill System Hardening SKILL-09 verified locally: docs generation is metadata-only, command-triggered generation defaults to dry-run, explicit `--write` updates `docs/native_skills/SKILL_CATALOG.md`, manual notes are preserved, missing docs are reported, deprecated/blocked skills remain visible, maturity is copied from manifests only, and no skill execution, dependency install, provider/connector call, plugin runtime execution, personal-data access, permission grant, or memory write is added.
- [x] Cross-Platform Core + Platform Bridge architecture roadmap verified as documentation-only: Python core stays platform-neutral, CLI-only mode remains required, future bridges are optional/lazy, platform-specific imports are forbidden at core startup, planned platform commands are tracked as non-executable, and future actions must use ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger.
- [x] Platform Capability Registry v1 verified locally: `agent.platforms` contains portable static metadata for planned/stubbed capabilities, imports no native platform modules, executes no platform actions, accesses no personal data, returns structured unknown/unsupported records, keeps planned personal-data capabilities disabled by default, and marks CRITICAL future capabilities as per-action approval-required.
- [x] Platform bridge base interfaces verified locally: `PlatformBridge`, `PlatformActionPayload`, `PlatformActionResult`, `PlatformPermissionStatus`, `PlatformHealthStatus`, `NullPlatformBridge`, and lazy `PlatformBridgeRegistry` exist; direct bridge `execute_action()` is blocked, `prepare_action()` is side-effect-free, NullPlatformBridge returns structured setup/unavailable/blocked results, no native imports or personal-data reads occur, and no real platform actions or send/write paths are enabled.
- [x] Platform-aware config, paths, and detection scaffolding verified locally: bridge flags default disabled/lazy, platform detection handles macOS/Windows/Linux/unknown with structured fallback, runtime mode defaults to CLI and can be forced for tests, path helpers return project-local config/data/cache/log/workspace/report/state paths without scanning personal files or creating directories, no native platform imports occur, and no real platform actions or bridge commands are enabled.
- [x] Platform doctor and capability commands verified locally: `platform doctor`, `platform status`, `platform capabilities`, `platform matrix`, and `platform explain <capability_id>` are brokered SAFE metadata commands; they audit status/capability inspection without native imports, personal-data reads, permission prompts, platform actions, network calls, subprocesses, app bridge server startup, memory writes, or bridge enablement.
- [x] Platform bridge stubs verified locally: macOS, iOS companion, Windows, and generic app/web bridge stubs lazy-load through `PlatformBridgeRegistry`, subclass fail-closed `NullPlatformBridge`, declare static capability metadata, block direct `execute_action()` calls, return `requires_setup` for broker-context execution, import no native frameworks, read no personal data, perform no side effects, and start no app bridge server.
- [x] App Bridge API contract verified locally: schema/model validation exists for future frontend requests, config defaults keep `APP_BRIDGE_ENABLED=false`, remote access is forced off, sensitive surfaces require pairing, approval payloads cannot bypass ApprovalManager, CRITICAL payloads require exact preview/per-action/no-reuse metadata, status payloads contain no personal data, and importing the contract starts no server.
- [x] Cross-Platform Core + Platform Bridge release gate verified for current groundwork: future macOS/iOS/Windows/app frontend build guides exist, platform commands smoke-tested as read-only metadata, `smart_agent` startup imports no platform stub bridge modules or checked native/heavy platform modules, App Bridge and platform bridges remain disabled/lazy by default, direct bridge execution is blocked, all real platform actions remain planned/stubbed/unavailable, and manifest mapping plus dedicated startup guardrails remain planned before real bridge behavior.
- [x] Generated artifact hygiene verified: local eval, lead, messaging draft, iOS compose, and Reddit thread export artifacts are narrowly ignored; source fixtures under `workspace/dogfood/` and `workspace/skills/` remain reviewable; no files were deleted automatically.
- [x] Tracker dashboard, index, maintenance guide, archive policy, and consistency report exist and preserve source-of-truth detail without broad rewrites.
- [x] Release Hardening Loop v2 recorded repo state, full tests, startup policy, capability manifest, command registry, safe eval, dogfood dry-run, security scan findings, zero-open-bug status, release blockers, hardening plan, and conservative YELLOW release readiness.
- [x] No forbidden capabilities introduced.
- [x] No accidental personal-data access.
- [x] Audit logs verified.
- [x] Policy behavior verified.
- [x] Approval behavior verified for high/critical actions in scope.
- [x] Approval lifecycle audit events verified for approval-required actions in scope.
- [x] Critical approval reuse is blocked.
- [x] Secrets redacted from debug and audit outputs.
- [x] Action Center export and lifecycle audit records minimize sensitive action bodies/drafts while exact local approval previews remain available for user review.
- [x] Capability manifest includes trust levels, audit fields, storage flags, and web rate limits.
- [x] Sensitive path denylist includes SSH, GPG, Keychain, Messages, Mail, Application Support, AWS, config, and `.env`.
- [x] Prompt-injection regressions cover web, email, messages, and workspace-file content.
- [x] Cost-aware provider defaults verified: `PROVIDER_COST_MODE=free_first`, `ALLOW_PAID_APIS=false`, `MAX_PAID_API_CALLS_PER_DAY=0`, `SEARCH_STORE_HISTORY=false`, no surprise paid provider selection, brokered provider-policy inspection commands, auditable provider decisions, query/secret redaction, and no provider API calls from policy inspection.
- [x] Search Provider Registry v1 verified: `agent.web_acquisition.search` defines provider interface, metadata registry, normalized search result/response schemas, query hash/redaction fields, setup/unknown-provider errors, brokered `web.search_providers`, and explicit provider setup hints; registry inspection performs no provider calls and writes no search history.
- [x] SearXNG provider v1 verified with mocks: provider uses only configured `SEARXNG_BASE_URL`, remains disabled until `SEARXNG_ENABLED=true`, uses JSON output only, returns setup/errors for missing config, disabled JSON, HTTP 403/429, malformed JSON, and timeouts, audits the configured provider domain, labels results `UNTRUSTED_WEB`, and stores no search history.
- [x] Brave provider v1 verified with mocks: provider is disabled until `BRAVE_SEARCH_ENABLED=true`, requires `BRAVE_SEARCH_API_KEY`, paid/quota policy opt-in, and a nonzero daily paid-call cap, is not selected by key presence alone, returns setup/errors for missing config, HTTP/API/429, malformed JSON, and timeouts, audits `api.search.brave.com`, redacts keys/queries, labels results `UNTRUSTED_WEB`, and stores no search history.
- [x] Cost-aware weather provider selector verified with mocks: auto current/forecast selects Open-Meteo, auto alerts selects NOAA/NWS, WeatherAPI is explicit or paid-policy allowed only, provider decisions are audited, and keys/locations are not stored in memory.
- [x] Free-first web acquisition verified: `web.robots`, `web.robots.check`, `web.sitemap`, `web.sitemap.fetch`, `web.feed`, `web.feed.fetch`, `web.acquire_url`, `web.acquire`, and no-fetch `web.source_status` are brokered, rate-limited, audited, cache-aware where applicable, robots-aware for direct URL acquisition, return blocked/CAPTCHA pages as unavailable without bypass attempts, label returned content/status as untrusted, enforce 500 sitemap URL / 50 feed item defaults, accept common RSS/Atom MIME types, deny sitemap/feed binary payloads, and skip paid providers by default.
- [x] Optional SerpAPI fallback verified with mocks: `web.search.serpapi`, `web search --provider serpapi`, `research --provider serpapi`, and config-only `web serpapi doctor` require explicit provider selection plus `SERPAPI_API_KEY`, `SERPAPI_ENABLED=true`, `ALLOW_PAID_APIS=true`, and `MAX_PAID_API_CALLS_PER_DAY>0` before provider calls; default `free_first` paths do not select SerpAPI, queries/secrets are redacted, provider/domain audit data is recorded, and no CAPTCHA/paywall/login/anti-bot bypass behavior is exposed.
- [x] Internet Access graduation roadmap reset verified: policy/strategy docs define free-first and cache-first provider ordering, web content as untrusted data, no CAPTCHA/anti-bot/login-wall bypass, blocked/unavailable responses instead of bypass attempts, no paid providers by default, no browser automation/profile access, and no web query/content memory storage by default.
- [x] News Intelligence roadmap and source policy verified as documentation-only: News docs define source policy, provider strategy, freshness controls, source grounding, retention defaults, and architecture boundaries; planned `news.*` commands are tracked as non-executable; no runtime news command, provider API call, paid API default, article fetch, browser automation, search/news history storage, full article-body storage, fabricated citation path, or paywall/login/CAPTCHA/anti-bot bypass was added.
- [x] News capability manifest and provider policy verified locally: 18 `news.*` capabilities are declared as disabled/planned with risk, trust, provider, rate-limit, memory, audit, docs, and status metadata; `NEWS_ALLOW_PAID_APIS=false`, `NEWS_STORE_HISTORY=false`, and article-body storage defaults are safe; provider policy selects/skips candidates without provider calls and keeps paid providers skipped by default.
- [x] Safe web fetch/extraction v1 verified locally: selected-URL fetch/extract/metadata commands are brokered, validate URL/scheme/domain, bound timeout/redirect/content-type/size behavior, block binary downloads by default, strip scripts/event handlers, wrap prompt-injection text as untrusted data, audit domains/result status, and return CAPTCHA/block pages as unavailable without bypass attempts.
- [x] Source-grounded research v1 verified locally: `research` supports provider/max-source/freshness/no-fetch options, routes search/fetch through ToolBroker, emits source and coverage sections, labels snippet-only evidence, reports fetch failures/unavailable pages, notes basic conflicts, preserves `UNTRUSTED_WEB`, and writes no search history or web content to memory by default.
- [x] Citation/source attribution layer verified locally: research outputs include stable source IDs, metadata-only source bundles, citation spans, claim attribution, snippet-only/fetched/failed labels, failed-source citation separation, retrieved timestamps, and audited `research sources/export-sources/verify-sources --last` inspection commands without provider calls or article-body storage.
- [x] Web cache/dedupe/local index v1 verified locally: cache/index commands are brokered and audited, cache keys include URL/provider/query hash, raw queries are not persisted, personal/authenticated content is denied, full content storage is disabled by default, blocked/CAPTCHA pages store status metadata only, dedupe uses content hashes, and local index search/rebuild perform no network refreshes.
- [x] Official API Connector Framework v1 verified locally: provider registry/domain matching, GitHub/Wikipedia/arXiv/Reddit stubs, brokered `web official-apis`, `web api-status`, and `web api-search`, setup hints, mocked public result normalization, ToolBroker rate limits, secret/query redaction, Reddit no-web-scraping-substitute metadata, provider-domain audit logging, and command registry entries pass without live API calls by default.
- [x] Internet Access dogfood/eval release gate verified locally: mock-safe `internet_core` and `web_providers` suites, opt-in/default-disabled live `web_fetch`, `web_research`, and `web_blocked_sources` suites, fixture-backed `eval run --internet`, source-list/citation/retrieved-at checks, fetch-failure separation, prompt-injection denial, free-first provider policy, no paid default, no query/web-content persistence, audit-domain fixtures, command registry entries, and runbook docs are in place.
- [x] Reddit + Multilingual Forum Intelligence planning verified: decision/policy docs define official-API-first Reddit/forum access, V2EX documented-API priority, Chinese forum discovery-only defaults for non-V2EX platforms, source grounding, generated translation labels, TTL-bounded/no-permanent forum retention, no forum-content training, no paid provider default, no scraping of logged-in/private pages, no cookie/session automation, and no CAPTCHA/anti-bot bypass. No runtime forum connector, Reddit API call, scraping path, or new CLI command is added by this planning gate.
- [x] Reddit provider policy/compliance scaffolding verified locally: Reddit env defaults are disabled/OAuth-gated, capability placeholders are disabled by default, unauthenticated mode and web fallback are policy-denied, posting/commenting/voting/DM capabilities are absent/denied, author metadata is disabled by default, retention defaults are TTL-bounded, `REDDIT_USE_FOR_TRAINING` is hard false, and tests validate the config/manifest without live Reddit calls or scraping.
- [x] Reddit OAuth/config doctor verified locally: `reddit doctor`, `reddit status`, and explicit `reddit auth-check` are brokered/audited diagnostics; `connectors status reddit` is metadata-only; secrets are redacted; tracked `.env`, repo-local token files, generic user agents, disabled connector state, rate-limit/retention config, and disabled write actions are reported; mocked auth-check success/failure tests pass without post/comment/thread fetches or user-content storage.
- [x] Reddit read-only connector v1 verified locally with mocks: official Data API-only client/provider, brokered `reddit search`, `reddit subreddit`, `reddit post`, `reddit comments`, `reddit cache clear`, and `reddit retention sweep`, source IDs/permalinks, nested comment normalization, deleted/removed content handling, rate-limit metadata/error handling, author redaction by default, TTL cache/retention sweeper, setup-required disabled/OAuth-missing behavior, no query-history persistence, no write capabilities, and command registry entries pass without live Reddit calls or web scraping.
- [x] Reddit search workflows verified locally with mocks: `reddit search` accepts subreddit/sort/time/limit/advisory-language options, returns normalized snippet-only results with source IDs/permalinks, redacts queries in audit logs, avoids readable query-history persistence, handles setup-required and rate-limit errors, and `reddit explain-result` explains cached source metadata without calling Reddit or scraping web pages.
- [x] Reddit thread fetch and conversation normalization verified locally with mocks: `reddit thread` and `reddit thread-export` are brokered/audited, official Data API-only, preserve source IDs/permalinks, return comment tree plus flattened comments, enforce max-comment limits, handle deleted/removed comments, redact author metadata by default, and export only under the approved workspace as `UNTRUSTED_DOCUMENT` without live Reddit calls or web scraping.
- [x] Reddit source-grounded summarization verified locally with mocks: `reddit summarize-thread`, `summarize-search`, `consensus`, `pros-cons`, `complaints`, and `buying-advice` are brokered/audited, official API-only through underlying search/thread calls, preserve source IDs/permalinks, label snippet-only versus fetched-thread evidence, exclude deleted/removed and prompt-injection-like comments from evidence, include anecdotal caveats and low-data warnings, write no summaries to memory, and return setup guidance without live calls when unconfigured.
- [x] Reddit retention/cache compliance verified locally with mocks: `reddit cache status`, `cache clear`, `retention status`, `retention sweep`, and `privacy-report` are brokered/audited; cache entries are TTL-bounded, source/content-hash tracked, author metadata minimized by default, query-like fields redacted before cache write, deleted/removed payloads skipped, zero-TTL cache writes disabled, and privacy reports return counts only without raw content, authors, or query text.
- [x] Multilingual forum language layer verified locally: `language detect`, `language translate`, `language translate-file`, and `language glossary` are brokered/audited; local heuristics detect English, Spanish, Chinese, Japanese, and Korean fixtures; simplified/traditional Chinese hints are reported; translations are labeled model-generated; source IDs/chunks are preserved; file commands read only through brokered workspace file access; no external/paid translation API or memory write occurs by default.
- [x] Cross-language forum research workflow verified locally with mocks: `forums research` and `forums compare` are brokered/audited, preserve source IDs/permalinks and original-language snippets, label translations as model-generated, report unavailable/setup-gated sources, exclude prompt-injection-like source text from evidence, write no memory/search history, use no paid provider or scraping fallback by default, and avoid cultural/statistical consensus claims from sparse data.
- [x] Global Forum Provider Registry verified locally: `forums providers`, `forums status <provider>`, `forums doctor`, and `forums capabilities <provider>` are brokered/audited metadata-only commands; Reddit is disabled/configured by OAuth policy, V2EX is disabled by default with documented read-only API metadata, non-V2EX Chinese platforms are discovery-only with site-filter guidance, provider status performs no network/personal/logged-in reads, and all write capabilities are absent/deferred.
- [x] V2EX read-only connector verified locally with mocks: `v2ex doctor`, `v2ex nodes`, `v2ex node`, `v2ex topic`, `v2ex replies`, `v2ex latest`, and `v2ex hot` are brokered/audited, disabled by default until `V2EX_ENABLED=true`, use documented API endpoints only, redact optional token and author metadata, normalize forum content as `UNTRUSTED_WEB`, enforce local rate limits/cache TTL, integrate optional local language workflows, and provide no member/profile/notification/write endpoints.
- [x] Forum Intelligence dogfood/eval suite verified locally with fixtures: default-disabled `reddit_core`, `reddit_research`, `forum_multilingual`, `v2ex`, and `chinese_forum_discovery` suites exist; `all_safe` excludes live Reddit/V2EX/Chinese forum checks; `eval run --forums` validates source grounding, generated translation labels, source IDs/original snippets, deleted/removed exclusion, prompt-injection ignoring, blocked-source unavailable reporting, no scraping bypass, no paid provider defaults, no memory write, retention/cache policy, provider-call audit evidence, command registry entries, and runbook docs without live provider calls.
- [x] Forum Intelligence release gate verified locally: full tests, startup policy validation, capability manifest validation, command registry validation, focused docs/command/forum/prompt tests, forum dogfood dry-runs, fixture-backed forum evals, Reddit/V2EX disabled-config status checks, Reddit count-only cache/retention/privacy reports, language/provider CLI smokes, and no-bypass/no-write scans passed. Live Reddit API, live V2EX API, live Chinese forum search/fetch, and live translation quality validation remain opt-in/config-gated.
- [x] Internet routing policy verified locally: deterministic router emits `needs_internet`, reason, suggested sources, provider policy, tools, risk hint, and clarification metadata; current/live/source-required and URL requests route to internet-capable tools only when needed; stable/local/no-tools prompts stay clean; prompt-injection-like text cannot force internet routing; provider availability remains handled by brokered web/research tools.
- [x] Secret/config doctor verified: SerpAPI, WeatherAPI, Gmail, and Telegram credential checks are redacted, config-only, no-API-call diagnostics; tracked `.env`, repo-local Gmail token path, broad Gmail scope, and Telegram chat-id warnings are covered; paid providers are not promoted by key presence; Gmail/Telegram sends remain disabled and CRITICAL.
- [x] Weather hardening verified: ToolBroker-only current/forecast path, manifest entries, audit metadata, rate limits, opt-in default location, no system location inference, structured outputs/errors, no live-network unit tests, and no personal-data tools enabled by weather.
- [x] Open-Meteo live safe eval verified for `Phoenix, AZ` after city/state geocode fallback hardening.
- [x] Weather cache avoids precise-looking street addresses and direct coordinates.
- [x] NWS provider verified with mocked geocoding, points/grid, forecast, hourly, alerts, unsupported-location, timeout, and audit-domain tests.
- [x] Safe weather preferences verified: no default location by default, explicit default only, default-use audit source, units/cache preferences, config CLI, and no memory write by default.
- [x] Weather-aware web research verified: simple weather remains weather-only, delay/closure/storm update prompts add web only when needed, web-disabled/provider failures become limitations, untrusted snippets are filtered, and weather/web results stay separated.
- [x] WeatherKit remains stub-only: decision record exists, credentials are checked by presence only, no JWT signing/API calls are implemented, and secrets are not logged.
- [x] Personal connector readiness gate passed: ToolBroker, PolicyEngine, approval UI, dry-run/preflight, AuditLogger, secret redaction, connector registry, personal default-disabled manifest entries, HIGH/CRITICAL approval rules, untrusted-content wrappers, memory defaults, denial tests, non-interactive blocking, and direct-call scans were verified.
- [x] No live personal-data reads were performed during the readiness gate.
- [x] Calendar read-only selected-range connector verified: disabled by default, HIGH risk, approval-required, selected-range only, no notes/body, location redaction by default, availability without event details, event text labeled as data, no memory storage by default, and audited access.
- [x] Calendar approved writes v1 verified: write capabilities disabled by default, CRITICAL per-action approval only, Action Center draft/create/update/delete flow, direct broker writes without verified Action Center action ids rejected, one-shot approved execution, rollback metadata where possible, no automatic invites, no recurring events, no full export, notes/body omitted unless explicitly allowed, and no memory storage by default.
- [x] Contacts read-only selected-scope connector verified: disabled by default, HIGH risk, approval-required, no bulk export, compact search first, selected-token reads only, no notes, phone/email/address redacted by default, contact text labeled as data, no memory storage by default, and audited access.
- [x] Contacts approved edits v1 verified: disabled by default, CRITICAL per-action approval only, Action Center draft/update/create stubs, direct broker writes without verified Action Center action ids rejected, submitted args match approved previews, exact field-level diff previews, bulk edit denied, delete deferred, sensitive contact values redacted in previews/audits, no memory storage by default, and brokered audited execution.
- [x] Email metadata + selected-thread + draft-only verified: disabled by default, HIGH risk, approval-required, no send/delete/move/archive, no bulk ingestion, metadata/body labeled `UNTRUSTED_EMAIL`, body wrapped as untrusted data, prompt-injection filtering, no body memory storage by default, body audit redaction, and audited access.
- [x] Email approved send v1 verified: disabled by default, CRITICAL per-action approval only, Action Center reviewed drafts required, direct broker sends without verified Action Center action ids rejected, submitted args match approved reviewed drafts, direct unreviewed sends blocked, no approval reuse, no bulk/background sends, attachments blocked in v1, editing invalidates approval, mock provider only, no credentials stored, no private Mail database scraping, no memory storage by default, and brokered audited execution.
- [x] Messages/text draft-only verified: disabled by default, HIGH risk, approval-required, no send/delete/move/archive/contact harvesting, no bulk history, no Messages database scraping, no Full Disk Access dependency, `messages.draft_from_text` workspace-only fallback, `UNTRUSTED_MESSAGE` labeling, no body memory storage by default, body audit redaction, and audited access.
- [x] Messages safe handoff v1 verified: no automatic send, no Messages database scraping, no Full Disk Access, workspace-only context reads, save/copy handoff through verified Action Center action ids, submitted save/copy args match approved previews, workspace-bounded saved drafts, clipboard copy explicit/audited, no memory storage by default, and automatic send deferred by decision record.
- [x] Messages draft-id handoff workflow verified: `messages draft`, `messages handoff`, draft-id save/copy approval flow, and `messages.draft_from_lead` keep drafts local to `./workspace/messaging/drafts`, create/save/copy only through ToolBroker/Action Center gates, and do not send.
- [x] Message Safety Policy and Action Center integration verified: `messaging draft-create` stores local workspace drafts only, `messaging create-send-action` queues CRITICAL exact-preview Action Center records only, draft edits invalidate approvals, approval reuse is disabled, untrusted content cannot create send actions without explicit user request, bulk recipients/attachments are rejected, audit/export records redact bodies/recipients, generic `messaging.send_approved` remains unregistered, and executable sends are adapter-specific plus default-disabled.
- [x] macOS approved iMessage send adapter v1 verified for disabled-by-default local gates: `messages.macos.send_approved` is CRITICAL, manifest-disabled by default, Action Center exact-preview gated, no-reuse, allowlist-gated, recent-live-probe-gated, rate-limited, rejects attachments and bulk/group recipients, does not read `~/Library/Messages`, does not require Full Disk Access, does not blind-click Send, and returns unsupported instead of fake success.
- [x] iOS user-confirmed compose bridge verified: `messaging ios-compose-payload` creates expiring local handoff payloads with nonce/integrity hash, approved-action payloads must match Action Center preview fingerprints, compose-only mode still requires user review in iOS compose UI, returned results are recorded locally, expired payloads cannot record sent/queued, and no silent-send tool or adapter is registered.
- [x] Incoming message strategy verified for manual/mock v1: `messages import --from-file`, `messages inbox list/show/draft-reply` stay workspace/manual/mock only, block private Messages database paths and outside-workspace files, label content `UNTRUSTED_MESSAGE`, create local drafts only, do not auto-reply or send, write no memory by default, and audit import/read/draft operations.
- [x] Lead Inbox and Lead Response Drafting verified: v1 uses mock/local lead records only, `leads list/classify/summarize/draft-response/suggest-followup/suggest-meeting` execute through ToolBroker, selected full reads are HIGH and disabled by default, lead content is `UNTRUSTED_MESSAGE`, classification/summarization create no actions, draft-response creates editable local `MessageDraft` records only, follow-up queues pending Action Center `tasks.create` records only, meeting suggestions read no calendar and create no event, no provider reads/sends/CRM sync/private Messages database access occur, no memory write occurs by default, and audit logs cover access/draft workflows.
- [x] Approved Lead Response Send verified: `leads create-send-action/send/handoff/mark-responded` are brokered or Action Center-routed, send proposals are CRITICAL exact-preview per-action with no approval reuse, draft edits invalidate approval, unsupported channels return fallbacks, iOS compose creates handoff payloads only, manual handoff creates save/copy actions only, macOS Messages remains disabled unless adapter gates pass, bulk/auto-send is forbidden, no memory write occurs by default, and audit logs cover lifecycle/failure/status events.
- [x] Apple Messages for Business provider stub verified: `apple-business doctor/status/mock-inbound/draft-response` execute through ToolBroker, provider remains disabled by default, no credentials are required or printed, mock inbound records map to local Lead Inbox files with `UNTRUSTED_MESSAGE` trust, draft responses create local `channel=apple_messages_for_business` MessageDrafts only, future send remains CRITICAL/default-disabled/per-action/no-reuse, no live provider call/webhook polling/personal iMessage automation/private Messages DB access/Full Disk Access/send/memory write occurs, and audit logs redact lead/message args.
- [x] Messaging Dogfood Suites verified: `messaging_core`, `messaging_handoff`, `messaging_ios_compose`, `messaging_macos_probe`, `messaging_send_dry_run`, and `lead_response` suites validate as default-disabled mock/fixture/preflight suites; `all_safe` excludes live sends; live-send probe is not included; dry-run suite commands do not execute real sends or access private Messages data.
- [x] Messaging / iMessage Track release gate verified: full tests, startup policy validation, capability manifest validation, command registry validation, focused docs/dogfood/prompt tests, safe messaging dogfood suites, dry-run send suite, macOS metadata probe, default-disabled send scan, private Messages database/Full Disk Access scan, bulk-send denial evidence, trust-label checks, and CRITICAL per-action/no-reuse checks passed locally; no live send was run and live delivery remains unavailable/unvalidated.
- [x] Browser selected URL and clipping v1 verified: canonical `browser.read_url`, `browser.summarize_url`, `browser.clip_url_to_workspace`, and disabled `browser.selected_tab` capabilities are tracked; explicit URL workflows only, URL fetches through `web.fetch_url`, clips write through `filesystem.write` under `./workspace`, content labels are `UNTRUSTED_WEB`/`UNTRUSTED_DOCUMENT`, selected-tab native integration is stubbed/disabled, no browser history/cookies/sessions/passwords/profile databases are accessed, and audit logs cover fetch/write/stub paths.
- [x] Knowledge Capture v1 verified: captures store only under `./workspace/captures`, notes/files/URLs route through brokered tools, URL/file content remains untrusted data by default, trusted file labeling requires explicit `--trusted-user`, secrets are rejected before writes, Apple Notes/private app databases are not accessed, memory promotion goes through Memory v2 policy, and personal-looking content is not stored in memory by default.
- [x] Privacy Center v1 verified: status/inventory use metadata only, personal connectors are not read, exports redact secret-looking values, audit summaries omit raw args, delete-memory requires explicit confirmation, and confirmed deletion uses brokered `memory.clear` with audit logging.
- [x] Prompt Ledger and Prompt Queue tracking verified: ledger/queue/audit docs exist, prompt record directories and template exist, prompt CLI commands are tested, queued prompts have prompt IDs, project state records `active_prompt_id` and `next_prompt_id`, AGENTS requires prompt updates, and blocked send/overnight prompts remain gated.
- [x] Prompt Pack import/splitting verified: pack format docs/template exist, importer rejects `execute_all`, validate-pack writes no files, import stores packs under `prompts/packs`, split prompts go under `prompts/queued`, prompt bodies are preserved, dependencies/approval gates affect `prompts next`, and no imported prompt executes automatically.
- [x] PromptOps Workbench verified: one-command import from stdin/file/clipboard works, raw single prompts can be queued, runner is disabled by default, autopilot is safe-only and refuses HIGH/approval-gated prompts, reports redact secret-like values, and no imported prompt executes automatically.
- [x] Prompt Tracker Maturity release gate verified: PTM-01 through PTM-10 are complete with evidence, prompt pack parser preserves embedded delimiter examples, prompt status CLI includes search/evidence/recovery commands, one-active and failed-reason rules are enforced, prompt tracker dogfood/eval suites exist, and recovery is report-only.
- [x] Canonical Runtime Gateway Hardening release gate verified: CANON-01 through CANON-10 plus EXTREV-01 completed locally, full suite passed with 1723 tests, focused release-gate tests passed with 58 tests, command registry validation passed with 589 commands, startup policy and capability manifest validation passed, all_safe dogfood dry-run passed, and the track remains metadata/read-only/dry-run scoped with no server, background service, runtime rewrite, personal-data enablement, send/write enablement, live provider call, package install, model download, commit, or push.
- [x] Command Registry + Manual QA verified: command registry/test matrix/legacy/runbook docs and templates exist, 150 commands are cataloged, command metadata validation passes, README and AGENTS reference the registry, and `commands qa-run` prints SAFE/LOW examples without executing commands.
- [x] Live Session Logging and Replay verified: session start/status/run/end/list/show/replay/export commands work locally, output is redacted by default, raw session reports are gitignored, environment variables are not captured, and delegated commands retain their existing ToolBroker/policy/approval/audit behavior.
- [x] Session Review and Bug Generator verified: reviews use redacted session previews and feedback, generate local redacted review reports, optionally create stable local bug records, classify policy/ToolBroker/PolicyEngine/Approval/Audit bypass and personal-data leak signals as P0, write no memory, perform no auto-fixes, and expose `bugs list/show/export`.
- [x] Live Dogfood + Session Review release gate verified: synthetic session `sess_20260523T182508Z_abdad880` recorded a safe command, feedback attached to the command, session review generated a redacted summary, `BUG-0002` was created, `tests/regressions/test_bug_0002.py` was generated as a skipped redacted scaffold, quality dashboard reported the session/bug/regression, fake secrets were redacted, no personal-data connector was accessed, and no HIGH/CRITICAL action executed.
- [x] Live Test Runbook and Daily Dogfood Workflow verified: `dogfood plan/next/checklist` are planning-only, daily/weekly runbooks require session logs, feedback, session review, bug generation, and regression-test follow-up, and personal-data dogfood remains dry-run/mock by default.
- [x] Product Quality Dashboard verified: `quality status/sessions/bugs/regressions/features/next` are read-only, count open bugs by severity, prioritize P0/P1 bugs, redact session feedback, list live-validation/regression gaps, and do not execute tools or read personal connectors.
- [x] Scheduler / Automation v1 verified: schedule create/list/run/pause/delete work locally, v1 is manual-run only with no hidden persistence, scheduled tool workflows retain ToolBroker/policy/approval/audit gates, optional `backup_create` runs through redacted brokered `backup.create`, personal scheduled sections require approval, and CRITICAL actions do not execute automatically.
- [x] Reminders / Tasks connector v1 verified: provider access disabled by default, list HIGH approval-required, task writes CRITICAL per-action, `tasks.draft_create` brokered into Action Center only, no native Reminders scraping/Full Disk Access, no memory writes, and mock provider tests pass.
- [x] Controlled self-improvement commit action checkpoint verified: `improve create-action-for-commit` runs tests and diff through ToolBroker, queues only an Action Center commit action, blocks action creation when tests fail, and never commits without later approval.
- [x] Overnight self-improvement safe-mode runbook verified: allowed/forbidden work, stop conditions, morning review, report template, and `improve overnight-plan` are documented/tested; planner is read-only, excludes HIGH/CRITICAL and personal-data work, and the 6-hour run remains blocked pending explicit approval.
- [x] Controlled actions and workflow batch release gate passed: full tests, startup policy, capability manifest, command registry validation, safe eval, ToolBroker/default personal-data/CRITICAL approval scans, feature maturity sync, and prompt queue sync completed.
- [x] Native Skills Program foundation verified: required native-skill docs and template exist, external-skill supply-chain risks are documented, native skills are defined as reviewed local workflows mapped to existing ToolBroker capabilities, and no external skills were installed, imported, run, or executed.
- [x] Native skill finder verified: `skills find` executes through ToolBroker, searches only local reviewed manifests/tracking docs, audits files read, writes no memory, and does not browse/install/execute external skills.
- [x] Native skill compatibility matrix verified: `skills compatibility`, `skills compatibility <skill_id>`, and `skills platform matrix` read manifest/setup metadata only, report platform/runtime/setup support, import no native platform modules, execute no skills, call no providers, install no dependencies, enable no platform behavior, grant no permissions, and write no memory.
- [x] Native skill conflict detector verified: `skills conflicts`, `skills conflicts --json`, and `skills explain-conflict <conflict_id>` read manifest/precedence/setup/policy metadata only, report conflict evidence, do not auto-resolve, enable/disable skills, install dependencies, call providers, execute scripts, import plugin runtimes, grant permissions, or write memory.
- [x] Native skill test/dogfood harness verified: `skills test`, `skills test --all-safe`, `skills dogfood`, `eval run --native-skills`, and native skill dogfood suites use metadata/fixture checks only, skip HIGH/CRITICAL/FORBIDDEN and personal-data skills by default, report missing dependencies as setup skips, and do not execute external skills, install dependencies, call providers, run plugin runtimes, grant permissions, or write memory.
- [x] PDF workspace native skill verified: PDF commands execute through ToolBroker, stay within approved roots, label output `UNTRUSTED_DOCUMENT`, enforce limits, audit file reads, write no memory, and do not execute OCR or external binaries.
- [x] Release-gate sync verified before the next feature set: full tests passed, startup policy and capability manifest validation passed, command registry validation passed, native skill validation passed, prompt audit passed, personal-data defaults remain disabled, HIGH capabilities require approval, CRITICAL capabilities require per-action approval with no reuse, and Native Skills / Next Product Feature / Overnight Self-Improvement tracks are documented.
- [x] Golden Eval Suite verified: data-backed router, policy, ToolBroker, prompt-injection, and workflow dry-run checks pass locally; scorecards are written; personal-data evals are skipped by default; live LM Studio/provider checks remain opt-in.
- [x] Model-router benchmark and prompt quality evals verified: fixture-backed route/tool/policy comparisons pass locally, no-tool prompts attach no tools, personal-data/send requests do not auto-execute, prompt-injection wrappers are checked, reports are written, and live answer-quality smoke remains opt-in.
- [x] Agent Runtime Orchestration v1 verified: runtime status/doctor do not call LM Studio, execute tools, read personal data, or start background persistence; service registry is lazy; risky feature flags are disabled by default; CRITICAL workflow jobs are blocked; scheduler blocks CRITICAL/background runs; untrusted events and frontend bridge requests cannot approve, execute tools, or change policy.
- [x] Agent Runtime Orchestration recovery verified: ORCH-01 through ORCH-10 completed prompt files exist, stale queued ORCH ledger rows were corrected, runtime tests passed, safe eval passed, full suite passed, and no partial ORCH prompt remained.
- [x] Agent DNA / Cloneability verified: Agent DNA, architecture principles, clone blueprint, migration guides, rewrite checklist, build history, provenance, decision index, reconstructed prompt-pack archive, README/SPEC/SDLC/AGENTS alignment, and cloneability release-gate docs exist; reconstructed packs are clearly labeled and not treated as exact originals unless source evidence proves it.
- [x] Full release gate and feature maturity review verified: full tests passed, startup policy and capability manifest validation passed, safe eval passed with live/personal skips documented, command registry and native skill validation passed, unknown tools are denied through ToolBroker, personal-data capabilities remain disabled by default, HIGH actions require approval, CRITICAL actions require per-action approval with no reuse, and conservative maturity/next-work candidates are documented.
- [x] Chinese forum discovery/search layer verified locally: `cn-forums` commands route through ToolBroker, use approved site-filter search and safe selected URL fetch only, report login/CAPTCHA/blocked pages unavailable, label fetched content `UNTRUSTED_WEB`, use optional local translation, write no memory/search history, use no cookies/browser sessions, and keep platform-specific scraping/CAPTCHA bypass forbidden.
- [x] Docs updated.
- [x] `docs/COMPLETION_REPORT.md` updated.
- [x] Next milestone identified.
- [x] Approval gates checked.

## Release Gate Result

M0-M11 implementation tests pass locally. The post-connector release gate passed locally on 2026-05-22 after the Weather connector pattern, feature maturity tracking, weather-only daily briefing, NWS path, safe Weather preferences, weather-aware web research, WeatherKit stub work, and the personal connector readiness gate.

The controlled actions and workflow batch release gate passed locally on 2026-05-23 after Action Center, approved-write stubs, tasks, messages handoff, browser clipping, knowledge capture, Daily Briefing v2, Meeting Follow-Up, Task Extraction, controlled self-improvement loop, PromptOps, Command Registry, and Scheduler v1 work. Full tests, startup policy, manifest validation, command registry validation, safe eval, default personal-data, CRITICAL approval, and ToolBroker path scans passed.

Real-world release/use still requires human review of disabled-by-default personal and write/send capabilities before enabling connectors. Live LM Studio, weather provider, and web-provider smoke tests should be run with the user's configured local services and keys before broader use.
