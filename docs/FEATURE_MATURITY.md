# Feature Maturity

This file tracks how developed, tested, safe, documented, and production-ready each feature is.

It is not enough to mark a feature as done. A feature can be implemented but still immature, under-tested, undocumented, unvalidated against live systems, or missing UX polish.

For the current short tracker summary, see `docs/TRACKER_DASHBOARD.md`. For tracker ownership and source-of-truth hierarchy, see `docs/TRACKER_INDEX.md`.

Codex and any coding agent must update this file whenever a feature, connector, workflow, command, policy, approval, audit, memory, or UI surface is added or changed.

This file complements:

- `docs/PROJECT_STATE.md`
- `docs/FEATURE_REGISTRY.md`
- `docs/FEATURE_ROADMAP.md`
- `CHANGELOG.md`
- `docs/COMPLETION_REPORT.md`
- `docs/RELEASE_CHECKLIST.md`

## Maturity Levels

| Level | Name | Meaning |
|---:|---|---|
| 0 | Idea | Mentioned or proposed, but not specified. |
| 1 | Specified | Requirements, non-goals, risks, and acceptance criteria are written. |
| 2 | Scaffolded | Basic files/classes/commands exist, but behavior is incomplete or stubbed. |
| 3 | Implemented | Core behavior works in code, but tests/docs/hardening may be incomplete. |
| 4 | Tested | Unit/integration/security/denial tests exist and pass. |
| 5 | Hardened | Policy, approval, audit, failure modes, prompt injection, and edge cases are covered for the feature's risk. |
| 6 | Live-Validated | Tested against real LM Studio/provider/local runtime conditions. |
| 7 | User-Ready | Clear UX, setup docs, diagnostics, known limitations, and release gate are in place. |
| 8 | Mature Pattern | Reusable as a pattern for future modules/connectors/workflows. |

## Maturity Signals

Prompt count is useful context but is not proof of maturity.

| Signal | Description |
|---|---|
| Prompt / iteration count | Number of focused implementation, polish, or hardening passes spent on the feature. |
| Prompt evidence fields | Track `prompt_count`, `last_prompt_id`, `last_prompt_batch`, implementation/hardening/dogfood/release-gate prompt counts, and `prompt_evidence_status` when a feature is changed by prompt-driven work. |
| Design completeness | Scope, non-goals, requirements, risks, and acceptance criteria are documented. |
| Implementation completeness | The feature works, is partial, or remains stubbed. |
| Test coverage | Success, error, denial, approval, security, and edge cases are tested. |
| Policy integration | ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger are enforced where applicable. |
| Approval integration | Approval behavior matches the feature risk level and non-interactive behavior is safe. |
| Audit coverage | Allows, denials, approvals, executions, failures, dry-runs, files, domains, and commands are logged where relevant. |
| Security hardening | Prompt injection, secrets, path traversal, personal data, unsafe writes, cache retention, and failure modes are considered. |
| Live validation | The feature has been smoke-tested against real LM Studio/provider/local runtime conditions. |
| UX/docs readiness | The user has clear commands, setup docs, diagnostics, limitations, and release gate notes. |
| Known limitations | Remaining risks or missing validation are named. |
| Next work needed | The next useful hardening, live validation, or feature pass is explicit. |

## Readiness Score

Use the maturity level as the main label. Use the readiness score as a secondary, approximate signal.

| Category | Max Points |
|---|---:|
| Spec and design | 10 |
| Implementation | 15 |
| Tests | 20 |
| Policy, approval, and audit integration | 20 |
| Security and threat hardening | 15 |
| Live validation | 10 |
| UX, docs, and diagnostics | 10 |
| Total | 100 |

| Score | Readiness |
|---:|---|
| 0-20 | Early idea or rough plan |
| 21-40 | Prototype |
| 41-60 | Implemented but needs hardening |
| 61-75 | Tested and usable with caution |
| 76-90 | Hardened or near user-ready |
| 91-100 | Mature reusable pattern |

## Current Assessment

| Feature | Category | Maturity Level | Readiness Score | Prompt / Iteration Count | Design Completeness | Implementation Completeness | Test Coverage | Policy/Audit Status | Security Hardening Status | Live Validation Status | UX/Docs Status | Known Limitations | Next Work Needed |
|---|---|---:|---:|---:|---|---|---|---|---|---|---|---|---|
| Canonical runtime state model and source-of-truth hierarchy | Runtime / tracker reconciliation | 4 Tested | 76 | 3 | Complete for CANON-01, CANON-08, and CANON-09 scope: canonical state field list, source-of-truth hierarchy, read-only state boundaries, dashboard/handoff summaries, tracker-sync migration plan, ADR, and command docs | Implemented `agent.runtime.canonical_state`, `agent.runtime.canonical_dashboard`, `agent.runtime.tracker_sync`, JSON serialization, dirty-worktree summary counts, prompt/tracker metadata extraction, source-of-truth hierarchy, reconciliation preview conflict markers, dashboard/handoff aggregators, tracker-sync preview/conflict reports, and `runtime canonical-state/source-of-truth/reconcile-preview/canonical-dashboard/handoff/tracker-sync-preview/tracker-conflicts` CLI commands | `tests/runtime/test_canonical_runtime_state.py`, `tests/runtime/test_canonical_state_dashboard.py`, `tests/runtime/test_tracker_canonical_state_sync.py`, and runtime CLI tests cover serialization, hierarchy order, conflict markers, secret-like redaction, dashboard JSON, handoff Markdown, gateway/kernel/recovery summaries, tracker role policy, no-auto-overwrite flags, conflict reports, and CLI output; command registry validation passes | Status-only CLI metadata; no tools, providers, approvals, personal connectors, file writes, server startup, auto-resume, broad rewrites, or tracker mutations are executed; future mutating reconciliation remains out of scope | Secret-like values are redacted; personal-data tools remain disabled; output is local repo/tracker/runtime metadata only; sync policy requires small anchored edits with evidence | Local tests and CLI smokes only; CANON-10 local release gate passed; manual/live validation remains pending | Runtime docs, dashboard/handoff docs, tracker-sync docs, prompt tracker integration docs, ADR, command registry/test matrix, feature registry, changelog, and completion report updated | Does not fix conflicts automatically, write handoff files, persist dashboard state, overwrite markdown trackers, or replace prompt trackers | Canonical runtime release gate passed locally; next recommended prompt is clean release-candidate boundary and generated artifact hygiene |
| Durable job, workflow, prompt, command, QA, self-heal, and approval-gated resume record contracts | Runtime / recovery scaffolding | 4 Tested | 69 | 1 | Complete for CANON-02 scope: prompt pack, prompt, job, workflow, command, approval-gated resume, QA, self-heal, future media generation, and future secret scan record contracts | Implemented `agent.runtime.execution_records`, record/status enums, JSON serialization, redacted stable hashing, schema validation, empty-store tolerance, and `runtime records list/show/latest/validate` read-only commands | `tests/runtime/test_execution_records.py` covers serialization, redaction, HIGH approval requirement, validation failures, empty/latest/validate CLI, and unknown record lookup; command registry validation passes | Read-only metadata commands execute no jobs/workflows/prompts/commands and do not consume approvals; schema requires HIGH/CRITICAL approval and links future audit IDs | Raw secrets and personal data are forbidden by default; args are redacted and secret-like raw values are flagged by validation | Local tests and CLI smokes only; no real durable writer/resumer or release gate yet | Runtime record docs, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Contract only; no background persistence, executor, writer command, prompt runner, or resume implementation exists | Continue CANON-03 Agent Gateway / Runtime Kernel boundary |
| Agent Gateway / Runtime Kernel boundary | Runtime / frontend architecture | 4 Tested | 70 | 1 | Complete for CANON-03 scope: gateway responsibilities, kernel-owned execution truth, frontend/channel boundaries, gateway API contract, and ADR | Implemented `agent.runtime.gateway_state`, `agent.runtime.kernel_contract`, request/response envelopes, gateway status, kernel status, frontend contract, and `runtime gateway-status/kernel-status/frontend-contract` CLI commands | `tests/runtime/test_gateway_kernel_boundary.py` covers direct tool execution denial, approval review response, secret redaction, JSON status, no server/listener flags, CLI-first frontend, frontend self-approval denial, and no server module import | Gateway commands execute no tools/providers, start no server/listener, and cannot approve actions or mutate policy/capabilities; future execution remains ToolBroker/PolicyEngine/ApprovalManager/AuditLogger controlled | Gateway payloads are redacted; direct tool attempts are blocked; no external tools are exposed and CLI remains first frontend | Local tests and CLI smokes only; no real app/channel/server integration exists | Gateway/kernel boundary docs, ADR, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Metadata contract only; no Fastify/TypeScript gateway, local web server, native UI, channel listener, or external API exists | Continue CANON-04 resume, recovery, and checkpoint model |
| Resume, recovery, and checkpoint model | Runtime / recovery scaffolding | 4 Tested | 70 | 1 | Complete for CANON-04 scope: checkpoint fields, recovery report fields, preview-only recovery rules, approval gate preservation, and prompt tracker evidence requirement | Implemented `agent.runtime.checkpoints`, `agent.runtime.recovery`, checkpoint serialization/list/show helpers, recovery preview reports, and `runtime recovery-preview/checkpoints list/checkpoints show` CLI commands | `tests/runtime/test_resume_recovery_checkpoints.py` covers checkpoint serialization/redaction, CRITICAL safe-resume denial, checkpoint listing/showing, interrupted prompt-pack recovery preview, and CLI behavior; command registry validation passes | Preview commands execute no resume, rollback, prompt-pack step, tool, provider, or approval; CRITICAL resume requires fresh approval | Recovery output is redacted, avoids secrets/personal data, and lists unsafe actions to avoid | Local tests and CLI smokes only; no real resume/rollback runner or checkpoint writer exists | Recovery/checkpoint docs, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Contract/preview only; no automatic resume, no checkpoint writer command, no rollback executor | Continue CANON-05 Code Mode / self-heal artifact hash checks and safety lints |
| Code Mode / self-heal artifact hashes and safety lints | Runtime / self-improvement hardening | 4 Tested | 71 | 1 | Complete for CANON-05 scope: deterministic artifact-hash coverage, safe-only lint rules, code-mode truth boundary, docs, and command tracking | Implemented `agent.self_improvement.artifact_hashes`, `agent.self_improvement.safety_lints`, and inspection-only `improve artifact-hashes`, `improve lint-diff`, and `improve verify-artifacts` commands | `tests/test_self_improvement_artifact_hashes.py` and `tests/test_self_improvement_safety_lints.py` cover deterministic hashes, missing-file handling, command/capability hash coverage, required lint categories, blocker behavior, redacted evidence, and combined verification | Read-only inspection commands do not patch, execute self-heal, run providers, commit, or push; future patch/commit actions remain ToolBroker/PolicyEngine/ApprovalManager/AuditLogger governed | Raw diff evidence is omitted from lint findings, secret-like text is redacted before hashing, and high-risk findings block safe-only self-heal plans | Local tests and CLI hash smoke only; current dirty worktree can legitimately make `lint-diff`/`verify-artifacts` return blocked | Docs, command registry/test matrix, feature registry, roadmap, changelog, risk/threat notes, and completion report updated | Not an executor and not a proof of safe patch correctness; lints are heuristic and conservative | Continue CANON-06 surface regression lanes |
| Surface regression lanes | QA / release hardening | 4 Tested | 70 | 1 | Complete for CANON-06 scope: lane registry, docs, matrix, fixture-safe dogfood suites, CLI metadata commands, and default safety rules | Implemented `agent.qa.surface_lanes`, `qa surfaces`, `qa surfaces run --dry-run`, and `qa surfaces matrix`; added six surface dogfood suites | `tests/qa/test_surface_regression_lanes.py` and `tests/test_dogfood_suites.py` cover required surface coverage, safe defaults, dry-run no-execution behavior, JSON serialization, CLI output, and suite YAML validity | Read-only/dry-run metadata commands do not execute lane commands or consume approvals; future execution lanes must remain command-registry gated and safe-tier by default | Personal-data and HIGH/CRITICAL commands are excluded by default; live provider checks are opt-in; planned/stubbed surfaces are labeled as such | Local tests and CLI smokes only; dogfood suites are fixture-safe and not live validation | `docs/qa/SURFACE_REGRESSION_LANES.md`, `docs/qa/SURFACE_REGRESSION_MATRIX.md`, command registry/test matrix, dogfood suites, feature registry, roadmap, changelog, and completion report updated | V1 is a registry and dry-run preview, not an automated lane runner; live provider/manual lanes remain future opt-in work | Continue CANON-08 canonical state dashboard and docs |
| Performance Bottleneck Scanner and Optimization Advisor | Performance / release hardening | 5 Hardened | 82 | 11 | Complete for PERF-01 through PERF-11 scope: architecture/policy docs, model/report schema, static/startup/benchmark/test-profiler/recommendation/baseline/patch-plan/dashboard docs, local release-gate docs, maturity review, active/planned command rows, safety boundaries, and conservative acceptance criteria | Implemented for static heuristic scans, bounded startup/import timing scans, safe command-registry benchmarks, bounded pytest duration profiling, advisory optimization recommendations, redacted baselines/regression comparison, metadata-only patch planning, read-only dashboard/status/next/trends, models, stable finding IDs, redacted report store, structured no-report handling, Markdown/JSON report writes, brokered `perf scan`, `perf scan --static`, `perf scan --startup`, `perf startup`, `perf benchmark --safe / --group / --command`, `perf tests --durations`, `perf tests --target`, `perf tests report --last`, `perf suggest-fixes`, `perf baseline create`, `perf baseline compare`, `perf regressions`, `perf patch-plan`, `perf dashboard`, `perf status`, `perf next-fix`, `perf trends`, `perf report --last`, and `perf findings` | Focused performance tests cover docs, safety phrases, planned commands, model serialization, stable IDs, redaction, no-report state, latest findings, tool registration, default exclusions, heuristic detection, heavy startup import detection, report writes, no scan execution, bounded subprocess startup scanning, missing-command skips, timeout handling, startup tool registration, safe benchmark gating, HIGH/CRITICAL/placeholder denial, raw output omission, benchmark report writes, pytest duration parsing, full-suite opt-in denial, outside-target denial, profile report reads, test-profile tool registration, recommendation mapping/no-patch behavior, baseline creation, regression ranking, finding-count regression, no-report/no-baseline states, baseline tool registration, patch-plan allow/block behavior, recommendation filtering, no-recommendations state, applied_patches=0, patch-plan tool registration, dashboard read-only behavior, status/next/trends behavior, and dashboard tool registration; PERF-11 release gate refreshed local scan/benchmark/profile/baseline/dashboard evidence | Active scan/benchmark/test-profile/recommendation/baseline/regression/patch-plan/dashboard/report/finding commands route through ToolBroker/PolicyEngine/AuditLogger; HIGH/CRITICAL or mutating optimization remains future approval-gated work | Static scanner does not execute scanned code or import scanned modules; startup scanner uses bounded subprocesses only; benchmark runner only executes active SAFE/LOW read-only registry commands; test profiler only runs repo-local pytest targets and stores no raw stdout/stderr; recommendation engine is advisory only; baselines store redacted metadata only; patch planner applies zero patches; dashboards are read-only and execute no commands | Not live-validated; startup, benchmark, test-profile, recommendation, baseline, patch-plan, dashboard, and release-gate smokes are local and approximate | `docs/performance/*`, `docs/decisions/performance_bottleneck_scanner.md`, command registry/test matrix active/planned rows | Local release-gated and hardened for safe performance evidence, but not user-ready/live-validated; clean release-candidate review and manual optimization follow-through remain | Review static findings and run clean release-candidate boundary before choosing a tiny optimization |
| Creative Media Generation safety preflight, asset scaffold, ComfyUI stub, image/video/audio dry-run planners, creative workflow templates, TTS/voice stubs, media workflow/NL planning, dogfood/evals, and release gate | Creative media | 4 Tested | 77 | 12 | Complete for MEDIA-01 through MEDIA-12 scope: roadmap/risk/license/asset/safety policy docs, provider-neutral models, static/mock provider registry, bounded asset metadata manager, redaction helpers, deterministic prompt safety checker, license report, consent policy, disabled ComfyUI provider stub, workflow metadata stub, image/video/audio/TTS provider candidates, image/video/audio/music dry-run planners, TTS/voice category planner, voice consent policy metadata, media workflow target inference, NL `media.plan` routing hooks, mock image/video/audio metadata providers, creative workflow templates, thumbnail/social creative dry-run planner, mock workflow asset metadata helper, brokered metadata/preflight/dry-run CLI commands, media dogfood suites, fixture-backed `eval --media` checks, release-gate docs, and conservative maturity review | Metadata/preflight/stub/dry-run/dogfood/eval/release-gate scaffolding only; no runtime media generation, real image/video/audio/music/TTS generation/editing, model install, model download, paid API, upload/publish, voice cloning, public/private person voice imitation, real-person likeness workflow, provider SDK import, ComfyUI runtime/server start/workflow submission, external moderation API, personal-media/source-video/audio/voice input access, consent record storage, automatic NL execution, or CLI generation/editing asset write exists | `tests/test_creative_media_docs.py`, `tests/media/test_media_models_registry.py`, `tests/media/test_media_asset_manager.py`, `tests/media/test_media_safety_license.py`, `tests/media/test_comfyui_provider_stub.py`, `tests/media/test_image_generation_scaffolding.py`, `tests/media/test_creative_workflows.py`, `tests/media/test_video_generation_scaffolding.py`, `tests/media/test_audio_generation_scaffolding.py`, `tests/media/test_tts_strategy.py`, `tests/media/test_media_workflow_planner.py`, and `tests/media/test_media_dogfood_eval.py`; focused media/dogfood/eval/docs/command registry suite passed locally; full suite evidence recorded in completion report; CLI smokes for dogfood show and media eval commands passed locally | MEDIA-02 through MEDIA-12 commands route through ToolBroker/PolicyEngine where command tools exist; dogfood delegates to existing safe status/dry-run commands and eval checks are fixture/local only; NL routing only suggests dry-run/preflight plans; real future generation/editing/publishing actions must still be capability-manifested, brokered, approved by risk, safety-preflighted, resource/license-reviewed, and audited | Voice cloning, impersonation, and public/private person voice imitation are denied/deferred; user-owned voice requires a future explicit consent system; real-person likeness requires consent/human review; copyrighted/brand/commercial uncertainty requires license review; artist/track imitation is flagged; unsafe deterministic categories are denied; path traversal is blocked; raw prompts/text are redacted; workflow JSON is UNTRUSTED_DOCUMENT; custom nodes and model/provider downloads are supply-chain risks; image/video/audio/music/TTS/thumbnail commands require dry-run or metadata planning only; upload/publish and NL auto-execution remain unimplemented; dogfood/evals are mock/fixture-only | Not live-validated; no real provider, generated/edited media, ComfyUI server, image/video/audio/TTS model, external moderation service, user media input, or hardware/resource live validation exists | `docs/media/*`, `docs/media/providers/COMFYUI_PROVIDER.md`, `docs/media/providers/IMAGE_PROVIDER_CANDIDATES.md`, `docs/media/providers/VIDEO_PROVIDER_CANDIDATES.md`, `docs/media/providers/AUDIO_PROVIDER_CANDIDATES.md`, `docs/media/TTS_VOICE_GENERATION_STRATEGY.md`, `docs/media/VOICE_CONSENT_POLICY.md`, `docs/media/MEDIA_WORKFLOW_COMMANDS_AND_NL_ROUTING.md`, `docs/media/MEDIA_DOGFOOD_RUNBOOK.md`, `docs/media/CREATIVE_MEDIA_RELEASE_GATE.md`, `docs/media/CREATIVE_MEDIA_MATURITY_REVIEW.md`, creative media ADR, ComfyUI ADR, command registry/test matrix, capability manifest, roadmap, risk/threat updates | Tested local scaffold with mock/fixture dogfood/eval and release-gate coverage but not hardened/user-ready; no real provider execution, live validation, generated-output review, or manual media dogfood session yet | Next recommended feature track: reviewed provider implementation stub-to-live path, starting with an opt-in local provider strategy and no generation enabled by default |
| Source-of-truth, tracker, wiring, and release-boundary reconciliation | Productization / release readiness | 4 Tested | 74 | 1 | Complete for SOURCE-TRUTH-RECONCILE-01 scope, non-goals, source-of-truth hierarchy, prompt tracker reconciliation, feature/maturity/command/capability/docs/git hygiene reports, and clean release boundary plan | Implemented docs/tracker reconciliation only; no runtime feature, provider, connector, command, or safety behavior added | Command registry validation passed; policy-check passed; doctor passed; prompt audit passed during run; safe dogfood dry-run passed; full suite if practical recorded in completion report | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed; manifest/policy validation remains green | Secret/artifact scan was best-effort and redacted; no tracked `.env`/token/private-key/raw-session-log found by tracked-path check; clean commit review still required | Not live validation; release-boundary audit only | `docs/reconciliation/*`, project state, prompt trackers, feature registry, changelog, completion report updated | External secret scan not run; stale queued prompt file archival deferred; large dirty worktree remains; manual/live validation gaps remain | Run safe commit-boundary review or continue `news-provider-registry-status-commands` after user decision |
| Command QA sandbox and self-healing loop | Product quality / command QA | 5 Hardened | 79 | 10 | Complete for safe command QA policy, tiers, result schema, sandbox rules, self-heal policy, progressive QA policy, dogfood/eval, release gate, maturity review, and reusable frontend/backend service boundary | Implemented registry-backed planner, Tier 0/1 safe runner, Tier 3 disposable workspace support, redacted reports, result ranking, bug generation, regression scaffolds, conservative self-heal plans/reports, progressive dry-runs, service/API models, dashboard adapter, dogfood/evals | QA boundary tests 9 passed; QA tests 56 passed; command registry validation 522 commands; policy-check passed; previous full suite 1484 passed, 1 skipped; `eval run --command-qa` passed; dogfood dry-runs ok; Tier 0/Tier 1 QA runs passed | Does not bypass ToolBroker/PolicyEngine for command internals; QA service uses command registry safety metadata, denies high/critical/personal/default unsafe tiers, and keeps ApprovalManager expectations intact | No arbitrary shell, placeholder examples blocked, timeouts, redaction, bounded reports, repo-local sandbox, traversal protection, read-only dashboard methods do not execute commands, no commit/push/package install/scheduler/personal-data/send behavior | Local/mock/safe validation only; no human-supervised live command QA session yet | `docs/qa/*`, service/API boundary docs, command registry/test matrix, release gate, and maturity review complete | Historical "oldest untested" selection is basic; generated regression scaffolds require human conversion; self-heal v1 does not auto-apply patches; no GUI/web server exists | Run human-supervised command QA dogfood session and add historical run-age tracking |
| Natural-Language command understanding release gate | Core UX / command interpretation | 4 Tested | 74 | 1 | Complete for NLCMD-10 local release-gate evidence, maturity review, docs, and tracker updates | Added release-gate and maturity review docs with full test/policy/manifest/registry/eval/dogfood/exact-command evidence | Full suite passed with 1437 passed, 1 skipped; NL eval passed 15/0/5; dogfood core/risky passed 8/8 and 4/4; focused exact-command/NL tests passed | Validation confirms NL remains advisory and exact-command execution remains under existing ToolBroker/PolicyEngine/ApprovalManager/AuditLogger paths | No-tools preserved, risky/personal/HIGH/CRITICAL requests do not execute, command registry remains source of truth | Local validation only; no long real user/manual NL dogfood session yet | `docs/natural_language/NL_COMMAND_RELEASE_GATE.md`, `docs/natural_language/NL_COMMAND_MATURITY_REVIEW.md`, README, USER_GUIDE, release checklist, completion report updated | Not Live-Validated/User-Ready as an execution layer; docs validator command still unavailable | Run a manual NL dogfood session and continue release hardening |
| Natural-Language bug feedback loop | Core UX / command interpretation | 4 Tested | 73 | 1 | Complete for NLCMD-09 feedback tags, local bug creation, sanitized eval fixture generation, list command, docs, and command tracking | Added NL feedback tags, `feedback nl-bug`, `bugs create-nl-regression`, `nl regressions list`, `docs/templates/nl_regression_case_template.yaml`, and triage docs | Tests cover feedback tag acceptance, redacted bug creation, personal-data redaction, generated fixture loading in the eval suite, regression listing, and command registry entries | Feedback/bug writes are local redacted QA artifacts; no mapped command/tool/provider/approval execution is added | Generated fixtures default to `should_execute=false`; personal data and secrets are redacted; no memory write or provider call | Local unit/CLI-style tests only; no long manual dogfood feedback loop yet | `docs/natural_language/NL_BUG_TRIAGE.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Release gate still pending; generated regression fixtures require human review before promotion | Continue NLCMD-10 release gate |
| Natural-Language dogfood suites and session feedback integration | Core UX / command interpretation | 4 Tested | 72 | 1 | Complete for NLCMD-08 safe core and risky preflight-only dogfood suites, session runbook, command tracking, and suite validation | Added `dogfood_suites/natural_language_core.yaml`, `dogfood_suites/natural_language_risky.yaml`, command registry entries, and runbook docs | Tests cover suite loading, required phrases, risky preflight-only constraints, command registry entries, existing dogfood runner validation, and local suite runs | Dogfood delegates to existing `nl`/`nl preflight` commands; no mapped commands/tools/providers/approvals are executed by the suites themselves | Risky send/email/delete/prompt-pack phrases are preflight-only; suites require no personal data and write no memory; session logs are redacted artifacts only | Local dogfood runs passed; no long manual session review with user feedback yet | `docs/natural_language/NL_DOGFOOD_RUNBOOK.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Bug feedback loop and release gate still pending | Continue NLCMD-09 natural-language bug feedback loop |
| Natural-Language eval fixtures | Core UX / command interpretation | 4 Tested | 70 | 1 | Complete for NLCMD-07 fixture schema, 15-category coverage, eval command flags, report alias, docs, and no-execution checks | Implemented `eval_cases/natural_language/`, natural-language eval runner checks, `eval run/report --natural-language`, fixture tests, and docs | Tests cover fixture loading, safe cases, risky no-execution, ambiguous clarification, unsupported help/missing-intent, command registry entries, and focused NL regression suite; live eval command passed with 15 pass / 0 fail / 5 personal-data skips | Evals inspect parser/preflight metadata and do not execute mapped commands or call ToolBroker; future real execution remains exact-command plus ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | Risky personal/send/write fixtures verify no execution and approval metadata; unsupported/news/forum/workspace/prompt-tracker gaps are recorded as safe non-execution behavior | Local fixture eval only; no manual dogfood/session feedback yet | `docs/natural_language/NL_EVALS.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | No dogfood suite, feedback integration, bug loop, or release gate yet | Continue NLCMD-08 dogfood suite and session feedback integration |
| Natural-Language conversational CLI UX | Core UX / command interpretation | 4 Tested | 69 | 1 | Complete for NLCMD-06 exact-command preservation, `nl`/`ask` explanation surfaces, no-tools behavior, risky-path guidance, ambiguity/unknown guidance, docs, and help text | Implemented `agent.natural_language.cli_ux`, bare `nl "<request>"`, and `ask "<request>"`; no execution mode was added | Tests cover exact command dispatch still working, NL weather mapping, ask command-search mapping, risky send no-execution output, ambiguity clarification, no-tools preservation, and help text | UX does not execute, so it does not call ToolBroker; all real execution remains exact-command plus ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | Risky requests show approval/preflight path; ambiguous requests ask clarification; outputs state no commands executed; no LM Studio call is required | Local unit tests, CLI smokes, and full suite after fixes; no live/manual UX validation yet | `docs/natural_language/NL_CLI_UX.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | No natural-language eval fixtures, dogfood, bug feedback loop, or release gate yet | Continue NLCMD-07 natural-language eval fixtures |
| Natural-Language safe execution preflight | Core UX / command interpretation | 4 Tested | 68 | 1 | Complete for NLCMD-05 plan schema, preflight/explain/suggest commands, provider/setup reporting, audit preview metadata, and no-execution rules | Implemented `agent.natural_language.execution_plan`, `preflight`, and `nl preflight/explain/suggest`; no actual command execution or ToolBroker tool call is performed | Tests cover safe weather plan, web provider requirements, email send approval denial, file write approval/preview, unknown denial/help, no tool execution metadata, and explain/suggest outputs | Preflight does not execute, so it does not call ToolBroker; all future real execution remains exact-command plus ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | HIGH/CRITICAL, personal-data, write, unknown, ambiguous, missing-provider, deprecated, and stubbed plans are not marked safe; audit preview records no tools or commands executed | Local unit tests and CLI smokes only; no live/manual conversational UX validation | `docs/natural_language/NL_PREFLIGHT.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | No conversational CLI UX, eval fixtures, dogfood, bug loop, or release gate yet | Continue NLCMD-06 conversational CLI UX |
| Natural-Language clarification and confirmation flow | Core UX / command interpretation | 4 Tested | 67 | 1 | Complete for NLCMD-04 clarification categories, safe choices, command examples, provider/setup hints, deprecated/stubbed handling, and redacted previews | Implemented clarification builders and previews integrated into the deterministic router; no command execution, tool call, provider call, personal-data access, or memory write path exists | Tests cover missing location, multiple command matches, provider/setup hints, deprecated replacement suggestions, personal-data approval/setup requirements, risky send/write no-execution previews, and existing router regressions | Clarification/previews do not call ToolBroker because they are metadata-only; future execution remains ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | HIGH/CRITICAL send/write and personal-data requests ask for preflight/approval context and never become safe-to-run-directly; preview redacts secrets and states no commands executed | Local unit tests only; no live/manual conversational UX validation | `docs/natural_language/CLARIFICATION_FLOW.md`, feature registry, roadmap, changelog, and completion report updated | No safe execution planner, CLI natural-language UX, eval fixtures, dogfood, bug loop, or release gate yet | Continue NLCMD-05 safe execution planner and natural-language preflight |
| Natural-Language deterministic parser/router | Core UX / command interpretation | 4 Tested | 66 | 1 | Complete for NLCMD-03 deterministic parser/router scope, route decision schema, no-tools/exact-command handling, ambiguous clarification flag, and high-risk planning flags | Implemented `agent.natural_language` models, parser, safety mapping, and router decisions; no execution planner CLI or direct command execution path exists | Tests cover weather, vague web lookup clarification, memory command help, last-session bug suggestion, send email approval/preflight, ambiguous short requests, no-tools mode, exact command recognition, and high-risk no-execution | Parser/router do not call tools/providers or consume approvals; future execution remains ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | HIGH/CRITICAL send/write and personal-data requests are preflight/approval-only; no-tools disables suggestions; ambiguous requests ask clarification; exact commands are not rewritten | Local unit tests only; no live/manual conversational UX validation | `docs/natural_language/NL_PARSER_ROUTER.md`, feature registry, roadmap, changelog, and completion report updated | No clarification UX, execution planner, CLI natural-language entrypoint, eval fixtures, dogfood, bug loop, or release gate yet | Continue NLCMD-04 clarification and confirmation flow |
| Natural-Language Command registry intent index | Core UX / command interpretation | 4 Tested | 64 | 1 | Complete for NLCMD-02 metadata index scope, explicit aliases, deterministic fuzzy suggestions, safety fields, setup hints, and command registry docs | Implemented `agent.commands` intent index plus read-only `commands intents` and `commands suggest` metadata commands; no parser/router/preflight/execution path yet | Fixture-backed tests cover index build, active suggestion, deprecated non-primary handling, stub labeling, provider setup hints, and HIGH/CRITICAL dry-run/approval-only flags | Metadata commands do not execute tools or call providers; future runtime execution remains ToolBroker/PolicyEngine/ApprovalManager/AuditLogger-controlled | Planned/stubbed/deprecated commands are not primary; HIGH/CRITICAL/FORBIDDEN and approval-required commands are not `safe_to_run_directly`; no memory write, provider call, or command execution | Local tests and CLI smokes only; no live/manual NL UX validation | `docs/natural_language/COMMAND_INTENT_INDEX.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Suggestions are deterministic heuristics, not a full parser; no clarification flow, execution planner, eval suite, dogfood, or release gate yet | Continue NLCMD-03 natural-language request parser and deterministic router |
| Natural-Language Command Understanding architecture | Core UX / command interpretation | 1 Specified | 42 | 1 | Complete for NLCMD-01 track architecture, intent taxonomy, safety outcomes, no-silent-execution policy, and decision record | Documentation/specification only; no parser/router/index/preflight/CLI UX/eval/dogfood runtime exists yet | Docs regression test added for required docs, safety boundary names, taxonomy, and safety outcomes | No runtime path changed; docs require deterministic rules plus Command Registry metadata first and PolicyEngine/ToolBroker/ApprovalManager/AuditLogger as execution authorities | Explicitly forbids LLM-only safety routing, silent HIGH/CRITICAL execution, silent personal-data access, silent sends/writes/deletes, untrusted-content execution, and command registry bypass | Not applicable; docs-only milestone | Natural-language docs created under `docs/natural_language/` with decision record | No command intent index, parser/router, clarification flow, preflight, CLI UX, evals, dogfood, bug feedback loop, or release gate yet | Continue NLCMD-02 command registry intent index |
| Codebase Bug Review and Hardening pack | Release hardening / productization | 4 Tested | 75 | 8 | Complete for CODEBUG-01 through CODEBUG-08 review scope, non-goals, release-gate checks, static-scan notes, and bugfix evidence | One scoped CLI bug fixed; no major feature, connector, policy, approval, personal-data, send/write, package, commit, or push behavior added | Full suite 1387 passed / 1 skipped; safety-control tests 61 passed; command registry tests 6 passed; runtime/brain tests 148 passed; connector/workflow tests 440 passed; prompt/tracker/docs tests 66 passed; docs-focused tests 43 passed; safe eval 40 pass / 0 fail / 6 skipped; all_safe dogfood dry-run ok | No ToolBroker, PolicyEngine, ApprovalManager, PermissionManager, or AuditLogger behavior weakened; command registry validation and policy-check passed | Closed-pipe command traceback fixed; static scans found no scoped P0/P1 code bug to fix immediately; unresolved subprocess/deletion allowlist and clean-boundary issues are documented | Local/mock/safe validation only; no broad live/manual QA in this pass | Bug review docs under `docs/bugfix/`, changelog/tracker updates, and release-gate summary are in place | Large dirty worktree remains; dedicated docs validator command is missing; prompt audit may still need broader historical reconciliation; clean release-candidate boundary remains open | Run clean release-candidate boundary and prompt tracker reconciliation before broad feature expansion |
| Full feature status, maturity, and prompt-tracker audit | Productization / release readiness | 4 Tested | 72 | 1 | Complete for MATURITY-AUDIT-01 review scope, evidence labels, prompt tracker classifications, maturity scorecard, next maturity queue, feature expansion candidates, and manual validation plan | Documentation/review milestone only; no runtime behavior, connector, provider, command, or queued prompt execution added | `tests/test_productization_audit_docs.py`; command registry validation; startup/capability validation; full suite result recorded in completion report | No runtime ToolBroker/PolicyEngine path changed; command registry and policy validation were run as evidence checks | Explicitly calls out stale prompt tracker rows, large dirty worktree, generated artifact hygiene, mock-only/live validation gaps, and overclaim risks | Not live-validated; audit is local tracker/code/test evidence only | Productization docs created under `docs/productization/`; trackers updated conservatively | Does not fix all tracker disagreements or clean the release branch; it records them and queues follow-up maturity work | Run prompt tracker reconciliation and clean release-candidate boundary before broad feature expansion |
| Hermes-inspired safe autonomy architecture | Safe autonomy architecture | 1 Specified | 40 | 1 | Complete for HERMES-01 architecture, safe groundwork tracks, high-risk gates, risk model, unauthorized bypass policy, and future release-gate prerequisites | Documentation/policy only; no runtime gateway, Telegram/mobile, skill proposal, scheduler UX, subagent, sandbox, model switching, memory search, or continuity implementation added | `tests/test_hermes_safe_autonomy_docs.py` verifies docs and boundaries; prompt completion also runs startup/capability/command validation | No new execution path; future runtime work must route through ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | Explicitly forbids unattended high-risk workflows, arbitrary browser automation, background persistence, send/write behavior, personal-data tools, cloud private-data execution, subagent write permissions by default, and anti-bot/CAPTCHA/login/paywall bypass | Not applicable; docs-only | Decision record and autonomy docs created; trackers updated | Not implemented; no commands, runtime behavior, dogfood, or release gate yet | Continue HERMES-02 gateway/channel process scaffolding only if no stop condition is hit |
| Gateway and channel process scaffold | Safe autonomy channel foundation | 4 Tested | 66 | 1 | Complete for HERMES-02 channel models, registry, gateway guards, redaction, safe status commands, docs, and acceptance tests | Implemented lightweight `agent.channels` scaffolding and brokered metadata tools; no real Telegram/mobile/email/native app connector, send path, background worker, or runtime autonomy exists | `tests/channels/test_channel_gateway.py` covers registry load, unknown denial, mock request normalization, disabled remote channel rejection, direct-tool/self-approval guards, correlation requirement, recursive secret redaction, CLI list/status/show, capability validation, and command registry validation | Channel status commands execute through ToolBroker, PolicyEngine, and AuditLogger; gateway submissions require correlation ids and cannot execute tools or approve actions | Remote channels disabled by default; all channel content except CLI is `UNTRUSTED_MESSAGE`; no sends, personal-data reads, listener startup, background persistence, browser automation, channel history storage, or approval bypass | Local tests and CLI metadata smokes only; no live channel validation by design | `docs/channels/GATEWAY_CHANNEL_ARCHITECTURE.md`, `docs/channels/CHANNEL_SECURITY_MODEL.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for external channels; it is a safety scaffold only. Future Telegram/mobile/web/native channels need separate config, policy, audit, approval, dogfood, and release gates | Continue HERMES-03 Telegram/mobile access scaffolding only if it stays docs/stub-only and disabled by default |
| Telegram/mobile access scaffolding | Safe autonomy channel foundation | 4 Tested | 67 | 1 | Complete for Telegram/mobile status scope, safe config defaults, allowlist policy metadata, mobile pairing-status metadata, docs, commands, and acceptance tests | Implemented status/config helpers and brokered CLI diagnostics; no Telegram bot, polling loop, webhook server, Telegram API call, message read/send, mobile pairing, mobile approval executor, or background service exists | `tests/channels/test_telegram_mobile_scaffolding.py` and existing secret/config doctor tests cover missing token hints, token redaction, disabled send/polling/webhook defaults, chat allowlist requirement, mobile disabled defaults, no network-call flags, CLI commands, startup policy, and command registry validation | Telegram/mobile commands execute through ToolBroker, PolicyEngine, and AuditLogger; future mobile approvals must still use ApprovalManager and cannot self-approve | `TELEGRAM_ENABLED=false`, `TELEGRAM_ALLOW_SEND=false`, `TELEGRAM_ALLOW_POLLING=false`, `TELEGRAM_ALLOW_WEBHOOK=false`, `MOBILE_COMPANION_ENABLED=false`, and `MOBILE_APPROVALS_ENABLED=false`; no personal-data tools, send/write behavior, remote command execution, or background persistence | Local tests and status CLI smokes only; no live Telegram/mobile validation by design | `docs/channels/TELEGRAM_MOBILE_ACCESS.md`, `docs/channels/MOBILE_CHANNEL_SECURITY.md`, README, env example, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not a usable Telegram/mobile connector; it is status scaffolding only. Future live access needs explicit setup, auth/pairing, rate limits, allowlists, approval/audit tests, dogfood, and release gate | Continue HERMES-04 repeated-task skill creation proposals if it remains proposal-only and cannot create/enable/execute skills |
| Repeated-task skill creation proposals | Safe autonomy native skill planning | 4 Tested | 68 | 1 | Complete for HERMES-04 proposal-only engine, brokered CLI commands, redacted local report storage, docs, command registry, and tests | Implemented deterministic proposal generation from command metadata and redacted session metadata; proposals include risk/privacy/review/test/doc suggestions but never become enabled skills | `tests/autonomy/test_skill_proposals.py` covers repeated safe patterns, personal-data skips, high-risk review flags, required manifest metadata, dry-run approval side effects, broker/audit path, CLI list/show/approve, and command registry coverage | Proposal commands execute through ToolBroker, PolicyEngine, and AuditLogger; future skill implementation still requires separate skill vetting and safety gates | Personal-data patterns skipped by default; reports store redacted metadata only under gitignored `reports/autonomy/`; no raw sessions, command history, external scripts, package installs, providers, skill imports, skill enablement, or execution | Local tests and CLI smokes only; no live/manual proposal review yet | `docs/autonomy/SKILL_CREATION_FROM_REPEATED_TASKS.md`, `docs/native_skills/SKILL_PROPOSAL_PROCESS.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for automatic skill creation; this is a proposal/review aid only. Human review and a separate implementation prompt are required before any native skill can be created | Continue HERMES-05 skill improvement from experience if it remains proposal-only and cannot mutate or enable skills |
| Skill improvement from experience | Safe autonomy native skill planning | 4 Tested | 68 | 1 | Complete for HERMES-05 proposal-only engine, brokered CLI commands, redacted local report storage, docs, command registry, and tests | Implemented deterministic improvement proposal generation from redacted bug and dogfood evidence; proposals include expected files, tests, docs, lockfile impact, rollback, and review status but never apply patches | `tests/autonomy/test_skill_improvements.py` covers bug evidence, dogfood evidence, no-evidence low confidence, personal/unredacted evidence skips, required tests/rollback/lockfile metadata, high-risk review flags, broker/audit path, CLI list/show, and command registry coverage | Improvement proposal commands execute through ToolBroker, PolicyEngine, and AuditLogger; future skill edits still require a separate reviewed implementation prompt, vetting, and release gates | Personal-data and unredacted evidence skipped by default; reports store redacted metadata only under gitignored `reports/autonomy/`; no raw sessions/bugs, skill edits, lockfile updates, external scripts, package installs, skill imports, skill enablement, or execution | Local tests only; no live/manual improvement review yet | `docs/autonomy/SKILL_IMPROVEMENT_FROM_EXPERIENCE.md`, `docs/native_skills/SKILL_IMPROVEMENT_REVIEW.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for automatic skill improvement; this is a proposal/review aid only. Human review and a separate implementation prompt are required before any skill can be changed | Continue HERMES-06 scheduler UX only if it stays dry-run/manual-run and adds no background runner |
| Scheduler UX for safe automations | Safe autonomy scheduler foundation | 4 Tested | 67 | 1 | Complete for HERMES-06 scheduler UX templates, previews, dry-runs, risk explanations, review command, docs, command registry, and tests | Implemented metadata-only scheduler UX over existing manual-run scheduler; dry-run does not execute workflows/tools or create Action Center items | `tests/autonomy/test_scheduler_ux.py`, `tests/test_scheduler.py`, and `tests/runtime/test_scheduler_policy.py` cover template listing, risk/approval previews, no-tool dry-run, high-risk approval requirement, CRITICAL auto-run block, personal workflow disabled default, CLI commands, and command registry coverage | UX commands do not execute tools; existing manual scheduled tool workflows retain ToolBroker/PolicyEngine/AuditLogger behavior where tools are run by explicit user command | No cron, LaunchAgent, daemon, login item, hidden background runner, polling loop, unattended HIGH/CRITICAL action, personal-data default, send/write behavior, or Action Center item from dry-run | Local tests and CLI metadata only; no manual QA of schedule review UX yet | `docs/autonomy/SCHEDULER_UX.md`, `docs/autonomy/SCHEDULED_ACTION_GATES.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for background automation; scheduler remains manual-run only and UX metadata only | Continue HERMES-07 subagent isolation only if write permissions stay disabled by default |
| Subagent isolation groundwork | Safe autonomy subagent foundation | 4 Tested | 66 | 1 | Complete for HERMES-07 static profiles, isolation policy, mock dry-run, docs, command registry, and tests | Implemented mock-only subagent profiles and CLI diagnostics; no real subagent runner or parallel workflow exists | `tests/autonomy/test_subagent_isolation.py` covers no personal-data defaults, CRITICAL denial, write-disabled defaults, locked-down no-tool profile, researcher network config gate, coder ToolBroker-bypass denial, mock dry-run, CLI commands, and command registry coverage | Subagent commands execute no tools; future real subagents must route every tool request through ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | No personal-data access, write permissions, CRITICAL execution, direct tool calls, approval authority, memory write, background persistence, or real subagent launch | Local tests and CLI metadata only; no live/manual subagent execution by design | `docs/autonomy/SUBAGENT_ISOLATION.md`, `docs/autonomy/SUBAGENT_PROFILES.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for subagent execution; this is isolation metadata only | Continue HERMES-08 sandbox backend abstraction only if it remains metadata/policy and does not execute arbitrary scripts |
| Sandbox backend abstraction | Safe autonomy sandbox foundation | 4 Tested | 66 | 1 | Complete for HERMES-08 backend metadata, mock backend, policy validation, brokered dry-run commands, docs, command registry, and tests | Implemented mock-only sandbox abstraction and CLI diagnostics; no real sandbox process or executable backend exists | `tests/sandbox/test_sandbox_abstraction.py` covers mock backend dry-run, unknown backend denial, network/personal-data/arbitrary-command rejection, planned backend setup stub, workspace-root policy, safe defaults, tool registration, and CLI smoke | Sandbox commands execute through ToolBroker, PolicyEngine, and AuditLogger; future executable backends must keep this route and add approval gates for HIGH/CRITICAL requests | No arbitrary command execution, networked sandbox, Docker/VM/browser/cloud execution, broad filesystem roots, personal-data access, memory write, background persistence, plugin runtime, or real untrusted-script execution | Local tests and CLI metadata only; no live sandbox execution by design | `docs/autonomy/SANDBOX_BACKEND_ABSTRACTION.md`, `docs/autonomy/SANDBOX_POLICY.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for code execution; this is sandbox policy/preview metadata only | Continue HERMES-09 model switching/session continuity only if it remains metadata/dry-run and preserves LM Studio/default safety |
| Model switching and session continuity groundwork | Safe autonomy model/runtime continuity | 4 Tested | 66 | 1 | Complete for HERMES-09 model switch records, provider compatibility checks, rollback metadata, redacted continuity status/export/clear, docs, command registry, and tests | Implemented metadata-only switch previews and continuity helpers; no provider/default config persistence, model generation, tool execution, personal-data carryover, or persistent continuity store exists | `tests/autonomy/test_model_switching_continuity.py` covers dry-run compatibility, blocked persistent switch, unavailable providers, tool-call incompatibility, cloud/paid denial, context redaction, no personal-data carryover, rollback metadata, CLI commands, and command registry coverage; provider-router regression tests still pass | Metadata commands do not execute tools or models; future executable provider/model switch must route through ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | LM Studio remains default, non-dry-run switch requests are blocked, paid/cloud switch is policy-blocked by default, continuity is opt-in, redacted exports write no files, and no memory write or raw context/session transcript storage occurs | Local tests and CLI metadata only; no live alternate-provider switch or continuity-store validation by design | `docs/autonomy/MODEL_SWITCHING.md`, `docs/autonomy/CROSS_SESSION_CONTINUITY.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Not user-ready for real provider switching or cross-session memory continuity; this is safety metadata only | Continue HERMES-10 long-term memory search only if it stays policy-aware, opt-in, redacted, and does not inject personal memory by default |
| Long-term memory search and cross-session continuity | Safe autonomy memory continuity | 4 Tested | 68 | 1 | Complete for HERMES-10 redacted continuity status/build-summary/clear, context preview, docs, command registry, manifest, and tests | Implemented brokered memory continuity helpers over existing local memory; no semantic embeddings, cloud calls, automatic prompt injection, separate continuity profile persistence, or personal-data inclusion by default | `tests/memory/test_cross_session_continuity.py` covers safe project facts, secret rejection/redaction, personal-memory approval/exclusion, PII redaction in preview, token limits, deletion search behavior, continuity clear no-op, and command registry coverage; existing memory tests still pass | Commands execute through ToolBroker, PolicyEngine, and AuditLogger as `memory.continuity_*` and `memory.context_preview`; selected memory IDs are audited | Personal categories and personal trust levels are excluded by default; secret/email/phone-like values are redacted in continuity/preview output; no query history, memory write, cloud embedding, or untrusted-content-controlled injection is added | Local tests and CLI smokes only; no live/manual continuity UX validation yet | `docs/memory/LONG_TERM_MEMORY_SEARCH.md`, `docs/memory/CROSS_SESSION_CONTINUITY.md`, `docs/memory/MEMORY_INJECTION_POLICY.md`, README, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Not user-ready for automatic cross-session prompt injection or personal continuity; semantic search and persistent continuity profiles are deferred | Continue HERMES-11 authorized web automation boundary only if it remains policy/docs/stubs and implements no browser automation or anti-bot bypass |
| Authorized web automation boundary | Safe autonomy web policy | 1 Specified | 42 | 1 | Complete for HERMES-11 policy boundary, blocked-source behavior, first-party testing rules, future deep-scan gates, planned command tracking, and docs tests | Documentation and planned/stub command tracking only; no browser automation, deep scan runtime, bypass code, or selected-page automation implementation added | `tests/web/test_authorized_web_automation_policy.py` verifies required docs, forbidden bypass categories, allowed authorized paths, blocked/unavailable behavior, deep-scan gates, and conservative planned command registry rows | No runtime path yet; future deep scan/browser automation must route through ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | CAPTCHA, Cloudflare/anti-bot, proxy/rate-limit evasion, login-wall/paywall bypass, cookie/session scraping, human impersonation, and stealth browser automation are explicitly forbidden for third-party or unauthorized sites | Not applicable; docs-only and planned commands only | `docs/decisions/authorized_web_automation_boundary.md`, `docs/web/AUTHORIZED_WEB_AUTOMATION_POLICY.md`, `docs/web/BLOCKED_SOURCE_AND_BYPASS_POLICY.md`, `docs/web/FIRST_PARTY_TESTING_POLICY.md`, `docs/web/DEEP_SCAN_POLICY.md`, command registry/test matrix, feature registry, roadmap, changelog, and completion report updated | Not implemented and not user-ready; future deep scan needs approval gates, source-map implementation, fixtures, dogfood/evals, and release gate | Continue HERMES-12 safe autonomy dogfood/eval suite if it remains mock/fixture-first and does not run live risky autonomy |
| Safe autonomy dogfood/eval suite | Safe autonomy validation | 4 Tested | 68 | 1 | Complete for HERMES-12 mock-first safe autonomy dogfood suites, fixture eval cases, runbook, command tracking, docs, and tests | Added safe autonomy dogfood suites and fixture-backed eval category/CLI flags; no live risky autonomy, external channel connection, bot polling, webhook/listener, background persistence, real subagent, executable sandbox, browser automation, paid/cloud provider, or HIGH/CRITICAL action execution exists | `tests/autonomy/test_safe_autonomy_dogfood_eval.py`, `tests/test_eval_harness.py`, and `tests/test_dogfood_suites.py` cover YAML validity, safe suite constraints, fixture checks, eval CLI flags, command registry rows, no personal data, no external scripts, no network requirement, and no high/critical execution | Dogfood delegates only to existing CLI commands; fixture evals do not call providers and do not execute tools directly. Existing commands retain ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger boundaries | Suites and evals are mock/fixture-first; no personal data, raw session text, search history, memory write, live network dependency, bypass behavior, external scripts, package installs, background runner, or risky autonomous execution are added | Local tests, dry-run dogfood, and fixture eval only; no manual session or live provider validation yet | `docs/autonomy/SAFE_AUTONOMY_DOGFOOD_RUNBOOK.md`, dogfood suites, `eval_cases/safe_autonomy/`, command registry/test matrix, test plan, feature registry, roadmap, changelog, project state, and completion report updated | Not a release gate and not proof of live user readiness; HERMES-13 must still run the conservative release gate and maturity review | Continue HERMES-13 release gate and maturity review with validation only |
| Hermes-inspired safe autonomy release gate | Safe autonomy validation | 4 Tested | 70 | 1 | Complete for HERMES-13 validation, maturity review, release-gate docs, tracker reconciliation, and conservative readiness decision | Validation/docs only; no new runtime autonomy, connector behavior, background service, browser automation, subagent execution, sandbox execution, or send/write capability added | Full suite rerun after one docs-link fix, focused HERMES tests, safe autonomy eval, dogfood dry-run, command registry validation, startup policy validation, and capability manifest validation recorded in release-gate docs | Release gate validates existing brokered/metadata/dry-run/fixture paths and adds no new execution path; future runtime autonomy must still use ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | High-risk autonomy remains forbidden/deferred: no hidden persistence, unattended HIGH/CRITICAL actions, messaging sends, subagent writes, browser automation, anti-bot/login/paywall bypass, cloud/private-data execution, or executable sandbox behavior | Local tests and fixture/dry-run validation only; no live/manual autonomy validation | `docs/autonomy/HERMES_INSPIRED_RELEASE_GATE.md`, `docs/autonomy/HERMES_INSPIRED_MATURITY_REVIEW.md`, release checklist, feature registry, roadmap, changelog, project state, and completion report updated | Safe autonomy groundwork is ready for future scoped implementation prompts but not user-ready autonomous operation | Return to standing queue or run another hardening pass before implementing high-risk autonomy |
| Brain Runtime Independence release gate | Core runtime architecture | 4 Tested | 74 | 1 | Complete for local release-gate review of BrainProvider interface, model registry, LM Studio provider, disabled local-provider scaffolds, health/benchmark/evals, fallback/router, MCP stubs, and safety defaults | Release-gate docs and maturity review complete; no new provider implementation or gateway wiring added | BRAIN/MCP focused tests passed, command registry validation passed, startup policy and capability manifest validation passed, safe benchmark/eval smokes passed, provider/MCP diagnostics passed, and optional import guard passed | No new execution path; existing ToolBroker/PolicyEngine/ApprovalManager/AuditLogger semantics remain unchanged. Providers do not execute tools, fallback is disabled by default, and MCP exposes no tools | No default-provider switch, model/runtime install, model download, paid/cloud fallback, MCP server/listener, personal-data enablement, memory write, or optional runtime import at `smart_agent` startup | Local tests and LM Studio health diagnostic only; alternate providers are not live-validated | `docs/brain/BRAIN_RUNTIME_RELEASE_GATE.md`, `docs/brain/BRAIN_RUNTIME_MATURITY_REVIEW.md`, README, command registry/test matrix, feature registry, roadmap, risk register, threat model, release checklist, project state, and completion report updated | LM Studio dependency is partially optional, not fully removed; normal live chat still uses the existing LM Studio-compatible path | Future approved Brain Runtime Gateway wiring prompt with no-tools/tool-call regression tests and live opt-in provider validation |
| MCP interop decision and adapter stubs | Core runtime / MCP interop | 4 Tested | 66 | 1 | Complete for decision record, explicit MCP-not-brain-runtime docs, adapter boundary docs, disabled config, and server/client stubs | Stub-only; no MCP server, client connection, package dependency, listener, tool exposure, external tool execution, or brain-runtime dependency exists | `tests/mcp/test_mcp_adapter_stubs.py` covers disabled defaults, no server startup, ToolBroker-required external tool-call denial, no personal tools exposed, safe config defaults, no MCP package import/server start, CLI smokes, and command registry rows | MCP stubs cannot execute tools; future MCP server/client paths must route through ToolBroker/PolicyEngine/PermissionManager/ApprovalManager/AuditLogger | Disabled by default; no network listener, external connection, tool exposure, personal tool exposure, package install, MCP server enablement, or memory write | Local stub tests and CLI smokes only; no live MCP server/client validation by design | `docs/decisions/mcp_interop_strategy.md`, `docs/brain/MCP_IS_NOT_THE_BRAIN_RUNTIME.md`, `docs/mcp/MCP_ADAPTER_BOUNDARIES.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | MCP interop is not usable yet; it is only a safety boundary and diagnostic stub | Future MCP runtime work requires a separate approval-gated prompt; keep MCP disabled by default |
| Brain provider fallback and model router | Core runtime architecture | 4 Tested | 69 | 1 | Complete for metadata-only provider routing, fallback policy, dry-run switch decisions, cloud fallback denial, and tool-call support gating | Implemented route/fallback helpers and CLI explanation commands; no persisted provider switch, live gateway integration, paid/cloud provider, or automatic failure switch exists | `tests/brain/test_provider_router_fallback.py` covers explicit provider selection, fallback disabled, fallback enabled to next healthy provider, tool-call support gating, no-tools allowance, cloud fallback block, decision metadata, CLI smokes, and command registry rows | Route decisions do not execute tools, call models, alter ToolBroker, change PolicyEngine, weaken ApprovalManager, or persist audit; they return `audit_event` metadata for future approved persistence | Fallback disabled by default; cloud fallback disabled; auto-switch disabled; max attempts bounded; `brain switch` is dry-run only; route command redacts long messages and stores no history | Local mocked tests and CLI smokes only; no live provider fallback/gateway validation | `docs/brain/PROVIDER_FALLBACK_POLICY.md`, `docs/brain/MODEL_ROUTING_POLICY.md`, README, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is still partial: normal chat still uses LM Studio/default path; router decisions are not wired into live chat gateway | Use BRAIN-11 release-gate evidence, then plan gateway wiring only under a separate approved prompt |
| Brain model health, benchmark, and quality evals | Core runtime validation | 4 Tested | 68 | 1 | Complete for mock-first health, benchmark, eval, report writing, safe fixture cases, and command docs | Safe mock path implemented; live provider evals are skipped unless future opt-in behavior is approved/configured; no model router/fallback or default-provider change | `tests/brain/test_brain_health_benchmark_eval.py` covers health report, mock benchmark, eval report generation, unavailable provider skip, no-tools eval, safe tool-call eval, and CLI benchmark/eval/report; broader brain tests and command registry validation pass | Eval/benchmark code does not execute tools by default and uses mock safe tool-call data only; future live provider runs must remain explicit and ToolBroker-safe if tool execution is ever introduced | Safe path uses mocks, no personal data, no high-risk tools, no paid/cloud provider default, no model downloads, no runtime installs, no provider starts, no memory writes, and redacted local reports under `reports/brain` | Local mock tests and CLI smokes only; no live provider quality baseline yet | `docs/brain/BRAIN_EVALS.md`, `docs/brain/BRAIN_BENCHMARKS.md`, README, eval cases, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Reports are local maturity evidence, not proof of user-ready quality for unconfigured providers | Use BRAIN-11 release-gate evidence, then add live provider QA only under explicit opt-in config |
| MLX BrainProvider strategy/stub | Core runtime architecture | 4 Tested | 66 | 1 | Complete for disabled-by-default experimental MLX provider stub, safe config defaults, Apple Silicon detection metadata, provider-specific diagnostics, and docs | Stub only; no MLX install, model download, server call, native import, model load, text generation, default-provider change, fallback router, streaming implementation, or verified tool-call support by default | `tests/brain/test_mlx_provider_stub.py` covers registry listing, disabled default, setup hints, no native import/model load, mocked Apple Silicon detection, model metadata, and provider-specific doctor/health CLI | Provider returns setup errors only and does not execute tools; status/health commands do not bypass ToolBroker semantics or change PolicyEngine/ApprovalManager/AuditLogger behavior | Provider is disabled by default; no optional dependency install, model download, startup import of `mlx`, server start/call, model load during health/status, paid/cloud call, MCP enablement, personal-data access, memory write, or unverified tool-call support added | Local mocked tests and disabled-provider CLI smokes only; live MLX/Apple Silicon model validation remains future work | `docs/brain/providers/mlx.md`, `docs/decisions/mlx_provider_strategy.md`, README, `.env.example`, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is partial: LM Studio remains default; MLX provider cannot generate text yet | Use BRAIN-11 release-gate evidence; real MLX implementation remains a future approved prompt |
| llama-cpp-python in-process BrainProvider | Core runtime architecture | 4 Tested | 68 | 1 | Complete for optional disabled-by-default in-process provider scaffold, safe config defaults, dependency detection without core-startup import, lazy explicit model loading, fake-backend chat, provider-specific diagnostics, and docs | No real model load in tests, no package install, no model download, no health/status model load, no default-provider change, no fallback router, no streaming implementation, and no verified tool-call support by default | `tests/brain/test_llama_cpp_inprocess_provider.py` covers missing dependency setup hints, missing model path setup hints, lazy import behavior, no model load on provider status, fake-backend chat, normalized provider error, registry disabled status, provider-specific doctor/health CLI, and tool-call unsupported fail-closed behavior | Provider returns model/tool-call data only and does not execute tools; status/health commands do not bypass ToolBroker semantics or change PolicyEngine/ApprovalManager/AuditLogger behavior | Provider is disabled by default; no optional dependency install, model download, startup import of `llama_cpp`, model load during health/status, paid/cloud call, MCP enablement, personal-data access, memory write, or unverified tool-call support added | Local mocked tests and disabled-provider CLI smokes only; live in-process model validation remains opt-in and setup-dependent | `docs/brain/providers/llama_cpp_inprocess.md`, README, `.env.example`, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is partial: LM Studio remains default; in-process provider has weaker process isolation and no live GGUF validation yet | Run BRAIN-08 model health, benchmark, and quality evals next |
| Ollama BrainProvider | Core runtime architecture | 4 Tested | 68 | 1 | Complete for disabled-by-default Ollama provider scaffold, safe config defaults, lazy disabled registry status, mocked OpenAI-compatible chat, native `/api/tags` health, explicit tool-call support flag, and provider docs | No live Ollama validation, install/model pull/daemon startup, default-provider change, fallback router, streaming implementation, native `/api/chat` implementation, or verified tool-call support by default | `tests/brain/test_ollama_provider.py` covers missing model setup hint, mocked health success, daemon unavailable, no-tool chat, malformed response, tool-call unsupported fail-closed behavior, registry disabled status, and provider-specific doctor/health CLI | Provider returns model/tool-call data only and does not execute tools; status/health commands do not bypass ToolBroker semantics or change PolicyEngine/ApprovalManager/AuditLogger behavior | Provider is disabled by default; no model/runtime install, model pull/download, daemon startup, paid/cloud call, MCP enablement, personal-data access, memory write, or unverified tool-call support added | Local mocked tests and disabled-provider CLI smokes only; live Ollama validation remains opt-in | `docs/brain/providers/ollama.md`, README, `.env.example`, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is partial: LM Studio remains default; provider fallback/router and live local runtime validation remain future work | Use BRAIN-11 release-gate evidence; live provider validation remains opt-in |
| llama.cpp server BrainProvider | Core runtime architecture | 4 Tested | 68 | 2 | Complete for disabled-by-default OpenAI-compatible llama.cpp server provider scaffold, safe config defaults, lazy disabled registry status, mocked chat/health, explicit tool-call support flag, and provider docs | No live llama.cpp validation, install/download/startup, default-provider change, fallback router, streaming implementation, or verified tool-call support by default | `tests/brain/test_llama_cpp_server_provider.py` covers disabled setup hints, mocked health success, server unavailable, no-tool chat, malformed response, tool-call unsupported fail-closed behavior, registry disabled status, provider-specific doctor/health CLI, and unknown provider health | Provider returns model/tool-call data only and does not execute tools; status/health commands do not bypass ToolBroker semantics or change PolicyEngine/ApprovalManager/AuditLogger behavior | Provider is disabled by default; no model/runtime install, model download, server startup, paid/cloud call, MCP enablement, personal-data access, memory write, or unverified tool-call support added | Local mocked tests and disabled-provider CLI smokes only; live llama.cpp server validation remains opt-in | `docs/brain/providers/llama_cpp_server.md`, README, `.env.example`, command registry/test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is partial: LM Studio remains default; provider fallback/router and live local runtime validation remain future work | Use BRAIN-11 release-gate evidence; live provider validation remains opt-in |
| LM Studio BrainProvider refactor | Core runtime architecture | 4 Tested | 72 | 2 | Complete for `LMStudioBrainProvider`, lazy LM Studio default registration, provider-neutral response handling in `Orchestrator`, metadata-only brain providers/status/doctor commands, and LM Studio compatibility docs | No fallback router, benchmark/eval command implementation, MCP adapter, default switch away from LM Studio, or live LM Studio validation in this prompt | `tests/brain/test_lmstudio_provider.py`, `tests/test_lmstudio_loop.py`, and brain/docs command tests cover request format, missing model errors, unavailable server normalization, no-tools through provider, tool-call normalization, malformed argument preservation, registry listing, doctor/status commands, command registry validation, and startup policy/capability validation | Existing tool calls still flow through ToolBroker; provider returns tool-call data only and does not execute tools; no PolicyEngine/ApprovalManager/AuditLogger semantics are weakened | LM Studio remains default; existing `LMStudioClient` remains; no model/runtime install, model download, paid/cloud call, MCP enablement, network listener, startup-heavy optional import, personal-data access, memory write, or new tool execution semantic added | CLI status/doctor smokes passed locally without model generation; live LM Studio chat smoke remains opt-in | `docs/brain/LM_STUDIO_DECOUPLING_PLAN.md`, `BRAIN_PROVIDER_INTERFACE.md`, `MODEL_REGISTRY.md`, README, COMMAND_REGISTRY, command test matrix, feature registry, roadmap, changelog, project state, and completion report updated | Runtime independence is partial: LM Studio is provider-wrapped and llama.cpp server/Ollama/in-process/MLX scaffolds exist, but fallback/router remains future work | Use BRAIN-11 release-gate evidence; live provider validation remains opt-in |
| BrainProvider interface and model registry | Core runtime architecture | 4 Tested | 66 | 1 | Complete for provider-neutral interface/models, lazy registry metadata/status, safe config defaults, normalized provider errors, and deterministic mock provider | Fallback router, benchmark/eval commands, MCP adapter, and default-provider switch do not exist yet | `tests/brain/test_brain_provider_interface.py` and `tests/brain/test_model_registry.py` cover mock provider contract, no-tool chat, provider-neutral tool-call records, error normalization, lazy registry status, configurable default provider, health summary, config defaults, and import guards | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed by BRAIN-02; providers return model/tool-call data only and cannot execute tools | Mock provider is test-only; no model/runtime install, model download, paid/cloud call, MCP enablement, network listener, startup-heavy import, personal-data access, memory write, or tool execution semantic change added | Not applicable; interface/registry scaffold only and no live runtime/provider behavior | `docs/brain/BRAIN_PROVIDER_INTERFACE.md`, `docs/brain/MODEL_REGISTRY.md`, README, feature registry, roadmap, changelog, project state, and completion report updated | LM Studio provider refactor and disabled llama.cpp server/Ollama/in-process/MLX scaffolds now exist, but fallback/router remains unimplemented | Continue with BRAIN-08 model health, benchmark, and quality evals next |
| Brain Runtime Independence architecture and roadmap | Core runtime architecture | 1 Specified | 48 | 1 | Complete for docs-only BrainRuntimeGateway architecture, provider strategy, LM Studio decoupling plan, provider requirements, MCP decision, planned commands, and staged prompt roadmap | Fallback router, benchmark/eval command implementation, MCP adapter, and default-provider switch do not exist yet; LM Studio is provider-wrapped and remains default | `tests/test_brain_runtime_architecture_docs.py` covers required docs and conservative planned command rows; docs/registry tests, command registry validation, startup policy/capability manifest validation, and full suite passed | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed; future tool-call behavior must continue through ToolBroker and provider selection must not grant permissions or approvals | Explicitly forbids removing LM Studio, installing/downloading runtimes/models, paid/cloud API default use, MCP requirement/server enablement, network listener startup, startup-heavy imports, personal-data enablement, and normal chat behavior changes without tests | Not applicable; documentation-only milestone and no live runtime/provider behavior | Brain docs, decision record, README, command registry planned rows, feature registry, roadmap, risk/threat notes, changelog, project state, and completion report updated | Planned benchmark/fallback/MCP commands are not implemented; runtime independence remains partial even though LM Studio is provider-wrapped and llama.cpp server/Ollama/in-process/MLX are scaffolded disabled-by-default | Continue provider scaffolds and release gates in order |
| News Intelligence roadmap and source policy | News/research architecture | 1 Specified | 45 | 1 | Complete for docs-only track scope, source policy, provider strategy, freshness policy, source grounding, retention policy, risk model, architecture decision, and planned commands | No runtime News module, provider calls, cache, dogfood suite, or CLI implementation exists yet; capability manifest/provider-policy scaffolding is tracked separately below | `tests/test_news_intelligence_docs.py` covers docs, provider order, risk model, source grounding, retention defaults, and planned command registry entries; full validation evidence recorded in completion report | No runtime capability path changed; docs require future news execution through ToolBroker, PolicyEngine, and AuditLogger with provider decisions audited and approval gates for paid/binary/HIGH risk | Explicitly forbids paywall/login/CAPTCHA/anti-bot bypass, paid API defaults, fabricated headlines/citations/dates, current claims without source data, search/news history storage, and full article-body storage by default | Not applicable; documentation-only milestone and no live provider behavior | News docs, decision record, command registry planned rows, feature registry, roadmap, risk/threat/test/release trackers, changelog, and completion report updated | Planned commands are not implemented; no provider registry, runtime workflow, cache/dogfood/eval, or live validation exists yet | Continue with provider registry/status commands after the manifest/provider-policy scaffold |
| News capability manifest entries and provider policy | News/research safety scaffold | 4 Tested | 66 | 1 | Complete for disabled/planned capability manifest declarations, safe `NEWS_*` config defaults, and free-first provider-policy selection/skipping rules | Implemented `agent.news` config/provider-policy scaffolding and 18 `news.*` manifest entries; no provider registry/status commands, live provider calls, article fetch/extract runtime, cache implementation, dogfood/eval suite, or CLI implementation exists yet | `tests/test_news_capability_provider_policy.py` covers manifest validation, unknown news capability denial, planned capability default-disabled denial, no CRITICAL news v1 actions, missing risk/trust rejection, safe config defaults, free-first provider choice, paid provider skip defaults, no-history defaults, and conservative planned command rows | Manifest entries validate through startup policy; PolicyEngine denies all planned `news.*` capabilities because `default_enabled=false`; future execution still must add ToolBroker tool specs and AuditLogger provider/domain/result events | Paid APIs are disabled by default, provider calls do not exist, article bodies and readable search/news history are not stored by default, content remains `UNTRUSTED_WEB`, and bypass behavior remains forbidden by docs/policy | Not applicable; policy/manifest scaffold only and no live provider behavior | News provider/retention/track docs, `.env.example`, capability manifest, feature registry, roadmap, risk/threat/test/release trackers, changelog, and completion report updated | No user-facing news command is active; no live provider, source-grounded news workflow, cache, dogfood/eval, or release gate exists yet | Build News provider registry and metadata/status commands next |
| Cross-Platform Core + Platform Bridge release gate and future build guides | Platform architecture / release gate | 5 Hardened | 76 | 1 | Complete for release-gate validation and future macOS/iOS/Windows/app frontend build guidance | Implemented as validation/docs/tracker work only; no runtime platform behavior, native app frontend, server, personal-data access, or send/write capability added | Cross-platform docs regression test covers release gate and future guides; platform tests, startup policy, capability manifest validation, command registry validation, platform command smokes, import checks, and full suite are release-gate evidence | Existing `platform.*` commands route through ToolBroker/PolicyEngine/AuditLogger metadata tools; release gate confirms future actions still require manifest declaration, ToolBroker mapping, PermissionManager, ApprovalManager, and audit correlation | Confirms disabled/lazy defaults, no native/heavy platform imports at startup, no platform doctor/status personal-data reads, direct bridge execution blocked, App Bridge server off by default, and all real platform actions unavailable/stubbed/planned | Local validation only; no live macOS/iOS/Windows/App Bridge behavior was tested or implemented | Added `CROSS_PLATFORM_RELEASE_GATE.md`, future macOS/iOS/Windows/app frontend guides, tracker updates, and platform command release-gate QA status | Manifest mapping safeguards and dedicated startup/lazy-load guardrails remain planned; no real platform action is user-ready | Run `PLATFORM-CAPABILITY-MANIFEST-MAPPING`, then `PLATFORM-STARTUP-LAZYLOAD-GUARDRAILS` before real bridge implementation |
| App Bridge API contract | Platform architecture | 4 Tested | 68 | 1 | Complete for local App Bridge API surfaces, pairing model, payload schemas, audit correlation fields, disabled config defaults, and security docs | Implemented `agent.platforms.app_bridge` dataclasses, schema constants, validation helpers, redaction helper, and safe App Bridge config defaults; no server, frontend, native dependency, provider call, personal-data read, send/write action, or platform action exists | `tests/platforms/test_app_bridge_contract.py` covers valid/invalid schemas, unpaired sensitive request denial, ApprovalManager bypass denial, user-interaction requirement, CRITICAL exact-preview/per-action/no-reuse rules, no personal data in status payloads, safe defaults, forced remote-off config, audit correlation requirement, no server import, and secret redaction | No executable ToolBroker path changed; validation requires future requests to carry audit correlation and to hand approvals to ApprovalManager. Future execution still requires ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | `APP_BRIDGE_ENABLED=false`, localhost/IPC only, remote forced off, pairing required, no server startup by default, no polling loops, no native app dependencies in Python core, no personal-data status payloads, no send/write actions | Not applicable; contract/schema only and no app frontend/server exists | App Bridge API/security/pairing/payload docs, README config, `.env.example`, feature registry, maturity tracker, roadmap, risk register, threat model, project state, changelog, and completion report updated | This is not a live app bridge; there is no server, pairing flow, native UI, manifest mapping, ToolBroker route, or runtime bridge behavior yet | Build Platform capability manifest and ToolBroker mapping safeguards next |
| macOS/iOS/Windows/web bridge stubs | Platform architecture | 4 Tested | 69 | 1 | Complete for lazy fail-closed stub modules, stable bridge IDs, platform capability declaration, and setup/unavailable behavior | Implemented as lightweight `agent.platforms.macos`, `ios_companion`, `windows`, and `web_bridge` packages subclassing `NullPlatformBridge`; default bridge registry registers lazy loaders without importing stubs at startup; no real platform behavior exists | `tests/platforms/test_platform_bridge_stubs.py` covers lazy loading, capability declaration, structured blocked/requires_setup action results, no native imports, no personal-data access, no side effects, platform doctor stub display, and import-time overhead; platform doctor/registry/interface tests still pass | No executable action path changed; direct bridge execution remains blocked and broker-context stub execution returns `requires_setup`; future execution still requires manifest mapping, ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | No EventKit, Contacts, Messages, Mail, Microsoft Graph, Windows UI Automation, native app frontend, network call, subprocess, personal-data read, permission request, app bridge server startup, memory write, or send/write behavior was added | Not applicable; stubs are intentionally not live platform bridges | Capability matrix, platform bridge strategy, future bridge guide, feature registry, maturity tracker, project state, changelog, and completion report updated | Stubs are implementation seams only; they are not user-ready platform integrations and do not validate any live OS/API behavior | App Bridge API contract now exists; add manifest mapping and startup/lazy-load guardrails before any real bridge behavior |
| Platform doctor and capability commands | Platform architecture | 4 Tested | 70 | 1 | Complete for read-only platform doctor/status/capability/matrix/explain command surface and metadata-only output contracts | Implemented as SAFE `platform.*` ToolBroker tools plus CLI dispatch; commands inspect static capability records, safe config flags, lazy registry metadata, and platform detection only; no bridge actions execute | `tests/platforms/test_platform_doctor_commands.py` covers mocked macOS/Windows/unknown output, capability listing, matrix rows for macOS/iOS/Windows/web, known/unknown explain behavior, no native imports, startup policy, and command registry validation; platform registry/detection tests still pass | Commands route through ToolBroker, PolicyEngine capability manifest entries, and AuditLogger; no approval required for SAFE metadata; future actions still require PermissionManager/ApprovalManager according to risk | No native framework imports, platform bridge implementation imports, permission prompts, network calls, subprocesses, personal-data reads, app bridge server startup, platform actions, memory writes, or personal-data capability enablement | Local CLI smoke for `platform status` only; no live platform bridge behavior exists by design | README, platform strategy, capability matrix, platform boundaries, command registry/test matrix, changelog, feature registry, project state, and completion report updated | Commands are user-facing metadata tools, but not evidence that any real platform bridge is live; manual QA and full release gate still pending | Build App Bridge API contract next while preserving disabled-by-default/no-server-startup behavior |
| Platform-aware config, paths, and environment detection | Platform architecture | 4 Tested | 68 | 1 | Complete for safe platform/runtime detection, disabled-by-default bridge config defaults, detection cache rules, and project-local path helper boundaries | Implemented under `agent.platforms` as lightweight `config`, `detection`, and `paths` modules; no real platform bridge behavior, bridge stubs, platform commands, native imports, personal file scans, directory creation, permissions, or executable platform actions exist yet | `tests/platforms/test_platform_detection_paths_config.py` covers mocked macOS/Windows/Linux/unknown detection, runtime mode defaults and test forcing, cache behavior, project-local no-scan paths, Windows metadata warning, safe config defaults, invalid config fallback, secret env omission, and no native imports; registry/interface tests still pass | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed; no action executes and future bridge work still requires brokered tools, manifest entries, policy evaluation, approval gates, and audit evidence | No native framework imports, bridge implementation imports, network calls, subprocesses, personal file scans, directory creation, permission prompts, app bridge startup, memory writes, or personal-data capability enablement | Not applicable; metadata/config/path scaffold with no live platform behavior | README, `.env.example`, performance overhead policy, platform boundaries, feature registry, maturity tracker, project state, changelog, completion report, risk register, threat model, test plan, and release checklist updated | Platform doctor commands, lazy stubs, app bridge contract, manifest mapping, startup lazy-load guardrails, and live unsupported-platform UX remain future work | Build read-only platform doctor and capability commands next |
| Platform bridge base interfaces and NullPlatformBridge | Platform architecture | 4 Tested | 67 | 1 | Complete for abstract bridge contract, action payload/result envelopes, permission/health status envelopes, NullPlatformBridge behavior, and lazy bridge registry rules | Implemented under `agent.platforms` as lightweight interface scaffolding and fail-closed null bridge; no real macOS/iOS/Windows/web bridge implementation, bridge stubs, platform CLI commands, or executable manifest mappings exist yet | `tests/platforms/test_platform_bridge_interfaces.py` covers null bridge behavior, lazy registry loading, side-effect-free prepare, direct execute guard, broker-context requires_setup result, registry capability mapping, no personal-data health checks, no native imports, and ToolBroker documentation; registry tests still pass | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed; direct `execute_action()` calls are blocked and future executable actions still require brokered tools, manifest entries, policy evaluation, approval gates, and audit correlation | No native framework imports, platform-specific bridge imports, personal-data reads, network calls, subprocesses, permission prompts, app bridge startup, memory writes, or platform action side effects; NullPlatformBridge fails closed | Not applicable; no live platform behavior exists | Platform bridge strategy, platform boundaries, future bridge guide, feature registry, maturity tracker, project state, changelog, completion report, risk register, threat model, test plan, and release checklist updated | Platform-aware config/path detection, doctor commands, platform stubs, app bridge contract, capability manifest mapping, and startup lazy-load guardrails remain future work | Build Platform-aware config, paths, and detection scaffolding next |
| Platform Capability Registry v1 | Platform architecture | 4 Tested | 68 | 1 | Complete for portable registry models, capability statuses, static capability records, safe detection metadata, unknown platform handling, and unknown capability handling | Implemented as lightweight metadata-only Python package under `agent.platforms`; bridge base interfaces and NullPlatformBridge now exist, but no bridge stubs, ToolBroker execution mapping, platform CLI commands, or executable platform capabilities are implemented yet | `tests/platforms/test_platform_capability_registry.py` covers registry load, platform/capability lookup, required fields, disabled personal-data defaults, CRITICAL per-action metadata, no native imports, safe detection, and unsupported records | No runtime ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior changed; records explicitly require future ToolBroker mapping and manifest declaration before execution | No native frameworks, platform bridge modules, network calls, subprocesses, permission prompts, personal file scans, app bridge startup, personal-data access, or action execution; planned personal-data capabilities default disabled | Not applicable; metadata-only registry with no live platform behavior | Capability matrix, platform bridge strategy, feature registry, maturity tracker, project state, changelog, completion report, test plan, and release checklist updated | Capability manifest placeholders, ToolBroker mapping safeguards, config/path detection, platform commands, stubs, app bridge contract, and startup lazy-load guardrails remain future work | Build Platform-aware config, paths, and detection scaffolding next |
| Cross-Platform Core + Platform Bridge architecture roadmap | Platform architecture | 1 Specified | 43 | 1 | Complete for documentation-only architecture, boundaries, capability matrix, performance policy, future bridge guide, and planned command tracking | No runtime platform behavior implemented; planned commands are registry entries only and platform capabilities remain non-executable | Docs validation tests added for required platform docs, core boundaries, capability matrix, overhead policy, planned command registry entries, and tracker presence | No runtime capability, policy, approval, or audit path changed; docs require future bridge execution through ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, and AuditLogger | Boundaries explicitly forbid native imports at core startup, direct bridge execution, personal-data status reads, send/write behavior, private app database access, Microsoft Graph, UI Automation, and app bridge remote access without future approval | Not applicable; docs-only milestone and no live platform bridge exists | ADR, strategy, capability matrix, boundaries, performance policy, future guide, command registry tracking, roadmap, registry, risk, threat, release checklist, project state, changelog, and completion report updated | Platform capability registry v1, bridge interfaces, config/detection scaffolding, doctor commands, and bridge stubs now exist; app bridge contract, manifest mapping, and startup guard tests remain future work | Build App Bridge API contract next |
| Generated artifact hygiene for release boundary | Release/quality | 4 Tested | 76 | 1 | Complete for generated workspace artifact classification and release hygiene rules | Implemented as narrow `.gitignore` rules plus release docs/tests; no runtime behavior changed | `tests/test_release_artifact_hygiene.py` passed and git ignore inspection confirmed generated local workspace outputs are hidden | No runtime capability, policy, approval, or audit path changed; ToolBroker/PolicyEngine/ApprovalManager/AuditLogger rules preserved | Generated eval, lead, messaging, iOS compose, and Reddit export artifacts are classified as local generated data while source fixtures remain reviewable | Local docs/test validation only; no live runtime behavior involved | Release hygiene doc, release audit/blocker/plan, changelog, registry, maturity, project state, completion report, risk/threat/test/release trackers updated | Does not by itself create a clean release candidate branch or reviewed commit series; no files were deleted | Continue with human-reviewed clean release candidate boundary or next safe blocker |
| Tracker Hygiene, Indexing, and Compaction | SDLC tracking and release process | 4 Tested | 78 | 1 | Complete for dashboard/index/archive/maintenance/consistency scope | Implemented as docs/tracker validation only; no runtime behavior changed | Tracker hygiene docs regression tests added and targeted validation run in this pass | No runtime capability or policy path changed; ToolBroker/PolicyEngine/ApprovalManager/AuditLogger rules preserved | Source-of-truth preservation, archive rules, anchored edit rules, and disagreement reporting documented | Local docs validation only; no live runtime behavior involved | Dashboard, tracker index, maintenance guide, archive policy, consistency report, README, AGENTS, command registry, project state, registry, maturity, roadmap, release checklist, and completion report updated | Exact automated crosswalks between feature registry/maturity/roadmap are still future work; dense append-only trackers remain intentionally detailed | Add machine-readable tracker crosswalks and archive older completion/prompt history after release boundary |
| Release Hardening Loop v2 | Release/quality | 5 Hardened | 83 | 1 | Complete for audit/reconciliation pass | Implemented as docs/tests/tracker hardening only; no runtime feature expansion | Full suite passed; startup policy, capability manifest, command registry, safe eval, dogfood dry-run, focused prompt/command tests, and setup regressions passed | Existing ToolBroker/PolicyEngine/ApprovalManager/AuditLogger gates preserved; no new bypass or approval path added | Security scans found no confirmed P0/P1; personal defaults and CRITICAL approval rules verified; release blockers documented conservatively | Local validation only; live/manual provider validation remains incomplete | Release audit, blockers, hardening plan, changelog, registry, maturity, roadmap, risk, threat model, test plan, release checklist, completion report, and project state updated | Large dirty worktree prevents clean release boundary; manual/live validation gaps remain; generated artifact hygiene needs follow-up | Continue hardening loop with clean release candidate boundary and full safe dogfood session |
| LM Studio no-tool chat | Core runtime | 5 Hardened | 84 | 5 | Complete | Implemented with Python 3.11 startup guard and local `scripts/agent` launcher | Broad unit, mocked runtime, Python version guard, and launcher tests | No tools attached in no-tool mode; audit not involved for pure chat | Debug redaction and no-tool regression covered | Needs repeat live local smoke with current model | README setup, `.venv`, launcher, LM Studio model-listing, and doctor diagnostics exist | Depends on Python 3.11+, LM Studio server/model availability | Run live smoke with `./scripts/agent --no-tools` and current `LMSTUDIO_MODEL` before next release tag |
| ToolBroker / PolicyEngine / AuditLogger | Safety control plane | 8 Mature Pattern | 95 | 10+ | Complete | Implemented | Broad policy, approval, audit, dry-run, preflight, and release-gate tests | Central enforcement path; unknown tools/capabilities denied | Hardened for denials, secrets, approvals, dry-run, preflight, and hash-chain audit | Validated through local test suite and CLI flows | Documented in SPEC, AGENTS, README, threat model, release checklist | Audit viewer verification could be richer | Keep as required pattern for every new tool |
| Unified Action Center | Safety and UX | 4 Tested | 76 | 2 | Complete for v1 review queue | Implemented as persisted action review records linked to approval requests; intentionally no direct execution path | Store, list/show, HIGH/CRITICAL approval requirements, one-time approval consumption, denial, edit invalidation, non-interactive blocking, no direct execution, required action metadata, audit lifecycle, export/audit minimization, redaction, and CLI tests | Integrates with ApprovalStore and AuditLogger; future execution still must go through ToolBroker and PolicyEngine | CRITICAL approvals are per-action only, previews require exact args, irreversible actions show rollback limits, edits invalidate approvals, secrets redacted, and export/audit views minimize sensitive bodies/drafts | Local tests only; no live approved write/send flow yet | README, threat model, risk register, test plan, release checklist, and project tracking updated | It is a review surface only; exact local previews may contain user-approved action details and must remain local | Use Action Center as the release gate before any new approved write/send/provider execution path |
| Privacy Center / Data Inventory v1 | Privacy and UX | 4 Tested | 74 | 1 | Complete for metadata-only v1 | Implemented `privacy status`, `inventory`, `export`, `delete-memory`, `audit-summary`, and `permissions` over local stores and config metadata | Status, disabled connector inventory, memory counts without contents, redacted export, delete confirmation denial, brokered memory clear, audit summary, permissions, CLI status, and no personal connector read tests | Metadata views are audited as `privacy.*`; memory deletion goes through ToolBroker/PolicyEngine/AuditLogger via `memory.clear`; personal connector providers are not queried | Secrets redacted by default, inventory returns counts/metadata only, export returns redacted previews, delete-memory requires explicit confirmation, audit-summary omits raw args, and reports explain exclusions/deletion limits | Local tests only; no manual full privacy review yet | README and tracking docs updated | Export is a redacted preview, not a full legal/privacy export; audit logs and external backups are not rewritten by memory deletion | Run manual `privacy inventory`, `privacy export`, and backup inventory review on real local state |
| Backup / Restore / Migration v1 | Privacy and operations | 5 Hardened | 80 | 2 | Complete for redacted local archive v1 plus CANON-07 roundtrip/restore hardening | Implemented brokered `backup create/list/inspect/verify/export/roundtrip --dry-run/policy-check/restore-check/restore`, manifest hashing, redacted exports, restore preview, restore readiness checks, and restore policy validation | Backup create, secret redaction/exclusion, manifest creation, inspect preview, verify tamper detection, roundtrip dry-run no-write behavior, policy guard reporting, restore-check hash validation, pre-restore copy, CRITICAL approval reuse refusal, unredacted secret refusal, path traversal refusal, restore approval requirement, policy-weakening restore block, audit lifecycle, CLI dispatch, and manifest tests | All commands route through ToolBroker, PolicyEngine, and AuditLogger; `backup.restore` is HIGH approval-required and denied safely in non-interactive mode; roundtrip/policy-check/restore-check are read-only and audited | `.env` and key files are excluded, secret-like text is redacted, no personal connectors are queried, restore verifies hashes and rejects capability manifests or archive contents that weaken policy, enable personal connectors, reuse CRITICAL approval, include bypass flags, expose unredacted secrets, or traverse paths | Local tests and CLI smokes only; no manual restore on a disposable project yet | README, backup/restore docs, CANON-07 backup docs, command registry, risk register, threat model, feature registry, project state, changelog, and completion report updated | V1 stores redacted memory/action/capture/audit exports rather than raw recoverable personal data; restore should be manually tested only on disposable copies; no live/manual restore validation yet | Run manual `backup create`, `backup inspect`, `backup verify`, `backup restore-check`, and disposable-project restore smoke before raising to live-validated/user-ready |
| Connector framework | Connector foundation | 4 Tested | 72 | 1 | Basic framework requirements documented | Implemented as metadata/status/health registry; no action execution | Registry, unknown connector, redaction, personal-data-safe health, disabled/missing provider, and brokered existing connector tests | Status-only; does not bypass ToolBroker for actions | Secrets redacted and personal connector checks are configuration-only | Local tests only | README documents module roles and safety boundary | Does not yet unify provider construction or live health probes | Use for future connectors before adding new provider-specific dashboard logic |
| Cost-aware provider policy | Connector foundation | 4 Tested | 81 | 6 | Complete for policy scaffolding, web inspection commands, provider order rules, registry handoff, and configured SearXNG/Brave/SerpAPI integrations | Implemented as a pure provider decision layer with web/search, weather, and personal-communications ordering; added brokered `web providers`, `web provider-policy`, `web provider-decision`, `web search-providers`, configured-only SearXNG routing, paid/quota-gated Brave routing, and explicit fallback SerpAPI routing without paid defaults | Unit tests cover free-first no-key selection, manual explicit selection, paid-provider denial, SerpAPI, Brave, and WeatherAPI gates, required decision fields, provider decision audit, query/secret redaction, setup hints, policy CLI behavior, search-provider registry metadata, SearXNG disabled/configured behavior, Brave disabled/configured/cost behavior, SerpAPI disabled/configured/daily-cap behavior, and the 2026-05-24 provider verification passes | Policy/registry/provider commands execute through ToolBroker/PolicyEngine/AuditLogger; SearXNG execution is brokered through `web.search`; Brave execution is brokered through `web.search` only when enabled and paid policy allows it; SerpAPI execution is brokered through `web.search.serpapi` only when explicitly selected, enabled, and paid policy allows it; provider doctors are brokered as `web.searxng.doctor`, `web.brave.doctor`, and `web.serpapi.doctor` | Paid/quota-limited providers are skipped unless allowed or explicitly selected where documented; SearXNG has no public-instance default, Brave and SerpAPI are not selected by key presence, secrets and sensitive queries are redacted, no query history is stored in memory, and cache config is operational TTL only | Local unit tests and CLI smokes only; live SearXNG/Brave/SerpAPI validation remains opt-in | README, connector cost-policy docs, decision record, env example, command registry, SearXNG/Brave/SerpAPI provider docs, provider-selection docs, search-provider registry docs, and tracking docs updated | Live provider validation remains pending by design; broader source-grounding/fetch hardening still needs dedicated prompts | Build safe fetch/extraction hardening next while preserving free-first and paid-provider gates |
| Search Provider Registry v1 | Web/research foundation | 4 Tested | 72 | 2 | Complete for metadata, schemas, setup/error normalization, brokered CLI inspection, and first configured provider integration | Implemented `agent.web_acquisition.search` with provider interface, metadata registry, normalized `SearchResult`/`SearchResponse`, setup hints, unknown-provider errors, brokered `web.search_providers`, and SearXNG provider wiring for explicit configured searches | Focused tests cover registry load, unknown provider denial, missing-provider setup hints, ToolBroker-only registry search, normalized result/response fields, provider error normalization, sensitive-query audit redaction, no search-history persistence, command registry presence, and SearXNG mock normalization/errors | Registry/provider execution requires ToolBroker context; CLI commands are registered as SAFE/LOW as appropriate and audited; future providers still must map to capabilities before execution | Results are `UNTRUSTED_WEB`; queries are hashed/redacted and not persisted; SearXNG uses only configured base URLs and paid provider behavior remains cost-policy gated | Local tests and config-only CLI smokes only; no live SearXNG/Brave smoke in this prompt by design | README, `docs/web/SEARCH_PROVIDER_REGISTRY.md`, `docs/web/providers/searxng.md`, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Local mocks are not proof of a real SearXNG instance or JSON plugin availability; future providers still need per-provider rate-limit/error/source-grounding tests | Build Brave provider hardening next as optional configured provider work |
| SearXNG provider | Web/research provider | 4 Tested | 71 | 1 | Complete for configured/self-hosted v1 | Implemented `SearXngSearchProvider` with disabled-by-default env config, configured base URL requirement, JSON query parameters, normalized search results, setup hints, timeout/403/429/malformed JSON handling, config-only doctor, connector status metadata, and CLI routing through explicit `--provider searxng` | Tests cover missing base URL setup hint, disabled provider behavior, mock result normalization, malformed/JSON-disabled response setup instructions, timeout, 403, 429, sensitive-query redaction, provider-domain audit, connector status, doctor output, and command registry entries | Search execution remains brokered through `web.search`; config doctor is brokered as `web.searxng.doctor`; connector status is metadata-only; no public instance is assumed | No API key required, no paid default, no public instance default, no query-history persistence, search results labeled `UNTRUSTED_WEB`, and sensitive query text redacted in audit metadata | Local mocked provider tests and config-only CLI smokes only; no live SearXNG instance configured or called in this run | `docs/web/providers/searxng.md`, README provider setup, `.env.example`, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, and completion report updated | Live validation requires the user to configure a self-hosted SearXNG instance with JSON output enabled; JSON-disabled public/private instances return setup guidance instead of scraping HTML | Next prompt: Brave provider hardening |
| Brave Search provider | Web/research provider | 4 Tested | 70 | 1 | Complete for optional quota-limited v1 | Implemented `BraveSearchProvider` with disabled-by-default `BRAVE_SEARCH_ENABLED=false`, API-key presence checks, per-provider timeout/result/safe-search config, normalized web/news result support, setup hints, timeout/401/403/429/malformed JSON handling, config-only doctor, connector status metadata, and CLI routing through explicit `--provider brave` | Tests cover no key setup hint, key redaction, not selected by key presence under `free_first`, paid/quota policy denial, mocked result normalization, timeout, API error, 429 rate-limit handling, provider decision payload, provider-domain audit, connector status, doctor output, and command registry entries | Search execution remains brokered through `web.search`; config doctor is brokered as `web.brave.doctor`; connector status is metadata-only; provider decision must select Brave before any API call | Requires explicit key, enablement flag, and paid/quota policy opt-in; API keys are never printed; no paid default; no query-history persistence; search results labeled `UNTRUSTED_WEB`; sensitive query text redacted in audit metadata | Local mocked provider tests and config-only CLI smokes only; no live Brave key configured or called in this run | `docs/web/providers/brave.md`, README provider setup, `.env.example`, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, and completion report updated | Live validation requires the user to configure Brave credentials and explicit paid/quota policy; source-grounded research/fetch hardening remains future work | Next prompt: safe fetch/extraction hardening |
| SerpAPI fallback provider | Web/research provider | 4 Tested | 71 | 2 | Complete for optional fallback v1 hardening | Implemented `SerpApiSearchProvider` with disabled-by-default `SERPAPI_ENABLED=false`, API-key presence checks, per-provider timeout/result config, normalized organic results, setup hints, timeout/401/403/429/API-error/malformed JSON handling, config-only doctor, connector status metadata, and CLI/research routing through explicit `--provider serpapi` | Tests cover missing key setup hint, disabled-provider setup hint, key redaction, not selected under `free_first`, paid/quota policy and daily-cap denial, mocked result normalization, timeout, provider API error, 429 rate-limit handling, provider decision payload, provider-domain audit, connector status, doctor output, source-grounded research routing, and command registry entries | Search execution remains brokered through `web.search.serpapi`; config doctor is brokered as `web.serpapi.doctor`; connector status is metadata-only; provider decision must select SerpAPI before any API call | Requires explicit key, enablement flag, paid/quota policy opt-in, and nonzero daily paid-call cap; API keys are never printed; no paid default; no query-history persistence; results labeled `UNTRUSTED_WEB`; sensitive query text redacted in audit metadata; docs state no CAPTCHA/paywall/login/anti-bot bypass dependency | Local mocked provider tests and config-only CLI smokes only; no live SerpAPI key configured or called in this run | `docs/web/providers/serpapi.md`, `docs/web/PROVIDER_SELECTION.md`, README provider setup, `.env.example`, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Live validation requires the user to configure SerpAPI credentials and explicit paid/quota policy; source-grounded research/fetch hardening remains future work | Next prompt: safe fetch/extraction hardening |
| Safe web fetch and extraction | Web acquisition | 4 Tested | 73 | 1 | Complete for local selected-URL v1 | Added shared `agent.web_acquisition` URL normalization, sanitization, extraction, and fetch helpers; brokered `web.fetch_url`, `web.extract_readable_text`, and `web.extract_metadata`; structured FetchResult fields; source metadata extraction; script/event-handler stripping; tracking-parameter stripping; and blocked/CAPTCHA-page unavailable reporting | Tests cover invalid URLs, non-http schemes, blocked domains, timeouts, redirect-limit errors, binary content blocking, script/event-handler stripping, prompt-injection wrapper, metadata extraction, CAPTCHA/block-page unavailable result, audit domains, command registry entries, and brokered extraction tools | Fetch, extraction, and metadata commands execute through ToolBroker, PolicyEngine, and AuditLogger as MEDIUM web capabilities; no direct bridge/provider path is added | Web content is `UNTRUSTED_WEB`, fetched documents remain untrusted, no cookies/sessions/form submission/browser automation/binary downloads/CAPTCHA/login/paywall/anti-bot bypass are implemented, and web content cannot instruct tools or policy changes | Local mocked fetch/extraction tests only; no live URL smoke was required for this milestone | `docs/web/SAFE_FETCH_AND_EXTRACTION.md`, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Live website variability, robots nuance for selected-URL workflows, richer readability extraction, and source-grounded citation workflows remain future work | Next prompt: source-grounded research workflow v1 |
| Source-grounded research workflow v1 | Web/research workflow | 4 Tested | 78 | 2 | Complete for local v1 source-bounded research with attribution metadata | Hardened `source_grounded_research` and `python smart_agent.py research` with brokered provider search, optional selected-source fetches, `--provider`, `--max-sources`, `--freshness`, `--no-fetch`, structured Answer/Sources/Coverage/Fetch-failure sections, provider policy metadata, retrieved timestamps, snippet-only labels, unavailable/fetch-failed reporting, basic conflict detection, stable source IDs, and metadata-only source bundles | Tests cover missing providers, explicit SerpAPI routing, no fabricated sources, summarized search/fetch sources, unavailable fetches, conflict limitations, prompt-injection filtering, foreign-language pass-through, no memory writes, search/fetch audit logs, CLI option parsing, stable source IDs, failed-source citation denial, snippet-only labels, and source-bundle inspection/export/verification commands | Research execution uses ToolBroker for `web.search`, explicit `web.search.serpapi`, and `web.fetch_url`; PolicyEngine and AuditLogger remain in the underlying tool path; last-source metadata commands audit local metadata reads and make no provider calls | Web content remains `UNTRUSTED_WEB`; source text is data only; failed fetches are not cited as support; no current facts are claimed without returned source data; no search history, article body, or summary memory write occurs by default; paid providers remain explicit/config-gated | Local mocked tests only; no live provider/source validation in this milestone | `docs/web/SOURCE_GROUNDED_RESEARCH.md`, `docs/web/CITATION_POLICY.md`, README, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Summaries are deterministic excerpts, not full LLM synthesis; source attribution is metadata-level and does not prove article truth or representativeness | Build official API connector framework next |
| Citation and source attribution layer | Web/research workflow | 4 Tested | 72 | 1 | Complete for metadata-only local v1 | Added `SourceReference`, `CitationSpan`, `ClaimAttribution`, and `ResearchSourceBundle`; research outputs now include stable source IDs, content hashes, reliability signals, citation spans, claim mappings, failed-source records, limitations, and metadata-only last-source commands | Tests cover stable source IDs, fetched source citation, failed source not cited as support, snippet-only source labeling, retrieved timestamps, fabricated/unknown source rejection, conflict representation, CLI last-source inspection/export/verification, and command registry coverage | No new provider execution path is added; source-bundle commands are read-only metadata inspections and audit through `AuditLogger`; underlying search/fetch remains ToolBroker-controlled | Metadata bundle stores source IDs, URLs, provider/domain, timestamps, hashes, labels, and limitations only; no article bodies, fetched full text, summaries, search history, personal data, or memory writes by default | Local mocked tests only; live provider/source validation remains opt-in | `docs/web/CITATION_POLICY.md`, `docs/web/SOURCE_GROUNDED_RESEARCH.md`, README, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Claim attribution is practical/heuristic rather than a full natural-language proof system; source URLs can still be inaccurate if returned by an upstream provider, so verification checks structure and provenance, not truth | Build official API connector framework next |
| Web cache, dedupe, and local lightweight index | Web/research retention and reuse | 4 Tested | 70 | 1 | Complete for local metadata-cache v1 | Added file-backed `WebCacheStore`, content-hash/source-ID dedupe helpers, `LocalWebIndex`, brokered cache/index tools, CLI commands for cache status/clear/show and index search/rebuild, and safe env defaults | Tests cover cache hit before refresh, expiry refresh, dedupe, index search, personal/authenticated content denial, cache clear, no sensitive query persistence, audit logs for cache/index operations, command registry, and startup policy validation | All user-facing cache/index operations run through ToolBroker, PolicyEngine, and AuditLogger; index rebuild is local-only and never refreshes sources directly | Public web data only by default; raw queries are hashed/redacted and not persisted, full content storage is disabled by default, blocked/CAPTCHA content stores status metadata only, and personal/authenticated content is denied | Local mocked tests and CLI smokes only; no live provider cache refresh validation in this milestone | `docs/web/WEB_CACHE_POLICY.md`, `docs/web/LOCAL_WEB_INDEX.md`, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Cache/index metadata can still be stale or source-poisoned; all entries remain untrusted and future refresh automation must respect robots/provider policy | Build official API connector framework next |
| Official API connector framework | Web/research provider foundation | 4 Tested | 66 | 1 | Complete for local framework/stub v1 | Added `agent.web_acquisition.official_apis` interfaces, models, registry/domain matching, GitHub/Wikipedia/arXiv/Reddit stubs, mocked public result normalization, brokered official API commands, capability manifest entries, and provider docs | Tests cover registry load, provider matching, setup hints, mocked public result normalization, rate limiting, secret redaction, Reddit no-web-scraping-substitute metadata, provider-domain audit, and command registry entries | Commands run through ToolBroker/PolicyEngine/AuditLogger; status/list perform no provider calls; `api-search` requires brokered execution and returns setup hints unless a provider is mocked/configured by a later implementation | All results are `UNTRUSTED_WEB`; no search history, article body, OAuth token, personal account data, write operation, paid default, or Reddit web-scraping fallback is added | Local mocked tests only; built-in live API calls are deferred by design | `docs/web/OFFICIAL_API_CONNECTORS.md`, provider docs, README, `.env.example`, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Framework does not prove live provider readiness; future live connectors need provider-specific API, rate-limit, credential, audit, and release-gate work | Build Internet dogfood/eval suite next |
| Internet Access dogfood/eval suite and release gate | Web/research validation | 4 Tested | 76 | 1 | Complete for mock-first local release gate | Added `internet_core`, `web_providers`, `web_fetch`, `web_research`, and `web_blocked_sources` dogfood suites; recursive `eval_cases/internet` loading; fixture-backed `eval run --internet`; and an internet dogfood runbook | Tests cover suite YAML validation, live suites default-disabled, internet eval case loading from subdirectories, source-list/citation/retrieved-at checks, fetch-failure separation, prompt-injection fixture denial, free-first provider policy, no paid provider default, no query/web-content persistence, audit-domain fixture checks, CLI `--internet` parsing, and command registry entries | Dogfood commands use existing CLI paths that route through ToolBroker/PolicyEngine/AuditLogger; fixture-backed evals do not call providers and assert audit metadata rather than bypassing tool paths | Web/article/source text remains `UNTRUSTED_WEB`; live/provider suites are opt-in; paid providers are not used by default; no CAPTCHA/login/paywall/anti-bot bypass is added; no web query/history/content memory write is added | Mock/fixture gate and dry-run dogfood validated locally; safe live provider/fetch validation remains opt-in and environment-dependent | `docs/web/INTERNET_DOGFOOD_RUNBOOK.md`, dogfood suites, `eval_cases/internet/`, README, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | The internet eval category proves release-gate invariants against fixtures, not live provider account health or source truth; live suites can fail due network/provider availability and should be recorded as manual QA evidence | Next track: Reddit + Multilingual Forum Intelligence planning |
| Reddit + Multilingual Forum Intelligence Track | Forums planning | 1 Specified | 39 | 1 | Complete for planning, policy, risk, and rollout definition | Created decision and policy docs for Reddit/forum access, Reddit-specific access, multilingual forum strategy, Chinese forum strategy, source grounding, retention, target sources, rollout, and future gates | Docs validation/prompt tracking plus startup policy and capability manifest validation; no runtime tests because no connector, command, provider, fetch, or storage code was added | Planning requires future runtime behavior to use ToolBroker, PolicyEngine, ApprovalManager where needed, AuditLogger, capability manifests, provider policy, retention gates, and no-bypass handling | Explicitly forbids scraping logged-in/private pages, CAPTCHA/anti-bot bypass, Reddit web scraping as an API substitute, paid-provider defaults, forum-content training, permanent forum-content storage by default, and write actions such as posting/commenting/voting/DMs | Not applicable; planning-only and no live Reddit/forum validation | `docs/decisions/reddit_forum_intelligence_track.md`, `docs/forums/*`, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | No runtime Reddit API, V2EX connector, language detection, translation, forum registry, Chinese forum discovery, dogfood, or release gate exists yet | Next prompt: Reddit provider policy and compliance scaffolding |
| Reddit provider policy and compliance scaffolding | Forums compliance | 4 Tested | 63 | 2 | Complete for config, manifest, compliance helpers, docs, and read-only connector gates | Added disabled-by-default Reddit env defaults, metadata-only policy helpers, hard-false training behavior, denied web fallback, read-only capability IDs, and retention/rate-limit compliance docs | Tests cover disabled defaults, missing OAuth hints, no unauthenticated mode, denied write capabilities, retention defaults, hard-false training, author metadata defaults, web fallback denial, manifest validation, and read-only connector capability allow/deny behavior | Reddit execution is brokered through ToolBroker/PolicyEngine/AuditLogger; read-only capabilities are declared but still require explicit Reddit enablement and OAuth setup at runtime | No web scraping fallback, post/comment/vote/DM/moderation action, permanent content storage, author metadata default, deleted/removed content retention, search history, or training use added | Local tests only; live Reddit validation remains opt-in | `docs/forums/REDDIT_COMPLIANCE.md`, `docs/forums/REDDIT_RETENTION.md`, `docs/forums/REDDIT_RATE_LIMITS.md`, `.env.example`, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | The scaffold plus connector tests do not prove real credentials, live API availability, or live Reddit rate-limit behavior | Next prompt: Reddit search workflows |
| Reddit OAuth/config doctor | Forums diagnostics | 4 Tested | 66 | 1 | Complete for local diagnostics and mocked auth-check v1 | Added brokered `reddit doctor`, `reddit status`, and explicit `reddit auth-check` commands plus `connectors status reddit`; diagnostics report OAuth env presence, generic user-agent warnings, tracked `.env`, repo-local token-file warnings, rate-limit/retention config, disabled write actions, denied web fallback, and hard-false training behavior | Tests cover missing config setup hints, secret redaction, token path in repo warnings, generic user-agent warnings, disabled connector status, mocked auth-check success/failure, auth-check audit logs, no post/comment content fetch, connector status redaction, CLI output, and command registry entries | `reddit doctor/status/auth-check` execute through ToolBroker, PolicyEngine, and AuditLogger; `connectors status reddit` is metadata-only | `reddit auth-check` is explicit-only and calls only OAuth/token-status endpoints when configured; no Reddit post/comment/thread fetch, web scraping fallback, unauthenticated traffic, write action, token printing, user-content storage, or training use is added by diagnostics | Local mocked auth-check tests only; live Reddit credential validation remains opt-in and was not run | README Reddit setup, `docs/forums/REDDIT_COMPLIANCE.md`, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Diagnostics prove setup safety, not live content access; use read-only connector commands only after explicit OAuth setup | Next prompt: Reddit search workflows |
| Reddit read-only connector v1 | Forums connector | 4 Tested | 72 | 1 | Complete for local mocked read-only Data API v1 | Added `agent.forums.reddit` client, models, normalizer, provider facade, errors, retention cache, brokered tools, and CLI commands for search, subreddit info, post fetch, comments fetch, cache clear, and retention sweep | Tests cover mock search normalization, post normalization, nested comments, deleted/removed handling, rate-limit header parsing and structured rate-limit errors, author metadata redaction, cache TTL, retention sweep, absence of write capabilities, setup-required CLI behavior, query redaction, OAuth-only API base usage, command registry entries, startup policy, and capability manifest validation | All commands execute through ToolBroker, PolicyEngine, and AuditLogger; capability manifest entries are declared; provider calls audit provider/domain/endpoint/status/rate-limit metadata; missing config fails closed with setup hints | Official Data API only; OAuth required; no web scraping fallback, unauthenticated mode, posting, commenting, voting, DMs, moderation, CAPTCHA/anti-bot bypass, permanent user-content storage, search-history persistence, deleted/removed retention, author metadata default, or training use | Local mocked tests and no-config CLI smokes only; live Reddit API validation remains opt-in and was not run | README Reddit setup, `docs/forums/REDDIT_COMPLIANCE.md`, `docs/forums/REDDIT_RETENTION.md`, `docs/forums/REDDIT_RATE_LIMITS.md`, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Live OAuth credentials, real Reddit API availability, real rate-limit headers, and real content/source QA still need explicit provider validation | Next prompt: Reddit search workflows |
| Reddit search workflows | Forums connector ergonomics | 4 Tested | 74 | 1 | Complete for local mocked search workflow v1 | Added search workflow options for subreddit, sort, time, limit, and advisory language; search payloads now include search parameters, language-support notes, snippet-only evidence labels, and local `reddit explain-result` metadata lookup | Tests cover global search, subreddit search, sort/time/limit/language pass-through, setup-required behavior, sensitive-query redaction, no readable query persistence, snippet-only labels, structured rate-limit errors, cached source explanation, CLI parsing, command registry entries, startup policy, and capability manifest validation | `reddit search` and `reddit explain-result` execute through ToolBroker, PolicyEngine, and AuditLogger; explain-result is local cache metadata only and makes no Reddit or web call | Official Data API only; OAuth required for live search; no scraping fallback, unauthenticated traffic, paid provider default, write action, search-history persistence, author metadata default, deleted/removed retention, or content training | Local mocked tests and no-config CLI smokes only; live Reddit API validation remains opt-in and was not run | `docs/forums/REDDIT_SEARCH.md`, README Reddit setup, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | `--language` is advisory because Reddit Data API search does not enforce a general language filter; explain-result only works for cached source metadata | Next prompt: Reddit thread fetch and conversation normalization |
| Reddit thread fetch and conversation normalization | Forums connector ergonomics | 4 Tested | 74 | 1 | Complete for local mocked thread workflow v1 | Added brokered `reddit thread` and `reddit thread-export` commands, `reddit.fetch_thread` / `reddit.thread_export` capabilities, normalized `comment_tree` and `flattened_comments`, source references, truncation metadata, collapse-depth handling, and workspace-only exports | Tests cover Reddit URL and ID parsing, comment tree normalization, nested comment flattening, max-comment limit enforcement, deleted/removed comment handling, author metadata redaction, workspace-only export, export trust labels, CLI audit records, command registry entries, startup policy, and capability manifest validation | Thread fetch/export execute through ToolBroker, PolicyEngine, and AuditLogger; fetches use the existing official Data API client; exports audit `files_written` and stay under the approved workspace | Official Data API only; OAuth required for live fetch; no Reddit web scraping fallback, unauthenticated traffic, paid provider default, write action, posting/commenting/voting/DM/moderation capability, content training, permanent memory storage, or out-of-workspace export | Local mocked tests and no-config CLI audit smokes only; live Reddit API validation remains opt-in and was not run | `docs/forums/REDDIT_THREAD_FETCH.md`, `docs/forums/REDDIT_COMPLIANCE.md`, `docs/forums/REDDIT_RETENTION.md`, README Reddit setup, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | Thread export intentionally creates an untrusted local document only when explicitly requested; real Reddit API behavior still needs credentialed live validation | Next prompt: Reddit source-grounded summarization |
| Reddit source-grounded summarization | Forums connector ergonomics | 4 Tested | 74 | 1 | Complete for local mocked summarization v1 | Added deterministic source-grounded thread/search summaries plus consensus, pros-cons, complaints, and buying-advice commands over existing official API search/thread payloads | Tests cover fetched-thread source references, snippet-only search labels, deleted/removed exclusion, prompt-injection exclusion, no fabricated source URLs, anecdotal caveat language, too-little-data messaging, no summary memory write, CLI audit/query redaction, command registry entries, startup policy, and manifest validation | Summary commands execute through ToolBroker, PolicyEngine, and AuditLogger; underlying fetch/search calls use the existing official Data API provider and report audit network domains/rate-limit status | Official Data API only; no Reddit web scraping fallback, unauthenticated traffic, paid provider default, write action, content training, permanent summary memory storage, deleted/removed evidence use, or prompt-injection evidence use | Local mocked tests and no-config CLI audit smokes only; live Reddit API validation remains opt-in and was not run | `docs/forums/REDDIT_SUMMARIZATION.md`, `docs/forums/REDDIT_COMPLIANCE.md`, `docs/forums/REDDIT_RETENTION.md`, `docs/web/CITATION_POLICY.md`, README Reddit setup, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | Summaries are deterministic and conservative; richer natural-language synthesis may need a future LLM step with strict citation verification | Next prompt: Reddit retention/cache compliance |
| Reddit retention and cache enforcement | Forums retention/privacy | 4 Tested | 76 | 1 | Complete for local TTL cache enforcement v1 | Added brokered `reddit cache status`, `reddit retention status`, and `reddit privacy-report`; tightened cache write metadata with source/content hash, raw query redaction, deleted/removed payload skipping, and zero-TTL cache disablement | Tests cover cache entry expiry, retention sweep deletion, author metadata absence by default, count-only privacy report, no query-history persistence, removed content not cached, cache clear, retention sweep audit logs, CLI status/privacy commands, command registry entries, startup policy, and capability manifest validation | Cache status/report/clear/sweep commands execute through ToolBroker, PolicyEngine, and AuditLogger; status/privacy return counts only and clear/sweep audit retention actions | TTL-bounded cache only; author metadata disabled by default; deleted/removed content not cached; query strings are hashed/redacted rather than stored as readable history; no model training or memory write | Local cache tests and no-config CLI smokes only; live Reddit API payload retention QA remains opt-in and was not run | `docs/forums/REDDIT_RETENTION.md`, README Reddit setup, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, project state, changelog, prompt tracking, and completion report updated | Retention enforcement covers local JSON cache semantics; real Reddit payload diversity should be rechecked during live provider validation | Next prompt: multilingual detection and translation layer for forum content |
| Multilingual detection and translation layer | Forums language | 4 Tested | 72 | 1 | Complete for local language v1 | Added `agent.language` detection, normalization, romanization, translation, and term extraction modules plus brokered language tools and CLI commands | Tests cover English/Spanish/Chinese/Japanese/Korean fixtures, simplified/traditional Chinese hints, mocked local-model Chinese translation, generated-translation labels, source chunk IDs, prompt-injection wrapping, no memory write, file reads through brokered `filesystem.read`, command registry entries, startup policy, and manifest validation | `language.detect`, `language.translate_text`, `language.summarize_multilingual`, and `language.extract_terms` execute through ToolBroker, PolicyEngine, and AuditLogger; file commands use brokered workspace file reads before language tools | No external/paid translation API by default, no translation/source text memory write, source text remains `UNTRUSTED_WEB`/`UNTRUSTED_DOCUMENT`, source-text instructions cannot call tools/change policy/reveal secrets/write memory, and missing local model returns setup guidance | Local mocked tests and setup-required CLI smoke only; live LM Studio/Qwopus translation quality validation remains opt-in | `docs/language/MULTILINGUAL_SUPPORT.md`, `docs/forums/MULTILINGUAL_FORUM_STRATEGY.md`, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Detection is heuristic, romanization is conservative, summaries are simple deterministic extracts, and external translation provider abstraction remains future/disabled by default | Next prompt: Cross-language Reddit/forum research workflow |
| Cross-language Reddit/forum research workflow | Forums research | 4 Tested | 70 | 1 | Complete for local mocked v1 | Added brokered `forums research` and `forums compare` commands plus a conservative workflow that searches configured source adapters, reports unavailable/setup-gated providers, detects languages, translates when requested, preserves source lists, and compares source/language item counts | Tests cover English-only mocked research, Chinese fixture translation labels, multiple-source comparison, unavailable source reporting, prompt-injection exclusion, no fabricated source URLs, CLI broker/audit path, no memory write, and command registry entries | `forums.research` and `forums.compare` execute through ToolBroker, PolicyEngine, and AuditLogger; provider/domain metadata is surfaced through `_audit`; unsupported providers return structured setup/unavailable output | No scraping behind login/CAPTCHA/anti-bot controls, no paid provider default, no workflow memory write, no search history, no cultural/statistical consensus claims from sparse data, and forum content remains `UNTRUSTED_WEB` | Local mocked tests and no-config CLI smoke only; live Reddit API, V2EX API, and approved web/Chinese discovery validation remain future/opt-in | `docs/forums/CROSS_LANGUAGE_RESEARCH.md`, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Default V2EX/web/Chinese discovery sources return unavailable/setup guidance until their own approved connectors or search-provider discovery paths exist | Next prompt: Global forum provider registry |
| Global Forum Provider Registry | Forums provider foundation | 4 Tested | 72 | 1 | Complete for local metadata registry v1 | Added static provider metadata and brokered read-only commands for Reddit, V2EX, Hacker News, Stack Exchange, Lemmy, Zhihu, Baidu Tieba, Douban Groups, Xiaohongshu, Weibo, and NGA | Tests cover registry loading, Reddit registration, V2EX disabled/default documented-API status, Chinese discovery-only provider site-filter metadata, unknown provider handling, no personal/logged-in/network status reads, CLI provider/capability output, command registry entries, startup policy, and manifest validation | `forums.providers`, `forums.status`, `forums.doctor`, and `forums.capabilities` execute through ToolBroker, PolicyEngine, and AuditLogger; commands return metadata only and do not initialize provider content reads | No provider status command reads personal data, cookies, private/logged-in pages, or provider content; Chinese non-V2EX platforms are discovery-only with site-filter guidance; write capabilities are absent/deferred; no content/query storage is introduced | Local mocked/metadata tests and CLI smokes only; live Reddit, V2EX, and Chinese discovery validation remain opt-in | `docs/forums/FORUM_PROVIDER_REGISTRY.md`, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Registry status is not provider health proof; Chinese discovery/fetch implementations still need separate connector/search-provider milestones and live validation | Next prompt: Chinese forum discovery/search layer |
| V2EX read-only connector | Forums connector | 4 Tested | 78 | 1 | Complete for local mocked v1 | Added documented API-only V2EX client/provider/cache/normalizer/tooling with disabled-by-default config, optional redacted token, brokered CLI commands, connector status metadata, generic forum model output, local rate limiting, TTL cache, and optional language/translation workflow integration | Tests cover missing config doctor, token redaction, mocked node topics, mocked topic, mocked replies, structured rate-limit error, mocked translation integration, absent write endpoints, command registry entries, startup policy, and capability manifest validation | `v2ex.status`, `v2ex.nodes.get`, `v2ex.node_topics`, `v2ex.topic.get`, `v2ex.topic_replies`, `v2ex.latest`, and `v2ex.hot` execute through ToolBroker, PolicyEngine, and AuditLogger; status/doctor make no provider calls | No posting/modifying/notification/member-profile endpoints, no web scraping fallback, no logged-in/private reads, no CAPTCHA/anti-bot bypass, token redacted, author metadata redacted by default, no memory write, TTL cache only | Local mocked tests and no-config/status CLI smokes only; live V2EX API validation remains opt-in after `V2EX_ENABLED=true` and token setup where needed | `docs/forums/providers/v2ex.md`, Chinese forum strategy, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | API 2.0 token requirements vary by endpoint; live endpoint behavior and translation quality are not validated by mocks | Next prompt: Chinese forum discovery/search layer |
| Chinese forum discovery/search layer | Forums discovery | 4 Tested | 74 | 1 | Complete for local mocked v1 | Added brokered `cn-forums providers`, `cn-forums search`, `cn-forums fetch`, and `cn-forums research` commands using approved search-provider site filters and safe selected-URL fetches only | Tests cover site-filter query generation, mocked search result normalization, blocked/login/CAPTCHA unavailable output, public fixture fetch and summary, language detection, mocked translation, no cookies/browser sessions, no memory write, CLI audit path, command registry entries, startup policy, and manifest validation | `cn_forums.providers`, `cn_forums.search`, `cn_forums.fetch`, and `cn_forums.research` execute through ToolBroker, PolicyEngine, and AuditLogger; search/fetch/research audit provider domains and setup/unavailable outcomes | No platform-specific scraping, login/cookie/session automation, CAPTCHA/anti-bot bypass, paid provider default, long-term storage, readable search history, browser automation, or forum-content training; all fetched content is `UNTRUSTED_WEB` | Local mocked tests and safe CLI smoke only; live search-provider/public-fetch validation remains opt-in and provider-config dependent | `docs/forums/CHINESE_FORUM_DISCOVERY.md`, provider stubs, README, command registry/test matrix, capability manifest, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Discovery depends on configured web search providers; many Chinese platforms may require login/CAPTCHA and will report unavailable rather than bypassing | Next prompt: Forum Intelligence dogfood/eval suite |
| Forum Intelligence dogfood/eval suite | Forums validation | 4 Tested | 76 | 1 | Complete for local mock-first v1 | Added default-disabled dogfood suites for Reddit core, Reddit research, multilingual forum language checks, V2EX, and Chinese forum discovery plus fixture-backed forum eval cases and `eval run/report --forums` CLI flags | Tests cover suite YAML validation, `all_safe` exclusion of live Reddit/V2EX/Chinese forum suites, fixture personal-data sanitation, forum eval pass conditions for source grounding/translation/no-bypass/retention/audit, CLI parsing, and command registry entries | Dogfood runner delegates to existing brokered CLI commands; fixture-backed evals perform no provider calls and validate expected audit evidence rather than bypassing ToolBroker or AuditLogger | No scraping, login/cookie/session automation, CAPTCHA/anti-bot bypass, paid provider default, forum-content training, memory write, live provider default, or author/query/body cache exposure is added by the harness | Local fixture tests, dry-runs, and forum eval only; live Reddit/V2EX/Chinese forum validation remains opt-in and provider-config dependent | `docs/forums/FORUM_DOGFOOD_RUNBOOK.md`, dogfood suite docs, README, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, prompt tracking, and completion report updated | Dogfood commands may still return setup-required results until providers/local model are configured; fixture evals are not proof of live provider readiness | Next prompt: Forum Intelligence release gate and maturity review |
| Forum Intelligence release gate and maturity review | Forums validation | 5 Hardened | 82 | 1 | Complete for local release gate | Ran the conservative release gate across Reddit policy/doctor/connector/search/thread/summarization/retention, language detection/translation, cross-language research, provider registry, V2EX, Chinese discovery, and dogfood/evals | Full suite passed; focused docs/command/forum/prompt tests passed; startup policy, capability manifest, command registry, forum dogfood dry-runs, fixture-backed forum evals, safe Reddit/V2EX status checks, Reddit cache/retention/privacy reports, language/provider CLI smokes, and no-bypass/no-write scans completed | Validation confirms existing forum commands and diagnostics remain brokered/audited; safe status checks did not fetch provider content; live content commands remain setup-gated | No new scraping, paid-provider default, posting/commenting/voting/DM/moderation action, login/cookie/session automation, CAPTCHA/anti-bot bypass, forum-content training, memory write, or permanent forum-content storage was added | Local release gate only; Reddit and V2EX are disabled/unconfigured locally, and live Reddit API, live V2EX API, live Chinese forum search/fetch, and live LM Studio/Qwopus translation quality validation remain opt-in/config-gated | Changelog, project state, feature registry, roadmap, command registry, command test matrix, completion report, risk register, threat model, test plan, release checklist, and prompt tracking updated | Release gate evidence supports local hardening, not live provider readiness or statistical representativeness of forum content | Next recommended task: release hardening loop or an explicitly scoped safe live-provider validation batch |
| Internet-only-when-needed router policy | Web/research routing | 4 Tested | 78 | 1 | Complete for deterministic local v1 | Router now emits explicit `needs_internet`, reason, suggested sources, provider policy, tools, risk hint, and clarification metadata; current/live/external/source-required prompts route to web search/fetch as needed; stable/local/no-tools prompts stay clean; `router explain` added as read-only diagnostics | Tests cover current-info routing, stable no-web routing, embedded URL fetch routing, citation/research routing, no-tools override, local-repo no-web behavior, prompt-injection no-route behavior, preflight metadata, router CLI output, and model-quality fixture updates | Router explanation is read-only and performs no provider/tool call; preflight uses ToolBroker dry-run for routed tools; downstream web/research tools still enforce PolicyEngine and AuditLogger | No LLM router by default, no user-message rewrite, no query-history persistence, no provider call during routing, prompt-injection-like text cannot force web, and paid-provider defaults remain disabled | Local deterministic tests only; live provider/source availability remains validated by downstream web/research tools | `docs/web/INTERNET_ROUTING_POLICY.md`, README, command registry/test matrix, feature registry, roadmap, threat model, risk register, project state, changelog, and completion report updated | Rule-based routing can be conservative for ambiguous stale facts; provider availability is reported at tool execution time via setup hints/unavailable results | Build citation/source attribution layer next |
| Secret/config doctor | Connector diagnostics | 4 Tested | 72 | 2 | Complete for config-only v1 | Implemented redacted credential presence checks for SerpAPI, WeatherAPI, Gmail, and Telegram plus focused Gmail/Telegram doctor commands; no real API calls or personal reads | Tests cover redaction, missing setup hints, tracked `.env` warning, repo-local token path warning, broad Gmail scope warnings, Telegram chat-id warnings, paid-provider disabled status, connector-status redaction, send disabled/CRITICAL policy, and CLI status/doctor output | Diagnostics are metadata-only, do not execute providers, and do not bypass ToolBroker for any provider action; provider calls remain future brokered work | Raw secrets are never printed; paid providers stay disabled under `free_first` unless explicitly allowed; Gmail/Telegram checks read env/config only, perform no inbox/chat access, and keep sends disabled by default and CRITICAL per-action | Local unit tests only; no live credential validation by design | README, command registry/test matrix, env example, feature registry/maturity, prompt tracking, completion report, risk/threat/test docs updated | Heuristic docs/tests/config secret scan can produce conservative warnings; actual credential validity is not verified until a future explicit live doctor is approved | Run `gmail doctor`, `gmail scopes`, `telegram doctor`, and `telegram status` manually with dummy/local env before any future real provider implementation |
| Secrets and API Key Management | Safety / configuration | 5 Hardened | 82 | 2 | SECRETS-01 through SECRETS-08 complete with local release gate | Added policy docs, API key inventory, env setup, Keychain guide, redaction policy, architecture decision, placeholder env template, `.gitignore` protections, static secret registry, recursive redactor, dependency-free `.env` parser, resolver, source metadata command, provider-specific doctor expansion, optional Keychain dry-run adapter, best-effort secret scanner/git preflight, user-guide setup flow, provider secret setup matrix, release-gate doc, and maturity review | SECRETS-01 docs tests, SECRETS-02 registry/redaction/CLI tests, SECRETS-03 resolver/env-loader tests, SECRETS-04 provider doctor tests, SECRETS-05 Keychain adapter tests, SECRETS-06 scanner/preflight tests, SECRETS-07 docs integration tests, SECRETS-08 release gate, command registry validation, policy-check, tracked/staged scan/preflight smokes, and full suite 1616 passed | `secrets list`, `secrets redaction-test`, `secrets policy`, `secrets sources`, Keychain dry-run commands, `secrets scan`, and `git preflight` route through ToolBroker/PolicyEngine/AuditLogger; provider doctors are config-only, value-redacted, and make no provider calls | Real secrets remain out of tracked files; `.env`, token/credential/private-key files, Keychain exports, and raw secret reports are ignored; no memory write; resolver reports presence/source only; provider doctors warn on paid/quota policy, repo-local token paths, and broad scopes; Keychain commands return metadata only; scanner/preflight reports redacted findings and does not rewrite history | Local tests only; no external scanner integration, real Keychain access, or live credential validation. Current tracked scan is ok with placeholder/test-fixture info findings only | `docs/secrets/SECRETS_MANAGEMENT_POLICY.md`, `API_KEY_INVENTORY.md`, `LOCAL_ENV_SETUP.md`, `MACOS_KEYCHAIN_GUIDE.md`, `SECRET_REDACTION_POLICY.md`, `SECRET_REGISTRY.md`, `SECRET_SOURCES.md`, `PROVIDER_SECRET_DOCTORS.md`, `MACOS_KEYCHAIN_INTEGRATION.md`, `SECRET_LEAK_SCANNING.md`, `PROVIDER_SECRET_SETUP.md`, `SECRETS_RELEASE_GATE.md`, `SECRETS_MATURITY_REVIEW.md`, `docs/git/SAFE_GIT_PREFLIGHT.md`, `docs/decisions/secrets_management_architecture.md`, `docs/USER_GUIDE.md` | Large dirty worktree still needs clean commit-boundary review; dedicated external scanners are not integrated | Recommended next: clean release-candidate boundary/commit review, then return to queued News provider registry or user-selected pack |
| Internet Access graduation track | Web/research governance | 4 Tested | 66 | 5 | Complete for docs-only roadmap reset plus local acquisition, provider registry, optional providers, safe fetch/extraction, source-grounded research, routing, citations, and cache/index v1 | Defined and partially implemented a staged internet access graduation path covering cost policy, acquisition core, robots/sitemap/feed support, provider registry, SearXNG, Brave, SerpAPI fallback, fetch hardening, source-grounded research, routing, citations, cache/dedupe, official APIs, dogfood/evals, and final release gate | Docs validation covers required policy files and no-bypass language; release-gate runs include full suite, startup policy, capability manifest, command registry validation, local search-provider registry tests, provider mock tests, safe fetch/extraction tests, source-grounded research workflow tests, and cache/index tests | Runtime acquisition, selected-URL fetch/extraction, source-grounded research, and cache/index commands remain ToolBroker/PolicyEngine/AuditLogger governed and paid/binary/personal paths remain gated; provider execution is explicit/configured-only and brokered | Web content remains untrusted, blocked pages return unavailable instead of bypass attempts, paid APIs are optional fallback only, no public SearXNG default exists, no fabricated citations are allowed, and no query/content memory storage is allowed by default | No live SearXNG/Brave/SerpAPI or arbitrary live website validation in this task by design | Internet access decision record, web policy docs, search-provider registry docs, safe fetch/extraction docs, source-grounded research docs, cache/index docs, provider docs, roadmap, registry, maturity, risk, threat, release checklist, project state, changelog, and completion report updated | This is not user-ready proof of mature internet research; official APIs, dogfood/evals, final release gate, and live validation remain future work | Next safe prompt: official API connector framework |
| Agent DNA / Cloneability | SDLC governance and provenance | 4 Tested | 74 | 6 | Complete for docs/provenance v1 | Implemented docs for Agent DNA, architecture principles, clone blueprint, model/platform migration, rewrite checklist, build history, provenance, decision index, reconstructed prompt packs, and release gate | Cloneability docs validation covers required files, README/AGENTS/SPEC/SDLC alignment, reconstructed pack labels, registry and maturity entries | Docs-only; no runtime execution path added; preserves ToolBroker/Policy/Permission/Approval/Audit invariants as clone criteria | Reconstructed prompt packs are clearly marked non-exact unless evidence exists; provenance rules warn against overclaiming and treating prompt text as authority | Not applicable; docs/provenance only | README, SPEC, SDLC, AGENTS, registry, maturity, roadmap, project state, completion report, changelog, prompt tracking, release gate docs updated | A clean-room clone has not yet been attempted; most reconstructed prompt packs are summaries, not exact originals | Run a future clone dry-run in a clean repo and promote only evidence-backed reconstructed packs |
| Apple Ecosystem + Lead Response Track | Roadmap and architecture planning | 1 Specified | 36 | 1 | Complete for architecture and workflow planning | No runtime implementation; decision records and workflow docs define Apple ecosystem boundaries, consumer Messages limits, Apple Messages for Business direction, LeadInbox schema, and lead response rollout | Docs validation/startup policy only; no runtime tests because no connector, command, provider, or capability was added | Future runtime work must use ToolBroker, PolicyEngine, ApprovalManager, AuditLogger, selected-scope personal-data gates, and CRITICAL per-action send approval | Planning rejects private Messages database scraping, broad Full Disk Access, hidden polling, automatic consumer iMessage sends, lead auto-response without a future policy, and memory storage of lead content by default | Not applicable; planning-only | Decision records, workflow spec, roadmap, feature registry, risk register, threat model, project state, changelog, and completion report updated | This track is not implemented; LeadInbox is a schema/rollout plan only and follow-on prompts remain queued or blocked | Build `MESSAGE-CHANNEL-ABSTRACTION` next if the user wants implementation, starting with metadata/interfaces and no sends |
| Apple Messaging / iMessage Roadmap Track | Roadmap and architecture planning | 1 Specified | 38 | 1 | Complete for message-specific planning | No runtime implementation; decision records distinguish iOS user-confirmed compose, macOS Messages automation probe, Apple Messages for Business, personal iMessage vs business messaging, and the messaging rollout order | Docs validation/startup policy/capability validation only; no runtime tests because no connector, command, provider, capability, send path, or personal-data read was added | Future runtime work must use ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, selected-scope gates, exact Action Center previews, and CRITICAL no-reuse send approval | Planning rejects private Messages database scraping, broad Full Disk Access, hidden polling, automatic/silent sends, bulk sends, non-allowlisted test sends, and platform-path invention when no safe path exists | Not applicable; planning-only | Messaging decision records, messaging rollout plan, roadmap, feature registry, risk register, threat model, project state, changelog, and completion report updated | This track is not implemented; macOS send adapter and approved lead send remain blocked until future explicit decisions and release gates | Build `MESSAGE-CHANNEL-ABSTRACTION` next as metadata/interfaces only; do not implement sends |
| Message Channel Abstraction v1 | Messaging foundation | 4 Tested | 72 | 1 | Complete for v1 schema, registry, validation, and preview boundary | Implemented channel-neutral models, channel registry metadata, workspace draft preview/validation tools, brokered CLI commands, and send request model defaults; macOS execution is separate adapter-specific gated work | Tests cover draft model validation, missing recipient, empty body, multiple recipients, unknown channel denial, CRITICAL send request defaults, approval reuse false, untrusted source trust labels, brokered channel/draft validation, and no direct/silent send path | `messaging.*` inspection commands route through ToolBroker, PolicyEngine, and AuditLogger; send request models are CRITICAL, approval-required, exact-preview, and no-reuse by default | Unknown channels denied; group/bulk recipients rejected; attachments unsupported for send requests; no Messages database, Full Disk Access, hidden polling, direct/silent send, or personal-data default enablement | Local targeted tests and CLI smoke only; no live provider validation by design | README messaging architecture, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | V1 is a foundation only; local draft store is JSON under `./workspace/messaging/drafts`; executable adapters must still pass safety policy and Action Center gates | Keep non-macOS channels draft/handoff only until separate adapter prompts |
| Message Safety Policy and Action Center integration | Messaging foundation | 4 Tested | 76 | 1 | Complete for v1 send-action review policy | Implemented brokered local draft creation, draft preview alias, CRITICAL send-action queuing, exact local Action Center previews, draft-edit approval invalidation, allowlist/rate-limit preview fields, generic future-disabled send manifest entry, and macOS-gated send action type | Tests cover send action requiring a draft, CRITICAL exact preview fields, approval reuse denial, draft-edit invalidation, non-interactive/no direct send path for generic channels, untrusted-content gating, bulk recipient rejection, lifecycle audit, redacted audit/export payloads, disabled generic send manifest, and macOS-gated action creation | Draft/action creation go through ToolBroker, PolicyEngine, Action Center, ApprovalStore, and AuditLogger; CRITICAL send proposals are explicit per-action only and no-reuse | Generic `messaging.send_approved` remains unregistered, attachments and bulk/group recipients are denied, untrusted content cannot create send actions without explicit user request, and audit/export records redact bodies/recipients while local previews remain exact | Local targeted tests only; macOS live send validation is separate and disabled by default | README messaging architecture, command registry/test matrix, feature registry, roadmap, risk register, threat model, release checklist, project state, changelog, and completion report updated | Message safety infrastructure is reusable; executable sends remain adapter-specific and must pass per-adapter gates | Build or validate adapter-specific sends only by explicit prompt; keep non-macOS channels draft/handoff only |
| iOS user-confirmed compose bridge | Messaging foundation | 4 Tested | 72 | 1 | Complete for agent-side mock/interface v1 | Implemented local handoff payload schema, integrity hash, expiration, brokered payload creation/status/result recording, and CLI payload/status commands; no iOS app, deep-link transport, attachment support, or send adapter implemented | Tests cover approved draft payload creation, unapproved blocking unless compose-only mode is used, payload expiration, approved-preview body/recipient mismatch rejection, sent/cancelled/failed result recording, audit logs, and absence of silent send tools | `messaging.ios_compose.*` tools execute through ToolBroker, PolicyEngine, and AuditLogger; approved-action mode verifies Action Center status and draft fingerprint before payload creation | Payloads require exact local draft data, expire, include nonce/integrity hash, never execute a send, never read Messages databases, write no memory, and cannot mark complete unless iOS reports sent or queued | Local tests only; no companion app or device handoff live validation yet | README messaging architecture, iOS companion decision record, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | V1 is a mock/local bridge; the user still needs a future iOS app or manual deep-link path, attachments remain deferred, and delivery tracking is limited to returned result metadata | Build macOS Messages feasibility probe next as probe-only; do not implement automatic sends |
| macOS Messages automation feasibility probe | Messaging foundation | 4 Tested | 70 | 1 | Complete for safe metadata-only feasibility probe | Implemented brokered `messages.probe` with platform detection, standard Messages.app location detection, harmless AppleScript app id/version lookup where possible, permission-denied interpretation, local connector-status result, and permission explanation output | Tests cover non-mac unsupported result, mac mock metadata result, permission-denied setup instructions, no send/private path access, result serialization, audit logging, and capability manifest safety fields | `messages.probe` executes through ToolBroker, PolicyEngine, and AuditLogger; connector status can show the last local probe result | Probe never reads `~/Library/Messages`, does not require Full Disk Access, does not read message content or account status, does not press Send, and leaves `send_capability_known=false` | Local mocked tests plus safe local CLI path only; no live send validation by design | README messages docs, macOS automation strategy, probe result decision record, command registry/test matrix, risk register, threat model, project state, changelog, and completion report updated | Probe only determines safe metadata feasibility; macOS live-send proof is a separate CRITICAL disabled-by-default probe | Use `messages macos status` before considering any explicitly approved live-send probe |
| macOS approved iMessage send adapter v1 | Messaging foundation | 4 Tested | 68 | 1 | Complete for disabled-by-default gated adapter | Implemented `messages.macos.status`, recipient allowlist management, CRITICAL live-send probe, CRITICAL `messages.macos.send_approved`, and `messages send --from-action`; adapter requires macOS, connector enablement, send allow flag, allowlisted recipient, recent live probe, exact approved Action Center item, and rate limit | Tests cover disabled default, CRITICAL exact action preview, approved-action requirement, allowlist gate, live-probe gate, approval one-shot consumption, rate limit, non-interactive block, unsupported platform, probe failure disabling send, no private DB access, bulk/group denial, and audit body redaction | All status/probe/send actions execute through ToolBroker, PolicyEngine, ApprovalManager, Action Center, and AuditLogger; CRITICAL sends are per-action/no-reuse and consume approval once | No private `~/Library/Messages` access, no Full Disk Access, no UI Send-button scripting, no background responder, no bulk/group sends, no attachments, no non-allowlisted recipients, no memory write, and unsupported machines return clear fallback guidance | Local mocked tests only; no real Messages.app send was attempted in this release gate | README, macOS automation strategy, probe results, messages handoff workflow, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Maturity is conservative because live send behavior depends on this Mac's Messages.app/AppleScript permissions and must be user-approved; default config blocks all sends | Run `messages macos status`; only if the user wants live validation, set explicit config and run the live-send probe to a self-test recipient |
| Incoming message strategy and manual/mock inbox v1 | Messaging foundation | 4 Tested | 72 | 1 | Complete for manual/mock inbox v1 | Implemented workspace-only manual import, local inbound store, mock provider, inbox list/show, Lead Inbox candidate metadata, and local draft-reply creation; no real provider reads, watcher, or sends | Tests cover workspace import, unsafe path and private `chat.db` denial, `UNTRUSTED_MESSAGE` labeling, list/show, mock inclusion, Lead Inbox candidate shape, draft-only replies, prompt-injection filtering, CLI commands, no send path, no memory write, and audit lifecycle | `messages.inbox.*` commands execute through ToolBroker, PolicyEngine, and AuditLogger; import audits file reads/writes and draft-reply audits draft writes | Manual imports are workspace-bounded; private Messages databases and Full Disk Access are blocked; no background watcher, auto-reply, bulk read, contact harvesting, provider read, or memory write exists | Local targeted tests and CLI path only; no live Apple/Gmail/Telegram/Apple Messages for Business provider validation by design | README, incoming strategy decision, workflow doc, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | Manual imports can contain personal data, so workspace files remain local and untrusted; real provider adapters and background/webhook ingestion remain future selected-scope work | Build Apple Messages for Business provider strategy next, or add messaging dogfood suite coverage before any provider adapter |
| Lead Inbox abstraction v1 | Lead response foundation | 4 Tested | 74 | 2 | Complete for mock-only v1 schema and workflow foundation | Implemented channel-neutral lead models, source registry, mock provider, deterministic classifier, brokered lead list/classify/summarize/draft/follow-up/meeting tools, CLI commands, MessageDraft creation, and Action Center follow-up task drafts; no real provider reads or sends | Tests cover mock lead listing, selected read approval behavior, selected read disabled-by-default manifest, classification with no actions, summaries, draft-response creating MessageDraft not send, no memory writes, prompt-injection filtering, follow-up task Action Center item, meeting suggestion no-calendar/no-event behavior, and audit logs | Lead commands execute through ToolBroker, PolicyEngine, Action Center where needed, and AuditLogger; selected full-message reads are HIGH and disabled by default, future sends are CRITICAL per-action/no-reuse | Lead content remains `UNTRUSTED_MESSAGE`, instruction-like content is filtered from drafts, no CRM sync or auto-response exists, no personal providers are enabled, no private Messages database is read, meeting suggestions do not read calendars, and no memory write occurs by default | Local targeted tests and mock CLI path only; no live Gmail/Telegram/Apple provider validation by design | README, `docs/workflows/lead_inbox.md`, `docs/workflows/lead_response_drafting.md`, command registry/test matrix, feature registry, roadmap, risk register, threat model, changelog, and completion report updated | V1 uses synthetic/mock/local records only; full-message selected reads and provider adapters remain disabled/future; follow-up task execution and sends remain separate CRITICAL Action Center approval paths | Build selected lead-source adapters only after provider-specific decisions and release gates; next safe prompt is messaging dogfood suites or provider-specific selected-source planning |
| Lead Response Drafting workflow v1 | Lead response foundation | 4 Tested | 74 | 1 | Complete for draft-only v1 | Implemented summarize, classify, draft-response, suggest-followup, and suggest-meeting commands over mock/local lead records; drafts include source and assumptions; follow-up suggestions create pending Action Center task actions only | Tests cover mock lead draft, selected-read approval requirement, prompt-injection filtering, response draft creation, pending follow-up task action, meeting suggestion with no event/calendar read, no send, no memory write, and audit logging | Workflow executes through ToolBroker, PolicyEngine, Action Center where needed, and AuditLogger; future full selected reads remain HIGH/disabled and sends remain CRITICAL per-action/no-reuse | Untrusted source text cannot request tools, approvals, policy changes, memory writes, or sends; no real providers are read, no calendar availability is checked, no event/task/message is executed, and no private app data is accessed | Local targeted tests only; no live Gmail/Telegram/Apple provider validation by design | README, `docs/workflows/lead_response_drafting.md`, command registry/test matrix, project state, completion report, registry, maturity, roadmap, risk register, threat model, and changelog updated | Uses mock/local Lead Inbox records only; meeting times are guidance text, not availability; approved sends and live provider reads are future/blocked | Run messaging dogfood suites next, then plan selected-source provider adapters separately |
| Approved Lead Response Send workflow v1 | Lead response foundation | 4 Tested | 71 | 1 | Complete for local approval-gated orchestration; live sends still adapter-gated | Implemented `leads create-send-action`, `leads send --from-action`, `leads handoff`, and `leads mark-responded` over reviewed lead-linked drafts; unsupported channels return fallbacks and iOS compose creates handoff payloads only | Tests cover CRITICAL send proposal creation, Action Center approval requirement, approval reuse denial, draft edit invalidation, unsupported manual-channel fallback, iOS compose handoff, handoff save/copy actions, local responded status updates, and macOS Messages gating | Create/handoff/status commands execute through ToolBroker, PolicyEngine, Action Center, and AuditLogger; execution routes only to existing channel gates and never through a direct `lead.send_approved` tool | CRITICAL sends require exact preview, single recipient, no attachments, no bulk response, no auto-send, no memory write, and no fake provider success; macOS remains disabled unless config/allowlist/live probe/rate limit all pass | Local mocked tests only; no live iOS companion, macOS Messages send, Apple Business provider, Gmail, Telegram, or email send validation by design | README, `docs/workflows/lead_response_send.md`, lead inbox/drafting docs, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | The workflow is safe to rely on for queuing and handoff; actual delivery remains channel-specific and may be unsupported until a provider is configured and live-validated | Run messaging dogfood suites next, then live-validate only selected channels with explicit user approval and self-test recipients |
| Apple Messages for Business provider stub | Lead response foundation | 4 Tested | 70 | 1 | Complete for mock/local provider v1 | Implemented provider model, config-only doctor/status, local mock inbound lead creation, local conversation status, and local draft-response creation for `channel=apple_messages_for_business`; no live provider calls or sends | Tests cover not-configured setup hints, mock inbound Lead Inbox mapping, `UNTRUSTED_MESSAGE` trust label, draft-only response, disabled future send manifest, connector status disabled, secret redaction, audit redaction, and CLI commands | `apple_business.*` commands execute through ToolBroker, PolicyEngine, and AuditLogger; live connector remains disabled by default; future send capability is CRITICAL, default-disabled, per-action, and no-reuse | No hardcoded credentials, no provider API call, no personal iMessage automation, no Messages database access, no Full Disk Access, no bulk/group send, no send action creation, no memory write, and untrusted inbound content is filtered in drafts | Local mocked tests only; no provider account, webhook, or live send validation by design | README, Apple Business provider decision record, strategy doc, Lead Inbox docs, command registry/test matrix, feature registry, roadmap, risk register, threat model, project state, changelog, and completion report updated | V1 stores mock lead records under `./workspace/leads/apple_business`; provider selection, live webhook security, credentials, and approved provider sends remain future work | Build lead response drafting/dogfood next; design live Apple Business provider adapter only after provider-specific setup and release gate |
| Agent Runtime Orchestration v1 | Core runtime | 4 Tested | 74 | 10 | Complete for metadata/control-plane v1 | Implemented lightweight runtime models, state, service registry, feature flags, kernel/lifecycle, event bus, metadata job queue, workflow runner, scheduler policy, frontend bridge contract, and read-only CLI commands | Tests cover model serialization, risky feature defaults, lightweight kernel status, service lazy loading, event redaction/untrusted-control denial, workflow/job blocking rules, scheduler policy, frontend bridge denial, runtime CLI commands, and command registry validation | Runtime does not execute tools, call LM Studio, grant approvals, change policy, read personal connectors, or replace AuditLogger; real side effects remain behind ToolBroker/PolicyEngine/ApprovalManager/AuditLogger | Personal-data and CRITICAL features are disabled by default; CRITICAL workflows become blocked jobs; scheduler blocks background persistence and CRITICAL actions; frontend bridge cannot approve, execute tools, or change policy | Local CLI/tests and 2026-05-23 ORCH recovery validation only; no app frontend or long-running runtime live validation yet | Runtime architecture docs, frontend contracts, command registry/test matrix, README, release gate, project tracking, risk register, threat model, completion report, and ORCH recovery report updated | V1 is in-process metadata only with no persistent job store, daemon, native app bridge, real executor, or background scheduler | Reuse as the governance layer for future app/dashboard integration; add persistent/runtime server only after a separate decision and approval gate |
| Weather connector | External connector | 7 User-Ready | 93 | 20+ | Complete | Implemented with cost-aware auto selector, Open-Meteo free-first provider, NWS U.S.-only official alerts/data provider, WeatherAPI optional fallback, WeatherKit stub, explicit safe preferences, and weather-aware web research | Broad provider, cache, preferences, router, CLI, audit, error, NWS forecast/alerts, WeatherKit stub, WeatherAPI selector/gating/mock normalization, provider-decision audit, secret-redaction, no-memory, weather+web impact tests, and city/state live-eval regression coverage | ToolBroker-only, LOW risk, audited, rate-limited; provider choice included in structured results and audit summaries | Hardened for no system location inference, opt-in default location, default-use audit source, structured errors/setup hints, no precise-location cache, free-first provider order, WeatherAPI paid/default gates, NWS U.S.-only guard, WeatherKit credential non-disclosure, web-only-when-needed impact routing, and Open-Meteo city/state abbreviation fallback | Open-Meteo safe eval passed on 2026-05-23; WeatherAPI is mock-validated only; NWS/web impact live smoke remains optional | CLI doctor/providers/provider auto/smoke/current/forecast/alerts/config, WeatherKit decision record, readable formatter, setup docs | WeatherKit is intentionally stubbed; NWS current uses first hourly forecast period as best-effort observational summary; WeatherAPI live use requires user key and explicit/cost-policy opt-in | Run optional live `weather provider auto`, NWS alerts, and explicit WeatherAPI smoke only with user-configured provider settings |
| Web search/fetch/research/acquisition | External connector and workflow | 5 Hardened | 88 | 12 | Complete | Implemented with Brave provider option, explicit SerpAPI fallback, disabled fallback, central `agent.web_acquisition` request/source/provider models, no-fetch source-status inspection, core robots/sitemap/feed parser modules, and free-first acquisition for cache lookup, robots.txt, sitemap, RSS/Atom, direct public URL, and query/domain acquisition limitations | Broad mocked search/fetch/research tests, including provider missing, SerpAPI missing-key/cost-policy/timeout/normalization/audit behavior, fetch failures, blocked domains, prompt injection, foreign-language content, no fabricated sources, no memory writes, audit logs, robots allow/disallow and malformed handling, sitemap XML/index/malformed handling, RSS/Atom parsing and MIME allowlist, sitemap/feed binary denial, timeouts, max limits, TTL cache reuse, CAPTCHA/block-page unavailability, untrusted wrapping, domain/source-status audit, alias capability execution, and paid-provider skip defaults | ToolBroker-only, audited, rate-limited, `UNTRUSTED_WEB`/`UNTRUSTED_DOCUMENT`; acquisition commands, alias capabilities, and `web.source_status` use the cost-policy provider ladder without paid default calls; SerpAPI is a separate explicit `web.search.serpapi` path | Prompt injection, fetch limits, blocked/private domains, robots disallow, no CAPTCHA/anti-bot/login bypass, first-class fetch failure reporting, no fabricated sources, no query-history memory, paid-provider skip reporting, no-fetch source inspection, and SerpAPI secret/query redaction covered | Live Brave/SerpAPI/provider smoke requires user key; live direct URL/feed/sitemap smoke still pending | README, command registry, connector cost docs, `docs/web/ROBOTS_AND_RATE_LIMITS.md`, `docs/web/FEEDS_AND_SITEMAPS.md`, and research/acquisition commands documented | Query acquisition without a direct URL/domain/cache hit returns a clear limitation unless a free configured provider exists; source-status is inspection only and does not prove the source can be fetched; SerpAPI remains paid/quota-limited and explicit only; live provider validation depends on configured services | Run live `web source-status https://example.com`, `web acquire-url https://example.com`, `web robots example.com`, `web sitemap example.com`, and `web feed <known-safe-feed-url>`, then record result |
| Browser selected URL and clipping | External/browser connector | 4 Tested | 74 | 2 | Complete for URL-workflow v1 | Implemented explicit URL read, summarize, and workspace clipping workflow plus selected-tab unavailable stub; canonical capability names are tracked with legacy aliases retained | URL summarize through brokered web fetch, blocked domain denial, prompt-injection filtering, workspace-only clip, canonical capability manifest names, no history/cookie access, selected-tab stub, and audit tests | URL workflows use `web.fetch_url` and `filesystem.write` through ToolBroker/PolicyEngine/AuditLogger; selected-tab stub remains disabled and audited as `browser.selected_tab` | No browser history, cookies, sessions, forms, passwords, profile database scraping, form submission, or browser automation; clips are workspace-only and labeled `UNTRUSTED_DOCUMENT` | Local mocked tests only; no live webpage smoke captured | README and decision record updated | Native selected-tab support is intentionally not implemented; explicit URL fetch depends on web access | Run live `browser summarize-url https://example.com` smoke when web access is available |
| Notes / Knowledge Capture v1 | Workspace workflow | 4 Tested | 75 | 2 | Complete for workspace inbox v1 | Implemented local workspace captures from explicit notes, workspace files, and URLs plus list, summarize, and memory promotion command | Capture note, from-URL brokered fetch, from-file workspace policy, explicit `--trusted-user` file marker, unsafe path denial, secret rejection, memory promotion policy path, personal-data default block, prompt-injection filtering, audit, and CLI tests | Captures write through `filesystem.write`; files read through `filesystem.read`; URLs fetch through `web.fetch_url`; promotion calls `memory.store` through ToolBroker/PolicyEngine/AuditLogger | No Apple Notes integration, no private app database scraping, workspace-only storage, secrets rejected before writing, URL/file content treated as untrusted by default, trusted file captures require explicit user marker, and personal-looking content blocked from default memory promotion | Local mocked tests only | README and tracking docs updated | No Apple Notes provider; capture deletion/editing UI is not implemented yet; personal memory promotion requires future approved design | Add capture edit/delete/export if needed, then live smoke on real workspace notes |
| Workspace file assistant | Project tools | 5 Hardened | 82 | 5 | Complete | Implemented for bounded project filesystem/git/test actions plus `files` CLI workflow | Broad path, denial, write, backup, patch diff, untrusted-document, git, pytest, CLI, and audit tests | ToolBroker-only, audited, allowed-root enforcement; read content labeled `UNTRUSTED_DOCUMENT` | Path traversal, denied paths, `.env`, private app folders, large file limits, backups, and instruction-injection summaries covered | Local repo tests validate behavior | README command examples and milestone docs cover usage | No arbitrary shell, delete shortcut, or git push by design | Live user workflow smoke on real project files when desired |
| Memory | Memory | 5 Hardened | 83 | 5 | Complete | Implemented with SQLite persistent memory, category-aware search, export/delete/clear lifecycle, and bounded context injection | Preference, project fact, workflow lesson, secret refusal, personal approval, email body refusal, category search, context budget, no personal injection, export/clear, CLI, and audit tests | ToolBroker-only and audited; personal memory approval-gated; context injection audits injected IDs | Secret refusal, personal-data default denial, scoped/category search, non-personal context injection, deletion lifecycle covered | Local tests only | README documents commands, categories, context injection, and deletion limits | No advanced embeddings; deletion cannot rewrite audit logs or filesystem backups | Live UX smoke and optional richer memory viewer later |
| Personal connector readiness gate | Safety release gate | 5 Hardened | 84 | 1 | Complete | Checklist and evidence run completed; no connector implementation added | Full suite, startup policy, capability manifest validation, direct-access scans, and ToolBroker-bypass scans passed | Confirms ToolBroker, PolicyEngine, ApprovalManager, AuditLogger, connector registry, and personal defaults before personal connector work | Confirms selected-scope defaults, HIGH/CRITICAL approval rules, untrusted wrappers, non-interactive blocking, and no personal memory by default | Local validation only; no live personal-data reads by design | Checklist, release checklist, risk register, registry, roadmap, project state, and completion report updated | Does not replace connector-specific approval/live-smoke gates | Proceed to calendar read-only selected date range with explicit approval gate |
| Calendar read-only | Personal connector | 5 Hardened | 78 | 6 | Complete | Adapter and optional AppleScript path implemented, disabled by default | Disabled-module, approval, selected-range, selected-event, broad-range, notes/body omission, availability privacy, memory, audit, CLI, and event-text-as-data tests | HIGH risk, disabled by default, approval-gated, audited as `LOCAL_PRIVATE_DATA`; event title/id args redacted | Range limits, selected event token/date-title access, notes omission, location redaction, no memory storage, and event text labeled as data rather than instructions covered | Not live-validated against Calendar.app in this environment | README setup and limitations exist | Needs explicit live local smoke with user approval | Run dry-run then selected-range/selected-event live smoke only with approval |
| Calendar approved writes v1 | Personal write connector | 4 Tested | 74 | 2 | Complete for Action Center-gated stub execution | Draft commands create Action Center records; `--from-action` consumes approved records once and calls brokered write tools; low-level write tools verify the approved Action Center action id; live external write provider remains deferred | Draft pending action, create/update/delete approval-required, denial block, approved create one-shot, direct broker bypass denial, rollback token capture, notes omission, audit lifecycle, manifest disabled-by-default, and CLI smoke tests | Uses Action Center plus ToolBroker, PolicyEngine, ApprovalManager, and AuditLogger; capabilities remain disabled by default and CRITICAL per-action; direct broker calls without verified Action Center action ids are rejected | No direct chat writes, no automatic invites, no recurring events, no full calendar export, notes/body omitted unless explicitly allowed, and no memory writes by default | Local tests and draft CLI smoke only; no live Calendar.app writes | README, changelog, feature registry, project state, threat model, risk register, and completion report updated | Current connector is a no-external-change stub unless a future safe native write provider is approved | Design/review native Calendar.app/EventKit write provider separately before any live writes |
| Contacts read-only | Personal connector | 5 Hardened | 76 | 5 | Complete | Adapter and optional Contacts.app path implemented, disabled by default | Disabled-module, approval, no-bulk, compact search, requested-field, sensitive-redaction/config, memory, audit, CLI, and contact-text-as-data tests | HIGH risk, disabled by default, approval-gated, audited as `LOCAL_PRIVATE_DATA` | No bulk export, selected tokens, no notes, sensitive field redaction/config gates, no memory storage, and contact text labeled as data rather than instructions covered | Not live-validated against Contacts.app in this environment | README setup and limitations exist | Needs explicit live local smoke with user approval | Run selected-scope live smoke only with approval |
| Contacts approved edits v1 | Personal write connector | 4 Tested | 74 | 2 | Complete for Action Center-gated stub execution | Draft update/create commands create Action Center records with field-level previews; `--from-action` consumes approved records once, injects the verified `action_id`, and calls brokered write stubs; live external write provider remains deferred | Disabled-by-default manifest, pending update/create action, approval-required, denial block, approved one-shot update, direct broker bypass denial, approved-preview arg matching, exact field diff, phone/email/address redaction, bulk-edit denial, delete-deferred, no-memory, and audit lifecycle tests | Uses Action Center plus ToolBroker, PolicyEngine, ApprovalManager, and AuditLogger; capabilities remain disabled by default and CRITICAL per-action; low-level write tools require verified Action Center action ids | No direct chat edits, no direct broker edits without Action Center verification, no bulk export/edit, no contact delete, sensitive field values redacted in stored previews/audits, and no memory writes by default | Local focused tests only; no live Contacts.app writes | README, changelog, feature registry, project state, risk register, threat model, test plan, release checklist, and completion report updated | Current connector is a no-external-change stub unless a future safe native write provider is approved; exact sensitive values are not persisted for live writes yet | Design/review native Contacts write provider separately before any live edits |
| Reminders / Tasks connector v1 | Personal connector | 4 Tested | 75 | 2 | Complete for adapter/mock v1 | Adapter interface, mock provider, brokered list/create/update/complete/delete tools, brokered `tasks.draft_create` Action Center queuing, and CLI commands implemented | Disabled module, list approval, brokered draft-create, create approval, complete/delete approval, mock provider success paths, no-memory, audit, manifest, and CLI smoke tests | ToolBroker-only execution; draft-create is MEDIUM and queues an Action Center record only; list is HIGH approval; writes are CRITICAL per-action; create executes from approved Action Center records | No full export, selected-scope list, no memory writes, task title/id/notes redaction, no native database scraping, no Full Disk Access dependency | Local tests and draft CLI smoke only; no live Reminders provider | README, decision notes, risk register, threat model, registry, roadmap, project state, and completion report updated | Native Reminders provider deferred; update/complete/delete direct CLI approval UX depends on interactive approval availability | Design native Reminders connector separately before live local writes |
| Email metadata + selected-thread + draft-only | Personal workflow | 5 Hardened | 80 | 6 | Complete | IMAP adapter option plus metadata, selected-thread read, summary, draft-only tools, and triage workflow, disabled by default | Disabled-module, metadata approval, thread approval, untrusted body, prompt-injection, draft-only, no-send, no-memory, audit, CLI, metadata-trust, and triage tests | HIGH risk, disabled by default, approval-gated, audited as `UNTRUSTED_EMAIL`, body args redacted | No send/delete/move/archive, no bulk read, metadata/body untrusted labels, untrusted body wrapper, prompt-injection filtering, triage body redaction, no body memory covered | Not live-validated with real account | README setup, triage commands, and limitations exist | Real provider setup and OAuth/keychain handling remain immature | Run with a deliberately configured non-production account only after explicit approval |
| Email approved send v1 | Personal send connector | 4 Tested | 75 | 2 | Complete for Action Center-gated mock/stub execution | Draft-new and reviewed draft-reply commands create Action Center records; `send --from-action` consumes approved records once, injects the verified `action_id`, and calls brokered send tool; real provider remains deferred | Disabled-by-default manifest, CRITICAL approval, no approval reuse, denial block, edit invalidation, full body preview, missing recipient block, attachment block, untrusted reply context, mock send, direct unreviewed send block, direct broker bypass denial, approved-reviewed-draft arg matching, and audit lifecycle tests | Uses Action Center plus ToolBroker, PolicyEngine, ApprovalManager, and AuditLogger; capability remains disabled by default and CRITICAL per-action; low-level send tool requires verified Action Center action ids | No direct unreviewed model-output sends, no direct broker sends without Action Center verification, no background sends, no bulk sends, attachments blocked, email thread content labeled `UNTRUSTED_EMAIL`, and no memory writes by default | Local tests and mock provider only; no live email send provider | README, changelog, feature registry, risk register, threat model, test plan, release checklist, project state, and completion report updated | Real provider/OAuth/keychain path not implemented; mock provider is test-only; body is intentionally visible in Action Center preview | Create provider decision record before any real email send integration |
| Messages/text draft-only | Personal workflow | 5 Hardened | 75 | 5 | Complete for safe fallback | Stubbed live connector plus explicit `messages.draft_from_text` workspace-file fallback, disabled by default | Disabled-module, no-send, bulk denial, unsafe-adapter, workspace-only fallback, untrusted content, prompt-injection, draft-only, no-memory, audit, CLI, and manifest tests | HIGH risk, disabled by default, approval-gated, audited as `UNTRUSTED_MESSAGE`; fallback file reads audited | No send/delete/move/archive/contact harvesting, no database scraping, no Full Disk Access, workspace-only fallback, untrusted wrapper, prompt-injection filtering, and no body memory covered | No live safe Messages connector by design | README setup and limitation docs exist | Live macOS Messages path intentionally absent | Keep live connector blocked until a safe permissioned integration exists |
| Messages safe handoff v1 | Personal handoff workflow | 4 Tested | 77 | 3 | Complete for draft-id save/copy handoff | Draft-from-text and manual draft commands create local `MessageDraft` records; handoff creates Action Center save/copy records; save writes inside workspace and copy uses clipboard/mock only; no automatic send path implemented | Workspace-only draft reads, unsafe path denial, untrusted labeling, prompt-injection filtering, no send tool, local draft-id handoff, lead-draft creation, save/copy approval, direct broker bypass denial, approved-preview arg matching, workspace save, mock clipboard copy, no-memory, audit lifecycle, and CLI draft/handoff tests | Draft, handoff, save, and copy execute through ToolBroker/PolicyEngine; save/copy require verified Action Center action ids and approved-preview arg matching; all actions audited | No Messages DB scraping, no Full Disk Access, no automatic send, no bulk read, no memory writes, clipboard risk documented, saved drafts constrained to workspace | Local tests only; no live clipboard smoke captured beyond mock | README, workflow doc, and decision record updated | Clipboard can expose personal data to local apps; automatic send remains deferred | Create a separate provider decision and approval model before any automatic text sending |
| Daily briefing | Workflow | 5 Hardened | 82 | 5 | Complete for v2 | Implemented configurable opt-in sections for weather, calendar date range, tasks/reminders, email metadata, web topics, memory preferences, suggested actions, dry-run, and config show/set | Weather-only, calendar/tasks/email approval skip, denied approval, suggested Action Center action, no write/send, dry-run, audit, no-memory, and config tests | All selected sections execute or dry-run through ToolBroker; calendar/tasks/email are HIGH risk and approval-gated when enabled; suggested actions queue Action Center records only | No inferred location, no email bodies/messages/contacts/browser history, no writes/sends, no memory writes, unapproved personal sections skipped, and each section can fail independently | Local tests only; live personal sections require explicit user approval/config | README commands, config commands, and limitations documented | Personal sections are skipped by default because personal tools remain disabled; suggested action is a conservative review task only | Live-validate weather/web locally and run approved calendar/tasks/email dry-run before raising maturity |
| Meeting prep | Workflow | 5 Hardened | 76 | 1 | Complete for v1 | Implemented with brokered selected calendar event read, optional contact search, optional web topic search, dry-run, and deterministic prep output | Missing approval, approved event read, contact approval, optional web, no write/send, dry-run, no-memory, audit, and CLI tests | Calendar/contact steps execute through ToolBroker and approval gates; web remains brokered LOW risk | No emails/texts sent, no calendar/contact edits, no bulk export, no memory writes, calendar/contact data private, web untrusted | Local tests only; live personal sections require explicit user approval/config | README commands and limitations documented | Attendee names are not returned by default; event-id must be a selected event token | Live-validate with explicit dry-run and user-approved selected event |
| Meeting follow-up | Workflow | 4 Tested | 72 | 1 | Complete for v1 | Implemented brokered selected event read, workspace notes read, optional contact lookup, deterministic decisions/action-items extraction, and Action Center task/email/calendar suggestions | Event approval, workspace notes path enforcement, pending task actions, pending email action, no write/send, denied approval skip, prompt-injection filtering, no-memory, audit, and CLI dry-run tests | Calendar/contact/file reads execute or dry-run through ToolBroker; suggested tasks/email/calendar updates are Action Center records only and are not executed | Notes are `UNTRUSTED_DOCUMENT`, instruction-injection lines are filtered, no sends/writes/contact edits/task creates/calendar updates execute, and no memory writes occur | Local tests only; live personal sections require explicit user approval/config | README commands and limitations documented | Email draft uses `review-required@example.invalid` placeholder until edited; synthesis is deterministic and intentionally conservative | Live-validate notes-only dry-run and approved selected event read before raising maturity |
| Personal task extraction | Workflow | 4 Tested | 72 | 1 | Complete for v1 | Implemented brokered extraction from workspace notes/captures, selected email thread, selected calendar meeting, and selected URL into Action Center task drafts | Notes extraction, email approval, calendar approval, prompt-injection filtering, pending-only actions, denied source no-action, dry-run no-read, audit, and CLI JSON tests | Sources execute or dry-run through ToolBroker; personal sources are approval-gated; extracted tasks are Action Center `tasks.create` records only | Source instructions are ignored, no `tasks.create` execution occurs, denied personal reads create no actions, and no memory writes occur | Local tests only; live personal sources require explicit user approval/config | README commands and limitations documented | Extraction is deterministic and line-pattern based; task due dates/owners are not inferred yet | Live-validate notes-only dry-run and an approved selected email/calendar extraction before raising maturity |
| Email triage | Workflow | 5 Hardened | 76 | 1 | Complete for v1 | Implemented with brokered metadata-only priority classification, optional selected-thread read, summary, draft-only reply, dry-run, and deterministic report | Metadata-only, body approval, prompt-injection, draft-only, no-memory, audit, provider-unavailable, and CLI tests | All steps execute through ToolBroker and approval gates; body args are redacted | No sending, deleting, moving, archiving, bulk body reads, or memory writes; selected body omitted from triage report | Local tests only; live email provider requires explicit user approval/config | README commands and limitations documented | Priority classification is heuristic and metadata-only | Live-validate with a non-production account after explicit approval |
| Self-improvement backlog + implementation loop | Workflow | 5 Hardened | 86 | 8 | Complete for v1 | Implemented read-only backlog/propose, safe-mode overnight planner, approved proposal implementation on `codex/` branches, brokered writes/tests/diff, explicit `create-action-for-commit` preflight, Action Center commit drafts, and approved commit execution path | Read-only propose, approved-file reads, policy-weakening blocked suggestions, ranked backlog, overnight safe-mode exclusions/ranking/docs tests, audit read, dry-run, approved proposal required, branch creation, policy/audit/package/persistence blocks, tests, diff, commit-action creation, commit approval denial, overnight report-template validation, and audit tests | Backlog/propose/overnight-plan use brokered `filesystem.read`; implementation writes through `filesystem.write`; tests/diff through ToolBroker; commit-action creation runs brokered tests/diff first; commits require Action Center and `git.commit` approval | Blocks policy weakening, audit disabling, protected safety file edits, personal-data grants, package installs without approval, persistence paths, send/write side effects, high/critical overnight work, and unapproved commits | Approved bounded overnight safe-mode run completed on 2026-05-23; no live personal-data or write/send validation | README, overnight runbook, report template, command examples, report directory, and limitations documented | Proposal approval store is JSON-file based in v1; no rich proposal review UI yet; `git.commit` still requires user-staged changes where git itself requires them; overnight safe-mode runs require explicit user approval each time | Return to `SESSION-LOGGING-REPLAY`, then consider future overnight runs only after explicit approval |
| Prompt Ledger, Prompt Queue, and Prompt Pack tracking | SDLC tracking | 5 Hardened | 86 | 13 | Complete for hardened v1 | Implemented ledger, queue, audit docs, prompt record directories, templates, prompt CLI commands, best-effort reconstruction, prompt pack import/splitting with embedded delimiter preservation, evidence classifier, conservative recovery commands, prompt tracker dogfood suites, and PTM release-gate docs | Docs existence, queue ids, list/next/search/audit/evidence/missing/missed/stale/recover-plan/reconcile, one-active enforcement, failed-reason requirement, mark-complete evidence requirement, CLI dispatch, pack parsing, duplicate id/order rejection, missing metadata/end rejection, invalid risk/dependency/cycle rejection, validate-no-write, import writes, dependency-aware next, approval gate blocking, body preservation, prompt tracker eval, dogfood suite validation, and PTM-01 through PTM-10 completion evidence | Tracking commands are metadata-only and do not execute agent tools; prompt pack import is `import_only`; recovery is report-only; PTM prompts are complete and the next queue item is explicit | Prevents missed/silent prompt drift, classifies completion evidence, flags stale/missed/superseded prompts, rejects execute-all packs, preserves prompt text as `UNTRUSTED_DOCUMENT`, updates AGENTS/PROJECT_STATE resume rules, and keeps prompt count as context rather than proof | Local tests and prompt tracker eval passed; no long-running live queue dogfood yet | Prompt docs, prompt tracker docs, pack format docs, AGENTS rules, project state fields, changelog, registry, roadmap, and completion report updated | Evidence matching is heuristic; markdown remains the source of truth; recovery does not auto-fix by design | Run real dogfood sessions for `prompt_tracker_core` and `promptops_workbench`; consider structured JSON index later |
| PromptOps Workbench v1 | SDLC tracking and orchestration | 5 Hardened | 84 | 3 | Complete for hardened v1 | Implemented one-command import from stdin/file/clipboard, raw single prompt import, next/copy/show/resume/status/review/audit, disabled-by-default runner, safe-only autopilot guardrails, and prompt tracker release-gate integration | Import from stdin/file/clipboard, raw single prompt, invalid pack rejection, dependency-aware next, blocked approval gate stop, copy-next, disabled run-next, safe autopilot rejection of HIGH/approval-gated prompts, report redaction, mark-complete evidence requirement, prompt tracker dogfood, and CLI tests | Workbench is metadata/orchestration only; prompt text is `UNTRUSTED_DOCUMENT`; runner is disabled by default; autopilot refuses HIGH/CRITICAL/FORBIDDEN and approval-gated prompts | Does not execute prompt packs automatically, does not grant approvals, does not weaken policy, does not add personal-data connectors, stops at approval gates, and redacts secret-looking values | Local tests and disabled-runner checks only; no live Codex runner execution by design | README, `docs/PROMPTOPS_WORKBENCH.md`, prompt tracker docs, Prompt Pack format docs, AGENTS rules, project state, registry, changelog, and completion report updated | Runner integration is intentionally disabled unless configured; autopilot v1 stops conservatively and does not auto-mark completion | Use `work import` for pasted mega prompts and `work next` or `work copy-next`; keep `run-next` disabled until separately approved |
| Command Registry and Manual QA System | SDLC tracking and QA | 4 Tested | 74 | 1 | Complete for v1 | Implemented static command catalog, generated registry/test matrix/legacy/runbook docs, validation helpers, and read-only `commands` CLI inspection commands | Command docs validation, required metadata validation, CLI list/show/search/legacy/validate/qa-plan/qa-run tests, and docs validation coverage | Metadata-only; no tool execution, connector access, personal-data read, approval consumption, or memory writes; `qa-run` prints safe command examples instead of executing them | Tracks command drift, status/maturity/risk/approval/test/manual-QA fields, legacy replacements, and safe manual QA plans; does not mark manual verification complete without evidence | Local tests only; manual QA runs are still pending for most commands | README command registry section, AGENTS rules, command docs/templates, feature registry, maturity tracker, roadmap, project state, changelog, and completion report updated | Registry is generated from curated metadata, so future command changes must update `agent/ui/command_registry.py` and regenerate docs; manual QA results remain mostly unverified | Run `python smart_agent.py commands qa-plan`, start with the Weather safe suite, and log manual QA findings in the matrix |
| Live Session Logging and Replay | Dogfood and QA | 5 Hardened | 80 | 2 | Complete for v1 | Implemented redacted session start/status/run/end/list/show/replay/export/last commands, JSON session store, separate redacted output files, replay formatter, visible audit-id linking, and gitignored raw reports | Start/end/list/show/replay CLI, command run capture, stdout/stderr/exit code, secret/email/phone redaction, command-line redaction, audit-id extraction, large output storage/truncation, malformed session file handling, `.gitignore` protection, command registry tests, and release-gate session smoke | Session logger itself is not a tool executor and does not approve actions; `session run` delegates to `smart_agent.py` so underlying commands keep ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior | Does not replace AuditLogger, does not log environment variables, does not enable personal-data tools, does not store session logs in memory, redacts common secrets/PII by default, and raw mode is explicit unsafe debug only | Synthetic safe release-gate session `sess_20260523T182508Z_abdad880` validated start/run/feedback/end/review and redaction; long manual dogfood replay still pending | README, `docs/SESSION_LOGGING.md`, command registry/test matrix, feature registry, project state, changelog, risk register, threat model, and completion report updated | Redaction is heuristic and not a substitute for avoiding secrets/personal data in manual tests; automatic capture of all CLI commands is intentionally deferred | Run a longer manual `all_safe --session` smoke and keep generated artifacts redacted |
| Manual Dogfood Command Suites and Live Test Workflow | Dogfood and QA | 5 Hardened | 81 | 3 | Complete for v1 | Implemented YAML-backed curated command suites, `dogfood list/show/run`, dry-run preview, failure-continuing runner, expected-exit handling, optional active-session capture, and planning commands `dogfood plan/next/checklist` | Suite YAML validation, CLI list/show/plan/next/checklist, mock run success/failure continuation, `all_safe` no-personal-data assertion, `personal_dry_run` preflight-only assertion, parse guard against non-agent commands, latest-session recommendation, runbook-doc existence, session integration tests, and release-gate dry-run | Dogfood runner is not a tool executor; it delegates only to `python smart_agent.py ...` commands so underlying commands keep their normal ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior; planner reads suite/session metadata only | Personal suite is preflight-only, `all_safe` excludes personal-data commands, no send/write/personal connector is enabled, no hidden session is created, command strings reject shell/non-agent execution, and live runbook keeps personal-data checks dry-run/mock by default | Release gate validated `dogfood run all_safe --dry-run`; first full `all_safe --session` dogfood run still pending | README, `docs/dogfood/DOGFOOD_GUIDE.md`, `docs/dogfood/COMMAND_SUITES.md`, live test runbook, daily/weekly checklists, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Suites are curated data and can become stale as CLI syntax changes; live provider suites depend on local LM Studio/web/weather config; planner is advisory and does not prove live readiness | Run the first full manual session with `session start`, `dogfood run all_safe --session`, `session review --last`, then create bugs/regression tests from confirmed failures |
| Messaging Dogfood Suites | Dogfood and QA | 4 Tested | 73 | 1 | Complete for no-send messaging QA coverage | Added default-disabled `messaging_core`, `messaging_handoff`, `messaging_ios_compose`, `messaging_macos_probe`, `messaging_send_dry_run`, and `lead_response` suites plus a mock iOS compose result CLI wrapper for dogfoodability | Suite YAML validation, messaging suite presence, `all_safe` no-live-send assertion, dry-run no-send assertion, lead-response mock/preflight assertion, session integration coverage, command registry validation, and iOS compose result command tests | Dogfood runner delegates to existing CLI commands only; suites do not bypass ToolBroker/PolicyEngine/ApprovalManager/AuditLogger and send-related commands remain Action Center/preflight/dry-run only by default | Live send probe is excluded, direct send execution is excluded outside `preflight`, private Messages database paths and Full Disk Access are not used, personal data uses fixtures/mock/preflight only, and no memory write is introduced | Local tests and dry-run smokes only; no real message send or provider live validation by design | Dogfood guide, command-suite catalog, session logging docs, bug triage docs, command registry/test matrix, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, changelog, and completion report updated | Suites can become stale with CLI syntax changes and are intentionally default-disabled; they prove dogfood coverage, not live delivery readiness | Run a recorded manual messaging dogfood session, then review failures and create bugs/regression tests before any live-send validation prompt |
| Messaging / iMessage Track release gate | Release gate | 4 Tested | 78 | 1 | Complete for no-send release gate | Validated channel abstraction, message safety, Lead Inbox, iOS compose, macOS probe, handoff, incoming strategy, macOS approved-send gates, Apple Business stub, lead response drafting/send orchestration, and messaging dogfood suites | Full suite, startup policy, capability manifest validation, command registry validation, focused docs/dogfood/prompt tests, safe messaging dogfood, dry-run send suite, macOS metadata probe, capability/default scans, and private-path/bulk-send scans | Release gate is validation-only; no new runtime executor, approval, policy, or send capability was added; executable sends remain ToolBroker/PolicyEngine/ApprovalManager/AuditLogger gated | No live send ran; send capabilities are default-disabled CRITICAL per-action/no-reuse; private Messages database and Full Disk Access paths remain blocked; message content is untrusted data; bulk/group sends remain forbidden | Local macOS metadata probe only; live message delivery remains unvalidated and unavailable by default | Changelog, project state, feature registry, maturity, roadmap, completion report, risk register, threat model, release checklist, and prompt tracking updated | This gate proves local safety and QA posture, not live provider delivery or user-ready sending | Run a separate explicit live-send self-test prompt only if the user configures an allowlisted recipient and approves the live probe/action |
| Product Quality Dashboard v1 | Dogfood and QA | 5 Hardened | 78 | 2 | Complete for read-only v1 | Implemented `quality status/sessions/bugs/regressions/features/next` over local sessions, feedback, bugs, eval reports, test status, feature maturity, dogfood recommendations, and release-gate docs | Quality status, open bug count, P0/P1 next recommendation, last session display, secret redaction, read-only behavior, features lacking live validation, regression-test gap, CLI subcommand tests, and release-gate artifact readout | Dashboard is not a tool executor; it reads local metadata/log files only and does not call connectors, use approvals, or grant permissions | Redacts session feedback and bug text, shows no raw personal connector data, writes no memory, creates no reports/logs during read-only views, and prioritizes safety bugs first | Release gate validated quality dashboard over synthetic redacted session `sess_20260523T182508Z_abdad880`, `BUG-0002`, and regression scaffold; broader real dogfood review still pending | README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Depends on tracking docs and redacted session/bug/eval artifacts staying current; quality status is only as good as the latest recorded evidence | Run after each real dogfood session, then use `quality next` to choose between P0/P1 bug fixes and dogfood suite follow-up |
| User Feedback Capture for Live Sessions | Dogfood and QA | 5 Hardened | 78 | 2 | Complete for v1 | Implemented redacted `feedback.jsonl` session records, `feedback good/bad/bug/confusing/slow/unsafe/rate/add/list/export`, command summary updates, show/export/replay integration, and unsafe severity escalation | Last-command and specific-command attachment, list/export, invalid score denial, unsafe severity escalation, secret/email/phone redaction, replay/show visibility, no-memory assertion, CLI, command registry tests, and release-gate attachment smoke | Feedback is not a tool executor and does not approve actions; feedback operations are audited through AuditLogger and session commands keep their original ToolBroker/policy behavior | Feedback text is redacted before storage, unsafe feedback becomes high severity, user notes are review evidence only and cannot weaken policy, grant permissions, or create sends/writes | Release gate validated feedback attachment to synthetic session command with secret redaction; broader real dogfood feedback still pending | `docs/SESSION_LOGGING.md`, `docs/dogfood/DOGFOOD_GUIDE.md`, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Redaction is heuristic; feedback is now consumed by the session review bug generator, but generated bugs still require human triage | Run an `all_safe` dogfood session and mark at least one good/bad/bug feedback item before generating real bug records |
| Session Review and Bug Generator | Dogfood and QA | 5 Hardened | 78 | 2 | Complete for v1 | Implemented `session review`, redacted review reports, optional `--create-bugs`, local `bugs list/show/export`, stable incrementing bug ids, and P0/P1/P2/P3/P4 classification | Clean session summary, failed command detection, feedback usage, create-bugs, stable bug ids, secret/personal redaction, P0 policy-bypass classification, bug CLI, command registry tests, and release-gate bug creation smoke | Review and bug operations are audited; review does not execute tools, approve actions, access connectors, or fix bugs | Reads redacted session previews/feedback only, avoids raw full outputs, redacts bug text, classifies policy/approval/audit/data-leak signals as P0, and writes no memory | Release gate validated redacted review, bug creation `BUG-0002`, and regression stub `tests/regressions/test_bug_0002.py`; broader real dogfood review still pending | `docs/BUG_TRIAGE.md`, `docs/SESSION_LOGGING.md`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Redaction remains heuristic; generated bugs are local triage records and need human review before fixes | Run a real `all_safe --session` dogfood session with feedback, then review it and feed approved bugs into regression tests |
| Release-gate tracking sync | SDLC tracking and release gate | 4 Tested | 78 | 2 | Complete for this gate | Documentation and validation-only sync of current project state, native-skills queue, next product feature track, and overnight self-improvement track | Full suite, startup policy validation, capability manifest validation, safe eval, command registry validation, native skill validation, prompt audit, unknown-tool denial, ToolBroker path scan, personal-data default check, and HIGH/CRITICAL approval checks all passed | No runtime capability added; confirms ToolBroker/PolicyEngine/ApprovalManager/AuditLogger posture from existing tests, manifest validation, and broker denial checks | No personal-data tools enabled by default; HIGH approval and CRITICAL per-action/no-reuse rules verified from manifest data; no memory writes | Local release-gate validation only | PROJECT_STATE, FEATURE_ROADMAP, FEATURE_REGISTRY, FEATURE_MATURITY, COMPLETION_REPORT, CHANGELOG, and RELEASE_CHECKLIST updated | This is a documentation/release-gate record, not a product feature; repo has a broad pre-existing dirty worktree, so this gate does not create a clean feature commit boundary | Start the next queued implementation only after this sync remains green |
| Native Skills Program foundation | Native skills governance | 2 Scaffolded | 48 | 1 | Complete for intake foundation | Governance docs, criteria, risk model, candidate registry, and reusable record template exist; no runtime loader or installer implemented | Docs validation checks required native skill files and safety boundaries | No runtime actions; future native skills must map to ToolBroker capabilities and PolicyEngine/ApprovalManager/AuditLogger rules before implementation | Explicitly rejects unreviewed external scripts, direct tool access, hidden network access, broad filesystem access, browser cookies, Keychain/password access, private database scraping, background persistence, opaque binaries, and approval bypass | Not applicable; no external skills were installed, run, or live-validated | Native skills docs and tracking docs updated | This is a program foundation only; marketplace survey, vetter, manifest, loader, and native skill implementations remain future work | Run `SKILL-MARKETPLACE-SURVEY` as research-only next; do not install or execute external skills |
| Skill marketplace survey and native shortlist | Native skills governance | 1 Specified | 44 | 1 | Complete for research shortlist | Survey, scoring matrix, and top candidate shortlist exist; no runtime behavior implemented | Docs validation checks survey docs and required scoring columns | No runtime actions; sources are untrusted data and recommendations are documentation only | High-risk categories are explicitly deferred; external skill install/import/run remains forbidden | Not applicable; no external skills were installed, imported, run, or live-validated | Native survey docs and tracking docs updated | Survey is a research artifact, not a vetter or loader | Build `NATIVE-SKILL-VETTER` next as a local docs/data-only review workflow |
| Native skill inspection and vetter v1 | Native skills safety tooling | 4 Tested | 76 | 1 | Complete for v1 static inspection, vetting, scoring, and local report lookup | Implemented brokered approved-path inspection, file/folder vetting, candidate scoring, and latest-report reading for `SKILL.md` packages and reviewed native skill ids; no installer or runtime skill import implemented | Unit tests cover frontmatter inspection, safe low-risk scoring, shell commands, package installs, network calls, secrets, filesystem escapes, prompt injection, approval bypass language, browser sessions, personal-data requests, opaque binaries, missing licenses/docs/tests, no script execution, scope denial, report writing, CLI commands, and audit logging | `native_skills.*` capabilities execute through ToolBroker, PolicyEngine, and AuditLogger; capabilities are LOW risk, rate-limited, and return `UNTRUSTED_DOCUMENT` analysis | Candidate skill text is data only, scripts are never executed, dependencies are never installed, network is never accessed, approved workspace/project bounds are enforced, memory writes are disabled by default, and unsafe/import-enable blockers are reported | Local mocked/static tests only; no external skill packages were installed, run, or live-validated | README, skill inspection/vetting docs, skill intake process, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Static heuristic vetting can miss obfuscated behavior and saved reports are review evidence, not permission to install, run, or enable an external skill | Continue SKILL-05 per-profile skill allowlists next if no stop condition is hit |
| Native skill manifest and discovery loader | Native skills foundation | 4 Tested | 72 | 2 | Complete for strict manifest schema and detection-only dependency gating | Expanded manifest models/schema with root/source/provenance, dependency fields, approval reuse, network/filesystem behavior, dogfood/license/setup/limitations metadata; added detection-only dependency checks and optional `skills validate/doctor <skill_id>` diagnostics | Unit tests cover valid manifests, missing risk/trust/memory fields, unknown capabilities, ToolBroker-bypass language, personal-data disabled-by-default rules, CRITICAL per-action/no-reuse approval, env redaction, missing env/binary dependencies, blocked unknown capability dependency, no package install/script execution, and list/show/validate/doctor commands | Manifest validation maps required capabilities to the capability manifest; dependency checks are advisory/detection-only; manifests cannot execute tools or grant permissions; actual native skill tools still must use ToolBroker | Rejects executable fields, unknown capabilities, bypass language, non-disabled personal-data manifests, and CRITICAL manifests without per-action/no-reuse approval; env values are redacted; dependency gates do not install packages, call providers/connectors, or execute scripts | Local tests and CLI smoke only; no external skill packages were installed, run, or live-validated | README, Native Skills Program docs, manifest schema docs, dependency gating docs, template, command registry/test matrix, feature registry, project state, changelog, and completion report updated | No provenance lockfile, profile allowlists, compatibility matrix, conflict detector, dogfood harness, docs generator, or native skill release gate yet | Continue SKILL-03 provenance/trust metadata and lockfile next |
| Native skill roots, scopes, and precedence | Native skills foundation | 4 Tested | 68 | 1 | Complete for metadata-only root/scoping model and deterministic precedence rules | Added default workspace/project/personal/managed/bundled/reconstructed/experimental root metadata, lazy root scanning, duplicate skill ID detection, precedence resolution, shadowing diagnostics, and read-only `skills roots/precedence/registry/explain-root` commands | `tests/native_skills/test_skill_roots_precedence.py` covers default roots, missing/disabled roots, deterministic order, duplicate detection, experimental/untrusted shadow blocking, explicit shadowing, no script execution, untrusted skill text labeling, and command outputs | Root and precedence diagnostics are metadata-only and do not execute tools; future executable native skills still require ToolBroker, PolicyEngine, ApprovalManager, AuditLogger, manifest validation, and vetting | Workspace/personal/reconstructed/experimental roots are untrusted by default, experimental is disabled by default, personal/user roots cannot override bundled/project skills without explicit shadowing config, and all skill text remains `UNTRUSTED_DOCUMENT` | Local tests only; no external skills installed, run, imported as trusted, or live-validated | `docs/native_skills/SKILL_ROOTS_AND_SCOPES.md`, `docs/native_skills/SKILL_PRECEDENCE.md`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | No provenance lockfile, profile allowlists, compatibility matrix, conflict detector, dogfood harness, or release gate yet | Continue SKILL-02 manifest schema/dependency gating next |
| Native skill provenance, trust metadata, and lockfile | Native skills foundation | 4 Tested | 66 | 1 | Complete for metadata-only provenance records, trust status, and read-only lockfile verification | Added provenance/trust/lockfile modules, source/review/trust/caveat records, SHA-256 file and manifest/dependency hashes, generated lockfile payloads, read-only lock status/verify, and provenance/trust CLI commands | `tests/native_skills/test_skill_provenance_lockfile.py` covers provenance validation, unknown source/missing license warnings, fixture lock generation, unchanged verify, changed manifest detection, pinned skill update detection, candidate disabled-by-default trust status, reconstructed labeling, and CLI commands | Commands are metadata-only and do not execute tools; future executable native skills still require ToolBroker/PolicyEngine/ApprovalManager/AuditLogger and a reviewed write path for lockfile pinning | No auto-update, no external marketplace call, no install, no script execution, no unreviewed skill enablement, no lockfile write by status/verify commands, and reconstructed skills are explicitly caveated | Local tests only; no live marketplace or external skill validation | `docs/native_skills/SKILL_PROVENANCE.md`, `docs/native_skills/SKILL_LOCKFILE.md`, `native_skills.lock.example`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Pin/unpin write commands are deferred; no reviewed real `native_skills.lock` is committed yet | Continue SKILL-04 inspection and vetting CLI only if no stop condition is hit |
| Native skill profile visibility and allowlists | Native skills foundation | 4 Tested | 66 | 1 | Complete for advisory profile visibility rules | Added default/research/coding/personal_assistant/lead_response/locked_down/experimental profiles, allowlist/blocklist/category/risk/personal-data/network/write/CRITICAL visibility evaluation, and read-only profile diagnostics | `tests/native_skills/test_skill_profiles_allowlists.py` covers default high-risk hiding, locked_down safe-only behavior, research/coding category visibility, blocklist override, risk ceiling, personal-data hiding, CRITICAL hiding, disabled experimental profile, policy-final notes, registry methods, and CLI commands | Profiles are metadata-only visibility filters and do not execute tools; ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, and capability manifest rules remain final authority | Profile selection does not enable skills, grant capabilities, modify policy, store state, run skills, delegate to subagents, or enable personal-data tools | Local tests only; no multi-agent/profile runtime mode live validation | `docs/native_skills/SKILL_PROFILES.md`, `docs/native_skills/SKILL_ALLOWLISTS.md`, `docs/templates/skill_profile_template.yaml`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Profiles are advisory only and are not yet integrated with runtime prompt/tool attachment | Continue SKILL-06 compatibility matrix next if no stop condition is hit |
| Native skill compatibility matrix | Native skills foundation | 4 Tested | 66 | 1 | Complete for manifest-derived platform/runtime/setup metadata | Added compatibility records, full matrix, one-skill compatibility lookup, platform/runtime matrix views, docs, template, and CLI commands | `tests/native_skills/test_skill_compatibility_matrix.py` covers manifest dependency computation, mocked macOS/Windows/Linux support, missing binary setup requirements, personal-data flags, native-app bridge planned status, safe all-OS matrix behavior, registry methods, and CLI commands | Compatibility is metadata-only and does not execute tools or skills; future executable native skills still require ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, capability manifests, and vetting | Checks do not import native platform modules, execute skills, call providers, install dependencies, enable platform-specific behavior, store state, grant permissions, or access personal data | Local tests and CLI smoke only; no live platform/app bridge validation and no external skills run | `docs/native_skills/SKILL_COMPATIBILITY_MATRIX.md`, `docs/templates/skill_compatibility_record_template.md`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Compatibility output is advisory readiness metadata, not a runtime allowlist or permission grant; conflict detector, dogfood harness, docs generator, and release gate remain future prompts | Continue SKILL-07 conflict detector next if no stop condition is hit |
| Native skill conflict detector | Native skills foundation | 4 Tested | 66 | 1 | Complete for metadata-only conflict reporting | Added deterministic conflict records for duplicate skill IDs, command/capability overlap, unsafe shadowing, missing dependencies, disabled providers, platform incompatibility, approval/risk/memory policy mismatches, and missing docs/tests; added conflict report and one-conflict explanation commands | `tests/native_skills/test_skill_conflict_detector.py` covers duplicate IDs, same command, same capability, CRITICAL approval reuse, HIGH without approval, personal-data memory mismatch, missing docs/tests, missing dependency/provider/platform conflicts, precedence shadowing, explain lookup, and CLI JSON output | Conflict detection is metadata-only and does not execute tools or skills; future executable native skills still require ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, capability manifests, and vetting | Detector never auto-resolves conflicts, enables/disables skills, installs dependencies, calls providers, imports plugin runtimes, executes external scripts, grants permissions, or writes memory | Local tests and CLI smoke only; no external skill packages were installed, run, or live-validated | `docs/native_skills/SKILL_CONFLICTS.md`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Conflict output is advisory release-gate evidence, not a runtime allowlist; dogfood harness, docs generator, and release gate remain future prompts | Continue SKILL-08 skill test and dogfood harness next if no stop condition is hit |
| Native skill test and dogfood harness | Native skills foundation | 4 Tested | 66 | 1 | Complete for metadata-only safe harness and fixture evals | Added safe per-skill/all-safe harness checks, dogfood plan output, fixture-backed native skill eval category, core/vetting dogfood suites, docs, and CLI commands | `tests/native_skills/test_skill_test_harness.py` covers safe skill pass, high-risk skip, personal-data skip, missing dependency skip, prompt-injection and secret fixture detection, CLI test/dogfood commands, dogfood suite validation, native skill eval report generation, eval CLI flag, and command registry coverage | Harness and dogfood planning are metadata-only and do not execute skills; dogfood suite commands delegate to existing safe commands and future executable native skills still require ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, capability manifests, and vetting | Harness skips HIGH/CRITICAL/FORBIDDEN and personal-data skills by default, never executes external scripts, installs dependencies, calls providers/connectors, imports plugin runtimes, enables skills, grants permissions, or writes memory | Local tests and CLI smoke only; no external skill packages were installed, run, or live-validated | `docs/native_skills/SKILL_TESTING.md`, `docs/native_skills/SKILL_DOGFOOD_RUNBOOK.md`, dogfood suites, eval cases, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Harness output is advisory evidence for maturity, not a user-ready claim; docs generator and release gate remain future prompts | Continue SKILL-09 skill docs generator next if no stop condition is hit |
| Native skill docs generator | Native skills foundation | 4 Tested | 66 | 1 | Complete for metadata-only generated catalog | Added deterministic catalog generation from manifests, command registry metadata, profiles, compatibility, provenance/trust, lock status, tests, dogfood declarations, docs paths, and known limitations; added dry-run-default docs generation, catalog display, and docs-check commands | `tests/native_skills/test_skill_docs_generator.py` covers dry-run no-write behavior, generated catalog writes from fixture manifests, manual note preservation, missing docs reporting, deprecated skill inclusion, conservative maturity copying, CLI commands, and command registry coverage | Docs generation is metadata-only and does not execute skills; future executable native skills still require ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, capability manifests, and vetting | Generator defaults to dry-run for command-triggered changes, preserves manual notes outside generated sections, reports missing docs, includes deprecated/blocked skills, never invents maturity, and never installs dependencies, calls providers/connectors, executes plugin runtimes, enables skills, grants permissions, or writes memory | Local tests and CLI smoke only; no external skill packages were installed, run, or live-validated | `docs/native_skills/SKILL_CATALOG.md`, `docs/native_skills/SKILL_DOCS_GENERATION.md`, `docs/templates/native_skill_doc_template.md`, README, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Generated catalog is release evidence and navigation, not a runtime allowlist or user-ready claim; release gate remains future prompt | Continue SKILL-10 native skill system release gate next if no stop condition is hit |
| Native skill system release gate | Native skills foundation | 5 Hardened | 74 | 1 | Complete for metadata-only native skill system release gate | Added release gate and maturity review docs; validated roots/precedence, manifest schema, dependency gating, provenance/trust, lockfile verify, inspection/vetting, profiles, compatibility, conflicts, harness/dogfood, docs generator, command registry, and conservative maturity | Full suite passed with 1205 passed and 1 skipped; startup policy and capability manifest validation passed; command registry validation passed with 428 commands; native skill manifests passed with 3 valid; conflicts passed with 0 conflicts; native skill evals passed with 3 pass/5 personal-data skips; native skill dogfood suites passed 6/6 in a redacted session; docs-check passed | Release gate validates metadata-only controls and does not execute external skills; future executable native skills still require ToolBroker, PolicyEngine, PermissionManager, ApprovalManager, AuditLogger, capability manifests, vetted implementation, and explicit approval where risk requires | No external skill installation, marketplace enablement, external script execution, dependency installation, plugin runtime execution, personal-data skill enablement, provider/connector call, memory write, permission grant, or safety bypass occurred | Local tests, fixture dogfood, and fixture evals only; not live-validated against third-party skill installation or external marketplace intake | `docs/native_skills/NATIVE_SKILL_SYSTEM_RELEASE_GATE.md`, `docs/native_skills/NATIVE_SKILL_SYSTEM_MATURITY_REVIEW.md`, release checklist, feature registry, project state, changelog, and completion report updated | Real reviewed `native_skills.lock` and lockfile write/pinning workflow remain future work; manual external-skill intake/live QA required before user-ready external skills | Next recommended track: reviewed lockfile/pinning workflow or a clean release-candidate boundary before external skill import/runtime work |
| Native skill finder v1 | Native skills discovery | 4 Tested | 72 | 1 | Complete for local discovery v1 | Implemented brokered `native_skills.find_skill` search over local manifests, native candidate docs, feature registry, and feature maturity docs | Unit tests cover implemented manifest matches, planned candidate matches, maturity and approval reporting, no-match creation path, no external search/install flags, and broker/audit logging | Finder executes through ToolBroker, PolicyEngine, and AuditLogger as LOW risk `UNTRUSTED_DOCUMENT` metadata; files read are audited | No external marketplace browsing, no installation, no external code execution, no memory writes, and prompt text is treated as search data only | Local tests and CLI smoke only; no external marketplace or live provider involved | README, Native Skills Program docs, command registry/test matrix, feature registry, project state, changelog, and completion report updated | Matching is lightweight keyword/synonym scoring and depends on docs staying current | Build `PDF-WORKSPACE-SKILL` next using this finder to expose planned/implemented skill readiness |
| PDF workspace native skill v1 | Native skills documents | 4 Tested | 74 | 1 | Complete for read-only workspace PDF v1 | Implemented brokered PDF metadata, embedded-text extraction, deterministic summary, and best-effort text-table extraction commands using existing Python dependency `pypdf` | Unit tests cover path traversal, denied paths, fixture info, text extraction, no-table behavior, no-memory summary, file-size limit, malformed PDF handling, audit, no external binary execution, CLI, manifest, and command registry docs | PDF capabilities execute through ToolBroker, PolicyEngine, and AuditLogger as MEDIUM risk `UNTRUSTED_DOCUMENT`; files read are audited and commands_run remains empty | Workspace guard enforced, OCR disabled, external binaries disabled, PDF actions/scripts not executed, size/page/text limits enforced, no memory writes, and instruction-like content filtered in summaries | Local fixture tests and CLI path only; no live user PDF corpus smoke yet | README, `docs/native_skills/pdf.md`, command registry/test matrix, feature registry, project state, changelog, and completion report updated | OCR, split/merge, generated PDF writes, and dedicated table extraction dependencies are deferred | Build session logging/replay next, or live-smoke PDF commands on a benign workspace PDF before raising maturity |
| Scheduler / Automation v1 | Automation | 4 Tested | 74 | 2 | Complete for manual-run v1 | Implemented local schedule store, explicit create/list/run/pause/delete commands, supported workflow allowlist, audited lifecycle events, manual-run execution, and optional redacted-only backup creation | Create/list, safe workflow run, personal approval-required section, CRITICAL non-execution, backup_create broker path, unredacted backup rejection, pause/delete, no hidden persistence, audit, and CLI tests | Scheduled tool workflows build a ToolBroker with PolicyEngine, ApprovalManager, and AuditLogger; connector/audit status workflows are metadata-only; personal sections keep existing approval gates; `backup_create` uses brokered `backup.create` | No LaunchAgent, cron, daemon, login item, or background runner; unsupported workflows are rejected; CRITICAL actions are not executed automatically; memory cleanup is no-op in v1; scheduled backups are redacted-only and read no personal connectors | Local tests only; no real background scheduling by design | README, `docs/SCHEDULER.md`, command registry, feature registry, roadmap, risk register, threat model, test plan, release checklist, project state, and completion report updated | V1 requires manual `schedule run`; recurrence parsing/background execution is deferred pending decision record and approval | Manually QA `connector_doctor`, weather-only `daily_briefing`, and redacted `backup_create`; create a decision record before any system-level automation |
| Agent dashboard | UX | 5 Hardened | 78 | 5 | Complete for CLI v1 | Consolidated read-only `dashboard` and `status` commands for runtime, LM Studio, tools, connectors, permissions, approvals, audit metadata, memory counts, risk settings, last test run, and setup hints | Dashboard load, secret redaction, personal connector disabled status, pending approval display, audit tail, status metadata-only, doctor, connector dashboard, config/audit viewer tests | Dashboard executes no tools and grants/uses no approvals; underlying action commands retain ToolBroker/policy gates | Secrets redacted; personal connectors status-only; audit is metadata-only; memory summary returns counts only and no content | Local tests and CLI smoke only | README dashboard/status docs exist | No local web dashboard; live LM Studio status depends on local server/model config | Run release gate, then consider optional local web dashboard only if it stays inspect-only |
| Golden eval suite, model-router benchmark, and prompt quality scorecards | Validation | 4 Tested | 83 | 3 | Complete for tested v1 scope | Added data-file backed Golden Eval cases for routing, policy, ToolBroker, prompt-injection, workflow dry-runs, model-router benchmarks, and prompt guardrail checks, plus structured scorecards and per-run reports | Eval list/run/report, data-file loading, mocked safe evals, skipped personal evals, policy denial checks, prompt-injection wrapper checks, scorecard output, ToolBroker/audit evidence, no-personal-access, model-router benchmark, prompt quality report, personal-data no-auto-execute routing, CLI dispatch tests, and safe eval release-gate run with 21 pass / 0 fail / 8 skipped | Safe tool checks execute through ToolBroker; policy cases assert unknown denial, disabled personal-data denial, CRITICAL no-reuse send gates, and model-router quality checks compare expected vs actual route/tool/policy outcomes without executing personal tools | No sends, no calendar/contact writes, no personal memory storage; workspace eval writes only under `./workspace/eval`; memory eval stores and deletes non-sensitive project fact; model-router/prompt evals write reports only | Local mocked tests and safe eval CLI passed; live LM Studio checks skipped because `LMSTUDIO_MODEL` is not set, and personal-data evals remain skipped by default | README, `docs/EVAL_REPORT.md`, `docs/PROMPT_QUALITY_REPORT.md`, `eval_cases/`, and command registry docs updated | Does not yet include approval-gated personal live evals, human-rated chat quality rubrics, or live LM Studio score baselines | Run `python smart_agent.py models benchmark --safe --live` only after LM Studio is configured, then record live result before raising maturity |

## Batch Release Gate: 2026-05-23 Controlled Actions and Product Quality

## Messaging / iMessage Release Gate: 2026-05-23

Result: pass locally for no-send, default-disabled, mock/dry-run, and metadata-probe coverage. This does not prove live message delivery.

| Feature | Classification | Evidence | Remaining limitation |
|---|---|---|---|
| Message Channel Abstraction | 5 Hardened | Full suite passed; channel/draft validation, unknown channel denial, CRITICAL send request defaults, untrusted labels, no direct send path, and dogfood channel listing validated. | No live provider send; abstraction remains a safe foundation only. |
| Message Safety Policy | 5 Hardened | Send-action proposals are exact-preview CRITICAL Action Center records with no approval reuse; draft edits invalidate approvals; bulk recipients and attachments are rejected. | Actual sends remain adapter-specific and disabled unless separate gates pass. |
| Lead Inbox | 4 Tested | Mock lead list/classify/summarize/draft/follow-up paths are tested and dogfooded; selected reads remain HIGH and disabled by default. | Real Gmail/Telegram/Apple Business/personal iMessage source adapters are not live-validated. |
| iOS compose bridge | 4 Tested | Agent-side handoff payload/status/result flows pass tests and dogfood; no silent-send tool exists. | No iOS companion app or device-level compose validation yet. |
| macOS Messages probe | 6 Live-Validated | Safe local probe on macOS reported Messages.app metadata only, `send_capability_known=false`, no private DB, no Full Disk Access, no content read, and no send. | Metadata probe does not prove send support. |
| Draft/handoff workflow | 5 Hardened | Handoff suite passes with safe default-disabled behavior; drafts stay local, copy/save require approval when applicable, and no send tool is called. | Real clipboard/manual handoff should still be manually QA'd with non-sensitive fixtures. |
| Incoming message strategy | 4 Tested | Manual/mock inbox tests and docs cover workspace-only import, `UNTRUSTED_MESSAGE`, no `chat.db`, no watcher, no send, and no memory write. | No live provider ingestion or webhook validation. |
| macOS approved send adapter | 4 Tested | Manifest and tests verify default-disabled, allowlist, live-probe, rate-limit, exact Action Center approval, no reuse, no bulk/group/attachments, and unsupported fallback. | Live send was not run and is not available until explicitly configured and approved. |
| Apple Messages for Business stub | 4 Tested | Doctor/status/mock inbound/draft-response are local only, disabled by default, untrusted, and no send/provider call occurs. | Real provider credentials/webhooks/API sends remain future work. |
| Lead response draft workflow | 4 Tested | Mock lead draft, summary, classification, follow-up pending task action, and meeting suggestion no-event paths are tested/dogfooded. | Real selected lead reads and provider-specific context remain future approval-gated work. |
| Approved lead response send workflow | 4 Tested | CRITICAL send proposals, iOS compose handoff, manual fallback, macOS gate delegation, approval no-reuse, draft-edit invalidation, and no bulk/auto-send are tested. | No live channel send executed. |
| Messaging dogfood suites | 5 Hardened | `messaging_core`, `messaging_handoff`, `messaging_ios_compose`, `messaging_macos_probe`, `messaging_send_dry_run`, and `lead_response` all passed locally; live send probe is excluded. | Suites prove QA coverage and safe dry-runs, not live delivery. |

Conservative release decision:

- Live sending is not user-ready.
- Current safe paths are draft, local save/copy handoff, iOS compose payload generation for a future user-confirmed app path, mock Apple Business inbound/draft, and macOS metadata probing.
- Future live send validation requires explicit configuration, allowlisted self-test recipient, Action Center approval, live-send probe approval, and a separate release gate.

Result: pass locally.

Evidence:

- Full test suite: `616 passed`.
- Startup policy validation: `startup policy ok`.
- Capability manifest validation: `77 capabilities`.
- Safe eval suite: `21 passed`, `0 failed`, `8 skipped`; skipped cases were live LM Studio or personal-data evals that remain opt-in.
- Command registry validation: `189` commands, no problems.
- Native skill validation: `3` manifests valid.
- Unknown tool denial: brokered denial confirmed.
- ToolBroker path scan: only `ToolBroker` calls registered tool handlers; preflight/status paths perform metadata/schema lookups only.
- Personal-data defaults: calendar, contacts, email, messages, and task-provider access remain disabled by default; `tasks.draft_create` remains the documented non-personal Action Center draft exception.
- HIGH actions require approval and CRITICAL actions require `per_action` approval with no reuse.

Conservative maturity decisions:

- Golden eval suite, model-router benchmarks, Action Center, Tasks / Reminders, calendar approved writes, contacts approved edits, email approved send, messages handoff, browser clipping, knowledge capture, Privacy Center, backup/restore, Scheduler v1, and the overnight runbook remain `4 Tested` unless feature-specific rows justify a higher level.
- Self-improvement implementation loop remains `5 Hardened` because policy/audit weakening, package-install, persistence, personal-data, branch, test, diff, commit-action, and overnight safe-mode denial paths are covered.
- No feature was raised to `6 Live-Validated` or higher in this gate unless an existing row already records separate live/provider evidence.

## Summary

Most mature features:

- ToolBroker / PolicyEngine / AuditLogger: `8 Mature Pattern`
- Weather connector: `7 User-Ready`
- Personal connector readiness gate: `5 Hardened`
- Connector framework: `4 Tested`
- Unified Action Center: `4 Tested`
- Browser selected URL and clipping: `4 Tested`
- Notes / Knowledge Capture v1: `4 Tested`
- Calendar approved writes v1: `4 Tested`
- Reminders / Tasks connector v1: `4 Tested`
- Contacts approved edits v1: `4 Tested`
- Email approved send v1: `4 Tested`
- Messages safe handoff v1: `4 Tested`
- Prompt Ledger, Prompt Queue, and Prompt Pack tracking: `5 Hardened`
- PromptOps Workbench v1: `5 Hardened`
- Command Registry and Manual QA System: `4 Tested`
- Live Session Logging and Replay: `5 Hardened`
- Manual Dogfood Command Suites: `5 Hardened`
- Product Quality Dashboard v1: `5 Hardened`
- User Feedback Capture for Live Sessions: `5 Hardened`
- Session Review and Bug Generator: `5 Hardened`
- Scheduler / Automation v1: `4 Tested`
- Native skill finder v1: `4 Tested`
- PDF workspace native skill v1: `4 Tested`
- Release-gate tracking sync: `4 Tested`
- LM Studio no-tool chat, web research, workspace tools, and memory: `5 Hardened`

Least mature features:

- Native Skills Program foundation: `2 Scaffolded`
- Skill marketplace survey and native shortlist: `1 Specified`
- Agent dashboard: `5 Hardened`
- Self-improvement backlog + implementation loop: `5 Hardened`
- Optional local web dashboard remains deferred; CLI dashboard is the current UX surface.

Features needing hardening:

- Native Skills vetter, manifest, finder, PDF workspace skill, and loader before any external skill-derived workflow is implemented
- Skill marketplace survey recommendations before converting any candidate into runtime behavior
- Calendar read-only
- Contacts read-only
- Email draft-only live provider path
- Messages draft-only
- Meeting prep live personal-data path
- Calendar approved writes native provider path
- Reminders / Tasks native provider path
- Contacts approved edits native provider path
- Email approved send real provider path
- Messages automatic send provider path
- Optional local web dashboard

Features needing live validation:

- LM Studio no-tool chat against the currently selected local model
- Web search/fetch/research with configured Brave Search
- Calendar read-only with explicit user-selected range and macOS approval
- Contacts read-only with explicit user-selected contact and macOS approval
- Meeting prep with explicit selected event and optional approved contacts
- Email draft-only with a deliberately configured non-production account

Features needing docs:

- Optional local web dashboard documentation if that path is selected later

Features needing tests:

- Optional local web dashboard workflow tests if that path is selected later

Current recommended work up next:

- Session logging/replay system, keeping logs redacted and workspace-bounded.

1. Run `python smart_agent.py work next` or `python smart_agent.py prompts next` and confirm `SESSION-LOGGING-REPLAY`.
2. Keep Action Center as the review surface before any approved write/send implementation.
3. Run approved dry-runs before any live calendar/tasks/email metadata briefing or meeting prep personal sections.
4. Keep personal-data evals skipped until an explicit approval-gated opt-in design is added.

## Next Work-Up Candidates

Early or stubbed features:

- WeatherKit provider: decision record and configuration stub only; JWT signing and Apple API calls are intentionally deferred.
- Browser selected-tab native reader: explicit URL workflows are implemented, but native selected-tab access is a stub until a safe permissioned path is designed.
- Native Reminders/Tasks provider: v1 uses an adapter/mock pattern; real native integration needs a separate decision record and live approval model.
- Live calendar/contact write providers: Action Center-gated stubs exist, but real native writes are not enabled.
- Real email send provider: mock-only execution path exists; OAuth/keychain/provider strategy remains deferred.
- Automatic Messages send path: intentionally deferred; safe handoff is the current supported path.

Features lacking live validation:

- LM Studio no-tool chat against the currently selected local Qwopus model.
- Web search/fetch/research with the user's configured search provider.
- NWS alerts/forecast against live `api.weather.gov`.
- Calendar and contacts selected-scope reads with explicit user approval.
- Email metadata/draft-only with a deliberately configured non-production account.
- Tasks, calendar writes, contact writes, and email sends against real providers remain deferred until provider-specific decision records and approval gates are reviewed.

Features needing UX polish:

- Action Center editing for rich draft bodies and provider-specific previews.
- Approval queue/session persistence ergonomics.
- Memory viewer/search/export presentation.
- Command QA result logging and bug capture from manual QA.
- Scheduler recurrence display and human-readable next-run previews.
- Dashboard grouping for large command/connector inventories.

Features needing security hardening:

- Any future native write provider for calendar, contacts, tasks, email, or messages.
- Any future background scheduler/LaunchAgent/cron integration.
- Native skill finder and marketplace flow.
- Provider credential storage and secret/config doctor improvements.
- Live personal-data eval path, which must remain opt-in and approval-gated.

Mature enough to reuse as patterns:

- ToolBroker / PolicyEngine / AuditLogger safety control plane.
- Capability manifest normalization and startup validation.
- Weather connector provider/status/cache/rate-limit pattern.
- Connector metadata/status framework.
- Action Center as the review surface for risky operations.
- PromptOps and command registry as SDLC tracking patterns.
- Workspace file assistant path policy and audit pattern.

## Maintenance Rules

Before starting a task, Codex must read this file to understand feature maturity.

Before finishing a task, Codex must update this file if the task changed any:

- feature
- command
- connector
- workflow
- policy behavior
- approval behavior
- audit behavior
- memory behavior
- user-facing documentation
- tests

A feature is not complete unless its maturity level, readiness score, tests, docs, policy/audit status, and next work needed are updated.

Do not mark a feature mature unless tests, docs, policy/audit behavior, and release gates justify it. Prompt count is useful context but not proof of maturity. A feature can be complete but still immature. A feature can be mature but still have known limitations.
