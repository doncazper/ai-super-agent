# Changelog

All notable project changes are tracked here. This file follows a Keep-a-Changelog style structure and must not contain secrets or private user data.

## Unreleased

### Added

- Canonical Runtime Gateway Hardening `CANON-01` canonical runtime state model and source-of-truth hierarchy. Added read-only JSON canonical state metadata, source-of-truth hierarchy, reconciliation-preview conflict markers, runtime docs/ADR, `runtime canonical-state`, `runtime source-of-truth`, and `runtime reconcile-preview` commands, command registry/test matrix rows, and targeted tests. No web server, Fastify/TypeScript gateway, runtime rewrite, background service, provider call, personal-data access, send/write behavior, package install, model download, commit, or push was added.
- Canonical Runtime Gateway Hardening `CANON-02` durable execution record contracts. Added JSON-serializable prompt/job/workflow/command/QA/self-heal/approval-gated/media/secret-scan record contracts, record status schema, stable redacted hashing, read-only `runtime records list/show/latest/validate` commands, runtime docs, command registry/test matrix rows, and targeted tests. No queued prompt execution, background worker, scheduler, executor, tracker mutation, raw secret/personal-data storage, web server, package install, commit, or push was added.
- Canonical Runtime Gateway Hardening `CANON-03` Agent Gateway / Runtime Kernel boundary. Added metadata contracts for gateway request/response envelopes, gateway status, runtime kernel ownership, frontend/channel contract, boundary docs/ADR, read-only `runtime gateway-status`, `runtime kernel-status`, and `runtime frontend-contract` commands, command registry/test matrix rows, and targeted tests. No Fastify/TypeScript gateway, local web server, listener, native UI, Telegram/mobile channel, external tool exposure, CLI replacement, tool execution, approval bypass, policy/capability mutation, package install, commit, or push was added.
- Canonical Runtime Gateway Hardening `CANON-04` resume, recovery, and checkpoint model. Added checkpoint and recovery report contracts, preview-only recovery behavior, read-only `runtime recovery-preview`, `runtime checkpoints list`, and `runtime checkpoints show` commands, docs, command registry/test matrix rows, and targeted tests. Recovery preview does not resume automatically, approval gates remain active, CRITICAL resumes require fresh explicit approval, prompt-pack resume depends on prompt tracker evidence, and no secrets/personal data, tracker mutation, executor, rollback, package install, commit, or push was added.
- Canonical Runtime Gateway Hardening `CANON-05` code-mode and self-heal artifact hash checks and safety lints. Added deterministic redacted artifact-hash reports, static safe-only blocker lints, `improve artifact-hashes`, `improve lint-diff`, and `improve verify-artifacts` inspection commands, docs, command registry/test matrix rows, and targeted tests. The checks do not patch files, execute self-heal, run providers, install packages, start services, commit, push, store secrets, or bypass ToolBroker, PolicyEngine, ApprovalManager, or AuditLogger.
- Canonical Runtime Gateway Hardening `CANON-06` surface regression lanes. Added a static safe surface-lane registry, read-only/dry-run `qa surfaces`, `qa surfaces run --dry-run`, and `qa surfaces matrix` commands, surface lane docs/matrix, fixture-safe dogfood suites for CLI/runtime/PromptOps/Action Center/app bridge/channels surfaces, command tracking, and tests. The lane runner is preview-only in v1 and excludes personal-data, HIGH/CRITICAL, and live-provider checks by default.
- Canonical Runtime Gateway Hardening `CANON-07` backup roundtrip and restore hardening lane. Added brokered read-only `backup roundtrip --dry-run`, `backup policy-check`, and `backup restore-check` commands, restore policy guard docs, capability manifest entries, command registry/test matrix rows, and regression tests for hash verification, pre-restore copies, CRITICAL approval reuse refusal, unredacted secret refusal, path traversal refusal, and dry-run no-write behavior. Restore remains HIGH approval-gated and live restore smoke remains disposable-workspace-only.
- Canonical Runtime Gateway Hardening `CANON-08` canonical state dashboard and docs. Added read-only `runtime canonical-dashboard` and `runtime handoff --for-chatgpt` metadata commands, dashboard/handoff docs, dashboard aggregation over canonical state, tracker conflicts, validation evidence, dirty worktree summary, safety status, gateway/kernel status, recovery hints, and tests. The dashboard writes no files, mutates no trackers, executes no tools, calls no providers, starts no server, and resumes nothing.
- Canonical Runtime Gateway Hardening `CANON-09` tracker-to-canonical-state migration plan. Added read-only `runtime tracker-sync-preview` and `runtime tracker-conflicts` commands, tracker migration/sync policy docs, prompt tracker integration docs, command registry/test matrix rows, and tests. Canonical state is positioned as the machine-readable active-work view while markdown trackers remain evidence/history records; no automatic tracker overwrite or broad rewrite was added.
- Canonical Runtime Gateway Hardening `EXTREV-01` external architecture review parity checks. Added external review findings, hardening plan, parity checklist, planned-only audit verifier/export command rows, and docs tests. Audit receipt commands remain planned and no runtime implementation was added.
- Canonical Runtime Gateway Hardening `CANON-10` release gate. Added canonical runtime release-gate docs, maturity review, external review hardening release gate, release-gate docs tests, prompt tracker completion evidence, and local validation evidence. Full suite passed with 1723 tests; focused release-gate tests passed with 58 tests; command registry validation passed with 589 commands; policy-check and all_safe dogfood dry-run passed. No server, background service, runtime rewrite, package install, model download, live provider call, personal-data enablement, send/write enablement, commit, or push was performed.
- Performance Bottleneck Scanner `PERF-01` architecture and policy. Added performance scanner track docs, bottleneck policy, finding schema, optimization policy, baseline strategy, architecture decision, docs tests, and planned command registry rows for future `perf` scan/report/benchmark/baseline commands. This milestone is docs/metadata only: no runtime scanner, benchmarks, live providers, paid APIs, model downloads, personal-data access, background services, automatic patches, commits, or pushes were added.
- Performance Bottleneck Scanner `PERF-02` report models and store. Added JSON-serializable performance finding/report/baseline/metric/evidence/recommendation models, stable finding IDs, redacted local JSON/Markdown report storage under `reports/performance/`, structured no-report states, brokered read-only `perf report --last` and `perf findings` commands, docs, manifest entries, and tests. No scans, benchmarks, live providers, paid APIs, model downloads, personal-data access, background services, automatic patches, commits, or pushes were added.
- Performance Bottleneck Scanner `PERF-03` static bottleneck scanner. Added safe static source scanning, default generated/cache/log/report/workspace exclusions, heuristic bottleneck patterns, redacted report writing, brokered `perf scan` and `perf scan --static` commands with `--max-files`, docs, manifest entries, and tests. The scanner does not execute scanned code, import scanned modules, run benchmarks/tests, call providers, use paid APIs, access personal data, apply patches, commit, or push.
- Performance Bottleneck Scanner `PERF-04` startup/import overhead scanner. Added bounded subprocess startup/import timing scans, safe default command/import targets, timeout handling, missing-command skips, brokered `perf scan --startup` and `perf startup --json` commands, docs, manifest entries, and tests. No LM Studio chat, live provider call, personal-data command, model loading/download, background service, package install, full-suite run, automatic patch, commit, or push was added.
- Performance Bottleneck Scanner `PERF-05` safe command benchmark runner. Added registry-gated benchmark execution for active SAFE/LOW read-only commands, explicit denial for HIGH/CRITICAL, approval-required, placeholder, personal-data, mutating, live-provider, paid-provider, background, network/model commands, brokered `perf benchmark --safe|--group|--command`, redacted timing reports, docs, manifest entries, and tests. Raw stdout/stderr content is not stored; only timing metadata, byte counts, return codes, and denials are retained.
- Performance Bottleneck Scanner `PERF-06` test suite performance profiler. Added bounded pytest `--durations` profiling for safe repo-local test targets, explicit `--full-suite` opt-in for broad targets, brokered `perf tests --durations`, `perf tests --target`, and `perf tests report --last` commands, redacted profile reports, module summaries, optimization suggestions, docs, manifest entries, and tests. The profiler does not delete, skip, xfail, or edit tests to improve results and stores no raw pytest output.
- Performance Bottleneck Scanner `PERF-07` optimization recommendation engine. Added advisory recommendation generation from redacted performance reports, category mapping for common bottlenecks, recommendation fields for impact/effort/risk/patch area/tests/docs/rollback/self-heal/review/status, brokered `perf suggest-fixes`, docs, manifest entry, and tests. The command writes redacted recommendation reports and applies zero patches.
- Performance Bottleneck Scanner `PERF-08` performance baseline and regression tracking. Added redacted local baselines under `reports/performance/baselines/`, baseline metadata for timings/finding counts/branch/commit/Python/platform/notes, tolerance-based regression comparison, brokered `perf baseline create`, `perf baseline compare`, and `perf regressions` commands, docs, manifest entries, and tests. Baseline commands do not run live providers or benchmarks by default and store no raw command, pytest, prompt, provider, or personal content.
- Performance Bottleneck Scanner `PERF-09` safe optimization patch planner. Added metadata-only patch plans from recommendation reports, patch-plan fields for expected files/behavior/tests/docs/rollback/review/self-heal status, brokered `perf patch-plan`, docs, manifest entry, and tests. Patch planning records `applied_patches=0` and does not apply patches, install packages, commit, push, or change safety controls.
- Performance Bottleneck Scanner `PERF-10` dashboard and QA integration. Added read-only performance dashboard/status/next-fix/trends summaries from redacted local artifacts, QA-oriented dashboard docs, brokered `perf dashboard`, `perf status`, `perf next-fix`, and `perf trends` commands, manifest entries, command registry rows, and tests. Dashboard commands execute no scans, tests, benchmarks, providers, patch plans, or fixes.
- Performance Bottleneck Scanner `PERF-11` release gate. Added local release-gate and maturity-review docs, refreshed safe static/startup/benchmark/test-profile/recommendation/baseline/dashboard evidence, validated command registry and startup/capability policy, and conservatively moved the track to local `5 Hardened` maturity. No live providers, paid APIs, model downloads, personal-data access, package installs, background jobs, automatic patches, commits, or pushes were performed.
- Added `docs/HANDOFF_TO_CHATGPT.md` as a concise handoff summary covering current git state, active/queued prompts, completed Performance pack evidence, changed files, commands, tests, source-of-truth conflicts, safety status, blockers, upload recommendations, and safe next actions.
- Secrets and API Key Management `SECRETS-08` release gate and maturity review. Added release-gate and maturity docs, recorded full-suite validation with 1616 passing tests, command registry validation with 541 commands, policy/capability validation, tracked/staged secret scans, git preflight evidence, and conservative maturity `5 Hardened` / readiness 82. No real secrets, Keychain reads/writes, provider API calls, paid APIs, commits, or pushes were added.
- Secrets and API Key Management `SECRETS-07` documentation integration. Added user-guide secrets workflow, provider secret setup matrix, provider-doc links, scan/preflight guidance, rotation checklist, and docs tests. No runtime behavior, providers, real secrets, or paid APIs were added.
- Secrets and API Key Management `SECRETS-06` secret leak scanner and Git preflight. Added dependency-free tracked/staged secret scanning, redacted findings, `.env`/sensitive-path warnings, `secrets scan`, `secrets scan --staged`, `git preflight`, `git preflight --staged`, docs, manifest/command registry rows, and fixture tests. Commands do not install external scanners, print raw values, rewrite history, commit, or push.
- Secrets and API Key Management `SECRETS-05` optional macOS Keychain strategy and adapter. Added status/dry-run-only Keychain commands, a brokered safe adapter that never calls Keychain or returns values, docs, manifest/command registry rows, and tests. Real Keychain reads/writes remain blocked and future-only.
- Secrets and API Key Management `SECRETS-04` provider secret doctors. Expanded config-only provider status checks for Reddit, SerpAPI, Brave, WeatherAPI, Telegram, Gmail, NewsAPI, Media Cloud, Microsoft, GitHub, media providers, LM Studio, Ollama, and llama.cpp; added `secrets doctor <provider>` / `all` support, docs, tests, and command tracking. Doctors report present/missing/setup/cost/scope/token-path metadata only and make no provider calls.
- Secrets and API Key Management `SECRETS-03` source resolver and local environment loader. Added dependency-free `.env` parsing, safe secret resolution from environment over ignored local `.env`, source-order metadata, brokered `secrets sources`, docs, manifest/command registry rows, and tests. Diagnostics report presence/source/warnings only and do not print values or access Keychain.
- Secrets and API Key Management `SECRETS-02` registry and redaction layer. Added `agent/secrets` registry/models/redaction helpers, shared `agent.safety.redaction.SecretRedactor` delegation, brokered `secrets list`, `secrets redaction-test`, and `secrets policy` metadata commands, capability manifest entries, command registry/test matrix rows, docs, and tests. Commands use fake fixtures or static metadata only and read/return no real secret values.
- Secrets and API Key Management `SECRETS-01` architecture and policy. Added secret-management policy docs, API key inventory, local environment setup guidance, macOS Keychain guide, corrected secret redaction policy docs, architecture decision, placeholder-only env template, `.gitignore` secret artifact protections, `.env.example` placeholders for future providers, and docs tests. No real secrets, Keychain access, paid API enablement, provider calls, commit, or push were added.
- Creative Media Generation `MEDIA-01` roadmap and risk model. Added docs for the media track, provider strategy, risk model, license policy, asset policy, safety policy, and architecture decision; added planned/stubbed media command registry rows; and kept the milestone documentation-only with no model install/download, provider API call, media generation, upload/publish workflow, voice cloning, real-person likeness workflow, personal-data tool, commit, or push.
- Creative Media Generation `MEDIA-02` provider registry and asset manager scaffold. Added provider-neutral media models, static/mock provider registry, bounded workspace media asset manager, redaction helpers, brokered read-only/dry-run `media` CLI commands, capability manifest entries, docs, and tests. No real provider, model install/download, paid API, media generation, upload/publish, voice cloning, real-person likeness workflow, or personal-data access was added.
- Creative Media Generation `MEDIA-03` safety, copyright, consent, and license preflight. Added deterministic local media prompt safety checks, provider license reporting, consent policy status, brokered `media safety-check`, `media license report`, and `media consent policy` commands, docs, manifest entries, and tests. No external moderation API, media generation, provider call, voice cloning, real-person likeness workflow, legal advice, or personal-data access was added.
- Creative Media Generation `MEDIA-04` ComfyUI provider strategy and stub. Added disabled-by-default ComfyUI config/status/doctor scaffolding, workflow-list metadata stub, docs, manifest entries, command tracking, and mocked tests. No ComfyUI install, server startup, model download, workflow submission, custom-node execution, or media generation was added.
- Creative Media Generation `MEDIA-05` image generation provider track. Added image-generation dry-run planning, mock image-provider metadata scaffolding, provider candidate docs, brokered `media generate image --dry-run`, `media image providers`, and `media image plan` commands, manifest entries, command tracking, and tests. No real image generation, model install/download, provider SDK import, paid API, asset write from the CLI, personal-photo input, upload/publish, voice cloning, or real-person likeness workflow was added.
- Creative Media Generation `MEDIA-06` image editing, thumbnail, and social creative workflow scaffolding. Added local creative workflow templates, thumbnail/social dry-run planning, mock workflow asset metadata helper, brokered `media thumbnail --dry-run`, `media creative plan`, and `media creative templates` commands, docs, manifest entries, command tracking, and tests. No real image editing, generation, personal-image input, upload/publish, model install/download, provider API, voice cloning, or real-person likeness workflow was added.
- Creative Media Generation `MEDIA-07` video generation provider strategy and scaffolding. Added video provider candidate metadata, dry-run video planning with resource warnings, mock video-provider metadata scaffolding, brokered `media generate video --dry-run`, `media video providers`, and `media video plan` commands, docs, manifest entries, command tracking, and tests. No real video generation, model install/download, provider SDK import, paid API, source-image input, upload/publish, voice cloning, or real-person likeness video workflow was added.
- Creative Media Generation `MEDIA-08` audio, music, and sound generation strategy and scaffolding. Added audio/music provider candidate metadata, dry-run audio/music planning with license and artist-imitation warnings, mock audio-provider metadata scaffolding, brokered `media generate audio --dry-run`, `media generate music --dry-run`, `media audio providers`, and `media music plan` commands, docs, manifest entries, command tracking, and tests. No real audio/music generation, model install/download, provider SDK import, paid API, upload/publish, voice cloning, TTS runtime, or commercial copyrighted-song/artist imitation workflow was added.
- Creative Media Generation `MEDIA-09` TTS and voice generation strategy. Added TTS/voice category planning, voice consent policy metadata, TTS/voice provider stubs, brokered `media tts plan`, `media voice consent-policy`, and `media voice providers` commands, docs, manifest entries, command tracking, and tests. No real voice audio generation, voice cloning, public/private person voice imitation, personal voice sample use, upload/publish, model install/download, provider API, or consent record storage was added.
- Creative Media Generation `MEDIA-10` media workflow commands and natural-language routing hooks. Added brokered `media plan "request"` dry-run planner, target inference for thumbnail/image/video/audio/music/TTS/creative requests, deterministic safety preflight before target planning, natural-language `media.plan` routing metadata, docs, manifest entries, command tracking, and tests. No real media generation/editing, provider call, upload/publish, unsafe prompt execution, personal-media read, model install/download, paid API, automatic natural-language execution, or safety-control bypass was added.
- Creative Media Generation `MEDIA-11` dogfood and eval suite. Added mock/fixture-first media dogfood suites, media eval cases, `eval run --media` / `eval report --media`, command registry/test matrix rows, and a media dogfood runbook. The checks validate no real generation, no paid APIs, no model downloads, unsafe prompt denial, voice-clone denial/deferment, license warnings, workspace-bounded assets, and command registry coverage without provider calls, uploads, publishing, personal-data access, model installs/downloads, or safety-control bypass.
- Creative Media Generation `MEDIA-12` release gate and maturity review. Added creative media release-gate and maturity-review docs, recorded full-suite, focused media, dogfood, media eval, command registry, startup policy, and capability manifest validation evidence, and kept maturity conservative at local `4 Tested` / readiness 77. No real generation/editing, provider call, model install/download, paid API, upload/publish, voice cloning, real-person likeness workflow, personal-data access, commit, or push was added.
- Source-of-truth, wiring, tracker, and release-boundary reconciliation for `SOURCE-TRUTH-RECONCILE-01`. Added `docs/reconciliation/` reports covering repo state, source-of-truth hierarchy, prompt tracker status, feature/maturity status, command registry status, capability manifest status, docs wiring, artifact/git hygiene, QA/test/dogfood wiring, clean release-candidate boundary, and final findings. Corrected stale prompt queue/ledger rows for completed Native Skill, CODEBUG, NLCMD, and QA prompts; marked recovered Media and Secrets prompt packs as `needs_review` rather than running them.
- Command QA frontend/backend separation for `QA-FE-BE-01`. Added JSON-serializable QA API envelopes, a reusable `QAService` backend boundary, service-backed dashboard/status output, frontend/backend boundary docs, API contract docs, command registry/test matrix updates, and regression tests proving read-only dashboard calls do not execute commands, action methods stay safe-tier only, HIGH/CRITICAL/personal-data commands are blocked, and summaries are redacted. No GUI, server, background persistence, personal-data access, HIGH/CRITICAL execution, send/write action, commit, or push was added.
- Recorded the blocked Creative Media Generation prompt pack import attempt. The requested `prompts/packs/creative-media-generation-v1.promptpack.md` source file is missing, so no `MEDIA-01` through `MEDIA-12` prompts were imported, queued, activated, or run, and no media runtime, provider, asset manager, generated media, paid API, model install/download, upload/publish, voice cloning, real-person likeness workflow, personal-data tool, commit, or push was added.
- Command QA Sandbox and Self-Healing Loop controlled batch for `QA-01` through `QA-10`. Added safe command QA policy/docs, registry-backed QA planner, bounded Tier 0/Tier 1 runner with redacted reports, disposable fixture workspace for Tier 3, redacted result ranking/bug generation/regression scaffolds, conservative self-heal planning/reporting, dry-run progressive QA policy, read-only dashboard, dogfood/eval suites, release-gate docs, and command registry/test matrix coverage. The track does not auto-run HIGH/CRITICAL or personal-data commands, send messages, commit, push, install packages, start schedulers, or weaken safety controls.

- Natural-Language Command Understanding release gate and maturity review for `NLCMD-10`. Added release-gate and maturity docs recording full-suite, policy, capability manifest, command registry, natural-language eval, dogfood, and exact-command regression evidence. The track is classified conservatively as local-tested advisory UX, not an autonomous execution layer.
- Natural-Language Command Understanding bug feedback loop for `NLCMD-09`. Added NL-specific feedback tags, `feedback nl-bug --last --expected-intent <intent>`, `bugs create-nl-regression <bug_id>`, `nl regressions list`, a redacted fixture template, triage docs, and tests. The loop creates local redacted bug/fixture artifacts only; it does not fix bugs automatically, store personal data, execute commands, or weaken safety policy.
- Natural-Language Command Understanding dogfood suites for `NLCMD-08`. Added `natural_language_core` and `natural_language_risky` dogfood suites, a dogfood runbook, command registry/test matrix rows, and tests. Core phrases use advisory `nl` output; risky phrases use `nl preflight` only and must not send messages, read personal data, delete files, run prompt packs, call providers, write memory, or execute mapped commands.
- Natural-Language Command Understanding eval fixtures for `NLCMD-07`. Added `eval_cases/natural_language/`, `eval run --natural-language`, `eval report --natural-language`, fixture tests, and docs covering weather, web/research, news, Reddit/forums, help, doctor/status, memory, workspace, prompt tracker, bug/session review, ambiguous, personal-data, send/write, unsupported, and legacy/deprecated phrasing. The eval runner checks parser/preflight metadata and executes no mapped commands, tools, providers, approvals, sends/writes, or personal-data reads.
- Natural-Language Command Understanding conversational CLI UX for `NLCMD-06`. Added `python smart_agent.py nl "<request>"` and `python smart_agent.py ask "<request>"` explanation surfaces that show understood intent, matched command, safety flags, extracted arguments, setup gaps, and next safe step while preserving exact command mode and executing nothing.
- Natural-Language Command Understanding safe preflight planner for `NLCMD-05`. Added metadata-only `nl preflight`, `nl explain`, and `nl suggest` commands plus structured execution plans with plan id, exact command candidate, risk/trust, approval and dry-run flags, ToolBroker requirement, side effects, provider/setup requirements, audit preview, memory behavior, and `safe_to_execute`. Preflight executes no tools or commands; HIGH/CRITICAL, personal-data, write, unknown, ambiguous, and missing-provider plans are not marked safe.
- Natural-Language Command Understanding clarification and confirmation flow for `NLCMD-04`. Added structured clarification questions and redacted previews for missing arguments, ambiguous matches, risky send/write requests, personal-data requests, provider/setup guidance, deprecated commands, stubbed commands, unsupported intents, and safe command examples. The flow executes no commands, calls no providers, accesses no personal data, writes no memory, and keeps all real execution behind exact command paths and existing safety gates.
- Natural-Language Command Understanding parser/router for `NLCMD-03`. Added deterministic `agent.natural_language` request parsing and structured route decisions with intent, confidence, command suggestions, safety outcome, risk, approval/dry-run/clarification flags, evidence, and audit-summary metadata. The parser/router does not execute commands, call tools/providers, access personal data, write memory, or bypass safety controls.
- Natural-Language Command Understanding command intent index for `NLCMD-02`. Added metadata-only `agent.commands` indexing over the structured command registry, deterministic alias/trigger/intent extraction, read-only `commands intents` and `commands suggest "<request>"` CLI commands, docs, command registry/test matrix rows, and tests. This does not execute commands, call providers, access personal data, write memory, or bypass ToolBroker/PolicyEngine/ApprovalManager/AuditLogger.
- Natural-Language Command Understanding architecture for `NLCMD-01`. Added track docs, intent taxonomy, safety policy, and decision record that define deterministic-first command interpretation, command-registry source-of-truth behavior, and strict no-silent-execution rules. This is documentation/specification only: no natural-language command runtime, no personal-data access, no send/write behavior, no risky execution, and no ToolBroker/PolicyEngine/ApprovalManager/AuditLogger bypass was added.
- Codebase Bug Review and Hardening controlled batch for `CODEBUG-01` through `CODEBUG-08`. Added baseline, safety, CLI, runtime, connector/workflow, tracker/docs, static scan, release-gate, and summary docs under `docs/bugfix/`; fixed a scoped closed-pipe CLI traceback in `commands list`; added regression coverage; and recorded green full-suite, policy, command registry, safe eval, and dogfood dry-run evidence without adding major features, enabling personal-data tools, committing, or pushing.
- Full feature status, maturity, and prompt-tracker audit for `MATURITY-AUDIT-01`. Added `docs/productization/FULL_FEATURE_STATUS_AND_MATURITY_AUDIT.md`, `FEATURE_MATURITY_SCORECARD.md`, `PROMPT_TRACKER_MISSED_PROMPTS_AUDIT.md`, `NEXT_MATURITY_QUEUE.md`, `NEXT_FEATURE_EXPANSION_CANDIDATES.md`, and `MANUAL_VALIDATION_PLAN.md` with conservative evidence labels, prompt tracker inconsistency findings, productization blockers, maturity queues, manual validation guidance, and no runtime feature implementation.
- HERMES-13 release gate and maturity review. Added `docs/autonomy/HERMES_INSPIRED_RELEASE_GATE.md` and `docs/autonomy/HERMES_INSPIRED_MATURITY_REVIEW.md` with local validation evidence for gateway/channel, Telegram/mobile, skill proposals, skill improvements, scheduler UX, subagent isolation, sandbox abstraction, model switching, memory continuity, authorized web automation boundaries, safe autonomy dogfood/evals, and high-risk gates. The gate keeps safe autonomy at conservative local-tested groundwork status and does not enable risky autonomy, background persistence, browser automation, personal-data access, send/write behavior, subagent writes, executable sandboxes, paid/cloud provider defaults, or bypass behavior.
- HERMES-12 safe autonomy dogfood/eval suite. Added mock-first dogfood suites for safe autonomy core, channel gateway, Telegram/mobile scaffolding, skill proposals, subagent isolation, sandbox abstraction, memory continuity, and authorized web boundaries; added fixture-backed `eval run --safe-autonomy` / `eval report --safe-autonomy`, runbook docs, command registry/test matrix rows, and tests. The suites are validation-only and do not start channels/bots/schedulers/subagents/sandboxes/browser automation, call paid/cloud providers, require personal data, execute external scripts, or run HIGH/CRITICAL actions.
- HERMES-10 long-term memory search and cross-session continuity enhancements. Added redacted memory continuity status/build-summary/clear helpers, `memory context-preview`, safe continuity docs, capability manifest entries, command registry/test matrix rows, and regression tests. Continuity v1 is preview/build-only: no cloud embeddings, no automatic prompt injection, no separate persistent continuity profile, no personal-data inclusion by default, no memory write, and no untrusted-content control over memory injection.
- HERMES-11 authorized web automation boundary. Added docs and tests defining allowed first-party/authorized testing paths, forbidden CAPTCHA/Cloudflare/anti-bot/proxy/rate-limit/login-wall/paywall/cookie/session/human-impersonation/stealth bypasses, future deep-scan gates, blocked-source reporting, and planned command registry rows for `web deep-scan`, `web source-map`, and `web blocked-report`. No browser automation or bypass implementation was added.
- HERMES-09 model switching and session continuity groundwork. Added metadata-only model switch records, provider compatibility checks, rollback plan metadata, safe blocked non-dry-run switch responses, redacted session-continuity status/export/clear helpers, docs, command registry/test matrix rows, env defaults, and tests. This does not persist provider/default config changes, generate text, execute tools, use paid/cloud providers by default, migrate context, carry personal data, write memory, or create a continuity store.
- Last-session bugfix review artifacts. Added `docs/bugfix/LAST_SESSION_REVIEW.md`, `docs/bugfix/LAST_SESSION_FIX_PLAN.md`, and regression-style docs validation for the latest redacted native-skills session review, existing setup-command bug coverage, and HERMES-09 prompt-state recovery.
- HERMES-08 sandbox backend abstraction. Added `agent.sandbox` backend models, registry, mock backend, policy validation, brokered `sandbox.backends`, `sandbox.policy`, and `sandbox.dry_run` tools, `sandbox backends`, `sandbox policy`, and `sandbox dry-run` commands, docs, command registry/test matrix rows, capability manifest entries, and tests. Sandbox v1 is dry-run/mock-only: it executes no commands, starts no sandbox, installs no Docker/VM/browser/cloud runtime, uses no network by default, mounts only workspace roots in policy, denies personal data by default, and keeps planned backends stubbed/deferred.
- HERMES-07 subagent isolation groundwork. Added mock-only `agent.autonomy.subagents`, `subagents list`, `subagents show <profile>`, `subagents policy`, and `subagents dry-run <profile> "task"` commands, docs, command registry/test matrix rows, and tests. Profiles define researcher/coder/tester/security/docs/planner/locked-down/experimental isolation metadata with no personal-data access by default, no write permissions by default, no CRITICAL execution, no direct tool calls, no approvals, no memory write, and no real subagent launch or parallel workflow execution.
- HERMES-06 scheduler UX for safe automations. Added `agent.autonomy.scheduler_ux`, `schedule explain`, `schedule templates`, `schedule preview <workflow_id>`, `schedule dry-run <workflow_id>`, `schedule risks <workflow_id>`, and `schedule review` commands, docs, command registry/test matrix rows, and tests. Scheduler UX is metadata/dry-run only: it executes no workflows or tools, creates no Action Center items, installs no cron/LaunchAgent/daemon/login item, starts no background service, keeps personal-data workflows disabled/approval-required, and blocks CRITICAL send/write workflows from automatic scheduling.
- HERMES-05 skill improvement from experience. Added proposal-only `agent.autonomy.skill_improvements`, brokered `native_skills.improve_propose`, `native_skills.improve_from_bugs`, `native_skills.improve_from_dogfood`, `native_skills.improvements_list`, and `native_skills.improvements_show` tools, CLI commands under `skills improve-*` and `skills improvements`, docs, command registry/test matrix rows, capability manifest entries, and tests. Improvement proposals use redacted bug/dogfood metadata only, skip personal-data and unredacted evidence by default, include tests/docs/rollback/lockfile-impact review fields, mark high-risk proposals for human review, and never modify skill files, update lockfiles, enable skills, install dependencies, execute scripts, or raise maturity.
- HERMES-04 repeated-task skill creation proposals. Added proposal-only `agent.autonomy.skill_proposals`, brokered `native_skills.propose_from_commands`, `native_skills.propose_from_sessions`, `native_skills.proposals_list`, `native_skills.proposals_show`, and `native_skills.proposals_approve_dry_run` tools, CLI commands under `skills propose-*` and `skills proposals`, docs, command registry/test matrix rows, redacted report storage under gitignored `reports/autonomy/`, and tests. Proposals use redacted command/session metadata only, skip personal-data patterns by default, mark high-risk candidates for review, and never create, import, enable, install, or execute skills.
- HERMES-03 Telegram/mobile access scaffolding. Added safe Telegram access policy helpers, mobile companion status/pairing metadata, brokered `telegram.doctor/status` and `mobile.status/pairing_status` tools, `python smart_agent.py mobile status|pairing-status` commands, safe Telegram/mobile config defaults, docs, command registry/test matrix rows, and tests. Telegram remains disabled by default; sends, polling, and webhooks remain disabled and unimplemented; mobile companion and mobile approvals remain disabled/unpaired; no bot starts, no webhook/listener starts, no network call is made by status commands, no messages are sent/read, no personal data is accessed, and no ApprovalManager bypass was added.
- HERMES-02 safe gateway/channel process scaffolding. Added `agent.channels` models, registry, gateway guards, metadata redaction, brokered `channels.list/status/show` tools, `python smart_agent.py channels list|status|show <channel_id>` commands, docs, command registry/test matrix rows, and regression tests. Remote channels remain disabled by default, unknown channels are denied, the gateway cannot execute tools or approve actions, and no Telegram/mobile/email/native app connection, background persistence, personal-data read, message send/write behavior, browser automation, or safety-control bypass was added.
- Hermes-inspired safe autonomy architecture and policy roadmap for safe gateway/channel access, Telegram/mobile scaffolding, repeated-task skill proposals, skill improvement proposals, scheduler UX, subagent isolation, sandbox backend abstraction, model continuity, memory search, and cross-session continuity. Added decision and autonomy docs that keep automatic skill creation, automatic scheduled actions, unattended workflows, arbitrary browser automation, background persistence, cross-platform messaging sends, subagent write permissions, and cloud/server private-data execution gated. Unauthorized CAPTCHA, Cloudflare, anti-bot, proxy-evasion, login-wall, paywall bypass, and human impersonation remain forbidden; no runtime autonomy, channel connection, background persistence, personal-data access, send/write behavior, browser automation, or safety-control bypass was added.
- Brain Runtime Independence `BRAIN-11` release gate and maturity review. Added `docs/brain/BRAIN_RUNTIME_RELEASE_GATE.md` and `docs/brain/BRAIN_RUNTIME_MATURITY_REVIEW.md` with local validation evidence for BrainProvider interface, lazy model registry, LM Studio provider wrapping, disabled alternate-provider scaffolds, mock benchmark/eval commands, safe fallback/router defaults, disabled MCP stubs, command registry validation, startup/capability validation, and optional import guard checks. LM Studio remains the compatibility baseline and normal live chat is only partially provider-independent; no default-provider change, model/runtime install, model download, paid/cloud API, MCP server/listener, personal-data enablement, or safety-control bypass was added.
- MCP interop decision and disabled adapter stubs for the Brain Runtime Independence track. Added `agent.mcp` metadata-only models, adapter boundary, server/client stubs, safe `MCP_*` defaults, docs, and `brain mcp-decision`, `mcp status`, `mcp doctor`, `mcp server --dry-run`, and `mcp clients` diagnostics. MCP remains optional interoperability, not the brain runtime; no MCP package is installed, no server/listener starts, no external connection is made, no tools are exposed, no personal tools are enabled, and no ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior is weakened.
- Brain provider fallback and model router scaffolding for the Brain Runtime Independence track. Added safe `BRAIN_*` fallback/router config defaults, deterministic provider route decisions, bounded disabled-by-default fallback, cloud fallback denial, dry-run `brain switch <provider> --dry-run`, metadata-only `brain fallback-status`, route explanation via `brain route "<message>"`, docs, command registry rows, and tests. The router does not call models, execute tools, persist provider switches, write memory, enable paid/cloud fallback, change the default provider, start runtimes/listeners, enable MCP, or weaken ToolBroker/PolicyEngine/ApprovalManager/AuditLogger behavior.
- Brain model health, benchmark, and quality eval scaffolding for the Brain Runtime Independence track. Added mock-first `brain benchmark --safe`, `brain eval --safe`, and `brain report --last` support, report writing under `reports/brain/`, safe eval fixtures, docs, command registry rows, and tests. Live provider evals remain opt-in; the safe path does not call paid/cloud providers, access personal data, download models, install runtimes, start providers/listeners, execute high-risk tools, change the default provider, write memory, or bypass safety controls.
- MLX provider strategy/stub for the Brain Runtime Independence track. Added disabled-by-default `MLXBrainProvider`, `MLX_PROVIDER_*` config defaults, lazy registry listing as `disabled`, Apple Silicon detection metadata, explicit `brain doctor --provider mlx` and `brain health --provider mlx` support, docs, and tests. The stub does not install MLX, download models, start a server, import native MLX modules at startup, load a model, generate text, change the default provider away from LM Studio, call cloud APIs, execute tools directly, write memory, or weaken safety controls.
- llama-cpp-python in-process BrainProvider scaffold for the Brain Runtime Independence track. Added disabled-by-default `LlamaCppInProcessBrainProvider`, `LLAMA_CPP_INPROCESS_*` config defaults, lazy dependency detection, provider registry listing as `disabled`, fake-backend tests for chat, explicit `brain doctor --provider llama_cpp_inprocess` and `brain health --provider llama_cpp_inprocess` support, docs, and tests. The agent does not install `llama-cpp-python`, download GGUF models, load real models in tests, load a model during health/status checks, change the default provider away from LM Studio, call cloud APIs, execute tools directly, write memory, or weaken safety controls.
- Ollama BrainProvider scaffold for the Brain Runtime Independence track. Added disabled-by-default `OllamaBrainProvider`, `OLLAMA_*` config defaults, lazy registry listing as `disabled`, mocked OpenAI-compatible chat handling, native `/api/tags` health diagnostics, explicit `brain doctor --provider ollama` and `brain health --provider ollama` support, docs, and tests. The agent does not install Ollama, pull models, start the Ollama daemon, change the default provider away from LM Studio, assume tool-call support by default, call cloud APIs, execute tools directly, write memory, or weaken safety controls.
- llama.cpp server BrainProvider scaffold for the Brain Runtime Independence track. Added disabled-by-default `LlamaCppServerBrainProvider`, `LLAMA_CPP_SERVER_*` config defaults, lazy registry listing as `disabled`, mocked OpenAI-compatible `/chat/completions` and `/models` handling, explicit `brain doctor --provider llama_cpp_server` and `brain health --provider llama_cpp_server` diagnostics, docs, and tests. The agent does not install llama.cpp, download models, start `llama-server`, change the default provider away from LM Studio, assume tool-call support by default, call paid/cloud APIs, execute tools directly, write memory, or weaken safety controls.
- LM Studio BrainProvider refactor for the Brain Runtime Independence track. Added `LMStudioBrainProvider`, lazy LM Studio provider registration, provider-neutral tool-call normalization, orchestrator support for `BrainChatResponse`, and metadata-only `brain providers`, `brain status`, and `brain doctor` commands. Existing `LMStudioClient`, `LMSTUDIO_*` environment variables, no-tools mode, ToolBroker tool-call loop, reasoning-content cleanup, and user-friendly LM Studio errors remain preserved. No new model provider, model download, runtime install, paid/cloud API default, MCP enablement, network listener, memory write, or safety-control bypass was added.
- BrainProvider interface and model registry scaffolding for the Brain Runtime Independence track. Added the provider-neutral `agent.brain` package with shared chat/tool/model/health dataclasses, the abstract `BrainProvider` contract, safe config defaults, a lazy `BrainProviderRegistry`, structured provider status/error handling, deterministic `MockBrainProvider`, docs, and focused tests. LM Studio remains the current runtime path until the later BRAIN-03 refactor; no llama.cpp/Ollama/MLX provider, paid/cloud API call, model download, MCP enablement, network listener, tool execution semantic change, prompt-history storage, or safety-control bypass was added.
- Brain Runtime Independence architecture and roadmap for decoupling the agent brain from the hardcoded LM Studio client while preserving current LM Studio/Qwopus behavior. Added a `BrainRuntimeGateway` decision record, provider strategy, LM Studio decoupling plan, provider requirements, MCP interop decision, and planned command registry rows for `brain providers`, `brain status`, `brain doctor`, `brain health`, `brain benchmark`, `brain eval`, `brain switch`, `brain fallback-status`, and `brain mcp-decision`. This is documentation/tracking only: no provider implementation, model download, runtime install, paid/cloud API default, MCP enablement, network listener, startup-heavy import, normal chat behavior change, or safety-control bypass was added.
- Native Skill System Hardening `SKILL-01` root/scoping and precedence support with metadata-only root records, lazy root scans, duplicate skill ID detection, deterministic shadowing diagnostics, read-only `skills roots`, `skills precedence`, `skills registry`, and `skills explain-root` commands, docs, and regression tests. Experimental/unreviewed roots remain unable to silently override trusted native behavior.
- Native Skill System Hardening `SKILL-02` manifest schema and dependency gating support with expanded required manifest metadata, updated built-in skill manifests, detection-only dependency checks, optional `skills validate <skill_id>` and `skills doctor <skill_id>` diagnostics, docs, template, and regression tests. Dependency checks redact env values and do not install packages, execute scripts, call providers, call connectors, scan personal files, or grant permissions.
- Native Skill System Hardening `SKILL-03` provenance, trust metadata, and lockfile diagnostics with static provenance records, trust status classification, SHA-256 file/manifest/dependency hashes, `native_skills.lock.example`, read-only `skills lock status`, `skills lock verify`, `skills provenance <skill_id>`, and `skills trust <skill_id>` commands, docs, and tests. No auto-update, external marketplace call, skill install, script execution, or lockfile write command was added.
- Native Skill System Hardening `SKILL-04` inspection and vetting CLI hardening with dedicated inspector, vetter, and risk-scoring modules; brokered `skills inspect <path_or_skill_id>` and `skills report --last`; richer `skills vet`/`skills score` reports; report output under `reports/native_skills/`; docs and regression tests. Static vetting remains read-only apart from local report writes, treats skill text as `UNTRUSTED_DOCUMENT`, and does not execute scripts, install dependencies, access network, enable skills, or run plugin runtimes.
- Native Skill System Hardening `SKILL-05` per-profile skill visibility with default `default`, `research`, `coding`, `personal_assistant`, `lead_response`, `locked_down`, and disabled-by-default `experimental` profiles; allowlist/blocklist/risk-ceiling/personal-data/network/write/CRITICAL-action visibility rules; read-only `skills profiles` and `skills profile show|allowed|validate` commands; docs, template, and tests. Profiles are advisory only and do not enable skills, grant capabilities, or bypass ToolBroker/PolicyEngine.
- Native Skill System Hardening `SKILL-06` compatibility matrix with manifest-derived platform/runtime/setup records, read-only `skills compatibility`, `skills compatibility <skill_id>`, and `skills platform matrix` commands, docs, template, and regression tests. Compatibility checks are metadata-only and do not execute skills, import native platform modules, call providers, install dependencies, enable platform-specific behavior, or grant permissions.
- Native Skill System Hardening `SKILL-07` conflict detector with metadata-only duplicate skill ID, command/capability overlap, shadowing, dependency, provider, platform, approval, risk, memory, docs, and tests conflict reports; read-only `skills conflicts`, `skills conflicts --json`, and `skills explain-conflict <conflict_id>` commands; docs and regression tests. The detector never auto-resolves conflicts, enables/disables skills, installs dependencies, calls providers, imports plugin runtimes, executes skill scripts, grants permissions, or bypasses ToolBroker/PolicyEngine/ApprovalManager/AuditLogger.
- Native Skill System Hardening `SKILL-08` test and dogfood harness with metadata-only `skills test <skill_id>`, `skills test --all-safe`, and `skills dogfood <skill_id>` commands; fixture-backed `eval run --native-skills`; `native_skills_core` and `native_skill_vetting` dogfood suites; docs and regression tests. The harness skips HIGH/CRITICAL/FORBIDDEN and personal-data skills by default, reports missing dependencies as setup skips, catches prompt-injection/secret fixtures, and does not execute external skill scripts, install dependencies, call providers/connectors, execute plugin runtimes, enable skills, grant permissions, or write memory.
- Native Skill System Hardening `SKILL-09` docs generator with metadata-only `skills docs-generate --dry-run`, dry-run-default `skills docs-generate`, explicit `skills docs-generate --write`, `skills catalog`, and `skills docs-check` commands; generated `docs/native_skills/SKILL_CATALOG.md`; docs-generation policy docs and template; and regression tests. The generator reads reviewed manifests and local tracker metadata only, preserves manual notes, reports missing docs, includes deprecated/blocked skills, copies maturity conservatively, and does not execute skills, install dependencies, call providers/connectors, execute plugin runtimes, enable skills, grant permissions, or write memory.
- Native Skill System Hardening `SKILL-10` release gate and maturity review with `docs/native_skills/NATIVE_SKILL_SYSTEM_RELEASE_GATE.md` and `NATIVE_SKILL_SYSTEM_MATURITY_REVIEW.md`. The gate records full tests, startup policy, capability manifest, docs, command registry, native skill manifest validation, lockfile verification status, conflict detection, safe dogfood suites, native skill evals, docs-check, conservative maturity, and remaining lockfile/manual-validation blockers. No external skills were installed, enabled, imported, or executed.
- News Intelligence roadmap and source policy for a future dedicated news module. Added docs for the News Intelligence track, source policy, provider strategy, freshness policy, source grounding, retention policy, and architecture decision; added planned command registry entries for `news providers`, `news top`, `news search`, `news topic`, `news source`, `news brief`, `news timeline`, `news compare`, `news multilingual`, `news cache status`, and `news dogfood`. This is documentation/tracking only: no provider API calls, paid API defaults, article fetching, browser automation, search-history storage, full article-body storage, paywall/login/CAPTCHA bypass, fabricated citations, or runtime news commands were added.
- News capability manifest and provider policy scaffolding for the News Intelligence track. Added 18 disabled/planned `news.*` capability manifest entries with provider/status/rate-limit/memory/audit/docs metadata; safe `NEWS_*` defaults in `.env.example`; and `agent.news` config/provider-policy helpers that choose or skip providers without live calls. Paid providers remain skipped by default, search/news history and full article-body storage remain off by default, command rows remain planned/non-executable, and no news provider calls, article fetches, browser automation, or bypass behavior were added.
- Cross-Platform Core + Platform Bridge release gate and future build guides for macOS, iOS companion, Windows, and app frontends. The gate verifies current groundwork remains safe and modular: platform bridges and App Bridge stay disabled/lazy by default, read-only `platform.*` commands are metadata-only, bridge stubs fail closed, direct bridge execution is blocked, no native/heavy platform imports load at `smart_agent` startup, and all real platform actions remain unavailable/planned/stubbed. No new platform behavior, native app frontend, personal-data access, or send/write capability was added.
- App Bridge API contract for future Mac app, iOS companion, Windows app, and local web dashboard frontends. The new `agent.platforms.app_bridge` package defines schema-validated request, action preview, approval decision, result, status, pairing, and audit-correlation payloads, plus safe App Bridge config defaults. The contract is disabled by default, localhost/IPC-only, remote access is unsupported, sensitive requests require pairing, ApprovalManager bypass attempts are rejected, CRITICAL payloads require exact preview and per-action/no-reuse approval metadata, status payloads reject personal data, and importing the package starts no server or native frontend dependency.
- Lazy macOS, iOS companion, Windows, and generic app/web bridge stubs for the Cross-Platform Core + Platform Bridge track. The new stub packages subclass the fail-closed `NullPlatformBridge`, are registered through lazy `PlatformBridgeRegistry` loaders, declare 27 static planned/stubbed platform capability records, and return only structured `blocked` or `requires_setup` results. They do not import native OS frameworks, execute platform actions, access personal data, request permissions, start an app bridge server, write memory, or add send/write behavior.
- Read-only platform doctor and capability commands for the Cross-Platform Core + Platform Bridge track. `python smart_agent.py platform doctor`, `platform status`, `platform capabilities`, `platform matrix`, and `platform explain <capability_id>` now execute through SAFE ToolBroker/PolicyEngine/AuditLogger metadata tools and report platform detection, bridge config/lazy-load state, static capability metadata, setup hints, and unsupported capability records. They do not execute platform bridge actions, request permissions, import native frameworks, access personal data, make network calls, start subprocesses, start an app bridge server, write memory, enable platform bridges, or add send/write behavior.
- Platform-aware config, path, and environment detection scaffolding for the Cross-Platform Core + Platform Bridge track. `agent.platforms` now has safe config defaults with all bridges disabled/lazy by default, `sys.platform`-based platform detection for macOS/Windows/Linux/unknown, explicit runtime mode detection, project-local path helpers, caching guardrails, and regression tests. This adds no real platform bridge behavior, imports no native frameworks, scans no personal files, requests no permissions, enables no personal-data tools, and adds no send/write path.
- Platform bridge base interfaces and `NullPlatformBridge` for the Cross-Platform Core + Platform Bridge track. The new `agent.platforms` contracts define side-effect-free action previews, guarded action results with audit correlation fields, metadata-only health/permission status shapes, a lazy `PlatformBridgeRegistry`, and a fail-closed null bridge for unavailable platforms. This adds no real macOS/iOS/Windows/web bridge behavior, imports no native platform frameworks, accesses no personal data, executes no platform actions, enables no personal-data capabilities, and adds no send/write path.
- Platform Capability Registry v1 with portable `agent.platforms` models, static capability definitions, safe platform detection metadata, structured unsupported/not-found capability handling, and registry tests. The registry now defines 27 planned/stubbed macOS, iOS companion, Windows, Microsoft Graph, and app/web bridge capability records without importing native frameworks, accessing personal data, executing platform actions, or enabling planned personal-data capabilities; CRITICAL future capabilities are marked per-action approval-required.
- Cross-Platform Core + Platform Bridge architecture roadmap with a decision record, platform bridge strategy, capability matrix, platform boundaries, performance/overhead policy, future bridge implementation guide, planned platform inspection command tracking, and docs validation tests. This milestone is documentation/roadmap only: no Windows, Microsoft Graph, UI Automation, EventKit, Contacts, Mail, Messages, iOS companion, native frontend, personal-data enablement, send/write action, platform-specific import, or runtime bridge behavior was added.
- Generated artifact hygiene hardening for Release Hardening Loop v2 continuation: added `docs/release/GENERATED_ARTIFACT_HYGIENE.md`, narrow `.gitignore` coverage for local eval, lead, messaging, iOS compose, and Reddit thread export artifacts, release blocker/plan/audit updates, and regression tests that preserve reviewable source fixtures while keeping generated workspace data out of release commits. This is release-process hygiene only and does not delete files, change runtime behavior, enable personal-data tools, add send/write actions, weaken policy, or bypass ToolBroker/PolicyEngine/AuditLogger.
- Tracker Hygiene, Indexing, and Compaction pass with a short tracker dashboard, tracker index, maintenance guide, archive policy, and consistency report; README tracking links; permanent AGENTS rules for anchored tracker edits and archive-preserving history; command registry/source-of-truth links; feature registry/maturity/roadmap tracking; and regression tests for tracker navigation. This is documentation/tracker validation only and adds no runtime behavior, commands, personal-data enablement, send/write action, policy relaxation, or audit change.
- Release Hardening Loop v2 audit with release readiness, blocker, and hardening plan docs under `docs/release/`; full local test/policy/manifest/command/eval/dogfood evidence; zero-open-bug follow-up for synthetic setup command reports; conservative YELLOW release classification; and tracker updates. No runtime feature expansion, personal-data default enablement, send/write capability expansion, policy relaxation, audit disabling, package installation, scraping bypass, or hidden persistence was added.
- Forum Intelligence release gate and maturity review for the Reddit + Multilingual Forum Intelligence Track: full test suite, startup policy validation, capability manifest validation, command registry validation, forum dogfood dry-runs, fixture-backed forum evals, Reddit/V2EX disabled-config status checks, Reddit retention/privacy status checks, language/provider CLI smokes, and no-bypass/no-write safety scans were run and recorded. The gate keeps live Reddit, live V2EX, live Chinese forum discovery, and local-model translation quality validation opt-in/config-gated, and adds no new scraping, paid-provider default, write action, memory write, forum-content training, or provider behavior.
- Forum Intelligence dogfood/eval suite with default-disabled `reddit_core`, `reddit_research`, `forum_multilingual`, `v2ex`, and `chinese_forum_discovery` dogfood suites; fixture-backed `eval run --forums` and `eval report --forums`; forum eval cases for source grounding, generated translation labels, original source IDs, prompt-injection exclusion, deleted/removed-content handling, no scraping or paid-provider defaults, no memory write, retention/cache policy, and provider-call audit evidence; runbook docs and regression tests. Live Reddit/V2EX/Chinese forum checks remain opt-in and setup-gated, and no scraping, login/cookie/session automation, CAPTCHA/anti-bot bypass, paid provider default, forum-content training, forum-content memory write, or new provider write action was added.
- Chinese forum discovery/search layer with brokered `cn-forums providers`, `cn-forums search`, `cn-forums fetch`, and `cn-forums research` commands; discovery uses approved search-provider site filters for Zhihu, Baidu Tieba, Douban Groups, Xiaohongshu, Weibo, NGA, and V2EX, selected direct fetches go through safe web fetch policy only, blocked/login/CAPTCHA pages return unavailable, fetched public content is labeled `UNTRUSTED_WEB`, language detection and optional local-model translation are integrated, and regression tests cover site filters, blocked/public fixtures, translation mocks, no cookies/browser sessions, no memory write, audit metadata, and command registry entries. No platform-specific scraping, login/cookie/session automation, CAPTCHA/anti-bot bypass, paid provider default, permanent forum-content storage, or training use was added.
- V2EX read-only connector v1 with documented API-only client/status/provider tooling, disabled-by-default `V2EX_ENABLED=false`, optional redacted `V2EX_TOKEN`, local 600/hour rate limiting, TTL cache, brokered `v2ex doctor`, `v2ex nodes`, `v2ex node`, `v2ex topic`, `v2ex replies`, `v2ex latest`, and `v2ex hot` commands, normalized `ForumThread`/`ForumPost`/`ForumComment` output, untrusted-web labels, optional local language detection/translation integration, connector status metadata, and regression tests. No V2EX posting/modifying, notifications, member-profile reads, web scraping fallback, logged-in/private reads, CAPTCHA/anti-bot bypass, paid API use, or memory write was added.
- Global Forum Provider Registry v1 with static metadata for Reddit, V2EX, Hacker News, Stack Exchange, Lemmy, Zhihu, Baidu Tieba, Douban Groups, Xiaohongshu, Weibo, and NGA; brokered `forums providers`, `forums status <provider>`, `forums doctor`, and `forums capabilities <provider>` commands report provider status, setup/access limitations, site-filter discovery policy, risk/trust/retention metadata, and absent/deferred write capabilities without network calls, personal/logged-in reads, cookies, scraping, CAPTCHA/anti-bot bypass, or permanent forum-content storage.
- Cross-language Reddit/forum research workflow with brokered `forums research` and `forums compare` commands; the workflow searches configured official/API forum sources, reports setup/unavailable status for V2EX/web/Chinese discovery-only providers until approved connectors exist, detects languages, labels translations as `MODEL_GENERATED_TRANSLATION`, preserves source IDs/permalinks and original-language snippets, excludes prompt-injection-like source text from evidence, avoids cultural/statistical consensus claims from sparse data, uses no paid providers or scraping fallback by default, writes no search history/source text/summary to memory, and adds docs/tests/command registry coverage.
- Multilingual forum language layer with brokered `language detect`, `language translate`, `language translate-file`, and `language glossary` commands; local heuristics detect English, Spanish, Chinese, Japanese, and Korean fixtures with simplified/traditional Chinese hints, translations default to local LM Studio/Qwopus and are labeled `MODEL_GENERATED_TRANSLATION`, source IDs/chunks are preserved, file commands read through brokered workspace access, prompt-injection-like source text is wrapped as untrusted data, no external/paid translation API is used by default, and no translations/source text are written to memory.
- Reddit retention/cache enforcement with brokered `reddit cache status`, `reddit retention status`, and `reddit privacy-report` commands; cache entries now expose TTL/count/content-hash policy metadata, status/privacy reports are counts-only, raw query fields are redacted before cache write, zero/invalid TTL disables cache writes, and regression tests cover expiry, sweep deletion, author minimization, removed-content non-retention, no query-history storage, clear/sweep audit, and command registry updates. No new Reddit API fetch, web scraping fallback, write action, content training, or permanent storage path was added.
- Reddit source-grounded summarization with deterministic brokered `reddit summarize-thread`, `reddit summarize-search`, `reddit consensus`, `reddit pros-cons`, `reddit complaints`, and `reddit buying-advice` commands; summaries preserve Reddit source IDs/permalinks, distinguish fetched-thread evidence from snippet-only search evidence, include consensus/disagreement/complaint/praise/caveat/source/fetch-limitation sections, exclude deleted/removed and prompt-injection-like comments from evidence, avoid summary memory writes and search-history persistence, and audit underlying Reddit API search/fetch calls. No Reddit web scraping fallback, unauthenticated traffic, paid provider default, write action, content training, permanent summary storage, or live API call by default was added.
- Reddit thread fetch and conversation normalization with brokered `reddit thread` and `reddit thread-export` commands, official Data API-only fetches, URL/ID parsing, bounded `--max-comments`, sort/collapse-depth options, normalized `comment_tree` plus `flattened_comments`, source references/permalinks, removed-comment handling, author redaction by default, workspace-only `UNTRUSTED_DOCUMENT` exports, and mocked tests. No Reddit web scraping fallback, unauthenticated traffic, write action, content training, permanent memory storage, or live API call by default was added.
- Reddit search workflow polish with subreddit/sort/time/limit/advisory-language options, snippet-only evidence labels, cached `reddit explain-result <source_id>` metadata lookup, setup-required behavior for disabled/missing OAuth config, query-redacted audit records, no readable search-history persistence, and mocked CLI/provider tests. No Reddit web scraping fallback, unauthenticated traffic, paid provider default, write action, content training, or live API call by default was added.
- Reddit read-only connector v1 with official Data API-only `agent.forums.reddit` client/models/normalizer/provider/errors/retention modules, brokered `reddit search`, `reddit subreddit`, `reddit post`, `reddit comments`, `reddit cache clear`, and `reddit retention sweep` commands, OAuth/setup-required failure mode, source IDs/permalinks, rate-limit metadata, author redaction by default, TTL cache/retention sweeper, deleted/removed-content exclusion, and mocked tests. No Reddit web scraping fallback, unauthenticated traffic, posting/commenting/voting/DM/moderation action, content training, permanent user-content storage, or live API call by default was added.
- Reddit OAuth/config doctor with brokered `reddit doctor`, `reddit status`, explicit `reddit auth-check`, and `connectors status reddit`; diagnostics redact secrets, warn on missing OAuth config, repo-local token files, tracked `.env`, generic user agents, disabled connector state, rate-limit/retention settings, and disabled write actions. `reddit auth-check` is explicit-only, audited, OAuth/token-status only, and fetches no posts/comments/threads.
- Reddit provider policy and compliance scaffolding with disabled-by-default Reddit config, hard-false training policy, OAuth-required metadata checks, Reddit capability manifest placeholders, retention/rate-limit defaults, compliance docs, and regression tests. This adds no OAuth flow, Reddit API calls, content fetch, web scraping fallback, posting/commenting/voting/DM behavior, permanent Reddit content storage, or new CLI commands.
- Reddit + Multilingual Forum Intelligence Track planning with `docs/decisions/reddit_forum_intelligence_track.md`, forum access policy, Reddit access policy, multilingual and Chinese forum strategies, source-grounding policy, retention policy, roadmap/risk/threat tracking, and prompt tracking for the next Reddit compliance scaffolding prompt. This is documentation and tracking only: no Reddit API calls, forum scraping, CAPTCHA/login/anti-bot bypass, forum-content training, permanent forum storage, or new CLI commands were added.
- Internet Access dogfood/eval release gate with mock-safe `internet_core` and `web_providers` suites, opt-in live `web_fetch`, `web_research`, and `web_blocked_sources` suites, fixture-backed `eval run --internet` / `eval report --internet`, recursive eval case loading under `eval_cases/internet/`, an internet dogfood runbook, and regression tests for source lists, retrieved timestamps, failed fetch reporting, prompt-injection handling, provider policy, no paid defaults, no query-history storage, and network-domain audit fixtures.
- Official API Connector Framework v1 with `agent.web_acquisition.official_apis` provider interfaces, registry/domain matching, GitHub/Wikipedia/arXiv/Reddit stubs, mocked public-result normalization to `SearchResult`-compatible records, brokered `web official-apis`, `web api-status <provider>`, and `web api-search <provider> "<query>"` commands, capability manifest entries, secret/query redaction, Reddit no-web-scraping-substitute metadata, provider docs, and tests. Built-in providers make no live API calls by default.
- Web cache, dedupe, and local lightweight index v1 with public-web-only `WebCacheStore`, content-hash/source-ID dedupe helpers, `LocalWebIndex`, brokered cache/index tools, CLI commands for `web cache status|clear|show` and `web index search|rebuild`, safe cache/index env defaults, no raw query-history persistence, no full-content storage by default, personal/authenticated content denial, blocked/CAPTCHA status-only retention, and regression tests for cache hits, expiry, dedupe, index search, privacy, audit, and command registry coverage.
- Citation and source attribution layer for source-grounded research: `SourceReference`, `CitationSpan`, `ClaimAttribution`, and `ResearchSourceBundle` now provide stable source IDs, metadata-only source bundles, snippet-only/fetched/failed evidence labels, failed-source citation separation, and `research sources/export-sources/verify-sources --last` inspection commands.
- Internet-only-when-needed router policy with deterministic routing outputs (`needs_internet`, reason, suggested sources, provider policy, tools, risk hint, clarification flag), read-only `python smart_agent.py router explain "<query>"`, richer `preflight` route metadata, current/live/source-required routing to web/search/fetch, stable/local/no-tools prompts staying clean, and prompt-injection-like text unable to force internet routing.
- Source-grounded research workflow v1 hardening: `python smart_agent.py research` now accepts `--provider auto`, `--max-sources`, `--freshness`, and `--no-fetch`; routes search/fetch through ToolBroker; emits Answer/Sources/Coverage/Fetch-failure sections; reports snippet-only and unavailable sources; detects basic source conflicts; preserves no-memory and untrusted-web handling; and adds regression tests for provider setup errors, fetch failures, prompt injection, foreign-language content, conflict notes, audit records, and CLI options.
- Safe web fetch and extraction hardening with shared `agent.web_acquisition` URL normalization, sanitization, extraction, and fetch helpers; brokered `web fetch`, `web extract`, and `web metadata` commands; structured FetchResult fields; metadata extraction; script/event-handler stripping; blocked/CAPTCHA-page unavailable results; audit-domain/result summaries; and tests for URL validation, scheme/domain denial, timeouts, redirect-limit errors, binary blocking, prompt-injection wrapping, metadata, and command registry coverage.
- SerpAPI fallback hardening with disabled-by-default `SERPAPI_ENABLED=false`, per-provider timeout/result config, stricter explicit paid/quota gates requiring `ALLOW_PAID_APIS=true` and `MAX_PAID_API_CALLS_PER_DAY>0`, brokered config-only `web serpapi doctor`, connector status metadata, setup/error normalization, query/key redaction, `serpapi.com` audit metadata, no search-history persistence, and clear no-CAPTCHA/paywall/login/anti-bot-bypass docs.
- Brave Search provider hardening with disabled-by-default `BRAVE_SEARCH_ENABLED=false`, per-provider timeout/result/safe-search config, explicit paid/quota policy gating, brokered `web brave doctor`, `web search "<query>" --provider brave`, `connectors status brave`, mocked result/error/rate-limit tests, query/key redaction, `api.search.brave.com` audit metadata, and no search-history persistence.
- SearXNG search provider v1 with configured-only/self-hosted `SEARXNG_BASE_URL`, disabled-by-default `SEARXNG_ENABLED=false`, JSON result normalization, setup hints for missing base URL or disabled JSON output, timeout/403/429/malformed-response handling, brokered `web.searxng.doctor`, `web search "<query>" --provider searxng`, `connectors status searxng`, redacted query/domain audit metadata, and no search-history persistence.
- Search Provider Registry v1 with `agent.web_acquisition.search` provider interfaces, metadata registry, normalized search result/response models, setup/error normalization, brokered `web.search_providers`, generalized explicit `web search "<query>" --provider <provider>` routing, and tests for unknown providers, setup hints, ToolBroker-only execution, result normalization, query redaction, and no search-history persistence.
- Robots/sitemap/feed Web Acquisition Layer reconciliation with core parser modules at `agent/web_acquisition/robots.py`, `sitemaps.py`, and `feeds.py`; brokered aliases `web.robots.check`, `web.sitemap.fetch`, and `web.feed.fetch`; default limits of 500 sitemap URLs and 50 feed items; focused tests for parsing, malformed input, timeout, limits, blocklists, audit domains, and ToolBroker alias execution.
- Web Acquisition Layer core package with `agent.web_acquisition` request/result/source/provider models, deterministic source planning, cost-aware provider-decision wrapping, and brokered `web source-status "<url>"` no-fetch inspection for URL source handling, trust labels, and blocked-domain/provider metadata.
- Cost-aware internet provider policy hardening with `SEARCH_ALLOWED_PROVIDERS`, `SEARCH_STORE_HISTORY`, `SEARCH_CACHE_ENABLED`, `SEARCH_CACHE_TTL_SECONDS`, brokered `web providers`, `web provider-policy`, and `web provider-decision` commands; decisions expose selected/skipped providers, skip reasons, cost mode, paid API use, cache use, and redacted audit summaries without making provider API calls or storing query history.
- Internet Access release gate and roadmap reset with `docs/decisions/internet_access_graduation_track.md`, `docs/web/WEB_ACCESS_POLICY.md`, `docs/web/INTERNET_PROVIDER_STRATEGY.md`, `docs/web/SOURCE_GROUNDING_REQUIREMENTS.md`, and `docs/web/BLOCKED_SOURCE_POLICY.md`; the track is docs/tracking only, keeps web acquisition free-first and cache-first, treats web content as untrusted, forbids CAPTCHA/anti-bot/login-wall bypass, and does not add provider calls or paid API defaults.
- Messaging Dogfood Suites with default-disabled `messaging_core`, `messaging_handoff`, `messaging_ios_compose`, `messaging_macos_probe`, `messaging_send_dry_run`, and `lead_response` suites; they exercise channel listing, draft validation, send-action previews, handoff workflows, iOS compose payload/result handling, macOS probe status, and lead-response dry-runs without live sends, Messages database access, Full Disk Access, or personal-data reads.
- Approved Lead Response Send workflow v1 with `leads create-send-action`, `leads send --from-action`, `leads handoff`, and `leads mark-responded`; send proposals are CRITICAL exact-preview Action Center records with no approval reuse, draft edits invalidate approvals, manual handoff saves/copies only, iOS compose produces a user-confirmed handoff payload, macOS sends remain disabled unless all adapter gates pass, unsupported channels return fallback options, and no auto-send, bulk send, private Messages database access, Full Disk Access, or memory write is added.
- Lead Response Drafting workflow v1 with brokered `leads summarize`, `leads classify`, `leads draft-response`, `leads suggest-followup`, and `leads suggest-meeting`; it creates editable local `MessageDraft` responses, queues follow-up task suggestions as pending Action Center items only, suggests meeting reply text without reading calendars or creating events, and performs no sends or memory writes.
- Apple Messages for Business provider strategy and connector stub with `apple-business doctor`, `apple-business status`, `apple-business mock-inbound`, and `apple-business draft-response`; the v1 provider is mock/local only, maps inbound events to local Lead Inbox records, creates local `MessageDraft` responses, and performs no live provider calls or sends.
- macOS approved iMessage send adapter v1 with `messages macos status`, `messages macos allow-recipient`, `messages macos live-send-probe --to ...`, and `messages send --from-action ...`; the send capability is disabled by default, CRITICAL per-action, allowlist-gated, recent-live-probe-gated, rate-limited, audited through ToolBroker/Action Center, and falls back to unsupported/iOS compose/manual handoff/Apple Messages for Business when the machine cannot safely send.
- Incoming message strategy and manual/mock inbox v1 with brokered `messages import --from-file`, `messages inbox list`, `messages inbox show`, and `messages inbox draft-reply`; imports are workspace-only, content is `UNTRUSTED_MESSAGE`, mock fixtures support dogfood/tests, Lead Inbox candidate metadata is exposed, draft replies create local drafts only, and no Messages database read, Full Disk Access, watcher, auto-reply, send, or memory write is added.
- Messages draft/handoff workflow v1 ergonomics with `messages draft`, `messages handoff <draft_id>`, draft-id `messages save-draft`/`messages copy-draft`, local drafts under `./workspace/messaging/drafts`, `messages.draft_from_lead`, and `messages.open_handoff_instructions`; save/copy still require matching Action Center approval and no message send, Messages database read, Full Disk Access, or memory write is added.
- macOS Messages automation feasibility probe with brokered `messages probe` and `messages probe --explain-permissions`; it performs metadata-only platform/app/AppleScript checks, records a local connector-status result, audits the probe, and keeps `send_capability_known=false` with no send adapter, Messages database read, Full Disk Access requirement, account scrape, or UI Send scripting.
- Agent DNA / Cloneability track with Agent DNA, architecture principles, clone blueprint, model/platform migration guides, rewrite checklist, build history, provenance rules, decision index, reconstructed prompt-pack archive, cloneability release-gate docs, and validation tests; this is docs/provenance only and does not change runtime behavior.
- iOS user-confirmed compose bridge v1 with brokered `messaging ios-compose-payload` and `messaging ios-compose-status` commands, local expiring handoff payloads under `./workspace/messaging/ios_compose`, nonce/integrity hashes, approved Action Center preview matching, local result recording, and a decision record at `docs/decisions/ios_companion_message_compose.md`; no iOS app, silent send, or provider send adapter is implemented.
- Lead Inbox abstraction v1 with `agent/leads/` models, mock provider, brokered `leads list/show/classify/draft-response/create-followup` commands, local MessageDraft generation, Action Center follow-up task drafts, untrusted lead-content filtering, and no real provider reads or sends.
- Agent Runtime Orchestration v1 with lightweight `agent/runtime/` models, service registry, feature flags, lifecycle kernel, in-process event bus, metadata-only job queue, workflow runner, scheduler policy, frontend bridge contract, runtime architecture docs, and `runtime status/doctor/services/features/health`, `jobs list/show`, `workflows list/run`, and `events tail` commands. Runtime status/doctor avoid LM Studio, personal-data connectors, tool execution, app bridges, and background persistence.
- Standardized Python runtime developer ergonomics: `scripts/agent` now prefers `./.venv/bin/python`, then `python3.12`, and otherwise prints setup guidance instead of falling through to Apple Python 3.9; added Makefile shortcuts for `doctor`, `test`, `policy-check`, `command-check`, and no-tool `run`.
- Message Safety Policy and Action Center integration with brokered `messaging draft-create` and `messaging create-send-action`; drafts stay in `./workspace/messaging/drafts`, send proposals become CRITICAL Action Center records with exact local previews, explicit per-action approval, no approval reuse, allowlist/rate-limit status, draft-edit approval invalidation, and no send adapter/tool.
- Message Channel Abstraction v1 with channel-neutral models for iOS compose, macOS Messages, Apple Messages for Business, Telegram, email, manual handoff, and mock channels; brokered `messaging channels`, `messaging draft show`, and `messaging draft validate` commands inspect local workspace draft metadata without reading private app data or sending.
- Apple Messaging / iMessage roadmap track and decision records, including Apple messaging architecture, iOS user-confirmed compose strategy, macOS Messages automation strategy, personal iMessage vs business messaging, and a messaging rollout plan; this is planning-only and does not enable sends, Messages database access, personal-data tools, Full Disk Access, or connector execution.
- Apple Ecosystem + Lead Response Track planning docs, including Apple ecosystem architecture, consumer Messages strategy, Apple Messages for Business strategy, LeadInbox abstraction, and a lead response workflow spec; this is planning-only and does not enable sends, personal-data reads, private Messages database access, or connector execution.
- Gmail and Telegram connector doctors with `gmail doctor`, `gmail scopes`, `telegram doctor`, and `telegram status`; checks are config-only, redact secrets, warn on broad Gmail scopes and repo-local token paths, warn on missing Telegram chat allowlists/defaults, and keep reads/sends disabled.
- Cost-aware weather provider selector with `weather providers`, `weather provider auto "<location>"`, `weather current --provider auto`, and optional explicit WeatherAPI fallback; auto mode prefers Open-Meteo, uses NOAA/NWS for U.S. alerts, skips WeatherAPI defaults unless allowed, audits provider decisions, and redacts keys.
- Optional SerpAPI fallback search provider through brokered `web.search.serpapi`, `web search --provider serpapi`, and `research --provider serpapi`; it requires `SERPAPI_API_KEY` plus paid-API opt-in, is not selected by default under `free_first`, normalizes results as `UNTRUSTED_WEB`, redacts queries/secrets, and audits provider decisions/domains.
- Secret/config doctor with redacted `secrets doctor/status` and optional SerpAPI, WeatherAPI, Gmail, and Telegram connector status checks that perform no provider API calls, inbox reads, or message sends.
- Free-first Web Acquisition Layer v1 with brokered `web robots`, `web sitemap`, `web feed`, `web acquire-url`, and `web acquire` commands for cache-first public retrieval, robots checks, sitemap/feed parsing, direct URL acquisition, untrusted-content wrapping, and paid-provider skip reporting.
- Prompt Tracker Maturity Track execution for PTM-01 through PTM-10, including hardened evidence classification, recovery commands, embedded-delimiter prompt pack parsing, prompt tracker dogfood/eval suites, and PTM-specific release gate docs under `docs/prompt_tracker/`.
- Prompt Tracker Maturity prompt pack import: `PTM-01` through `PTM-10` are split into `prompts/queued/`, copied from `prompts/packs/prompt-tracker-maturity-v1.promptpack.md`, and placed at the top of `docs/PROMPT_QUEUE.md` without executing the prompts.
- Cost-aware provider policy with free-first provider selection rules, config defaults, audit/redaction support, and connector docs for web/search, weather, and personal communications provider ordering.
- Personal connector readiness checklist at `docs/checklists/personal_connector_readiness.md`, with evidence for ToolBroker, policy, approval, audit, memory, untrusted-content, and disabled-by-default personal connector gates.
- Selected-scope calendar read-only connector validation with brokered `calendar read` and `calendar availability` commands, optional Calendar.app AppleScript adapter, and event-text-as-data safety labeling.
- Selected-scope contacts read-only connector validation with brokered `contacts search` and `contacts read` commands, optional Contacts.app AppleScript adapter, sensitive-field gates, and contact-text-as-data safety labeling.
- Email metadata + selected-thread + draft-only assistant validation with brokered `email metadata/read/summarize/draft-reply` commands, optional IMAP adapter, untrusted email labeling, and no-send draft output.
- Messages/text draft-only assistant validation with brokered `messages read/summarize/draft-reply/draft-from-text` commands, explicit `messages.draft_from_text` capability, and workspace-only manual fallback.
- Daily Briefing v1 with optional brokered weather, calendar selected-range, email metadata, web topic search, reminders stub, dry-run, and JSON output.
- Meeting prep workflow with brokered selected calendar event read, optional approved contact lookup, optional web topic search, dry-run, and JSON output.
- Email Triage v1 with brokered metadata-only priority classification, optional selected-thread summary, draft-only reply generation, dry-run, and JSON output.
- Self-improvement backlog/propose commands that inspect approved project files through brokered, audited `filesystem.read` calls and produce ranked read-only improvement proposals.
- Agent Dashboard v1 with `dashboard` and `status` commands for read-only runtime, LM Studio, tools, connectors, permissions, pending approvals, audit metadata, memory counts, risk settings, last test run, and setup hints.
- Live eval harness commands `eval list`, `eval run`, and `eval report` for controlled feature validation without personal-data access by default.
- Unified Action Center v1 with `actions list/show/approve/deny/edit/clear-denied/export`, persisted action records, redacted exact previews, approval queue integration, one-time approval consumption, and action lifecycle audits.
- Calendar approved writes v1 with `calendar draft-create/create --from-action`, `calendar draft-update/update --from-action`, and `calendar draft-delete/delete --from-action`; drafts create Action Center records and approved executions still go through `ToolBroker`.
- Reminders / Tasks connector v1 with adapter interface, mock provider, brokered `tasks.list/create/update/complete/delete`, and Action Center `tasks draft-create` plus `tasks create --from-action`.
- Contacts approved edits v1 with `contacts draft-update/update --from-action` and `contacts draft-create/create --from-action`; drafts create Action Center records with field-level diffs and approved executions still go through `ToolBroker`.
- Email approved send v1 with `email draft-new`, Action Center backed `email draft-reply`, and `email send --from-action`; send execution requires an approved Action Center item and supports only a mock provider until a real send provider is separately approved.
- Messages safe handoff v1 with `messages.save_draft` and `messages.copy_draft` Action Center handoff actions plus a decision record at `docs/decisions/messages_send_path.md`.
- Browser selected URL and clipping v1 with `browser read-url`, `browser summarize-url`, `browser clip-url --to workspace`, and a selected-tab stub plus a decision record at `docs/decisions/browser_selected_tab_clipping.md`.
- Browser clipping capability-name compatibility for canonical `browser.read_url`, `browser.summarize_url`, and disabled `browser.selected_tab` names while retaining legacy selected-URL aliases.
- Knowledge Capture v1 with `capture note`, `capture from-file`, `capture from-url`, `capture list`, `capture summarize`, and `capture promote-to-memory` for a workspace-only local knowledge inbox.
- Knowledge Capture `capture from-file` now supports an explicit `--trusted-user` marker for user-authored workspace files while keeping file captures untrusted by default.
- Daily Briefing v2 with configurable `weather`, `calendar`, `tasks`, `email`, `web`, `memory`, and `suggested_actions` sections, `briefing config show/set`, brokered dry-run previews, independent section failure handling, and Action Center queued suggestions.
- Meeting Follow-Up workflow with `meeting follow-up --event-id`, `meeting follow-up --notes-file`, dry-run/JSON output, workspace-bounded notes reads, deterministic decisions/action-items extraction, and Action Center queued task/email/calendar suggestions.
- Personal Task Extraction workflow with `tasks extract --from-notes`, `--from-email-thread`, `--from-meeting`, `--from-url`, `--from-capture`, dry-run/JSON output, untrusted source filtering, and Action Center queued task drafts.
- Controlled self-improvement implementation loop with `improve implement`, `improve run-tests`, `improve show-diff`, `improve create-action-for-commit`, and approval-gated `improve commit --from-action`.
- Overnight self-improvement safe-mode runbook, report template, report directory, and `improve overnight-plan` planner command for docs/tests/hardening candidate selection without execution.
- Prompt Ledger and Prompt Queue tracking with `docs/PROMPT_LEDGER.md`, `docs/PROMPT_QUEUE.md`, `docs/PROMPT_AUDIT.md`, prompt record directories, a prompt record template, and `prompts list/next/show/add/mark-active/mark-complete/mark-skipped/mark-failed/audit/missing` CLI commands.
- Prompt Pack import/splitting support with strict `<<<PROMPT_PACK_START>>>` format validation, pack storage under `prompts/packs/`, split prompt files under `prompts/queued/`, ledger/queue/audit integration, and `prompts validate-pack/import/split/mark-superseded` commands.
- PromptOps Workbench v1 with `work import`, `work import-clipboard`, `work next`, `work copy-next`, `work show-next`, `work resume`, `work status`, `work review`, `work run-next`, `work autopilot`, and `work audit` commands for one-command prompt ingestion and resume-safe queue handling.
- Command Registry and Manual QA System with generated `docs/COMMAND_REGISTRY.md`, `docs/COMMAND_TEST_MATRIX.md`, `docs/COMMAND_LEGACY.md`, `docs/COMMAND_QA_RUNBOOK.md`, command record templates, and read-only `commands list/show/search/legacy/deprecated/validate/qa-plan/qa-run` CLI commands.
- Scheduler / Automation v1 with explicit local `schedule list/create/run/pause/delete` commands for manual-run daily briefing, connector doctor, safe eval, memory cleanup, and audit summary workflows.
- Scheduler / Automation v1 now also supports optional manual-run `backup_create` schedules, implemented as redacted-only brokered `backup.create` calls.
- Native Skills Program foundation with intake process, native skill criteria, candidate registry, risk model, and reusable native skill record template under `docs/native_skills/`.
- Skill marketplace survey and native-candidate shortlist with category scoring, high-risk deferrals, and a recommended first native skill candidate.
- Native skill vetter v1 with brokered `skills vet`, `skills vet-folder`, and `skills score` commands for workspace-only static analysis of candidate `SKILL.md` files and skill folders.
- Native skill manifest discovery with metadata-only `skills list`, `skills show`, `skills validate`, and `skills doctor` commands plus the first built-in `native_skill_vetter` manifest.
- Native skill finder v1 with brokered `skills find "<query>"`, `native_skills.find_skill`, and a built-in `native_skill_finder` manifest for local-only discovery across manifests, candidate docs, feature registry, and maturity tracker.
- PDF workspace native skill v1 with brokered `pdf info`, `pdf extract-text`, `pdf summarize`, and `pdf extract-tables` commands over approved workspace PDFs.
- Golden Eval Suite and quality scorecards with data-backed cases in `eval_cases/`, `eval run --routing/--policy/--tools/--workflows/--prompt-injection/--lmstudio-live`, per-category scores, and per-run JSON reports under `reports/evals/`.
- Privacy Center / Data Inventory v1 with `privacy status`, `privacy inventory`, `privacy export`, `privacy delete-memory`, `privacy audit-summary`, and `privacy permissions` for metadata-only local data visibility.
- Backup / Restore / Migration v1 with brokered `backup create/list/inspect/verify/export --redacted/restore`, redacted local archives, manifest integrity hashes, restore previews, and approval-gated restore.
- Model-router benchmark and prompt quality evals with `models list`, `models benchmark --safe`, `router eval`, `prompts eval`, and `prompts report` over reviewed fixture prompts and report output.
- Local launcher script at `scripts/agent` that prefers `AI_AGENT_PYTHON`, `./.venv/bin/python`, and the bundled Codex Python runtime before falling back to a Python 3.11+ system interpreter.
- Live Session Logging and Replay with `session start/status/run/end/list/show/replay/export/last`, redacted output capture under `reports/sessions/`, audit-id linking where visible, and `docs/SESSION_LOGGING.md`.
- Manual Dogfood Command Suites with `dogfood list/show/run`, curated suite YAML under `dogfood_suites/`, safe synthetic fixtures under `workspace/dogfood` and `workspace/skills`, and docs under `docs/dogfood/`.
- User feedback capture for live sessions with `feedback good/bad/bug/confusing/slow/unsafe/rate/add/list/export`, redacted session feedback records, replay/show integration, severity escalation for unsafe feedback, and audit events for feedback operations.
- Session Review and Bug Generator with `session review <session_id>`, `session review --last --create-bugs`, `bugs list/show/export`, redacted review reports under `reports/session_reviews/`, local bug records under `bugs/`, and `docs/BUG_TRIAGE.md`.
- Regression test generator validation with `bugs create-regression`, a redacted skipped scaffold under `tests/regressions/`, and bug lifecycle tracking from live session review evidence.
- Live Test Runbook and Daily Dogfood Workflow with `dogfood plan`, `dogfood next`, `dogfood checklist`, daily/weekly dogfood docs, and a dogfood session-notes template.
- Product Quality Dashboard v1 with `quality status/sessions/bugs/regressions/features/next` over redacted local sessions, feedback, bugs, evals, tests, maturity, dogfood recommendations, and release gates.
- Eval report template at `docs/EVAL_REPORT.md`.
- Durable project tracking files: `docs/PROJECT_STATE.md`, `docs/FEATURE_REGISTRY.md`, `docs/FEATURE_ROADMAP.md`, and tracking templates.
- Feature maturity tracking with `docs/FEATURE_MATURITY.md` and `docs/templates/feature_maturity_template.md`.
- Documentation validation for project tracking and feature maturity files.
- Weather-only daily briefing command that uses only brokered weather tools and avoids personal-data sources.
- NOAA/National Weather Service as a U.S.-only no-key weather provider with forecast and alerts support.
- Safe weather preferences with explicit default-location config, units, cache controls, and CLI show/set/clear commands.
- Weather-aware web research routing/workflow for delays, closures, and storm updates while keeping simple weather weather-only.
- WeatherKit connector decision record and provider stub without implementing JWT signing or Apple API calls.
- Reusable connector metadata/status framework under `agent/connectors/` and CLI connector dashboard routing through it.
- Prompt-free `smart_agent.py preflight "<request>"` command for brokered dry-run previews of routed or exact tool/capability requests.
- Workspace file assistant CLI commands for brokered list/read/summarize/search/write/patch/diff operations inside approved roots.
- Memory v2 CLI commands for brokered add/search/list/export/delete/clear/context operations.

### Changed

- Revalidated robots.txt, sitemap, and RSS/Atom feed support for the Web Acquisition Layer. Common feed MIME types (`application/rss+xml`, `application/atom+xml`, `application/rdf+xml`) are now accepted by the shared safe fetcher, sitemap/feed binary payloads are blocked even under injected fetchers, and focused regressions cover TTL cache reuse for robots/sitemap/feed results. No paid provider calls, browser automation, article-body feed fetches, search-history storage, CAPTCHA/login/paywall/anti-bot bypass, personal-data enablement, or ToolBroker/PolicyEngine/AuditLogger bypass was added.
- Re-verified the cost-aware internet provider policy implementation idempotently: brokered `web providers`, `web provider-policy`, and `web provider-decision` commands still make no provider API calls, persist no query history, redact queries, skip paid providers by default, and pass focused/full validation.
- Messaging / iMessage Track release gate now records full-suite, policy, manifest, docs, command registry, dogfood, dry-run send, macOS metadata probe, default-disabled send, no-bulk, no-private-database, no-Full-Disk-Access, trust-label, and approval/no-reuse validation evidence. The `messaging_handoff` dogfood suite now accepts the intended default-disabled `messages.draft_from_text` denial as a passing safe outcome unless the personal connector is explicitly enabled.
- ORCH batch recovery now records `docs/runtime/ORCH_BATCH_RECOVERY_REPORT.md` and corrects stale queued ORCH rows in `docs/PROMPT_LEDGER.md`; ORCH-01 through ORCH-10 remain completed and `MACOS-MESSAGES-PROBE` remains the next queued prompt.
- Roadmap and prompt tracking now mark `APPLE-MESSAGING-ROADMAP` as the active/completed planning prompt and keep follow-on messaging/lead implementation prompts queued or blocked behind approval gates.
- Prompt tracking now marks `PTM-01` through `PTM-10` complete with targeted test/docs evidence; `APPLE-MESSAGING-ROADMAP` is the next queued prompt.
- Prompt tracking now marks `SECRET-CONFIG-DOCTOR` complete out of queue order by explicit user request.
- Prompt tracking state now sets `next_prompt_id` to `PTM-01` and records the Prompt Tracker Maturity pack import in the ledger, queue, audit, project state, roadmap, maturity tracker, and completion report.
- Prompt tracking now marks `COST-AWARE-PROVIDER-POLICY` complete out of queue order by explicit user request while keeping `PTM-01` as the next recommended prompt.
- Live dogfood release-gate tracking now records a redacted synthetic session (`sess_20260523T182508Z_abdad880`), feedback attachment, session review, `BUG-0002`, regression scaffold creation, and quality dashboard verification.
- Overnight self-improvement tracking now records the explicitly approved bounded safe-mode run on `agent/overnight-2026-05-23`, with a live report under `reports/overnight/` and stricter report-template validation.
- Session logs are ignored by git by default except for `reports/sessions/.gitkeep`; committed session fixtures must be sanitized.
- Messages safe handoff tools now reject direct broker execution unless Action Center verifies the matching approved action id and submitted save/copy args match the approved reviewed draft.
- Email approved send tools now reject direct broker execution unless Action Center verifies the matching approved action id and submitted args match the approved reviewed draft.
- Contact approved write tools now reject direct broker execution unless Action Center verifies the matching approved action id and submitted args match the approved preview.
- Reminders / Tasks `draft-create` now routes through brokered `tasks.draft_create` before queuing an Action Center task-create item, so even local draft creation follows ToolBroker policy/audit flow.
- Calendar approved write tools now reject direct broker execution unless Action Center verifies the matching approved action id.
- Unified Action Center exports and lifecycle audit entries now use minimized/redacted payloads for sensitive action bodies and drafts while preserving exact local previews for user approval.
- Release-gate tracking now marks Golden eval scorecards complete for tested local scope while keeping live LM Studio/provider validation opt-in.
- Eval reporting now includes category scorecards and data-file case loading instead of relying only on hardcoded validation checks.
- Release-gate tracking now records the next product feature set, including Golden eval scorecards, privacy/backup/model-router work, controlled-action follow-ups, and the overnight self-improvement track.
- Project tracking now records the next feature set as Controlled Actions and Proactive Workflows, starting with a live validation and eval harness before any approved write/send implementation.
- Codex working rules now require prompt ledger/queue reads, active/completed prompt status updates, prompt audit updates after major batches, and final reports with `prompt_id` plus `next_prompt_id`.
- Codex working rules now direct pasted mega prompts through `work import` and restrict `work autopilot` to safe categories with approval-gate stops.
- Codex working rules now require command registry and command test matrix updates whenever CLI commands are added, renamed, deprecated, removed, or behaviorally changed.
- Native skill intake now includes the static vetter as a required evidence-gathering step before native porting or installation decisions.
- Native skill program docs now define manifest discovery, required manifest fields, validation rules, and metadata-only loading boundaries.
- Native skill candidate tracking now marks the skill finder as tested and accepted while keeping external marketplace browsing/install/execution out of scope.
- Native skill candidate tracking now marks the PDF workspace skill as tested and accepted while deferring OCR, split/merge, generated PDF writes, and external binaries.
- Feature roadmap release-gate status now marks the accumulated post-baseline workflow batch as synced after the full test suite, startup policy validation, capability manifest validation, and docs validation passed.
- Full feature maturity release gate now marks the controlled actions and workflow batch complete after full tests, startup policy validation, capability manifest validation, command registry validation, safe eval, ToolBroker/default personal-data/CRITICAL approval scans, and docs sync.
- Full release-gate and feature maturity review now records the next product feature batch as locally validated through full tests, startup policy validation, capability manifest validation, safe eval, command/native-skill validation, ToolBroker path scan, personal-data default check, and HIGH/CRITICAL approval checks.
- Source-grounded research reports fetch failures in a first-class `fetch_failures` field and includes an explicit no-fabricated-citations source policy.
- `filesystem.read` results and audit trust now label file content as `UNTRUSTED_DOCUMENT`.
- Memory search now supports category filters, and memory context injection returns a bounded non-personal context block with injected memory IDs.
- Runtime `doctor` now reports normalized capability manifest validation, `ToolBroker` initialization, connector registry loading, personal connector defaults, and CRITICAL action defaults without sending model prompts or executing tools.
- Connector dashboard status now includes normalized capability summaries and `setup_hint` in addition to the existing docs setup hint.
- Capability manifest entries now use a normalized schema with capability/tool/connector names, approval reuse, rate limit, memory behavior, setup hint, and docs reference fields.
- Connector dashboard status is now backed by reusable connector metadata/status primitives instead of UI-local status logic.
- Project roadmap now prioritizes batch order, approval gates, and resume-safe handoff state.
- AGENTS working rules now require PROJECT_STATE, changelog, feature registry, roadmap, and completion report updates after meaningful runs.

### Fixed

- Running `python3 smart_agent.py` with Apple Python 3.9 now exits with a clear Python 3.11+ setup message before importing agent modules.
- Open-Meteo and NWS geocoding now retry comma-separated city/region inputs such as `Phoenix, AZ` by searching the city and preferring matching administrative regions when the upstream geocoder does not resolve the original query.

### Security

- Messaging / iMessage release-gate validation confirms no live send ran, no message send is enabled by default, live sends remain unavailable unless a future explicit live probe and approval gates pass, all executable send capabilities are CRITICAL/per-action/no-reuse/default-disabled, bulk/group sends are rejected, message content remains untrusted data, and private Messages database/Full Disk Access paths remain blocked.
- Lead Response Drafting treats all source lead/message content as `UNTRUSTED_MESSAGE`, filters instruction-like text from draft context, includes source and assumptions in generated drafts, creates no send action, sends nothing, reads no calendars, creates no calendar events, queues follow-up tasks only as pending Action Center records, and writes no lead content to memory by default.
- Apple Messages for Business provider v1 is mock/local and disabled for live provider use: doctor/status perform no provider API calls, mock inbound content is `UNTRUSTED_MESSAGE`, local leads are stored only under `./workspace/leads/apple_business`, draft responses create local `MessageDraft` records only, future sends remain CRITICAL/default-disabled/per-action/no-reuse, and no personal iMessage automation, private Messages database access, Full Disk Access, hidden polling, provider webhook polling, send action creation, send execution, credential printing, or memory write path was added.
- iOS compose handoff is user-confirmed only: payload creation and result recording go through `ToolBroker`, approved-action payloads verify the current draft fingerprint against the exact Action Center preview, payloads expire, attachments are deferred, expired payloads cannot record sent/queued, the user must tap Send in iOS compose UI, no `messaging.ios_compose.send` or `messaging.send_approved` executable tool is registered, and no Messages database, Full Disk Access, background send, bulk send, or memory write path was added.
- Message send proposals can only be created from local `MessageDraft` records, are CRITICAL Action Center actions with exact local previews and no approval reuse, reject bulk recipients and attachments, invalidate older approvals when a draft changes, block untrusted-content-created sends without explicit user request, redact audit/export copies of bodies and recipients, and still have no registered `messaging.send_approved` execution tool.
- Message send request models now default to CRITICAL, exact-preview, approval-required, and approval-reuse-disabled; all v1 messaging channels report `supports_send=false`, no `messaging.send` tool exists, and validators reject unknown channels, missing recipients, empty bodies, group/bulk recipients, and unsupported send attachments.
- Apple messaging planning now explicitly classifies message reads as HIGH, personal copy/handoff as HIGH when personal data is present, message sends as CRITICAL with exact per-action approval and no approval reuse, bulk sending as FORBIDDEN in v1, and private Messages database scraping as FORBIDDEN unless a future explicit decision overrides after review.
- Apple ecosystem and lead response planning explicitly rejects private Messages database scraping, broad Full Disk Access, automatic consumer iMessage sends, hidden inbox polling, and auto-response behavior without a future decision record and release gate.
- Gmail and Telegram doctors do not read inboxes/chats, do not send messages, do not validate tokens against live services, do not store tokens, and report send paths as CRITICAL, disabled by default, per-action approval-only.
- Live dogfood release-gate validation confirmed the synthetic secret string was redacted from session, feedback, review, bug, and regression artifacts; no personal-data connector read, HIGH action, or CRITICAL action was executed.
- Native skill vetting treats candidate skill content as `UNTRUSTED_DOCUMENT`, never executes scripts, never installs dependencies, never grants permissions, and audits all vetting operations through `ToolBroker`.
- Native skill manifests are metadata only: unknown capabilities fail validation, personal-data manifests must be disabled by default, CRITICAL manifests require per-action approval, and executable/bypass-oriented manifest fields are rejected.
- Native skill finder searches only reviewed local manifests/docs, executes through `ToolBroker`, audits files read, writes no memory, and does not browse marketplaces, install skills, execute external code, or grant tool access.
- PDF workspace tools resolve paths through approved workspace roots, label output as `UNTRUSTED_DOCUMENT`, enforce file/page/text limits, execute no OCR or external binaries, audit files read, and write no memory by default.
- Golden Eval Suite defaults remain safe: personal-data evals are skipped, live LM Studio is opt-in, tool checks use `ToolBroker`, prompt-injection cases treat fixture text as untrusted data, and evals perform no sends or calendar/contact writes.
- Model-router and prompt quality evals are fixture-backed, do not send LM Studio prompts by default, do not access personal data, keep live answer-quality checks opt-in, and report personal-data/send requests as routing/policy gate checks rather than executing tools.
- Privacy Center uses connector/config metadata only, does not read personal connector data, audits all privacy operations, redacts export previews by default, summarizes audit logs without raw args, and clears memory only after explicit confirmation through brokered `memory.clear`.
- Backup / Restore excludes `.env` and key/certificate files, redacts secret-like config/docs/memory/action/capture values, never fetches personal connector data, audits all backup/restore tool calls, and blocks restore of capability manifests that would weaken policy or enable personal connectors.
- Personal connector readiness gate passed locally before any new personal-data connector work; no personal-data tools were enabled and no live personal-data reads were performed.
- Calendar read-only remains HIGH risk, disabled by default, approval-required, selected-range only, audited, non-writing, and non-persistent by default.
- Contacts read-only remains HIGH risk, disabled by default, approval-required, selected-scope only, audited, non-writing, no-bulk, and non-persistent by default.
- Email assistant remains HIGH risk, disabled by default, approval-required, selected-thread only, audited, no-send/no-delete/no-move/no-archive, no-bulk, and non-persistent by default.
- Messages/text assistant remains HIGH risk, disabled by default, approval-required, selected-thread or workspace-file only, audited, no-send/no-delete/no-move/no-archive/no-contact-harvesting, no-bulk, no private database scraping, and non-persistent by default.
- Daily Briefing v1 executes only explicitly selected sources through `ToolBroker`, skips unapproved or disabled calendar/email metadata sections, performs no writes/sends, reads no email bodies or messages, and writes no memory by default.
- Daily Briefing v2 keeps all sections opt-in/configurable, skips approval-gated personal sections when approval is denied, reads email metadata only, avoids message reads, performs no writes/sends, and queues suggested actions instead of executing them.
- Meeting prep executes selected calendar event, contact lookup, and web search only through `ToolBroker`; it sends no emails/texts, edits no calendar/contact data, performs no bulk contact export, and writes no memory by default.
- Meeting Follow-Up reads selected events and workspace notes only through `ToolBroker`, treats notes as `UNTRUSTED_DOCUMENT`, filters instruction-injection lines, creates Action Center records for suggested tasks/email/calendar updates, performs no writes/sends, and writes no memory by default.
- Personal Task Extraction reads exactly one selected source through existing brokered tools/connectors, requires approval for personal sources, ignores untrusted source instructions, queues `tasks.create` Action Center drafts only, performs no task writes, and writes no memory by default.
- Controlled self-improvement implementation is branch-based, requires approved proposal records, writes through brokered `filesystem.write`, runs brokered tests/diff, creates Action Center commit records only, offers an explicit brokered test/diff `create-action-for-commit` checkpoint, and blocks policy weakening, audit disabling, personal-data grants, persistence paths, package installs without approval, and send/write side effects.
- Prompt tracking is SDLC metadata only: it does not add connectors, personal-data access, send/write tools, background automations, or policy exemptions.
- Prompt pack import is import-only: it rejects `execute_all`, preserves prompt bodies as data, and does not run imported prompts automatically.
- PromptOps Workbench treats imported prompt text as `UNTRUSTED_DOCUMENT`, keeps `work run-next` disabled by default, refuses HIGH/CRITICAL/FORBIDDEN or approval-gated prompts in autopilot, and redacts secret-looking values in PromptOps reports.
- Command Registry commands are metadata-only and do not execute agent tools, access connectors, read personal data, grant approvals, or write memory. `commands qa-run` prints SAFE/LOW manual QA examples and does not execute them in v1.
- Session logging redacts secret-looking values, email addresses, and obvious phone numbers by default; it does not capture environment variables, replace AuditLogger, grant permissions, enable personal-data tools, or bypass delegated command policy.
- Dogfood suites execute only `python smart_agent.py ...` commands, preserve the delegated command's ToolBroker/policy/approval/audit path, do not enable personal-data tools, keep personal connector checks preflight-only by default, and can capture redacted output only through an explicit active session.
- Feedback records are redacted before storage, write no memory by default, are treated as review evidence rather than policy-changing instructions, and audit feedback add/list/export operations without exposing raw notes.
- Session review reads redacted session previews and feedback only, writes local redacted QA artifacts, classifies policy/ToolBroker/PolicyEngine/Approval/Audit bypass and personal-data leak signals as P0 bugs, does not send data externally, does not access personal connectors, does not write memory, and does not automatically fix bugs.
- Live dogfood planning commands are metadata-only: they read suite/session status and feature maturity notes, do not run suites by themselves, do not access personal data, do not grant approvals, do not send messages, and keep personal-data dogfood dry-run/mock by default.
- Product Quality Dashboard is read-only and metadata-only: it does not execute tools, access personal connectors, expose raw personal data, grant approvals, or write memory; session feedback and bug text are redacted before display.
- Scheduler v1 does not install background persistence. Scheduled runs are manual, audited, reuse ToolBroker for tool workflows, require normal approvals for personal sections, and never execute CRITICAL actions automatically.
- Email Triage v1 reads metadata only by default, reads at most one selected thread body after approval, redacts body content from the triage report/audit args, sends nothing, mutates nothing, and writes no memory by default.
- Self-improvement backlog/propose mode is read-only, uses only approved project file reads through `ToolBroker`, audits reads/dry-runs, and blocks safety-weakening suggestions such as disabling audit logs or relaxing policy gates.
- Agent Dashboard v1 is metadata/status-only: it does not attach tools, execute connector actions, read personal data, grant permissions, consume approvals, or start background work; secrets are redacted and memory content is not displayed.
- Eval harness safe runs execute tool checks only through `ToolBroker`, skip personal-data evals by default, perform no sends, perform no calendar/contact writes, and delete the non-sensitive memory test fact before completion.
- Unified Action Center does not execute actions directly; it is a review and approval queue only. Future execution must still pass through `ToolBroker`, `PolicyEngine`, approval rules, and `AuditLogger`; CRITICAL actions remain per-action only, edits invalidate prior approvals, and action exports/audit lifecycle records minimize sensitive bodies/drafts.
- Calendar write capabilities remain disabled by default, CRITICAL per-action approval only, Action Center gated, one-shot on approval consumption, audited for draft/approval/execution/failure, direct broker execution without a verified Action Center action id is rejected, no automatic invites, no recurring events in v1, no full calendar export, and no notes/body in drafts unless explicitly allowed.
- Tasks connector provider capabilities remain disabled by default; task listing is HIGH risk and approval-required, task writes are CRITICAL per-action approval only, task draft creation is brokered into Action Center, no full task export is performed, no task contents are written to memory by default, and no native Reminders database scraping or Full Disk Access dependency is introduced.
- Contact write capabilities remain disabled by default, CRITICAL per-action approval only, Action Center gated, one-shot on approval consumption, audited for draft/approval/execution/failure, direct broker execution without a verified Action Center action id is rejected, submitted write args must match the approved preview, no bulk edits, no delete implementation, sensitive field values redacted from previews/audits, and no contact details written to memory by default.
- Email send remains disabled by default, CRITICAL per-action approval only, Action Center gated, one-shot on approval consumption, direct broker execution without a verified Action Center action id is rejected, submitted send args must match the approved reviewed draft, no direct unreviewed model-output send, no bulk sends, no background sends, attachments blocked in v1, body/thread context previewed before approval, and no credentials or private Mail databases are used.
- Messages safe handoff remains no-send: no Messages database scraping, no Full Disk Access dependency, no automatic text sending, save/copy requires verified Action Center approval with submitted args matching the approved preview, drafts stay in `./workspace` or clipboard only, message content remains `UNTRUSTED_MESSAGE`, and message bodies are not written to memory by default.
- Browser clipping v1 is explicit-URL only: it reads no browser history, cookies, sessions, forms, passwords, bookmarks, or private browser profile databases; URL fetches go through `web.fetch_url`, clips write only through `filesystem.write` under `./workspace`, selected-tab native reading remains disabled/stubbed, and clipped content is labeled `UNTRUSTED_DOCUMENT`.
- Knowledge Capture v1 stores captures only under `./workspace/captures`, rejects secret-looking content before writing, uses brokered `filesystem.read/write` and `web.fetch_url`, treats URL/file captures as untrusted data by default, requires an explicit `--trusted-user` marker for trusted user-authored file captures, performs no Apple Notes integration or private app database scraping, and promotes to memory only through `memory.store` policy.
- Web research excerpting is stricter: instruction-injection-only page text is not reused as fallback evidence.
- File assistant workflows treat document content as untrusted data, filter instruction-like text in summaries/search excerpts, and continue to rely on workspace root checks, backups, and audit logging.
- Memory v2 refuses secrets, keeps personal memory approval-gated, excludes personal memory from default context injection, and audits injected memory IDs.
- Runtime and connector diagnostics remain read-only: they do not send model prompts, attach tools, read personal data, grant permissions, or change config.
- Startup capability validation now rejects missing normalized fields, missing trust/approval/risk rules, personal-data capabilities enabled by default, CRITICAL actions without per-action approval, and manifest bypass flags.
- Connector status and health checks remain metadata-only and do not execute personal-data reads.
- Connector status output redacts secret-like fields.
- Preflight evaluations run through `ToolBroker.dry_run()`, execute no tools, show approval requirements, and redact sensitive arguments.
- Tracking rules explicitly preserve ToolBroker, PolicyEngine, ApprovalManager, and AuditLogger gates.
- Native Skills Program intake does not install, import, run, or execute external skills. External skill text is treated as untrusted data, and accepted behavior must map to local ToolBroker-controlled capabilities with policy, approval, audit, memory, and trust rules.
- Skill marketplace survey remains research-only: marketplace pages and skill descriptions are treated as untrusted web/document data, no external skill code is installed or run, and high-risk categories such as sends, browser automation, cloud deployment, and self-evolution are deferred.

### Deprecated

- Ad hoc connector status logic in UI modules is superseded by `agent.connectors`.

### Removed

- No runtime features removed.

### Known Limitations

- Live LM Studio, web provider, weather provider, and personal connector smoke tests still require the user's local configuration and explicit approvals where applicable.
- Personal-data tools remain disabled by default and are not release-ready for broad unattended use.
- Prompt reconstruction is best-effort; queued or blocked prompt groups should be checked against `docs/PROMPT_AUDIT.md` before being treated as missed work.
- Command status and manual QA results are conservative. Most commands are cataloged, but manual verification should be recorded in `docs/COMMAND_TEST_MATRIX.md` as QA suites are run.
